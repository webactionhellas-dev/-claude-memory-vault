---
name: cloudskin-shopify-integration
description: "Connecting the CloudSkin Vercel site (cloudskin-preview.vercel.app) to the client's Shopify store (rta3sf-47) headlessly — store state, pricing, and remaining steps"
metadata: 
  node_type: memory
  type: project
  originSessionId: e1eeb82a-d4f0-480e-8378-2fd699b0992a
---

**➡➡➡ NEXT-SESSION TASK (owner-approved 2026-07-15, Option 1): make ALL PRODUCT-PAGE text editable via the Content Studio, per-product, with a safe fallback, and TEST on a live product page before calling it done.** Owner wants Larissa to edit every product's description + feature bullets + fabric + fit, plus the shared Fit / Shipping & Returns / care / "Complimentary shipping…" banner. It is currently HARDCODED in `cloudskin-live-fix/js/product.js`: the `PRODUCT_CONTENT` object (~lines 20–125, keyed by product handle; each has `desc`, `features`[array], `fit`[label,text], `fabric`) + a generic `CARE` array (line 19). The DETAILS accordion is built ~lines 385–405 (Fabric&Care = `pc.fabric` + `CARE`; Fit; Shipping&Returns); the description + feature bullets render in the buybox (~lines 200–260 — locate). **PLAN:** (1) **content.js** — expose the loaded map as `window.CLOUDSKIN_CONTENT` + a re-apply fn `window.CLOUDSKIN_APPLY_CONTENT()`, and add handling for a new `[data-content-list]` attr (value = newline-separated text → set innerHTML to one `<li>` per line) for the features + care lists. (2) **product.js** — add `data-content="product.{handle}.desc"`, `data-content-list="product.{handle}.features"`, `data-content="product.{handle}.fabric"`, `data-content="product.{handle}.fit"` to the rendered PDP + shared `data-content="product.detail.shipping"` / `product.detail.shipbanner` / `data-content-list="product.detail.care"`; call `CLOUDSKIN_APPLY_CONTENT()` after the PDP renders (product.js waits on `C.shopify.ready`, so re-apply after). KEEP the hardcoded values as the rendered fallback so live product pages never break. (3) **studio.js** — add a "Product text" area (per-product: a section per product OR a product dropdown → desc[textarea] / features[textarea, one per line] / fabric[text] / fit[textarea]) + a "Shared details" section (shipping, shipbanner, care). CLEANEST for defaults = move `PRODUCT_CONTENT`+`CARE`+shared into a NEW shared file `js/product-content.js` (`window.CS_PC`) included by BOTH product.js and studio.js (single source, no duplication); studio.js reads CS_PC for the product list + default values. (4) include product-content.js in product.html + studio.html. (5) bump `?v` (currently **v=34 → v=35**) + `vercel deploy --prod --yes --cwd "…\cloudskin-live-fix"`. (6) VERIFY on a live PDP (e.g. https://www.cloudskin.com/product?handle=the-club-skirt) that desc/features/accordion still render AND that a Studio override actually applies. Studio facts: cloudskin.com/studio, pw `CloudskinStudio1`, Supabase project `ocszztflphqsaoyhlerx`, table `cloudskin_content`, RPCs `studio_auth`/`studio_save`; content.js applies `[data-content]`/`[data-content-img]`; home already has 11 editable fields (see [[cloudskin-content-studio]] + [[cloudskin-shopify-theme]] which is SHELVED). **Direction reminder: keep cloudskin.com EXACTLY as-is (NO Shopify-theme rebuild — abandoned); only make text/images editable. Payments = PayPal only, owner must add a card provider. Owner guide published as an Artifact.**

**✅✅✅ INVENTORY LOADING COMPLETE (2026-07-13) — all 15 products now have SKUs + Track-quantity ON + Athens(Greece) & Dubai on-hand set, all saved in Shopify.** Locations: Dubai Warehouse 114489229611, Athens Warehouse 115126796587. Standard qty = Athens 7/10/5/2 · Dubai 8/10/5/3 (S/M/L/XL). Exceptions: Flow Dress Dubai S=13; both jackets (Drift FE-10 White / Elevate FE-09 Black) S/M=10·L=5·**XL=0** (no sheet data → tracked out-of-stock); Perf Shorts D5/G5·15/15·20/20·10/10; Ace Dress 10/10 (S/M only); **Perf Tank** Black FE-14 / White FE-13 = Athens 2/10/10/2 · Dubai 3/10/10/3; **Perf Tee** Black FE-12 / White FE-11 = Athens 2/7/10/5 · Dubai 3/8/10/5. **Wrong pre-existing SKUs overwritten:** Club Quarter Zip (FE-07→FE-15), Club Skirt (FE-08→FE-16), Flow Dress/S (FE-07→FE-17), both jackets/S (FE-07→FE-10/FE-09), Perf Tank (FE-01/FE-03 from Sig Bra→FE-14/FE-13), Perf Tee (FE-01/FE-03→FE-12/FE-11). Foundation Tank/Sculpt Bra/Court Skirt/Signature Skirt/Signature Bra already had CORRECT SKUs.

**⚠️ Perf Tee interpretation flag (client should confirm):** the original xlsx labels the Tee's SKU stickers S/M/L/XL but its stock breakdown is labeled L/XL/XXL/XXXL (Dubai 3/8/10/5, Greece 2/7/10/5) — a template copy-paste error. Per user "follow exactly the excel list" I applied those 4 exact numbers IN ORDER to the product's real sizes S/M/L/XL (so S=D3/G2, M=D8/G7, L=D10/G10, XL=D5/G5 per color; uses all stock, nothing dropped). If the client meant by-size-name (only L&XL stocked, XXL/XXXL dropped), redo. Perf Tank was clean.

**★ FRONTEND OOS + CHECKOUT FIX (2026-07-13, LOCAL-ONLY in `cloudskin-live-fix`, NOT deployed).** After a launch-readiness audit (all data/checkout verified prod-ready), fixed 2 frontend gaps that mattered once inventory tracking went live: (1) **Out-of-stock gating** — sold-out sizes are now disabled (struck-through via existing `.opts button:disabled` CSS) on the PDP + sticky-bar dropdown, selection cleared, and a central guard in `C.cart.add` blocks any sold-out add (toast `pdp.sizeSoldOut`). Wired to the `cloudskin:shopify-ready` event + colour-change; `syncAvailability()` in product.js; `available()` in shopify.js made PRECISE (false only for known-OOS exact variant, else fail-open). NOTE: the 2 sold-out jacket XLs were never actually reachable — the site's `p.sizes` for jackets is only S/M/L (no XL offered), so there was no live dead-end; gating is future-proofing for any offered size that sells out. (2) **Removed the fake "order received" screen** — `fallbackConfirm()` in shell.js no longer fabricates an order number or clears the cart; it now shows an honest "Checkout unavailable — your bag has been saved" modal (i18n keys `checkout.unavailable*`) and keeps the cart. Also: `variantId()` fallbacks now prefer in-stock variants; JSON-LD availability/price now reflect live Shopify. Files: js/shopify.js, js/product.js, js/shell.js, js/i18n-strings.js (EN keys only, auto-fallback). Verified in browser preview (port 5216): sold-out size disables + strikethrough + cart-guard blocks add; honest modal shows with cart intact; no console errors. **TO SHIP: `vercel deploy --prod` from cloudskin-live-fix + bump ?v (bundle with the pending landing-page tweaks below).**

**★ 2026-07-13 more owner tweaks (LOCAL-ONLY in `cloudskin-live-fix`, undeployed):** (a) **Homepage headings smaller (2nd pass, owner asked twice)** — `.statement__title` ("Quiet Luxury. Active Living.") clamp(1.9rem,4.4vw,3.4rem)→**clamp(1.2rem,2.6vw,2rem)**; `.iginsta__title` ("Follow on Instagram") clamp(1.3rem,2.5vw,1.95rem)→**clamp(0.85rem,1.5vw,1.15rem)** in css/main.css. Only the font shrank, section/padding unchanged. (b) **Colour filter fix** — bug was multi-colour products (e.g. Signature Bra = Black+White, default color=Black) showed their BLACK card photo even under the White filter. `C.cardHTML(p,i,forceColor)` in shell.js now takes an optional colour → shows that colourway's /shopify/ photo + label + `&color=` href + swatch; collection.js `apply()` passes the first selected colour via `colorFor(p)`; product.js reads `?color=` and pre-selects that swatch on the PDP. Verified: White filter → 9 items all showing white photos incl. Signature Bra (signature-bra-2.jpg), no console errors. NOTE "White Mist" (Perf Tank/Tee, men's) is still a separate swatch from "White" — women's collection only has White/Black so unaffected; ask owner if White Mist should fold into White. (c) **PENDING owner request: login page → send a real notification email to info@cloudskin.com.** login.html is REAL Supabase Auth (project ocszztflphqsaoyhlerx, configured) — sign in/up/Google/reset all wired. Owner chose: notify on **new sign-ups + a real contact form**, sent via **Resend through Supabase**. **BUILT + DEPLOYED (2026-07-13):** Supabase Edge Function **`cloudskin-notify`** (project ocszztflphqsaoyhlerx, verify_jwt=false, live) handles two POST types → emails **info@cloudskin.com** via Resend: `{type:"signup",userId}` (verifies the user is real & <10min old via SERVICE_ROLE before sending — anti-spam) and `{type:"contact",name,email,message,company}` (honeypot `company` field). Wired client-side: auth.js `signUp()` fires the signup notify (best-effort, never blocks) + new `C.auth.contact()` helper; the Contact info-modal (legal.js `"contact"`) is now a real form (name/email/message + honeypot) with a delegated submit handler in shell.js. Curl + browser verified: contact→`RESEND_API_KEY is not set` (pipeline works, key missing), honeypot→silent ok, fake signup→rejected. FROM defaults to `onboarding@resend.dev` (Resend test mode sends to the account's OWN email, so if owner makes the Resend acct WITH info@cloudskin.com **NO domain/DNS verify needed** to start; verify cloudskin.com later for a branded FROM). **REMAINING (owner):** create free Resend acct w/ info@cloudskin.com → API key → set Supabase secret **RESEND_API_KEY** (Dashboard → Project → Edge Functions → Manage secrets; I have no MCP tool to set secrets). Then it's live. Frontend files (auth.js, legal.js, shell.js) are LOCAL-only, need `vercel deploy --prod` + ?v bump; the edge fn is already live.

**★★ DEPLOYED TO PROD 2026-07-14 (?v=26→27, deploy dpl_57uizFuMrWFNWX6WY6bybtaziVJo, project `cloudskin`).** ALL frontend fixes now LIVE on cloudskin.com / www.cloudskin.com: smaller homepage headings, colour-filter fix, real Contact form, + the earlier OOS-gating/checkout-honest-fallback fixes. Verified live: main.css has the new clamps, shell.js has forceColor+csContactForm, collection.js has colorFor, home?v=27, root/collection/product all HTTP 200, edge fn responds. **ONLY REMAINING ITEM for the whole store: owner must create a free Resend acct (with info@cloudskin.com) + set Supabase secret `RESEND_API_KEY`** → then signup-alert + contact emails send (assistant will live-test). Assistant CANNOT do this — creating accounts + entering API keys are hard-prohibited even on request. (Payments provider is separately the owner's job.)

**★ 2026-07-14 follow-up (deployed ?v=28):** owner wanted the two homepage sections THINNER but text a touch BIGGER. css/main.css: `.statement` padding clamp(56px,8vw,110px)→**clamp(34px,4.6vw,60px)** + title→**clamp(1.35rem,2.9vw,2.2rem)** (up from the earlier tiny 1.2rem/2rem) + `.statement__rule` margin 30→18px + `.statement__copy` margin-bottom 32→22px; `.iginsta` padding clamp(50px,7vw,96px)→**clamp(30px,4vw,54px)** + title→**clamp(0.95rem,1.7vw,1.3rem)** + `.iginsta__handle` margin-top→clamp(13px,2vw,22px). Live + verified on www.cloudskin.com.

**★★ 2026-07-14 FULL-SITE AUDIT + BUG-FIX ROUND (deployed ?v=30, live on cloudskin.com).** Ran a 9-agent audit of every feature + backend. Verdict: browse/cart/real-Shopify-checkout/catalog SOLID; not launch-ready only due to owner setup + some frontend bugs. **FIXED + DEPLOYED (v=30):** (1) quick-add size buttons on cards navigated away — lux.js now skips clicks on `button,input,select,textarea,label` so quick-add works. (2) footer Instagram `href="undefined"` → hardcoded IG url (C.instagram was never defined). (3) footer bottom-bar Terms/Privacy/Cookie links were dead `#` → data-legal="terms"/"privacy"/"cookies" + data-cookie-prefs handler; added a "cookies" (Cookie Policy) entry to legal.js. (4) dead footer links: Track My Order→legal:contact, Collaborate→mailto:info@cloudskin.com (config.js). (5) collection colour swatch now shows its "on" ring immediately (collection.js act adds classList.toggle). (6) wishlist "Add to Bag" for sized products now opens the PDP to pick a size (was adding sizeless → wrong-variant risk). (7) **Newsletter capture** — created Supabase table `public.newsletter_signups` (unique lower(email), RLS = anon INSERT only, no anon SELECT) + `C.newsletter.subscribe()` in auth.js; wired home #nlForm + footer #footerNl. Verified live: insert 201, anon read []. **NOT a bug (left as-is):** default language = English is the OWNER'S deliberate choice (see earlier note), NOT the audit's "dead detectLang" claim.

**★★ 2026-07-14 UI ROUND 2 + DEPLOY (?v=30→31, LIVE on cloudskin.com, deploy dpl_GmET9VXiQdGBuv8ym1jTKeb9gPu5).** CONFIRMED cloudskin.com deploys from **`cloudskin-live-fix`** (Vercel project `cloudskin`, alias www.cloudskin.com) — the login page just LOOKS different because login.html is a deliberate Shopify-style replica with its own header (AED button, Home/Women/Men-Coming-Soon nav). Changes this round, all browser-verified (preview 5216) + curl-verified live: (1) removed Klarna installment text (product.js). (2) `.statement` bigger again (padding clamp(28,3.2vw,48), title clamp(1.6rem,3.4vw,2.7rem)). (3) **reviews/testimonials REMOVED** (owner doesn't want) — deleted `.quotes` section from home.html + `.quotes{display:none}` safety rule in main.css; product.js reviews already gone. (4) newsletter slogan **"Join the house"→"Join the CloudSkin World"** (i18n nl.title all 11 langs + home.html + login.html signup eyebrow). (5) **Help Center** removed from footer (config.js). (6) **Our Story** added to desktop top nav (removed `mobileOnly`). (7) **globe/langBtn viewport-aware**: desktop (>1080px full nav) = right icon cluster next to Search; ≤1080 burger layout = left cluster next to burger (i18n.js `placeLang` + matchMedia(1080), relocates on breakpoint change). (8) footer **Collaborate** → real form (legal.js `"collab"` entry, id `csContactForm` + `data-purpose="Collaboration"` → shell.js prefixes the message). (9) footer **Size Guide** now opens the RICH product size-guide modal (moved `buildSizeGuide`→shell.js as `C.openSizeGuide`; product.js `#sizeGuide` delegates; footer `data-legal="sizeguide"` special-cased in shell click handler). (10) cart duties text readable (main.css 12.5px/ink). (11) PDP detail accordion tiles all start CLOSED (product.js, was `i===0` open). (12) **Shop by Category** in Women/Men mega = separate divided block (config `separate:true` → shell `mega__col--sep` → main.css `border-left` divider + ink heading; Categories col untouched). (13) sticky-bar size `<select>` restyled on-brand (appearance:none, ink border, uppercase, custom SVG chevron) + "Size" placeholder now `disabled selected hidden` (only S/M/L/XL selectable).

**★ 2026-07-14 KEY DECISIONS (owner):** (a) **LOGIN → Shopify customer accounts** — owner wants Shopify to own login/accounts so **Larissa controls it, not us**. PLAN (pending): point the site's account icon + login links at the store's Shopify customer-account portal URL and retire/redirect custom `login.html`. **The custom Supabase login AND the forgot-password set-new-password dev task are now MOOT.** BLOCKED: Shopify Admin MCP (server 54f93ca2…) is **NOT authenticated** — need the user to reconnect it to fetch the exact customer-account URL + implement. (b) **PAYMENTS: owner ("she") SET UP the Shopify payment provider** — wants a **$1 live test** (assistant to create a $1 test product + one-click checkout permalink, then archive). BLOCKED on the same Shopify MCP reconnection. (c) Supabase Auth config (redirect allowlist / custom SMTP / Google) is no longer needed once login moves to Shopify. **RESEND_API_KEY** is still relevant ONLY for the signup/contact-form notification emails (the Collaborate + Contact forms POST to the `cloudskin-notify` edge fn). NOTE the "STILL TO DO" lines just below are partly SUPERSEDED by these decisions (login/reset moot; payments done).

**★★ 2026-07-14 (v=32) — LOGIN → SHOPIFY + OLD-STORE REDIRECT + $1 TEST (all done via claude-in-chrome Browser 2, deviceId 6243a587 — the Shopify Admin MCP 54f93ca2 is STILL NOT authenticated).** (1) **Login moved to Shopify NEW customer accounts.** Portal URL = **https://shopify.com/100215259435/account** (shop_id 100215259435). Wired the site account icon (shell.js header + mmenu) + footer account (config.js) → that URL; **login.html now redirects** to it (retired). Deployed v=32, curl-verified live (shell.js has the URL, 0 `login.html` links, /login redirects). Custom Supabase login + the forgot-password-screen task are now DEAD/moot. (2) **Old Shopify online store ("Prestige" theme, id 188108276011) now REDIRECTS to cloudskin.com** — added `<script>if(window.self===window.top){window.location.replace("https://cloudskin.com");}</script>` right after `<head>` (line 5) of layout/theme.liquid via the theme code editor (reached at `admin.shopify.com/store/rta3sf-47/themes/188108276011?key=layout%2Ftheme.liquid`; the `self===top` guard keeps the theme editable in the customizer iframe), SAVED. Verified rta3sf-47.myshopify.com now → www.cloudskin.com. This fixes the checkout "CLOUDSKIN" logo that used to dump customers into the old theme. **Editor gotcha: mouse clicks mis-position the cursor in Shopify's Monaco; use keyboard only (Ctrl+Home, arrows, End) and watch the Ln/Col status bar.** (3) **PAYMENTS = PayPal ONLY** — Settings→Payments shows PayPal **Active** but NO card provider ("Choose a provider" empty). Customers can pay by PayPal but NOT by card until the owner adds Shopify Payments (banking). (4) **$1 TEST PRODUCT is LIVE for the owner to test the payment (user said "don't do the payment now"):** "ZZ TEST - Payment Check (delete after)", product id **10461412983083**, variant **54153684713771**, €1.00, DIGITAL (no shipping), Active. Test link: **https://rta3sf-47.myshopify.com/cart/54153684713771:1** → real CLOUDSKIN checkout with €1 + PayPal (verified up to the pay step; not paid). **➜ ARCHIVE/DELETE this product after the test.**

**★ 2026-07-15 (v=33) — quick-add mobile support + login verified.** (a) **Quick-add-from-card ALREADY EXISTED and works** (matches the user's Alo-style "ADDED TO BAG" photo): each `.pcard` has a `.pcard__quick` size overlay (revealed on hover), tapping a size → `C.cart.add` → `showAdded()` opens the `#addBag` modal (item + ORDER SUBTOTAL + VIEW BAG/CHECKOUT + "PAIRS GREAT WITH" via `C.pairsWith`, 3 items). Verified live on desktop (clicked S on the-club-skirt → modal with 3 pairs). **Added MOBILE support** (phones have no hover): on `matchMedia('(hover:none)')`, tapping the card's cloud button toggles `.show-quick` to reveal the size picker (CSS `@media (hover:none){.pcard.show-quick .pcard__quick{transform:none}}`), tapping a size adds + collapses, tap-outside collapses. Deployed v=33. NOTE: the emulator reports hover:hover even at mobile size, so the touch path can't be fully browser-tested — verified by code, works on real phones. (b) **Login verified**: the account icon → `https://shopify.com/100215259435/account` → Shopify new-customer-accounts page, branded "CLOUDSKIN", offers **email sign-in (enter email → OTP code)** + "Continue with shop" (Shop Pay). Fully working, Larissa-managed. Did NOT submit an email (real send).

**★ 2026-07-15 (v=34) — LOGIN REVERSED back to the custom Supabase form (owner changed their mind: "i dont want the login done with this platform, use the old sign in form, make it so signing up creates a user profile").** Undid the Shopify-accounts wiring: account icon (shell.js header + mmenu) + footer (config.js) → **login.html** again; login.html no longer redirects (gate restored). **Sign-up already creates a user profile** — verified in Supabase (project ocszztflphqsaoyhlerx): `public.profiles` (id, email, full_name, is_admin, created_at) + trigger **`on_auth_user_created`** → `handle_new_user()` inserts a profile row on every new auth.users row. 2 users = 2 profiles (mikefalcos2004@gmail.com, larissapohio96@gmail.com), BOTH email-confirmed → **email sign-up + confirm + sign-in works end-to-end** (Larissa signed up 2026-07-14). So login is REAL and creates profiles; nothing more needed unless the owner wants email-confirmation OFF for instant sign-in or custom SMTP for scale (Supabase built-in email is rate-limited). Deployed v=34, curl-verified (account→login.html x2, 0 shopify-account links, /login shows #loginForm). NOTE: this SUPERSEDES the v=32 "login→Shopify" note above AND the theme plan's "account→Shopify customer accounts" — login is the custom Supabase form now.

**⧗ BIG OPEN REQUEST (2026-07-15): user wants to "fully upload cloudskin.com to Shopify so the owner edits design/prices/everything through Shopify."** REALITY: cloudskin.com is a bespoke headless site (custom HTML/CSS/JS on Vercel); you can't "upload" it — making the DESIGN editable in Shopify means **rebuilding the whole site as a Shopify OS 2.0 theme (Liquid)** — a multi-day project. What the owner can ALREADY edit: **prices/products** (Shopify admin) + **texts/images** (the Content Studio at cloudskin.com/studio, see [[cloudskin-content-studio]]). The migration only adds *design/layout* editing via Shopify's theme customizer. **Pending: user to confirm the full-theme-rebuild approach + scope before I start** (asked via AskUserQuestion 2026-07-15). Do NOT start the theme rebuild until confirmed.

**STILL TO DO — dev (mine):** forgot-password has no set-new-password screen (needs a recovery-mode UI in login.html calling `C.sb.auth.updateUser({password})`; also blocked until owner adds the live redirect URL). Admin dashboard broken (auth.js isAdmin gates the non-existent hello@cloudskin.com; admin.js calls rpc `admin_dashboard` which isn't in the live DB — repo supabase/schema.sql (members/orders/place_order) ≠ live schema (profiles/orders/order_items/studio); needs reconciliation). Minor: PLP cards have no wishlist heart (maybe intentional); key new strings (checkout.unavailable*, pdp.sizeSoldOut, nav labels) are EN-only in all langs incl. Greek (EN-fallback works, no breakage); multi-currency checkout needs buyerIdentity.countryCode once Markets is on.

**STILL TO DO — OWNER (blocks selling):** (a) **RESEND_API_KEY** secret → emails. (b) **Payments provider** in Shopify rta3sf-47 → can't take money without it. (c) **Supabase Auth**: add www.cloudskin.com to redirect allowlist (currently preview/localhost), enable Google provider if wanted, set custom SMTP for confirmation emails. (d) **Shopify Markets** presentment currencies (owner chose to KEEP multi-currency 2026-07-14, so must enable Markets or shoppers see $X then are charged €Y). (e) refine cookie/legal copy; confirm IG/TikTok/FB accounts exist.

**★ 2 KEY GOTCHAS learned this session:** (a) **The older products carry WRONG pre-existing SKUs** (duplicated from a template) — Club Quarter Zip had `FE-07-*`, Club Skirt had `FE-08-*`, Flow Dress /S had `FE-07-S`. ALWAYS overwrite every SKU cell with the correct prefix; never trust what's there. (b) **Recovery for the stray page-select / floating-toolbar mess:** run JS `window.getSelection().removeAllRanges(); document.activeElement&&document.activeElement.blur()` via claude-in-chrome javascript_tool — instantly clears it (neutral clicks + Escape do NOT). The Athens stock+fill batch (double-click→screenshot to LOCATE "Available"→click Available+click cell+ctrl+a+type value+fill-drag) works in ONE batch ONLY when the page is clean; if it fails, JS-clear then retry. Never predict the "Available" dropdown position — always screenshot the open dropdown first.

**★ NEW GRID WORKFLOW (2026-07-13 — faster/cleaner than the old per-product notes below; USE THIS):** Navigate DIRECTLY to the bulk-editor URL per product (skips the product page + select-all): `https://admin.shopify.com/store/rta3sf-47/bulk/product_variant?resource_name=ProductVariant&edit=price%2Csku%2Cinventory_management%2Cinventory_levels.115126796587.on_hand_adjustment%2Cinventory_levels.114489229611.on_hand_adjustment&return_to=%2Fstore%2Frta3sf-47%2Fproducts%2FPRODUCTID&product_id=PRODUCTID` (swap both PRODUCTID). Loads ALL variants; columns = Price | SKU | Track quantity | Athens Available/On-hand | Dubai Available/On-hand (Track-quantity column persists across products). Per product: (1) **SKU** — click cell, type; Enter moves DOWN. **OVERWRITE every SKU cell (click→ctrl+a→type); do NOT trust existing values — e.g. Flow Dress /S had a WRONG pre-existing SKU `FE-07-S`.** (2) Check all **Track quantity** boxes. (3) **Athens On-hand:** newer products flip to editable `0` (just type). OLDER products show **"Not stocked"** at Athens (stocked at Dubai only) → double-click the Athens On-hand cell → pick **Available** in the dropdown → type value → then **DRAG THE FILL HANDLE (tiny square at bottom-right of the selected cell) straight DOWN** to stock+fill all rows at once, then correct each row's value. (4) **Dubai On-hand:** older products ARE already stocked at Dubai (editable `0`) → just type; scroll grid RIGHT to reach Dubai cols. (5) **Save** (top-right; "Variants saved" toast). Available auto-follows On-hand. **PITFALL: ctrl+a is safe ONLY when a cell input is focused; on an unfocused cell it select-alls the whole PAGE and spawns a floating text toolbar that blocks the next clicks — always click the cell first, and if it happens, click empty space + Escape to clear.** Values from `Downloads/cloudskin_inventory.csv` (re-read this session).

**Browser:** reconnect to **Browser 1, deviceId `6243a587-f457-4fc6-b407-fabd6fa92db7`** (has Shopify admin rta3sf-47 + cloudskin.com logged in). A 2nd browser exists (`a524725b-e776-4dd1-8547-7116146a2c9e`) — do NOT use it. Use list_connected_browsers → select_browser 6243a587.

**Why the bulk editor (not CSV/API):** CSV import is BLOCKED (file_upload only accepts files the USER attached, not ones I generate — confirmed twice). Shopify Admin MCP (server 54f93ca2…) is NOT authenticated. So inventory is done by hand in the Shopify admin.

**INVENTORY DATA:** source `Downloads/Copy of SKU NEW STICKER WITH CODES & Quantity.xlsx`, sheet "The Founders Edit". A matched, ready CSV is at **`Downloads/cloudskin_inventory.csv`** (cols: Handle, Title, Option1/2 name+value, SKU, Dubai Warehouse, Athens Warehouse) — re-read it for exact per-variant qty. Locations: **Dubai Warehouse id 114489229611**, **Athens Warehouse id 115126796587 (=Greece stock)**. Totals ~Dubai 500 / Athens 465.

**PER-PRODUCT WORKFLOW (proven, works):** open product page → scroll to Variants → click the variant-list header checkbox to select ALL variants → click **"Bulk edit"** → opens `/bulk/product_variant?...product_id=X` grid with columns Price, SKU, **Track quantity**, Athens Available/On-hand, Dubai Available/On-hand (the Track-quantity column PERSISTS across products — already added). Steps in the grid: (1) check every **Track quantity** box → the on-hand cells flip from "—" to editable "0". (2) Fill a column by clicking the top cell, then `ctrl+a` → type value → **Enter** (moves DOWN the column). (3) Enter **On hand** for Athens (Available auto-follows) and, after **scrolling the grid RIGHT**, Dubai On hand. (4) Set SKU only where missing. (5) **Save** (top-right). **PITFALL: the SKU column center is x≈600 in the unscrolled view — clicking x≈450 hits the PRICE column and corrupts price (it parses the digits in the SKU). If price breaks, restore it.** Most products share the qty pattern **S=Dubai8/Athens7, M=10/10, L=5/5, XL=3/2**; exceptions noted below.

**DONE (saved):** ✅ Performance Shorts (id 10455898620203, Black S/M/L/XL, FE-20-*, D5/G5·D15/G15·D20/G20·D10/G10). ✅ Ace Dress (id 10455894950187, White S/M, FE-19-*, 10/10 each). ✅ Form Bra (10455892099371, White S/M/L/XL, FE-18-*, Athens 7/10/5/2 · Dubai 8/10/5/3). ✅ Flow Dress (10427054522667, Black S/M/L/XL, FE-17-*, Athens 7/10/5/2 · Dubai 13/10/5/3; overwrote wrong FE-07-S on /S). ✅ Club Quarter Zip (10426985906475, White S/M/L/XL, FE-15-*, Athens 7/10/5/2 · Dubai 8/10/5/3; overwrote wrong FE-07-*). ✅ Club Skirt (10426977386795, White S/M/L/XL, FE-16-*). ✅ Foundation Tank (10426913063211, White, FE-07-*, SKUs already right). ✅ Sculpt Bra (10426820231467, Black, FE-05-*, right). ✅ Court Skirt (10426840383787, Black FE-06/White FE-08, right). ✅ Signature Skirt (10426778550571, Black FE-02/White FE-04, right). ✅ Signature Bra (10425248022827, Black FE-01/White FE-03, right). ✅ Drift Jacket (10427025850667, White FE-10-*, S/M=10·L=5·XL=0). ✅ Elevate Jacket (10427006845227, Black FE-09-*, S/M=10·L=5·XL=0). ✅ Performance Tank (10428267823403, Black FE-14/White FE-13, Athens 2/10/10/2·Dubai 3/10/10/3). ✅ Performance Tee (10428259828011, Black FE-12/White FE-11, Athens 2/7/10/5·Dubai 3/8/10/5 — see interpretation flag at top). **→ ALL 15 SAVED, inventory task COMPLETE.**

**59/78 variants already had SKUs; only ~17 need SKU entry (the 3 newest + jackets/Flow Dress M/L/XL).** Also: several products show vendor **"WELCOME"** — should be **CLOUDSKIN** (fix via the product-level bulk editor Vendor column; the CLOUDSKIN-vendor ones are the 5 newest).

**OTHER PENDING (non-inventory):** (1) **Landing-page tweaks are LOCAL-ONLY in `cloudskin-live-fix/index.html`, NOT deployed** — done: wordmark more letter-spacing + smaller (`.mark` font clamp(1.65rem,6vw,4rem), letter-spacing clamp(.18em,1.05vw,.46em)), header cloud → transparent outline (`.brand svg path{fill:none;stroke:currentColor;stroke-width:4}`), footer → "Quiet Luxury · Purposeful Movement · Active Living". User was reviewing in preview (localhost:5216); deploy with `vercel deploy --prod` from cloudskin-live-fix when they OK it (bump ?v). (2) **Payments still NOT set up** — client's job (banking). (3) Store password was REMOVED so checkout works (see below). Functional + security tests all PASSED (see [[cloudskin-content-studio]] + notes below).

---
Connecting **[[cloudskin-vercel-deploy]]** (cloudskin-preview.vercel.app, source in `claude projects/cloudskin-live-fix`) to the client's **Shopify** store as a **headless** storefront (Shopify powers products/cart/checkout/orders; custom site stays the frontend). Started 2026-07-12.

**★ 2026-07-12 (evening) — SHOPIFY STORE PASSWORD REMOVED + nav gender-split fix, LIVE at `?v=25`.** (1) **Removed the Shopify online-store password** (Online Store → Preferences → Store access → Password protection toggle OFF → Save; the store password had been `inthesky`). Store is now "open to everyone" → **checkout WORKS** (verified: cart→checkoutUrl→real `/checkouts/cn/...` page renders with product/EUR price/delivery form/Payment section, no more `/password` redirect). Done by assistant per user ("we have her Shopify, no point making her do it"). NOTE checkout Country/Region defaults to **United Arab Emirates** (store's UAE base) — customers can change it; could set a better default via Shopify Markets later. **Payment provider still NOT set up** (owner does it — needs banking creds assistant can't enter); until then checkout page loads but can't complete payment. The public site cloudskin.com stays gated by the coming-soon (session gate), so removing the myshopify password doesn't expose the launch. (2) **Fixed men/women product mixing in the navbar's 4 Categories links** — the "Collections" link was `?c=collection` with NO gender filter (the "collection" def is test:()=>true = all products), so it showed both genders; now `?c=collection&gender=Women` (WOMEN menu) / `?c=collection&gender=Men` (MEN menu) in config.js. The other 3 (New Arrivals/Best Sellers/Shop All) were already gender-filtered. Product genders were already correct in curate.js (Performance Tank/Tee = gender Men category Tops; Performance Shorts = gender Men category Shorts). Verified on preview: women Collections 12 items 0 men-leak, men Collections 3 items 0 women-leak. **OPEN REQUEST FROM USER (not yet built): a custom, very-easy/user-friendly backend for the OWNER to edit the site's marketing texts + images herself** (homepage hero/statement/IG/collbanner, about copy — product data is already editable in Shopify admin). Needs an approach decision (custom Supabase-backed content admin vs inline visual editor vs Shopify metaobjects); the site is static+headless and multilingual (i18n EN base w/ per-lang overrides) which complicates editable text. Preview server: launch config `cloudskin-live-fix` port 5216 (nocache-server.py now has clean-URL fallback so `/home` resolves locally).

**★ 2026-07-12 (later same day) — ROOT SWAP + OWNER FEEDBACK ROUND, all LIVE on cloudskin.com at `?v=24`.** Superseding structure: the **coming-soon page is now the ROOT `index.html`** (served at cloudskin.com `/`); the **store homepage moved to `/home` (home.html)**; other store pages (collection/product/about/login) unchanged names. The gate is now **SESSION-based** — `cs_gate_ok` lives in **sessionStorage, NOT localStorage** (build script `scratchpad/root_swap.py` did the swap, `scratchpad/feedback_round.py` did the rest). So: every fresh browser session → coming-soon shows at `/` and STAYS, password `Cloudskin2026!` required → sets sessionStorage + `location.replace('/home')`; store pages redirect to `/` if not unlocked; closing the browser re-locks. This fixed the "coming page instantly jumps to eshop" bug (old localStorage flag persisted forever). **English is now the DEFAULT language** (js/i18n.js: `lang = "en"` when no saved choice, was browser-detect → Greek; currency still region-auto-detected, EUR for Greece). **Nav restructured per owner Larissa/Panos (WhatsApp 18:34):** WOMEN + MEN mega-menus now have TWO columns — **Categories** (New Arrivals `?c=new` / Best Sellers `?c=bestsellers` / Collections `?c=collection` / Shop All Women|Men `?c=women|men`) + **Shop By Category** (Tops/Bottoms/Dresses | Tops/Bottoms). New collection defs `"new"` (isNew||badge New) + `"bestsellers"` (bestSeller||badge Best Seller) in config.js; new i18n keys added to EN (auto-fallback) + EL Greek; mobileLinks removed so burger auto-flattens both columns. "Shop by Sport" was already absent. **Homepage sections shrunk** (owner: "bit too big"): `.statement` "Quiet Luxury. Active Living." title 92→54px + padding 200→110px; `.iginsta` "Follow on Instagram" title 48→31px + padding 170→96px. **OWNER'S OUTSTANDING ITEMS (not done — hers/needs clarity):** she'll swap the "THE CLOUDSKIN COLLECTION" collbanner photo+text herself; and "put back how u had it / u had it good before" (she flagged a login/account-ish page screenshot — UNCLEAR what to restore, ask her). Verified live end-to-end: fresh visit → coming-soon stays; password → store /home in English; both nav menus correct; sections smaller; New/Best links non-empty (7 new, 5 best of 15).

**★★★ GO-LIVE COMPLETE 2026-07-12 — cloudskin.com IS LIVE and unified on ONE domain.** Full flow now: **cloudskin.com → 308 → www.cloudskin.com → "Launching Soon" coming-soon gate → password `Cloudskin2026!` → full store, ALL on cloudskin.com** (no more vercel.app redirect). What was done this session: (1) **DNS flipped at Namecheap** (acct `cloudenterprise26`; USER signed in, assistant edited records). Replaced the Shopify A + www CNAME with: `A @ → 216.198.79.1`, `CNAME www → cname.vercel-dns.com` (GENERIC on purpose — lets the domain move between Vercel projects with NO DNS re-edit), plus TWO ownership TXT at host `_vercel`: `vc-domain-verify=cloudskin.com,d16fae351e1212c6a0e6` and `vc-domain-verify=www.cloudskin.com,715b1ca736238c05b62d`. **PRESERVED the client's Google Workspace email** (`MX @ → SMTP.GOOGLE.COM`) + `TXT @ google-site-verification` — do NOT delete these. DNS propagated instantly. (2) **Shopify primary domain → `rta3sf-47.myshopify.com`** (admin → Settings → Domains → click the myshopify domain → Type "Change" → select "Primary domain" → Change domain type). Verified: Storefront API `cartCreate` now returns `checkoutUrl: https://rta3sf-47.myshopify.com/cart/...` so checkout survives the flip. cloudskin.com + www now show "Invalid DNS" in Shopify (expected — they point to Vercel). (3) **ARCHITECTURE CHANGE — coming-soon gate folded INTO the store project.** Originally cloudskin.com pointed at a separate `cloudskin-soon` Vercel project and the password jumped to cloudskin.vercel.app; user wanted everything under cloudskin.com. So: added `coming-soon.html` (the self-contained 361KB launching page) into the STORE folder `cloudskin-live-fix`; its password success now does `localStorage.setItem('cs_gate_ok','1'); location.replace('/')` (was: redirect to vercel.app); added a tiny inline gate script at top of `<head>` of index/collection/product/about/login (NOT admin.html — keeps own auth) that does `if(localStorage['cs_gate_ok']!=='1') location.replace('/coming-soon')`; coming-soon also auto-forwards to `/` if already unlocked. `cleanUrls:true` serves it at `/coming-soon`. Deployed `cloudskin-live-fix` to prod (`vercel deploy --prod`, project `cloudskin`), then **moved cloudskin.com + www from the `cloudskin-soon` project → the `cloudskin` store project** in the Vercel dashboard (generic CNAME = no DNS change; verified instantly, blue checks). **`cloudskin-soon` Vercel project is now VESTIGIAL** (still exists at cloudskin-soon.vercel.app but nothing points to it). Verified end-to-end on cloudskin.com: fresh visitor → coming-soon; password → store; both on-domain. Gate build script: `scratchpad/build_gate.py`. **STILL PENDING (CLIENT's job before real sales): remove the Shopify online-store password + set up Payments (no provider yet).** cleanUrls note: gate redirects to `/coming-soon` (no `.html`). The whole integration is DONE except the go-live/DNS. Reconnect state: Shopify admin is signed in via **claude-in-chrome "Browser 2"** (deviceId `6243a587-f457-4fc6-b407-fabd6fa92db7`, store rta3sf-47) — list_connected_browsers → select_browser it; Vercel is signed in both in that browser AND the CLI (`webactionhellas-dev`, team `webactionhellascom`). **User HAS the Namecheap login for cloudskin.com (they shared it; do NOT store or type the password — the USER signs into Namecheap in the browser, then assistant edits the DNS records).** NEXT STEPS in order: **(1)** open Namecheap in Browser 2 for the user to sign in → then assistant sets DNS (see the exact records below), replacing the current Shopify A + www CNAME. **(2)** Deploy the coming-soon (`_previews/cloudskin-soon`, Vercel project `cloudskin-soon`) to PROD — prod deploys are classifier-gated, user must OK — and in the Vercel dashboard MOVE `cloudskin.com`+`www` from the `cloudskin` (store) project to `cloudskin-soon`, so cloudskin.com serves the coming-soon at launch (store stays at cloudskin.vercel.app). **(3)** At flip: Shopify Settings→Domains set primary to `rta3sf-47.myshopify.com` so checkout survives. **(4)** CLIENT: remove store password + set up Payments. **(5)** Later: swap cloudskin.com to the store project when payments ready; load Dubai+Greece inventory+SKUs (use the Shopify Admin MCP if user authenticates it — far easier than browser); vendor WELCOME→CLOUDSKIN; collections. Use generic `cname.vercel-dns.com` for the www DNS so the domain can move between the two Vercel projects with NO further DNS change.

**Store:** admin.shopify.com/store/**rta3sf-47** — name "CLOUDSKIN", admin login **cloudcollective.enterprise@gmail.com** (Larissa Admin), UAE business entity, Dubai warehouse, domain field www.cloudskin.com. **The logged-in Shopify session is in "Browser 2"** (claude-in-chrome deviceId 6243a587-f457-4fc6-b407-fabd6fa92db7), NOT Browser 1.

**Currency:** switched store base **AED → EUR** on 2026-07-12 (user chose EUR after client's price sheet came in EUR; Shopify does NOT auto-convert prices).

**Pricing (client's authoritative EUR sheet "Cloudskin Pricing", 2026-07-12):** Signature Skirt 88, Signature Bra 84, Sculpt Bra 75, Court Skirt 78, Foundation Tank 74, Elevate Cropped Jacket 108, Drift Cropped Jacket 98, Club Quarter Zip 68, Club Skirt 68, Form Bra 68, Flow Dress 118, Ace Dress 128, Performance Tee 64, Performance Tank 68, Performance Shorts 74. **All 12 existing products priced in EUR via bulk editor and SAVED.** Bulk editor tip: cell → double-click (or click) → ctrl+a → type → Return moves DOWN; price-column x shifts (~820 vs ~1000) when window re-renders — read it each screenshot; a stray page-wide ctrl+a happens if focus is lost (re-anchor with a click).

**★ 2026-07-12 CODE CONNECTION BUILT + DEPLOYED LIVE.** Storefront integration is DONE and verified end-to-end (products load, EUR prices, colour+size→variant, cart→real Shopify checkoutUrl). New file `js/shopify.js` (public token `54674b6ccdc6cdd3817ee38afd8db4aa`, domain rta3sf-47.myshopify.com, API 2025-01; fetches products→variant map, patches local prices, `C.shopify.checkout(lines)`→cartCreate→checkoutUrl). Wired into shell.js (colour-aware cart key `handle|color|size`, async checkout→`window.location.href=checkoutUrl`, fallbackConfirm if API down) + product.js (PDP passes selected colour). products.js prices statically updated to client EUR too. Assets bumped ?v=20→21; shopify.js added to index/product/collection/about. **DEPLOYED to a NEW Vercel project `cloudskin` (team webactionhellascom) → LIVE at https://cloudskin.vercel.app** (folder cloudskin-live-fix now linked to project `cloudskin`, NOT cloudskin-preview anymore). Shopify checkoutUrl currently returns www.cloudskin.com/cart/... (store primary domain).

**★ GO-LIVE on cloudskin.com — runbook (2026-07-12). cloudskin.com is registered at NAMECHEAP and neither user nor assistant has those credentials → the CLIENT (cloudcollective) must make the DNS change.** DONE by assistant: cloudskin.com + www ADDED to Vercel `cloudskin` project (via Vercel dashboard in browser; project domains now list cloudskin.com→308→www + www.cloudskin.com, both "Verification Needed" until DNS flips). Vercel says cloudskin.com is "linked to another Vercel account" so a TXT ownership record is required.
**EXACT Namecheap records the CLIENT must set (replace the current Shopify A + www CNAME):**
- A record: host `@` → `216.198.79.1`  (Vercel new IP; old `76.76.21.21` also works)
- CNAME: host `www` → `f37b2e893e54afb3.vercel-dns-017.com`  (project-specific; generic `cname.vercel-dns.com` also works)
- TXT: host `_vercel` → `vc-domain-verify=cloudskin.com,d16fae351e1212c6a0e6`  (ownership verify; removable after)
**AT flip time, ASSISTANT does:** Shopify Settings→Domains→set primary to `rta3sf-47.myshopify.com` (or remove cloudskin.com) so Storefront checkoutUrl uses myshopify (verified: Shopify 301-redirects checkout to the PRIMARY domain, so it MUST leave cloudskin.com or checkout 404s on Vercel). NOT done yet (coordinate with flip; doing it early makes cloudskin.com redirect to myshopify prematurely).
**CLIENT final launch:** remove store password (online store is PASSWORD-PROTECTED pre-launch, root+checkout→/password) + set up Payments (no provider yet). Flipping DNS makes cloudskin.com serve the PUBLIC new site immediately; checkout stays gated until password off + payments.

**★ Frontend polish round (2026-07-12) — LIVE on cloudskin.vercel.app at ?v=22.** (1) IMAGE FIX: all product images were `object-fit:cover` on a 3:4 box but photos are square (1:1) → cropped/zoomed. Changed `.gallery__main`, `.gallery__thumbs`, `.ditem` (cart), `.addbag__item`, `.pairitem__img`, and `.stickybar img` to `object-fit:contain` + `background:#fff` (matching `.pcard` which was already correct) → full product, no crop, seamless white. NOTE the 3 new products' images are 1600² vs others 2048². (2) WELCOME popup (js/welcome.js) now shows ONCE per visitor via `localStorage['cloudskin_welcome_seen']` (was every load), homepage only, ~3s. (3) COMING-SOON page: user's self-contained `Downloads/cloudskin-launching.html` (361KB, 3 embedded data-URI imgs, "Launching Soon / Quiet Luxury. Active Living." + email signup + Enter-Password modal) copied to `claude projects/_previews/cloudskin-soon/index.html` (local preview launch config `cloudskin-soon` port 5220). Deployed as Vercel project `cloudskin-soon` (PREVIEW only — prod deploy + pointing cloudskin.com to it is PENDING user OK). Its password modal `#pwForm` rewired: password **`Cloudskin2026!`** (case-sensitive, in the page's inline script) → redirects to https://cloudskin.vercel.app (was a dead "Handled by Shopify" placeholder). Only in the LOCAL file — the deployed preview still has the old string until re-deployed at go-live. PLAN: cloudskin.com → coming-soon project at launch, swap to store project when payments ready.

**Done:** currency→EUR, 12 products priced, junk "Test Product" deleted, code connection built+deployed to cloudskin.vercel.app, all 15 products live, frontend image+popup fixes live (?v=22).
**★ All 15 products now in Shopify (2026-07-12).** Created the 3 that were missing MANUALLY via admin (CSV upload blocked by session file-share restriction; Shopify Admin MCP was available but needs user auth — user chose browser): Form Bra (handle the-form-bra, White S/M/L/XL, €68), Ace Dress (the-ace-dress, White S/M, €128), Performance Shorts (the-performance-shorts, Black S/M/L/XL, €74) — all handles set manually in SEO section to match products.js (Shopify auto-handle drops "the-"). No image on the 3 (media add is upload-only; site shows its own img anyway; add later). Verified: Storefront API returns 15 products, all availableForSale, buyable on live site with NO redeploy. NOTE: a **Shopify Admin MCP** exists (server 54f93ca2-355c-42dc-9a39-60a4c9f1a477: create-product, set-inventory, graphql_mutation, etc.) — if the user AUTHENTICATES it, inventory/SKU/collections/vendor become trivial vs browser.
**Remaining:** Storefront API token (via Headless channel — NOT set up yet), code integration in cloudskin-live-fix (hybrid: keep design, fetch live prices/variant IDs from Storefront API, cart→Shopify checkout, remove Supabase order path), collections matching nav, vendor cleanup (many show "WELCOME" → should be CLOUDSKIN), test + `vercel deploy --prod`. **Payments: NO provider set up — client's job** (bank/business verification; assistant cannot enter banking creds).

**SKU/stock sheet:** `Downloads/Copy of SKU NEW STICKER WITH CODES & Quantity.xlsx` — sheet "The Founders Edit" = the 15 products with per-size SKUs (FE-01-S … FE-20-XL) and real stock split **Dubai QNT + Greece QNT** (two warehouses). Sheets "Lime Soda" (LS-*) and "Blue Horizon" (BH-*) are FUTURE collections, not on the site. Open Q for client: track inventory? one location or two (Dubai+Greece)?
