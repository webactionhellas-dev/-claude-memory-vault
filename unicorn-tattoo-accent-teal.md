---
name: unicorn-tattoo-accent-teal
description: "Unicorn Tattoo's \"violet\" Tailwind token is actually petrol-teal"
metadata: 
  node_type: memory
  type: project
  originSessionId: 67eaeddd-0ee3-481f-8509-123812466636
---

In `C:\Users\mikef\unicorn-tattoo\tailwind.config.ts` the brand accent tokens are DELIBERATELY misnamed: `violet: '#2E96B0'` and `magenta: '#46B7CE'` are petrol/teal blues from the studio's logo crest. The config comment says the names were kept as violet/magenta only so existing classes wouldn't need renaming. `accent.DEFAULT #2E96B0`, `accent.bright #46B7CE`; `::selection` = #46B7CE, `:focus-visible` outline = #2E96B0.

So the site's single accent is TEAL, not violet. The Zeus storm's lightning is a slightly different blue (cornflower/periwinkle ~215° vs brand teal ~192°) — that is the ONLY real color tension, and the fix is to retint the lightning's *glow/falloff* toward the teal while keeping the bolt core white-hot (do NOT tint the core). Do not describe or "reconcile" a violet-vs-blue clash — there is only one accent.

Source: `zeus-storm-integration` design workflow (2026-07-11) — its ranked plan for making the storm feel integrated: (1) retint lightning to teal [top], (2) caption+credit the real Zeus tattoo [need artist name, don't fabricate — else credit "Unicorn Tattoo, Glyfada"], (3) tame the 5-8s ambient strike cadence, (4) resolve the storm on scroll-exit into FeaturedWork; optional: whisper cool-grade the warm arm photo, deep-link caption to portfolio, one afterglow ember downstream, dial cloud/flash brightness. Relates to [[unicorn-tattoo-site]] and [[nami-design-standards]] (Gate J data fidelity: no heavy filter on the real tattoo photo).

BUILT 2026-07-11 (moves 1,2,4; user said "build all except #3" so the ambient strike cadence was LEFT as-is): (1) RETINT - ZeusLightning.tsx photo-light wash rgba(196,218,255)/(128,168,220)->rgba(206,236,246)/(120,190,210); cloud glow rgba(215,232,255)/(150,190,240)->rgba(220,242,250)/(130,196,216); boltBase got `filter:'hue-rotate(-16deg) saturate(0.92)'` to nudge the sprite halo teal (core stays white). (2) CAPTION - new i18n key intro.credit ("Zeus · black & grey · Unicorn Tattoo" / el "Ζευς · μαύρο & γκρι · Unicorn Tattoo"), rendered as a static `<p className="eyebrow mt-6 flex items-center gap-3">` + hairline tick below the photo in IntroTeaser (studio credited, NO fabricated artist name); tattoo-13 alt fixed to a real descriptor. (4) EXIT-RELEASE - added a 2nd useScroll on the same ref offset ['end center','end start'] -> cloudExit useTransform([0.2,1],[1,0.4]); cloudOpacity is now `useTransform(()=>cloudEntry.get()*cloudExit.get())` so the storm resolves to 0.4x as the section exits up into FeaturedWork (entry-lock + LOCK_T detection untouched; max still 0.78). VERIFIED live: caption text+tick+uppercase, alt, 0 console errors, fresh build serving. NOT verifiable live this session: the strike's teal color + exit scrub feel - pane tab was hidden (strikes gate on document.hidden) AND framer scroll broken; changes are build-clean + simple, confirm visually. CLOUD REMOVED - "BACKLIT CHARGE" (2026-07-11): user decided the cloud was unnecessary; wants ONLY the electro strike, coming "from the background to the Zeus image". ZeusLightning.tsx fully rewritten: NO cloud (removed cloud img/float/interior-glow/scroll-scrub cloudEntry+cloudExit+cloudOpacity/useMotionValueEvent lock/all framer-motion imports/CLOUD_WRAP+CLOUD_SRC). New effect = 3 keyed layers per strike: (1) BACKLIGHT `-inset-[18%]` teal radial screen-blend blooming out of the dark BEHIND/around the statue, (2) FULL-IMAGE PULSE (whole photo flashes, feathered by PHOTO_MASK, teal-white radial), (3) the 3-sprite crackle bolts (top:-6% height:80%, hue-rotate teal). Triggers simplified: IO entry (threshold 0.35) fires a strike on EVERY entry/re-entry + starts ambient loop (5-8s) + pointerenter hover strike; reduced-motion = no strikes. Removed `zeusFloat` keyframe + `.zeus-float` from globals.css. cloud.webp now unused (left on disk). Build clean, 0 console errors; backlight/pulse NOT verified live (pane strikes gate on document.hidden=true + framer scroll dead). User picked this "backlit charge" over "bolt from beyond frame" / "rim-electrify" via AskUserQuestion.
GALLERY LIGHTBOX = EXPANDING STRIP (2026-07-11): user pasted a 21st.dev expanding-hover-strip component; wants the PORTFOLIO lightbox (open a photo) to browse via that strip. Adapted (NOT copied - dropped Poppins/Unsplash/slate) into Gallery.tsx lightbox: replaced single-image+arrows with a horizontal `overflow-x-auto` strip of all `shown` photos; each button `flex-shrink-0` collapses to `w-[9vw] min-w-[52px] max-w-[104px]`, the OPEN one (`(hovered ?? index)===i`) expands to `w-[64vw] max-w-3xl` via `transition-[width] duration-500 ease-ink`; hover a sliver -> it expands (setHovered), leave the row -> falls back to the opened index; collapsed slivers dimmed `bg-ink/50`; counter `(hovered??index)+1 / N`; opened item scrollIntoView-centered on open/arrow-nav (activeRef); keyboard arrows still nav (clear hovered). VERIFIED live: 48 thumbs, click opens modal, opened item = 768px wide + exactly 1 wide + others collapsed, counter "3 / 48". HOVER-expand NOT verifiable headlessly (React synthetic mouseenter/mouseover dispatch doesn't fire onMouseEnter; real-cursor hover needs a screenshot which times out) - logic is standard React, test with a real mouse.
GALLERY REVERTED (2026-07-11): user disliked the expanding-strip preview - REVERTED Gallery.tsx lightbox to the previous single-image + prev/next arrows + counter, but added a SMOOTH change: new `lightboxIn` keyframe (globals.css: opacity 0->1 + scale 1.035->1) applied `animate-[lightboxIn_0.5s_cubic-bezier(0.22,1,0.36,1)]` to the keyed image. Removed hovered/activeRef/scrollIntoView/useRef. VERIFIED: single image 795px, no strip, arrows back, counter "3/48", smooth anim class present.
ZEUS PHOTO SMALLER + GAP (2026-07-11): IntroTeaser photo wrapper `<div ref={photoRef}>` got `md:ml-auto md:max-w-[340px] lg:max-w-[380px]` (was full col-span-5 ~500px -> now 380px, right-aligned); section `sm:py-28` -> `sm:pt-28 sm:pb-14` (bottom gap 112->56px). VERIFIED at 1280: photo 380px, pt 112 / pb 56.
DIAGONAL BACKGROUND STORM BUILT (2026-07-11): user chose the diagonal-background direction + said the photo got too small. ZeusLightning.tsx rewritten AGAIN: now a SECTION-WIDE diagonal storm (no longer over the photo). Root `absolute inset-0 z-0 overflow-hidden`, rendered as a DIRECT child of the #intro <section> (moved out of the photo wrapper); IntroTeaser wraps kicker+grid in `<div className="relative z-[1]">` so all content paints above the storm. On strike: (1) soft teal flash `radial-gradient(...at 72% 22%...)` lighting the whole bg from the top-right, (2) two ElectroStrike bolts rotated `rotate(38deg)` / `rotate(42deg) scaleX(-1)`, height 155%/150%, centered (left-58%/40%, top-1/2, translate -50%/-50%), DIAG_FILTER teal hue-rotate - raking top-right -> bottom-left, clipped by overflow-hidden. Triggers: IO entry(0.35)+re-entry + ambient 5-8s; no hover; reduced-motion none. Photo bumped `md:max-w-[400px] lg:max-w-[440px]` (was 340/380 = too small); section stays `sm:pt-28 sm:pb-14`. VERIFIED structure @1280: storm = direct child of #intro, class `absolute inset-0 z-0 overflow-hidden`, content wrapper z-1, storm z-0, photo 440px, ZeusLightning NOT in photo wrapper, 0 console errors, build clean. NOT verified live: the diagonal bolts/flash firing + the exact diagonal DIRECTION (rotate 38deg SHOULD be top-right->bottom-left per rotation math, but tab hidden=strikes gated) - confirm visually; if the diagonal is mirrored, flip the rotate signs (38->-38 / 42->-42). Positions (left-58/40%, height 155%) are tunable.
STORM MOVED BEHIND THE PHOTO (2026-07-11): user said the section-wide storm read "in front of pictures" + wanted the diagonal strikes BEHIND the Zeus photo, sized right. Reverted IntroTeaser (removed the section-level `<ZeusLightning/>` + the `relative z-[1]` content wrapper). ZeusLightning is now a child of the photo wrapper `<div ref={photoRef}>`, root `absolute -inset-[22%] z-0 overflow-hidden` (extends 22% past the photo so bolts rake behind AND around it), and the photo's masked aspect box got `relative z-[1]` so the PHOTO sits in FRONT of the storm. Same diagonal bolts (rotate 38/42, teal) + a teal backlight radial; sized to the photo area now (height 132/128%, left-56/44%). VERIFIED @1280: storm behind photo (child of photo wrapper, z-0, DOM-before photo box), photoBox z-1, no section-level storm, 0 console errors. Photo still 440px. Direction/size still not seen firing (tab hidden) - confirm visually; flip rotate signs if mirrored.
HERO POSTER REMOVED (2026-07-11): user wanted NO poster before the video, video plays immediately, no low-power freeze "like drip astro". HeroVideo.tsx: removed the `<img hero-poster>` overlay + the `filmVisible` useState + the reveal()/'playing'/'timeupdate' listeners; kept ALL the autoplay hardening (muted/loop/playsInline/autoPlay, play() re-assert on ready/pause/visibility/pageshow/focus + persistent gesture kicks + IO re-assert, never pause off-screen). `bg-ink` on the container covers the split-second before the first frame (no white/black flash, no poster). Also removed the `<link rel=preload hero-poster.jpg>` from app/[locale]/page.tsx. VERIFIED: 0 poster imgs, video autoplay/muted/loop/playsInline all true, readyState 4, reached currentTime 12.6s (plays; only pauses when the preview TAB is backgrounded, resumes on focus via onVisible). hero-poster.jpg asset now unused on disk.
INTRO CHOREOGRAPHED ENTRANCE (2026-07-11): user wants the studio TEXT (left) to reveal first, slower+smooth, THEN the Zeus photo appears WITH the electro strike; + photo a bit bigger/higher; + credit caption centered UNDER the photo. Added a `duration` prop to Reveal.tsx (default 0.9; was hardcoded). IntroTeaser left reveals slowed to duration 1.1-1.2 + staggered delays (kicker 0, heading 0.18, stat 0.4, body 0.6, cta 0.8); PHOTO Reveal `delay={1.3} duration={1.2}` (appears after the text) + `md:-mt-10` (higher). ZeusLightning arrival strike now DELAYED 2400ms after IO entry (was immediate) so it cracks once the photo has revealed; ambient starts from there. Photo `md:max-w-[440px] lg:max-w-[480px]` (was 400/440; ~448 at 1280, cell-limited, up to 480 wider). Caption `<p>` got `md:ml-auto md:max-w-[440px] lg:max-w-[480px]` so its centered text sits centered UNDER the right-aligned photo (verified captionCenterX==photoCenterX==983). VERIFIED @1280: photoReveal dur 1.2 delay 1.3 mt -40px, stat dur 1.2 delay 0.4, photo 448px, caption centered under photo, 0 console errors, build clean. Sequence timings NOT watched firing (pane can't observe the ~2.5s reveal + hidden tab gates the strike) - all delays/durations verified in computed styles; tune delays if the photo should come strictly after ALL left content (bump photo delay 1.3->~2.0).
SINGLE STRIKE + FROZEN SECOND STRIPE (2026-07-11): user wants the strike to fire ONCE at the photo's appearance (no ambient), and the SECOND of the two stripes to FREEZE (stay lit behind the photo). ZeusLightning effect rewritten to a single-fire: IO (threshold 0.05 + rootMargin '0px 0px -12% 0px' to MATCH the Reveal component's trigger point) -> setTimeout 1300ms (= the photo Reveal's delay, so photo+stripes materialise together) -> setStrike(1) ONCE (fired flag, io.disconnect, no ambient/tryFire/visible/last). b1 = zeus-stroke-1 (flashes+fades). b2 className zeus-stroke-2 -> `zeus-freeze`: new globals.css `@keyframes zeusFreeze` (0->1 strike, flicker, settle to 0.55) + `.zeus-freeze{animation:zeusFreeze 1.3s linear forwards}` - `forwards` HOLDS 0.55 so the 2nd stripe stays lit behind the photo permanently. flash still fades. VERIFIED: zeusFreeze keyframe registered, .zeus-freeze animation has fill-mode forwards, build clean, 0 console errors. NOT verified firing: the pane tab is hidden so IntersectionObserver never triggers there (can't watch the strike/freeze) - logic is straightforward, confirm in a real browser. If the frozen stripe is too faint/strong tune the 0.55; if the strike should be after the photo fully lands (not during its fade) bump the 1300ms.
SYNC (DECOUPLE STORM FROM FADE) + SLOWER STRIKE + CENTER-GAP FIX (2026-07-11): user: only ONE stripe was visible (the frozen one) because firing during the photo's opacity-fade made the FIRST stripe invisible; wants both stripes + 2nd frozen, photo+stripes to appear TOGETHER, strike a bit slower; AND English layout had a big center gap (text crammed left). FIXES: (1) DECOUPLED the storm from the photo's reveal-fade - IntroTeaser photo column restructured: outer `<div md:col-span-6 md:-mt-10>` > `<div ref={photoRef} relative ml-auto max-w>` > `<ZeusLightning/>` (NOT faded) + `<Reveal z-[1]>`(ONLY the photo fades). So the strike is full-brightness regardless of the photo's opacity -> both stripes visible. Strike delay 2400->1700ms (fires as the photo appears). (2) SLOWED the strike: globals.css `.zeus-flash` 1.1->1.5s, `.zeus-stroke-1/crackle/stroke-2` 1.1->1.5s, `.zeus-freeze` 1.3->1.9s. (3) CENTER GAP: measured EN gap body-right(512) to photo-left(759) = 247px (text max-w-md + photo far-right col-span-5). Rebalanced grid to col-span-6/6 (was 7/5); body `max-w-md`->`max-w-lg`; photo `max-w-[480px] lg:max-w-[520px]` (bigger, 520 @1280); caption max-w matched + is now a `<Reveal as="p" delay=1.5>`. Narrower text col pushed the heading to 3 lines -> reduced heading clamp `2rem,5vw,4.25rem`->`1.9rem,4.4vw,3.7rem` (56px @1280) to keep it 2 lines. VERIFIED @1280 EN: heading 2 lines/56px, photo 520px, centerGap 247->111, storm z-0 decoupled (not in the photo Reveal), 0 console errors, build clean. NOT watched firing (pane tab hidden gates IO/strike) - confirm the together-appearance + both-stripes + freeze in a real browser; strike delay 1700 / slow durations / frozen 0.55 all tunable. NOTE: Greek heading is longer so may be 3 lines at col-span-6 (user asked re English).
SYNCED ENTRANCE - LEFT+PHOTO TOGETHER (2026-07-11): user changed their mind - no longer wants the left text to reveal BEFORE the photo; wants left studio text + Zeus photo/effect to appear at the SAME TIME, one smooth entrance. Retimed IntroTeaser Reveal delays (all clustered, was photo trailing at 1.3s): heading 0.12, photo 0.2, stat 0.28, body 0.42, cta 0.56, caption 0.6 (durations 1.1-1.2 kept for smoothness); ZeusLightning strike delay 1700->900ms (fires with them). Verified delays via computed transitionDelay. Build clean, 0 errors. NOT watched firing (pane tab hidden).
CTA SAMURAI KEPT: user considered changing the CTAFooter background (`BACKGROUNDS.cta = /images/bg/cta.jpg`, a dark samurai-warrior realism piece they called "creepy") - offered petros-03 rose etc., but user REVERTED ("I liked the first samurai"). No change made - keep the samurai.
ARTIST GLOW (2026-07-11): home ArtistsStrip only (not /artists detail pages - different component) - each portrait card got a teal backlight `<div aria-hidden absolute -inset-4 opacity-40 blur-2xl group-hover:opacity-90>` radial rgba(70,183,206,0.34), placed BEFORE the `relative` Link so it sits behind. Verified: 4 cards, opacity 0.4, blur(40px), teal, behind link.

STRIKE ON EVERY RE-LOCK (2026-07-11): user wanted a strike each time the cloud re-locks (scroll up off it, back down). The old lock listener only re-armed after cloudOpacity dropped <0.4, so a partial scroll-up (opacity stayed 0.4-0.72) then down did NOT re-fire. Replaced with UPWARD-CROSSING detection: `useMotionValueEvent(cloudOpacity)` fires whenever prevOp < LOCK_T(0.72) && v >= LOCK_T, with a 700ms cooldown (lastLockRef) to de-jitter; removed lockedRef, added prevOpRef + lastLockRef. Fires on every re-lock now. Build clean; NOT verified live (pane framer-scroll stuck).

Optional un-built moves still on the table: whisper cool-grade the photo, deep-link caption to portfolio, one afterglow ember into FeaturedWork, dial cloud/flash brightness.
