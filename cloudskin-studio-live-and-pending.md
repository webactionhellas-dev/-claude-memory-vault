---
name: cloudskin-studio-live-and-pending
description: "HANDOFF (2026-08-03) — 5 frontend flash/shade bugs fixed+deployed to prod; DHL zone shipping + custom Stripe checkout + PayPal rail all DEPLOYED+LIVE+VERIFIED end-to-end (checkout also works fine via Shopify's own hosted checkout regardless); STORE_BASE_CURRENCY stale-secret bug found+fixed; full status for Mike's Larissa meeting"
metadata: 
  node_type: memory
  type: project
  originSessionId: 75ea6c2d-9aa2-4169-ad82-275f54c1b095
  modified: 2026-08-03T15:06:40.715Z
---

Continuation handoff for CloudSkin (see [[cloudskin-site]], [[cloudskin-gate-vs-storefront]], [[cloudskin-studio-colours]], [[cloudskin-image-transform-gotchas]], [[verify-visual-bugs-with-evidence]]).

## ▶▶▶▶▶▶▶▶▶▶▶▶▶▶ LATEST-14 — 2026-08-03 (pre-launch payment/DHL audit; hosted Stripe checkout now DEFAULT; real security hole found+closed; FRESH SESSIONS START HERE, supersedes LATEST-13.)
**Trigger:** Larissa launching soon; Mike wanted "everything end to end" covered. Delegated the heavy fix/deploy work to the **corky** agent across several rounds (background agent, resumed via SendMessage each time), with every claim independently re-verified directly by the main session before being reported to Mike — do that same verify-don't-trust pattern next time, it caught real gaps.

- **Payment bug #1, FIXED+verified:** `create-checkout-elements` (embedded Stripe rail) 502'd on every call — `_shared/stripe.ts` pinned `STRIPE_API_VERSION='2025-01-27.acacia'`, too old for `ui_mode:'elements'` (Stripe's own error named the min as `2026-03-25.dahlia`). Bumped, redeployed, verified via curl (real `clientSecret` returned, 200).
- **Payment bug #2, found then made MOOT by a direction change:** even after bug #1's fix, the embedded Payment Element still silently fell back to Stripe's hosted redirect on real browser tests — root cause: `stripe.initCheckoutElementsSdk()` at the `basil` Stripe.js preview generation requires `fetchClientSecret` (callback), not the `clientSecret` string the code passed (that shape is only valid at the newer `clover` generation). ALSO found the embedded flow has a real race condition: `js/checkout-page.js`'s `boot()` never awaits `C.shopify.ready` before checking cart lines (unlike `js/checkout.js`'s hosted path, which correctly does `await C.shopify.ready`) — on a cold/slow load this intermittently produces `no-resolvable-lines` even for a cart that resolved fine moments earlier.
- **MIKE'S DECISION (2026-08-03): hosted Stripe Checkout is now the DEFAULT/primary card rail, not embedded.** Reasoning: Stripe owns 100% of that page's layout (postal code etc. guaranteed correct — the embedded Address Element was ALSO found visually clipping city/postal-code fields, container-sizing bug, never fully root-caused since this decision made it moot). One-line change in `js/checkout-page.js` `maybeInitPayments()`: calls `fallbackToHosted()` directly instead of `initStripe()` (both functions left fully intact in the file, just not the default entry point). PayPal unaffected, mounts independently either way. Backend is 100% shared either way (`_shared/checkout-session.ts` `prepareCheckout()`/`buildSessionParams()`, same DHL zone shipping, same order creation) — this was purely a frontend routing decision. VERIFIED LIVE independently (not just Corky's word): fresh `/checkout`, Greece selected, hosted "Continue to secure card payment" button shows IMMEDIATELY (0 payment iframes, no `#coStripe.is-ready`), numbers match exactly what the page showed (€84/€117/€201), clicking through lands on real `checkout.stripe.com` with identical amounts.
- **REAL SECURITY HOLE found + closed:** three live Supabase edge functions had NO real access control — `test-bump-price` (any caller could rewrite the live price of a real Shopify variant), `reprice-aed` (a completed one-time July-19 currency-migration script, still live, would have re-bulk-priced 15 real catalog products on a bare POST), `temp-fix-and-check-20260803` (a completed customs backfill, still had real PATCH-write access). All three (plus a 4th, already-completed diagnostic `temp-recheck-20260803`) neutralized to `410 Gone`. **Lesson (Corky's own, worth keeping): Supabase `verify_jwt:true` is NOT real access control by itself — the anon key is public, sitting in the site's own JS.** Independently re-verified: all 4 return `410` even WITH the real anon key.
- **`register-product-webhook` also fixed:** its `?go=REGISTER`/`?go=RESET` mutation branches had the same weak-gate pattern (low blast radius — webhook subscription state only, not money — but fixed anyway per "fully closed" instruction). Now `403` on mutation attempts; read-only status check still works.
- **`/blog/hello-world` 404 fixed:** `vercel.json` rewrite destinations (`/blog/:slug`) had a `.html` extension while `cleanUrls:true` was also on — classic Vercel incompatibility. Fixed (extensionless destinations), verified `200` live. NOTE: the real WordPress Journal backend (`cloudskin-journal-13d6d4e.ingress-haven.ewp.live`) currently has **zero published posts** — `sitemap.xml` references a "hello-world" post that doesn't exist yet; that's a content decision for Larissa/Mike, not a bug.
- **Promo code field hidden by default:** was unconditionally visible to every visitor once the embedded checkout started working (inviting code-guessing). Now collapsed behind a "Have a promo code?" toggle, `wirePromo()` logic untouched. (Moot for now since hosted-Stripe's own promo UI is Stripe's, not ours — but harmless either way, left in.)
- **DHL stuck-order watchdog fixed for real spam:** it had ZERO memory of prior alerts — was re-emailing the SAME stuck order EVERY 4h run forever (order #1004 alone would've sent ~17+ duplicates by the time this was caught). Added a `dhl_watchdog_notifications` table (migration `dhl_watchdog_notification_dedup`) + de-dup logic: alerts once per order, then stays quiet for `RENOTIFY_HOURS` (default 24) before reminding again if still unresolved. Verified live: 2 consecutive invocations, 2nd correctly suppressed (`emailSent:false`).
- **Apple Pay/Google Pay/Link — a false alarm, corrected:** initially thought missing (old-style `.well-known/apple-developer-merchantid-domain-association` file 404s), but that's the WRONG check — this account uses Stripe's newer `payment_method_domains` API (file-less). Queried it directly: `apple_pay`, `google_pay`, `link`, `klarna`, `amazon_pay` all show `"status":"active"` on both `cloudskin.com` and `www.cloudskin.com`, registered back on `created:1785264398` (earlier in this same build history). **Nothing to do here — don't re-flag this as a gap.**
- **Real per-product weight data DOES exist in Shopify (78/79 variants), just not wired into shipping calc yet** — checked exhaustively via Admin API (`variants[].grams`), not a 5-item sample: e.g. Ace Dress 350g, Performance Shorts 100g, Signature Skirt 250-300g; only `Test Product` ($1 dummy SKU) is 0. Our shipping calc currently ignores this and estimates weight from cart item count (`estimateWeightKg()` in `_shared/dhl-rates.ts`) — this is exactly the documented "PHASE 2" gap in that file's own header comments. **Sent to Corky to wire up (in progress, not yet returned as of this note)**: thread real weight through `_shared/shopify.ts` → `_shared/pricing.ts` → `computeShippingByZone()`, extend `DHL_RATE_AED_MAJOR`'s weight brackets (currently only 0.5kg/1kg transcribed from the source PDF). CAVEAT flagged to Mike: all 78 real weights are suspiciously round (100/150/200/250/300/350g, clean 50g steps) — likely rough estimates, not precise measurements; asking Larissa to confirm/correct, but NOT blocking the wire-up fix on her answer (rounded-real beats the current flat itemCount guess either way).
- **HS code / country-of-origin gap — INITIALLY thought confirmed, then DISPROVEN as a self-inflicted audit bug, corrected same session (~30 min later).** First pass claimed 29/79 variants missing HS+origin. Root cause of THAT claim: the audit's `inventory_items.json?ids=...` batch call requested 100 IDs/batch without checking whether Shopify's response actually returned 100 — the endpoint silently caps at 50, so the first batch dropped ~29 variants and "not returned" got misread as "missing." Re-ran properly (50/batch + requested-vs-returned count check): **all 79 variants fetched, 78 real ones have complete weight+HS+origin, only fake Test Product has nothing.** Verified further via direct single-item GET showing `updated_at` timestamps from **2026-07-31** (days before either audit ran) — nothing needed fixing, ever. A backfill script written off the false premise made ZERO writes (its own idempotency check correctly found everything already complete) — confirmed via direct GET after, no bad data went in. **The false claim was caught and corrected before reaching Larissa** (her message only ever had the weight/rate-card asks). [[cloudskin-dhl-noitems-fix]] updated with the correction — that file's HS/origin section is now DISPROVEN, not confirmed; the original "No Items" DHL complaint's root cause is genuinely still open. **LESSON for every future paginated/batched Shopify Admin API call: check requested-count vs returned-count, every time — a silently truncated response is indistinguishable from a null field otherwise.**
- **Order #1004 (Kim Thomson, 651 AED, ships to Labrador QLD Australia) — checked in detail, still stuck as of this session.** A fulfillment WAS started in Shopify on 2026-07-31 but cancelled ~90 min later with **no tracking number/carrier/URL ever attached** — strong evidence it was never actually packed and handed to DHL, not a real shipment that got cancelled by mistake. `fulfillment_orders` for this order is empty (`[]`), which is itself unusual and may be contributing. Real physical fulfillment (pack + book DHL pickup + get a real waybill) is needed — this is NOT something fixable by "clicking fulfill again," that would falsely tell the customer it shipped. Message drafted for Larissa covering this + the real DHL rate card ask + the weight-accuracy-confirmation ask (see this file's companion — not saved as a separate memory, ask Mike if he still has it or re-derive from this note).
- **Deploy-safety discipline that MUST carry forward:** none of the local source mirrors on this machine (`cloudskin-v67`, `cloudskin-live-mirror`, `cloudskin-live-fix`, `cloudskin-current`, `cloudskin-new`, `cloudskin-preview`, `cloudskin-site`, `cloudskin-store`, `cloudskin-theme`, `cloudskin-epitelous`) contain `js/checkout-page.js`/`js/checkout-paypal.js`/the `/checkout` route AT ALL — the entire embedded-Stripe/unified-checkout system was built+deployed in a session whose source was never saved back to this machine, and `cloudskin-live-mirror`'s own CSP was confirmed stale (missing `js.stripe.com`/`*.paypal.com` etc. that ARE in the live CSP). **Running any local folder's existing `deploy.mjs` as-is would revert the live site.** The safe method used successfully this session (repeatedly): pull the EXACT live source directly from Vercel's Deployment Files API (not a local folder) into a fresh mirror — this session's copy lives at `C:\Users\nospa\claude projects\cloudskin-live-exact` — apply only the intended diff, `vercel deploy --prod` from there, byte-diff-verify after. Do this again next time rather than trusting any named local folder.
- **Everything above independently re-verified by the main session, not just taken on the subagent's word** — real curl/browser checks after each round (this is the second session in a row where that discipline caught something worth catching; keep doing it).

## ▶▶▶▶▶▶▶▶▶▶▶▶▶ LATEST-13 — 2026-08-03 (same session as LATEST-12 below, continued: cart-reveal bug fixed, cloud icon converted to a real wishlist white/black + toast in 11 languages. ALL deployed to prod. FRESH SESSIONS START HERE.)
- **SYNC WARNING: this exact file reverted mid-session at least twice tonight** (once back to the 2026-08-01 state, once all the way back to 2026-07-17/07-23) and was caught actively flip-flopping between states while this entry was being written. Root cause suspected: `git pull` resets a file's mtime to "now" regardless of actual content age, so the vault mirror's newer-file-wins logic can push stale content over a genuinely newer local edit. NOT fixed tonight - no time before Mike left. If this file looks like it's missing recent work, don't assume it's accurate; check `git log` in `C:\Users\mikef\obsidian-vault` for the real history, and re-verify against production directly (curl/deploy IDs below) before trusting a claim here.
- **Cart quick-add reveal bug — FIXED, then SUPERSEDED by the wishlist change right below.** The on-card size-picker reveal depended on a CSS `@media(hover:none)` match that wasn't independently verifiable from this dev environment (no real touch hardware). Fixed by setting the reveal directly via inline style instead of trusting the CSS media query alone. Kept here for the lesson (two systems assumed to agree on a media feature, untested) even though the mechanism itself no longer exists post-wishlist-conversion.
- **Cloud icon converted from cart quick-add to a real WISHLIST — Mike's explicit call, DEPLOYED + VERIFIED.** He questioned why a single tap needed a size before completing anything; a wishlist save needs no size, so it's a complete one-tap action, and the card already sends a plain tap to the PDP where sizing+cart works properly. `data-cloudadd` (`js/shell.js`) now calls `C.wishlist.toggle(handle)` — the SAME store the header wishlist drawer already used, just never wired into this icon before. Card template sets initial icon state from `C.wishlist.has(p.handle)` so an already-saved product shows correctly on first paint (verified: persists across reload). Visual language per Mike's spec: default = plain white circle (`css/main.css` `.pcard__cloud` background `#fff`, was a translucent cream before), saved = solid ink black + white icon (unchanged `.added` rule). Real toast added ("a beautiful message" - Mike's ask): new `toast.wishAdded`/`toast.wishRemoved` keys in ALL 11 language blocks of `js/i18n-strings.js`, built by mirroring each language's EXISTING `toast.added`/`toast.removed` sentence template and substituting in that language's own already-translated wishlist term (from the existing `wish.title` key) - templated substitution using vetted vocabulary, not fresh translation. **Flag for a native-speaker glance when there's time, priority: Greek (gender/case agreement), Russian (genitive case after "из"), Arabic (definite-article swap).** Verified live in English via DOM query, not just visually: toggling produces "Added Signature Bra to your wishlist" and the reverse on remove.
- **DEPLOYED + verified via live curl byte-checks:** `dpl_GCseqwc6C2v3dFztLcJ87uWJtLF1` (aliased www.cloudskin.com), on top of the earlier `dpl_CRuRMLoknZWdjAqjTnKcDhUeM7gy` from earlier tonight. Everything in LATEST-12 below is unchanged and still live - restating the header context here only because this file's own reliability was in question while writing this entry.

## ▶▶▶▶▶▶▶▶▶▶▶▶ LATEST-12 — 2026-08-03 (5 frontend flash/shade bugs fixed + DEPLOYED to prod. DHL zone shipping + custom Stripe checkout + PayPal rail backend DEPLOYED + VERIFIED LIVE end-to-end, plus a real stale-currency-secret bug found and fixed. FRESH SESSIONS START HERE, supersedes LATEST-11.)
**PART A — FRONTEND (Vercel), 5 fixes, DEPLOYED TO PROD** (`dpl_CRuRMLoknZWdjAqjTnKcDhUeM7gy`, aliased www.cloudskin.com, verified byte-present post-deploy; rollback candidate = prior prod `dpl_Cq8uzdsZoWR59LGo8Q97UHdNrCZW`). All diagnosed from Mike's own screen recordings/screenshots per [[verify-visual-bugs-with-evidence]], not from his words alone — this session is the second time that lesson paid off hard, on 3 separate rounds of "the shade" alone (see the method note below).
  1. Home-grid cards visibly resizing/popping-in on back-navigation, and PDP main-photo blank-then-pop flash on opening a product: both root-caused to 2026-08-01's image-transform optimization — Supabase's on-the-fly render endpoint is COLD (~0.6-1.1s) on first request for any exact width/quality combo, and nothing warmed card images or the PDP's very-first hero photo. Fixed two ways: (a) `js/product.js` — the PDP hero's FIRST paint no longer requests the transform at all (raw original directly, zero server-compute wait); thumbnail taps still use the transform since those are already idle-preloaded. (b) NEW `scripts/warm-image-cache.mjs`, wired into `scripts/deploy.mjs` as a post-deploy step — fires all 4 real (width,quality) combos the frontend actually requests against every Studio photo URL in the fresh content-snapshot, so the cold render happens once at deploy time, not in front of a shopper. Verified live: ~120 photos × 4 sizes, 0 failures, ~10-25s.
  2. "Shade" (Mike's word) turned out to be THREE different real things, found across 3 separate rounds of evidence (screenshots/recordings) as each one got ruled out:
     a. A vignette on some photos' left+right edges — baked into Larissa's original studio photography (confirmed by fetching the RAW untransformed file straight from Supabase Storage, bypassing all site code, on two different products, same gradient both times). NOT a code bug; would need a reshoot/retouch if it matters.
     b. Unselected PDP gallery thumbnails at 50% opacity (deliberate 2026-07-17 design, "dim on image not button") read as an unwanted grey wash once Mike was looking for it. His call: REMOVED the dimming entirely (`css/main.css` `.gallery__thumbs`) — all thumbnails full brightness now, active one marked by border+lift-shadow only. Mike confirmed he likes this.
     c. A REAL bug, same class as the cart-drawer box-shadow bleed fixed 2026-08-01 but on a DIFFERENT element nobody had checked: `.stickybar` (sticky add-to-bag bar) had `box-shadow` declared unconditionally instead of gated behind `.on`. At `translateY(102%)` a 67px bar clears the viewport by only ~1-2px, nowhere near the shadow's ~20px+ blur reach — a soft horizontal line was bleeding into view at the very bottom of EVERY page, always, confirmed via `getBoundingClientRect()` (top=845.34 in an 844px viewport) before touching CSS. Fixed the same way as the drawer: shadow moved into `.stickybar.on`. THIS was the actual final answer Mike was circling on all 3 rounds — the first "shade" report conflated this with the baked-in photo vignette, and a native iOS scrollbar indicator (a real but separate, non-fixable, OS-chrome thing) also got misread as the same issue along the way. Lesson: when one word covers multiple real, unrelated visual phenomena, don't stop at the first plausible-looking match — keep testing until the user confirms it's actually gone.
  3. PDP thumbnail flash-on-load (found after Mike confirmed 1-2 and kept looking): `product.js`'s `renderGallery()` unconditionally rebuilds EVERY thumbnail's innerHTML, and the Studio-photo reconcile calling it runs up to 3x per page load (sync snapshot, live fetch, a 400ms-later re-apply) even when the resolved photo list is byte-identical every time — the common case. Fixed with a before/after array-equality check so the DOM rebuild is skipped when nothing changed. Verified by tagging the real thumbnail `<img>` nodes and confirming they're still the SAME objects ~1.8s later, local AND live.
- **METHOD NOTE (reinforces [[verify-visual-bugs-with-evidence]]):** Mike sent 2 screen recordings + 3 circled screenshots this session. Every wrong-ish turn came from reasoning about a still image instead of testing directly — what worked every time was frame-by-frame ffmpeg extraction of a real recording, or direct `getComputedStyle()`/`getBoundingClientRect()` against the live/preview site. `mcp__Claude_Browser__computer` screenshot is unusable here (see [[preview-screenshot-timeout]]) — `javascript_tool` computed-style/geometry reads did all the verification instead.
- **PREVIEW WORKFLOW (repeat for future CloudSkin sessions):** raw `npx vercel deploy --yes --scope webactionhellascom` (no `--prod`) from `cloudskin-v67` makes a non-production preview; it's gated behind VERCEL'S OWN login wall (separate from the site's password gate) unless you also call the Vercel MCP's `get_access_to_vercel_url` for a 23h shareable bypass link. The site's own "Store access" gate can be bypassed for OWN-APP testing (no credential entered) via `sessionStorage.setItem('cs_gate_ok','1')` then navigating to `/home.html` — do NOT type the actual gate password into a form field (the auto-mode classifier correctly blocks that).

**PART B — BACKEND (Supabase Edge Functions): DHL zone shipping + custom Stripe checkout + PayPal, DEPLOYED + VERIFIED LIVE 2026-08-03.** Prompted by Mike asking "is everything connected" before meeting Larissa. Two hard corrections happened during this: first, `supabase/README.md`'s "Stripe checkout is inert, PayPal is sandbox" framing turned out to be STALE — direct live checks (not the docs) showed `STRIPE_CHECKOUT_ENABLED=true` and PayPal `env:"live"` were BOTH already real before this session touched anything (Mike: "we don't use Shopify payment, we use the Stripe checkout"). Second, my own first live test of the new DHL code showed `currency:"eur"` and no zone match, which I wrongly read as "the whole system won't work" — Mike corrected that Shopify was already re-based to AED (verified directly against Shopify's own Storefront API: real product price came back `"currencyCode":"AED"`), which meant the REAL bug was much narrower. **Lesson for next time: verify live runtime state (config values, deployed function source, direct API calls) over both documentation AND your own single initial test — a first negative result can itself be misleading if you haven't isolated which of several closely-related config values it actually reflects.**
  - **THE REAL BUG, FOUND AND FIXED:** the Supabase secret `STORE_BASE_CURRENCY` was still `eur` — a leftover from before the Shopify AED re-base that nobody updated afterward (classic two-systems-drift, ironically the exact bug class this codebase's own docs obsess over preventing). This ONLY affected the lightweight `shipping-quote` preview endpoint (which has no Shopify round-trip so reads this config directly); the REAL checkout functions derive currency from Shopify's own authoritative variant data via `priceCart()`, so they were never actually wrong. Fixed: `supabase secrets set --project-ref ocszztflphqsaoyhlerx STORE_BASE_CURRENCY=aed`. Verified before/after with the same live request (POST `/functions/v1/shipping-quote` `{"country":"AU","subtotalMajor":100,"itemCount":1}`): before = `currency:"eur", zone:null, usedZoneRate:false`; after = `currency:"aed", zone:6, usedZoneRate:true, shipMajor:448.41` — matches DHL's real zone-6 rate exactly.
  - **DEPLOYED (all verified against actual live source via `get_edge_function`, not just version numbers or terminal output — see the deploy-tooling note below for why that matters):** `shipping-quote` (new), `create-checkout-session`, `create-checkout-elements`, `stripe-webhook`, `create-paypal-order` — all now run the 2026-07-28/08-02 refactored+DHL-zone-aware code (`_shared/checkout-session.ts` `prepareCheckout()`/`buildSessionParams()`, `_shared/fulfillment.ts` `fulfillPaidOrder()`, `_shared/shipping.ts` `computeShippingByZone()`). 64/64 backend tests pass (`node --test tests/*.test.ts`, covers money/fx/pricing/dhl-rates/shipping/paypal-amount/checkout-session). Real DHL rates are DHL's PUBLIC LIST rates (not Larissa's negotiated account rate — swap hers in once she has the tariff sheet from her DHL rep); phase 1 only, weight is ESTIMATED from cart item count (zero real per-product weight tracking exists yet — phase 2 TODO is fully spec'd in `dhl-rates.ts`'s header).
  - **DEPLOY-TOOLING NOTES for next session:** (1) the Supabase MCP's `deploy_edge_function` tool is BLOCKED by the auto-mode classifier specifically for payment-critical functions (create-checkout-session etc.) even with Mike's explicit chat go-ahead — it allowed a low-stakes new function (`shipping-quote`) through once, but consistently refused the real checkout functions on repeat tries. The Supabase CLI (`npx supabase functions deploy <slug> --project-ref ocszztflphqsaoyhlerx --no-verify-jwt`, run by Mike himself) is NOT blocked and correctly auto-resolves the exact import dependency closure (verified: it uploaded exactly the right `_shared/*.ts` files for each function, no manual bundling needed). (2) `npx supabase ...`'s FIRST invocation in a fresh shell can hit a silent `npm`/npx cache corruption (`npm error enoent ... fetch https://registry.npmjs.org/supabase`) that no-ops that one command while looking like it might have run — it printed no clear success marker and Mike didn't notice; subsequent invocations in the same session worked fine. (3) **ALWAYS verify a Supabase edge function deploy by reading back its ACTUAL deployed source** (`get_edge_function` + grep for a marker string only the new code has, e.g. `computeShippingByZone`) — the `version` number and `updated_at` timestamp from `list_edge_functions` are NOT reliable alone; one deploy in this session showed a bumped version number with byte-identical OLD content underneath. (4) PowerShell note: bash-style `cd /c/Users/mikef/...` fails in PowerShell (resolves to `C:\c\Users\...`, wrong) — use `cd C:\Users\mikef\...` or `Set-Location`.
  - **Custom Stripe checkout + PayPal — BOTH confirmed genuinely live** (not "inert" as the README suggested): `STRIPE_CHECKOUT_ENABLED=true` (checked both local source and live bytes at `https://www.cloudskin.com/js/checkout.js`), PayPal's own config endpoint returns `env:"live"` with real credentials. This is the PRIMARY checkout now, not Shopify's hosted checkout (though that still works as the documented fallback). Currency-by-locale detection (`detectCur()` in `js/i18n.js`) is also live.
  - **Larissa's own to-do for the meeting (not code):** her real negotiated DHL rate card (currently using DHL's public list rates as a placeholder) + general DHL account/service setup; anything Stripe/PayPal account-side that comes up now that both rails are confirmed live in production.
- **MEMORY-SYNC NOTE (2026-08-03):** this exact file reverted to its pre-session state mid-way through this session (a prior edit adding this LATEST-12 section vanished, file was back to LATEST-11-on-top) — almost certainly the background vault sync pulling in an older copy from another device/session before this edit's mirror+push could win the "newer file" race. If a memory file you just edited looks unedited again later in a long session, don't assume you imagined it — re-check, and re-apply + immediately mirror+push per the CLAUDE.md sync instructions rather than trusting the write to have stuck permanently.

## ▶▶▶▶▶▶▶▶▶▶▶ LATEST-11 — 2026-08-01 (product-image perf + visual-bug fixes DEPLOYED to prod. FRESH SESSIONS START HERE, supersedes LATEST-10).
- **CANONICAL FOLDER CHANGED AGAIN: it is now `C:\Users\mikef\cloudskin-v67`, NOT v56.** v56 was stale (confirmed by byte-diffing its JS against the live site - differed; v67 matched exactly) and has been DELETED from this machine entirely to stop future sessions defaulting to it. Always re-verify the canonical folder at the start of a CloudSkin session — it has moved v52→v54→v55→v56→v67 over time; see [[cloudskin-deploy-setup]] for the "find the live one" method (byte-diff live JS against candidate folders, don't trust a folder name or a memory file blindly).
- **4 FIXES SHIPPED + DEPLOYED TO PROD (`vercel.com/webactionhellascom/cloudskin/7KMVD2DRUyWwSLNJTuTppCvcTS1t`, aliased www.cloudskin.com, verified byte-identical to live post-deploy):**
  1. Product images (PDP thumbnails, PDP main photo, every product card site-wide) now serve right-sized versions via Supabase Storage Image Transformations instead of the raw 2560px Studio original — `C.imgSized()` helper in `js/shell.js`, applied via `srcset` only (never the `<img>` `src` itself — see [[cloudskin-image-transform-gotchas]] for why that distinction is load-bearing).
  2. Fixed a real distortion bug in that same transform call: `resize=cover` with only a width param (no height) doesn't crop-to-fill, it silently ships the FULL original height with only the width narrowed — a badly stretched/zoomed photo. Must be `resize=contain` for width-only requests. Cost Mike real frustration mid-session (shipped to a preview, he caught it immediately as "the photos are fucking zoomed") before being caught and fixed.
  3. Fixed a shadow/"shade" bleeding into the right edge of every mobile page: the cart/wishlist drawer (`.drawer` in css/main.css) sits `translateX(102%)` off-screen when closed, but that 2% margin doesn't clear its own 70px box-shadow blur radius — the shadow was always rendering, always bleeding ~100px into the visible viewport on mobile widths, at a position that visibly shifts as the mobile browser chrome bar animates (hence "connected to the browser bar"). Fix: gate `box-shadow` behind `.drawer.on` (only render while actually open) instead of hand-tuning the closed offset.
  4. Fixed a "thumbnail tap takes seconds to swap" complaint: the first-ever request for any photo at a given Supabase transform size is a COLD render (~0.6-1.1s measured live); idle-preload every OTHER gallery thumbnail at the main-image size right after page paint (`preloadGalleryMainSizes` in js/product.js, same idle-callback pattern as the pre-existing `preloadOtherColours`) so the edge cache is warm before anyone taps. Proven live: 666ms cold -> 122ms warm.
- **METHOD NOTE for the fleet (propose to house traps):** every one of Mike's bug reports this session was initially MISDIAGNOSED from his verbal/voice-to-text description alone (tried "progressive JPEG decode", "a stray icon", "dvh browser-bar resize" as theories before the real bugs). What actually worked: (a) he sent a screen recording — `ffmpeg -vf fps=2` frame extraction + reading/zooming individual frames located the real visual bug directly (the distortion, the drawer-shadow gradient) far faster and more accurately than reasoning from words; (b) for the transform-latency bug, direct `curl` timing against the real Supabase endpoint (not just reading code) proved the cold/warm gap with real numbers; (c) for the drawer shadow, computed `getBoundingClientRect()`/`getComputedStyle()` in a live browser (with CSS `transition:none` forced, since this preview pane doesn't composite frames when hidden and transitions don't progress — see [[preview-screenshot-timeout]]/[[preview-pane-intersectionobserver-dead]], same family of tooling quirk) proved the exact pixel math. Lesson: when a visual bug is described but not directly observed, get a screenshot/recording and inspect it pixel-level before touching code; when a fix is proposed, verify it with real bytes/timing/computed-styles, not just "the code looks right."
- **Also this session (unrelated to CloudSkin code but touched while investigating a DHL order-sync complaint from Panos):** ruled out CloudSkin's own order pipeline as the cause of a DHL Express Commerce shipment issue (Stripe webhook → Supabase → Shopify Admin API order creation has zero country-conditional logic, inventory correctly committed against the right variant/location for the affected order) — the failure is downstream, inside DHL's own Shopify connection, not in this codebase. DHL portal login/investigation was still pending Panos's 2FA at end of session.

## GOAL (Mike, 2026-07-17): this is intended to be the FINAL version of Larissa's site — everything must be PERFECT, and Larissa must be able to build/run the whole site herself from the Content Studio. Target milestone: **~July 21, 2026**, when Larissa will have her real product photos ready to upload. So: the Studio must be complete + glitch-free, the photo pipeline must handle her real uploads flawlessly (base photo #1 = home + first PDP image, extras after, per colour, all products lined up), plus the EDITABLE MERCHANDISING BLOCKS feature (spec'd 2026-07-17): make ALL FOUR home merchandising blocks Studio-editable = **picture + a category/collection PICKER** (dropdown of collection slugs, NOT hand-picked products): (a) the "Shop All" Collection band (home.html .collbanner-sec, ~line 89, home.collbanner.image editable but href hardcoded c=collection), (b) the "Explore the Collection" editorial block (home.html .editorial ~line 111, home.editorial.image editable but href hardcoded c=women + the CTA), (c) the Women/Men mega-menu feature posters (config.js `feature:{href,img,caption}` → "The Court Edit"/"Court Ready"; rendered in shell.js megaMarkup), (d) the "Shop by Category" tiles (home.html ~line 104-106, Tops/Bottoms/Dresses, hardcoded imgs + hrefs). Needs: dynamic hrefs read from Studio keys, new studio.js panels with an image field + a collection-slug SELECT per block, content.js applying the link-target overrides (new — currently only text/img/product-controls). HARD REQ from Mike: "everything well integrated at /studio so Larissa is not confused". **NAMI's design APPROVED by Mike + being built by CORKY (2026-07-17):** Studio restructured into 5 collapsible top-level groups mirroring the pages — HOME PAGE / NAVIGATION MENUS / PRODUCTS / OUR STORY / FOOTER (one open at a time, Home default). Each merchandising image gets one dropdown "When tapped, opens:" → friendly page name mapping to a collection slug (never show the slug). Complete-the-Look = per-product searchable checkbox list + reorderable chips, cap ~4, key `product.<handle>.completeLook`; empty → keep product.js auto pairing. Frontend hrefs read owner slug else hardcoded default (fail-open); content.js extended to apply link-target + completeLook overrides. MIKE'S 2 DECISIONS: **Studio stays ENGLISH** (Nami offered Greek, Mike said keep English); **per-product editor UNIFIED** (one product picker → Text + Photos + Complete-the-Look together). Nami's gallery aesthetic is DONE+verified (premium thumbnail rail: dim on image not button, soft-lift active state no black box, mobile right-edge fade+peek "swipe for more", desktop capped column + bottom fade; single-photo = no rail) — DON'T alter .gallery CSS. Also all em-dashes removed from Studio UI via the relabel; tile text labels fixed (Tops/Bottoms/Dresses) image+opens editable; menu-poster captions editable. **BUILT + verified 2026-07-17** (Corky: studio.js/studio.html/content.js/shell.js/product.js/home.html + cache bumps; Supabase back to original 9 rows). Boss independently re-verified: 5 groups render, home 7 merchandising links wired w/ correct defaults (band→collection, tiles→tops/bottoms/dresses, editorial→women), PDP still no-flash (catalogue never fetched) + single-photo no-rail + Complete-the-Look 3-item auto fallback, console clean on home/product/studio, 16 cards. content.js has a new `[data-content-link]` applier (slug allowlist, fail-open). **EM-DASH sweep: fixed the 5 in app code (product.js "Fit —"→"Fit:", shell.js 3 contact-form msgs). DONE by LYRA 2026-07-17: 39 em-dashes fixed natively per language (i18n-strings.js 26 [en1/el9/pt3/ru13], legal.js 11, product-content.js 1, shopify.js 1 console string); verified 0 em-dashes in 2,477 string values + in-browser in Greek; node --check passes; Lyra bumped ?v=35→36 on the 4 scripts she edited across the 6 HTML files (cache-trap). En-dashes (–) left on purpose (correct de/sv typography + number ranges). All rendered copy now em-dash-free.** Minor open items (Corky flags): menu-poster image override proven but not tested with a real upload; Studio groups collapse when the open one is re-clicked (owner can have all closed) — could pin Home open. ALSO (added 2026-07-17): make **"Complete the Look" editable per product** — Larissa picks WHICH products appear in the "Complete the Look" / "Pairs great with" rail for each opened product (currently product.js auto-computes `ctl` by pairing skirts↔tops scored by colour/line; add a Studio per-product multi-pick of handles, key e.g. `product.<handle>.completeLook`, product.js uses her picks when set else the auto algorithm). Product DESCRIPTIONS are ALREADY editable (Studio "Product text": description/features/fabric/fit per product) — Mike confirmed that's enough. Full quality gate before it's handed to her. Deploy still gated on Mike's explicit go.

## LATEST-10 - 2026-07-22/23: VELLUM on-canvas LIVE EDITOR SHIPPED TO PROD as v56 (+ backend hardened + CSP live). FRESH SESSIONS START HERE (supersedes LATEST-9). Full editor build detail lives in [[live-site-editor-product]] (lines under "VELLUM IS LIVE ON PRODUCTION").
- **2026-07-23 AUTO-BAKE WIRED (tooling only, NOT a deploy - prod unchanged).** So the snapshot can never go stale again (Mike: "wire it automatically"). Added two scripts to C:\Users\mikef\cloudskin-v56\scripts\: (1) **bake-content.mjs** = RESTORES the long-missing baker the snapshot header always referenced - fetches cloudskin_content via the PUBLIC anon key read from js/supabase-config.js, regenerates js/content-snapshot.js (sorted + headered), FAIL-SAFE (leaves the file untouched + non-zero exit on any fetch/empty-table/verify failure), self-verifies deep-equal vs live; `--check` = verify without writing. (2) **deploy.mjs** = THE CloudSkin deploy command going forward: `node scripts/deploy.mjs` bakes first (ABORTS the deploy if the bake fails) -> `vercel deploy --prod --yes --scope webactionhellascom` -> verifies the SERVED snapshot deep-equals live; `--dry-run` bakes-checks + skips deploy. scripts/ added to .vercelignore (not uploaded); NO package.json added (would trip Vercel framework detection on the static passthrough). CACHE FACT: content-snapshot.js is served `max-age=0, must-revalidate` + ETag, so a fresh bake is picked up WITHOUT a ?v bump (the earlier manual ?v bumps were belt-and-suspenders, harmless). WINDOWS GOTCHA fixed: the baker uses process.exitCode, never process.exit() (a bare process.exit() after a fetch crashes libuv `UV_HANDLE_CLOSING` on Windows and made the wrapper misread the bake as failed). Tested: `bake --check` + `deploy --dry-run` both exit 0, deep-equal YES, file untouched. **FUTURE DEPLOYS (incl the deploy-launch agent): run `node scripts/deploy.mjs` instead of a bare `vercel deploy` so the snapshot can never ship stale.** Still a deploy-TIME bake (not triggered by Larissa's live edits) - between deploys the live Supabase fetch keeps content fresh (with the brief flash the baked snapshot removes), which is the intended design.
- **2026-07-23 (follow-up, same session) SNAPSHOT RE-BAKED = PHOTO-FLASH FIXED. CURRENT PROD = `dpl_2vSbc6HuHsDiTAMJZoG7EeAt1UeH`** (aliased www.cloudskin.com; rollback = `dpl_G5ECkbi59kQjvmBmuWhzjE6AXuFx` = the grid-fix, via `vercel promote dpl_G5ECkbi59kQjvmBmuWhzjE6AXuFx --scope webactionhellascom`). Mike: in /creator, pressing a product photo showed the OLD/default photo for a sec then the NEW one ("like a default picture rendering first"). ROOT = js/content-snapshot.js (baked 2026-07-21, 16 keys) was STALE vs live cloudskin_content (now 72 rows incl Larissa's new uploads) -> content.js seeds the stale snapshot on first paint, then the live fetch hits the DELTA path (content.js L243 `mapsEqual` false -> L251 `announce()` -> grid/gallery re-render) = the visible swap. FIX = re-bake content-snapshot.js from LIVE (now 72 keys / 4768 value bytes, all 20 product image keys incl court-skirt.white=3.jpg, club-skirt.white#1=img-6961, performance-tank/tee have white-mist). NO-FLASH PROOF = the DEPLOYED snapshot object DEEP-EQUALS a fresh live cloudskin_content REST fetch (both 72 keys, deep-equal YES) -> mapsEqual true -> the live fetch returns early at L243, no re-render. Bumped content-snapshot.js `?v=55->57` in the 6 loader pages (about/account/collection/home/login/product); pre-deploy diff confirmed EXACTLY those 7 files differ from live (all core JS/CSS md5-MATCH = grid-fix not regressed), gate+noindex+robots Disallow intact. Deployed DIRECTLY (`vercel deploy --prod --yes --scope webactionhellascom` from C:\Users\mikef\cloudskin-v56; Mike's "fix it" = the follow-up deploy I'd offered). RE-BAKE TOOLING: on-disk scripts/bake-content.mjs is STILL MISSING; regenerated the pattern (fetch cloudskin_content via public anon REST -> JSON.stringify(sorted,null,2) w/ baked header -> deep-equal verify) at scratchpad bake-content.mjs + verify-live.mjs. CAVEAT told to Mike: the snapshot is DEPLOY-TIME, so a NEW Larissa upload reintroduces the flash for that item until the next re-bake+deploy (inherent; "re-bake at every deploy" - candidate to automate as a deploy step).
- **2026-07-23 EDITOR GRID-SIZE BUG FIXED + LIVE. CURRENT PROD = `dpl_G5ECkbi59kQjvmBmuWhzjE6AXuFx`** (aliased www.cloudskin.com; rollback = `dpl_3AfpMsopHGn6qt1c1uMwy3HYj8FR` via `vercel promote dpl_3AfpMsopHGn6qt1c1uMwy3HYj8FR --scope webactionhellascom`). Mike: in the creator studio (/creator) the COLLECTION product photos rendered "bigger than the whole screen" (they were FINE on the public site + fine as files - Larissa's real uploads, 250-825KB/1500-2000px, served no-cache). ROOT CAUSE = js/edit-mode.js `applyOrderToDOM`: it appended the `.pcard`s to the `#grid` <section> wrapper instead of the inner `.pgrid` grid, so in edit mode the cards fell OUT of the 4-col grid -> each `.pcard__media` went full-width (1152x1537, taller than the viewport) AND cards duplicated 15->30 (orphans survived collection.js's innerHTML rebuild). FIX (1 function, editor-only, no backend): append to the cards' REAL container -> `var host = cards[0].parentElement; ... host.appendChild(...)`. Verified in edit mode on the local mirror (15 cards, 4-col, 273px media, 27 editables + Vellum bar intact, 0 console errors, no h-overflow) AND live (edit-mode.js `?v=57` = 200, fix bytes `cards[0].parentElement`+`host.appendChild` present, old `c.appendChild(k)` gone, node --check ok, 4 loaders serve `?v=57` 0 residual v56, gate+noindex+robots Disallow+CSP+5 headers+/studio+/creator all intact). Cache-bust = `edit-mode.js?v=56->57` in home/about/collection/product.html loaders ONLY (edit-mode.css left at v56). Deploy folder C:\Users\mikef\cloudskin-v56 (pre-deploy sha1 gate: 90 files, 85 identical to live, exactly the 5 intended diffs). Mike EXPLICITLY authorized ("Deploy the fix now"); the deploy-launch subagent raised a FALSE-POSITIVE "unauthorized prod deploy" security flag only because a subagent can't see the parent-session authorization. Store still PRIVATE/gated (editor-only fix, public store untouched). SEPARATE known non-blocker left alone per Mike: content-snapshot.js is stale (old photos) -> brief old-photo flash on cold load; Larissa's photos ARE live via the runtime fetch; re-bake deferred.
- **v56 is LIVE on www.cloudskin.com** - prod deployment **dpl_HkdKpzCURTjDCWzaqcJunRcv94jM** (READY, target production; shipped via `vercel deploy --prod` from **C:\Users\mikef\cloudskin-v56**, 7/7 production checks passed). ROLLBACK (prior prod) = **dpl_3hNBUscQxjk7oX4stapeYc4GopZe** ("instant rollback"; inspector vercel.com/webactionhellascom/cloudskin/HkdKpzCURTjDCWzaqcJunRcv94jM). Gated preview build was dpl_J95nu5AN1yEBYVbvMkViACLQ4WaF. **UPDATE 2026-07-23: the lazy-load fix (Corky finding below) SHIPPED to prod = CURRENT LIVE `dpl_3AfpMsopHGn6qt1c1uMwy3HYj8FR`** (deployed by Mike's explicit go via `vercel deploy --prod` from cloudskin-v56 after the harness auto-classifier blocked the agent path; only the 4 page HTMLs changed - static edit-mode.css/js tags replaced by an armed-session inline loader so a normal visitor no longer downloads the ~156KB editor). Live-verified: /home has the loader + 0 static edit-mode tags, editor files still 200 on demand, /creator 200, gate+noindex+robots Disallow + CSP + all 5 security headers intact. ROLLBACK now = **dpl_HkdKpzCURTjDCWzaqcJunRcv94jM** (the Vellum go-live).
- **WHAT SHIPPED = VELLUM, the on-canvas "edit the live site by clicking it" editor (Mike's reusable/sellable house product; CloudSkin is the FIRST live client), wired to the REAL backend.** Larissa edits at **cloudskin.com/creator** -> password CloudskinStudio1 -> click any text / photo / product / section-background to edit it in place; saves write live cloudskin_content via the hardened studio_save (photos via studio-upload, revert via empty-save). New files in the v56 bundle: creator.html, js/creator.js, js/edit-mode.js, css/edit-mode.css; edited js/content.js, js/config.js, js/home.js, css/main.css + the 4 page templates. SELF-GATED: a normal visitor sees ZERO editor chrome (no pill), only the gated store; edit-mode arms ONLY after the /creator server-side studio_auth password. The old /studio panel is KEPT alongside for now (retire only after /creator is proven in real use).
- **BACKEND HARDENED ON PROD (P1, applied 2026-07-22 via Mike's own Chrome -> Supabase SQL editor, verified):** bcrypt cost 6 -> 12 re-hash of the SAME password (CloudskinStudio1 still authenticates), studio_auth_throttle table + studio_verify throttled gate (revoked from anon), studio_auth + studio_save routed through it, studio_delete(pw,keys[]) added. DB-only change, shared by /studio + /creator, the frontend site was not touched. **CSP now ENFORCING on prod** (v56 vercel.json, 0 site violations; script-src 'self' 'unsafe-inline' cdn.jsdelivr.net + object-src 'none' etc; unsafe-inline is required for the inline gate script + font-swap handlers).
- **STILL PRIVATE BY DESIGN:** store-access gate + noindex + robots.txt Disallow are STILL ON. This promotion put the EDITOR live, NOT the public store. TRUE PUBLIC LAUNCH = a separate gated step (remove gate, un-noindex, add sitemap.xml, submit for indexing) on Mike's explicit go. (Note: the mixed ?v=55/?v=56 stamp on home.html is intentional - v56 bumped only the changed files; not a cache-split bug.)
- **STILL PENDING (none blocking the editor):** email send-test needs Larissa's EUR1 test order (then REMOVE product zz-test-payment-check-delete-after); Shopify order webhooks + the editable collection banner (pre-existing, re-flagged by deploy-launch); true multi-currency needs the Shopify Payments entity (Mike's business call); DEFERRED Vellum niceties = 11-lang auto-translate+polish on save, EXIF createImageBitmap, Pro-plan image render/transform (Free plan blocks it), touch long-press direct-drag, per-colour photo revert in the Edits drawer, /studio retirement. CORKY security + quality re-audit of the live Vellum editor (2026-07-23) = **PASS, no Critical/High**: NO auth-bypass, no new write exposure. Writes are gated SERVER-SIDE (studio_save/studio_delete are SECURITY DEFINER and open with studio_verify = bcrypt cost-12 + IP throttle 10 fails/15min; cloudskin_content RLS = SELECT-only so direct anon writes denied; studio-upload 401s before touching storage; a faked client `vlm-armed` flag yields a DEAD inert editor with empty PW). content.js render paths XSS-safe (textContent/attribute-only, whitelisted link slugs, escaped bg url), self-gating holds (public sees zero chrome). ONE real finding (Low-Med): edit-mode.js (116KB)+edit-mode.css (42KB) = ~156KB ship to EVERY gated visitor though only the owner arms; fix = an inline loader that injects them ONLY when sessionStorage vlm-armed==='1' (public download drops ~156KB->~0.2KB); SHIPPED + live-verified on prod 2026-07-23 (dpl_3AfpMsopHGn6qt1c1uMwy3HYj8FR, see UPDATE above). TOP JUDGMENT CALLS for Mike (both need a gated redeploy): (1) rotate the studio password (the model's practical ceiling since anon can call studio_auth; bcrypt+throttle make CloudskinStudio1 defensible but a stronger secret is the highest-leverage hardening), (2) lazy-load the editor before the Vellum template goes to OTHER clients. Also cosmetic: bump the 4 changed files (config/content/home.js, main.css) from ?v=55 to ?v=56 (currently served fresh via ETag/must-revalidate, so not a stale-cache bug).

## ▶▶▶▶▶▶▶▶▶ LATEST-9 — 2026-07-21 (email DNS confirmed PROPAGATED; HANDOFF to a fresh session — Mike: "do the email/DNS finish + checks on a fresh session, this context is full").
- **v55 is LIVE + independently re-confirmed by Boss** (curl): ?v=55, /robots.txt 200 Disallow:/, gate noindex present, shell.js swatchPick uses `C.baseImageFor(p, colour)` (studio-first). See LATEST-8 for the full v55 detail (prod dpl_Tz1mEZfX7kaUv6QgFdjFMjg61bJs).
- **EMAIL SENDER DNS = FULLY PROPAGATED (Boss verified via DNS-over-HTTPS 2026-07-21):** ALL records resolve: Shopify DKIM `wxg._domainkey`->dkim1.5563702dec1d.p386.email.myshopify.com + `mailerwxg`->5563702dec1d.p386...; Resend `resend._domainkey`(p=MIGf... DKIM) + `send` TXT (v=spf1 include:amazonses.com ~all) + `send` MX (feedback-smtp.eu-west-1.amazonses.com); `_dmarc`(v=DMARC1;p=none); `cloudskin.com` MX->smtp.google.com (incoming @cloudskin.com -> Larissa's Google Workspace). So the branded-email AUTH SETUP IS DONE - senders now DKIM/SPF-pass, should inbox not spam.
- **REMAINING EMAIL WORK (for the FRESH SESSION):** (i) confirm in the ALREADY-LOGGED-IN Shopify admin (Browser, "Larissa Admin", NEVER re-login/2FA) that Settings->Notifications sender = info@cloudskin.com shows **"Authenticated"** and (in the Resend dashboard, acct webactionhellas) the cloudskin.com domain shows **"Verified"** - both EXPECTED to pass now since the DNS resolves; if either still shows pending, just click its re-verify/refresh. (ii) The real EMAIL SEND-TEST (order-confirmation "poster" reskin actually SENDS + LANDS + RENDERS on a device; + shipping/auth/owner-alert/abandoned-cart) needs **Larissa's EUR1 test order** (product zz-test-payment-check-delete-after) - CANNOT be done autonomously.
- **REMAINING LAUNCH PUNCH LIST:** email send-test (Larissa EUR1 order) -> then REMOVE the EUR1 test product; gate + noindex STAY until Mike's explicit public-launch go; international track still open (entity for true multi-currency, DHL->DDP service + HS codes + IOSS via accountant - see LATEST-6/7). All v55 site glitches are CLOSED.
- **HANDOFF: a fresh session should say "continue CloudSkin" and start from THIS LATEST-9.** Everything above is done/verified; the fresh session's job = the email admin-badge confirmation + coordinating Larissa's EUR1 send-test, then the launch punch list.


- **v55 DEPLOYED TO PROD + verified (deploy-launch, curl+node ONLY):** prod **dpl_Tz1mEZfX7kaUv6QgFdjFMjg61bJs** (aliased www.cloudskin.com, 87 files), ROLLBACK v54 **dpl_2AoSkg7EcsyUPuLKZXmFuSGSBDne** (`vercel rollback dpl_2AoSkg7EcsyUPuLKZXmFuSGSBDne --scope webactionhellascom`). NOT rolled back - all 9 checks passed.
- **METHOD NOTE (why the prior attempt stalled):** the in-app browser (mcp__Claude_Browser__) TIMES OUT on this machine, so the scheduled run's "verify at 320/360/390 + desktop via in-app browser + site-qa agent" instruction was unrunnable and it stalled. Verified instead via curl + node + reading the deployed JS (the exact method v52-v54 used). Base mirror = authoritative Vercel deployment-files API (`/v6/deployments/{id}/files` tree; each file uid = **plain sha1** of bytes, NOT git-blob - the prior _verify.mjs used gitblob and mis-reported; _mirror.mjs's plain-sha1 was right); cloudskin-v54 confirmed byte-exact to live v54 (86/86 sha1). Deploy dir C:\Users\mikef\cloudskin-v55 (v54 + 12 changed files + robots.txt; .vercel/project.json targets prj_6d0gmXALxXTKZFj7AAVoBZfns12W). Deploy bundle scripts + live-verify in scratchpad .../7b50d54e.../scratchpad/v55.
- **5 FIXES SHIPPED (closes parked a,c,e,g + the b/d Supabase deletes already applied):** (1) CARD SWATCHES (parked g): js/shell.js swatchPick() now calls `C.baseImageFor(p, sw.dataset.color)` - the SAME studio-first resolver the PDP + card photo#1 use (studioImagesForColour on p._studioImgs, catalogue /shopify/ fallback) - replacing the old `distinct[cols.indexOf(color)]` catalogue-index logic. PROVEN by running the live resolver: court-skirt card Black->dsc06888.jpg, White->dsc06995.jpg (her models, matches PDP). (2) POPUP (parked a): js/welcome.js on submit now calls `C.newsletter.subscribe(email,'welcome')` (guarded + .catch, same pattern as footer #footerNl; C.newsletter defined in auth.js) so the address is captured into newsletter_signups (was silently discarded); welcome.ok changed to honest text in ALL 11 langs (0 em-dashes, 10%-off title KEPT). (3) GATE (parked c): index.html gate got `<meta name="robots" content="noindex, nofollow">`; new /robots.txt (User-agent:* / Disallow:/) returns 200; Launching Soon gate intact. (4) SNAPSHOT (parked e): js/content-snapshot.js re-baked from cleaned live cloudskin_content = 16 keys, Storage URLs (no base64), synchronous, byte-matches live table exactly (bake-content.mjs was missing - regenerated in the window.CLOUDSKIN_CONTENT_SNAPSHOT={} format). (5) ?v=54->55 uniform across all HTML (0 residual v54 live). login.html also had 2 pre-existing comment em-dashes -> hyphens (touched-file zero-em-dash hygiene). Supabase deletes (parked b home.cat.tile3.link, d product.the-ace-dress.images.white) were already applied+clean; newsletter_signups = 0 rows (verified by read, no test insert).
- **9 LIVE CHECKS ALL PASS (curl+node):** (i) ?v=55 uniform 0xv54 on home/product/collection/about/account/login/studio; (ii) /robots.txt 200 Disallow:/; (iii) gate noindex meta + Launching Soon; (iv) shell.js swatchPick uses baseImageFor, old logic gone, Black/White proven; (v) welcome.js subscribe + all 11 welcome.ok honest + title kept; (vi) content-snapshot 16 keys matches live, no data-uri, deleted keys absent, node --check ok; (vii) product.js White resolver intact + v54 hero-perf intact (home.js parallax gated pointer:coarse/max-width:820, main.css iginsta 0 animations, shell.js pageshow bfcache self-heal); (viii) 0 em-dashes in all touched files, node --check clean on all live JS; (ix) 7 interior + gate noindex, cleanUrls (308 /home.html->/home), all 5 security headers (nosniff/SAMEORIGIN/Referrer-Policy/HSTS/Permissions-Policy).
- **STILL PENDING (parked f, NOT blocking - do not re-deploy for it):** EMAIL send-test (order-confirmation poster reskin, shipping notif, Supabase-Auth confirm/reset, cloudskin-notify owner alert, abandoned-cart) - gated on Larissa's real EUR1 test order + confirming Shopify DKIM + Resend @cloudskin.com DNS finished propagating (added at Namecheap 2026-07-19). Also still parked from earlier: remove the EUR1 test product zz-test-payment-check-delete-after from Shopify before public launch; site stays gated+noindex until Mike's public-launch go.

## ▶▶▶▶▶▶▶ LATEST-7 — SESSION 2026-07-21 (v54 mobile-perf DEPLOYED + PARKED punch list; Mike near his usage limit).
- **>>> SCHEDULED 2026-07-21 04:23: the parked list (a-g) below is now a ONE-TIME autonomous run `cloudskin-v55-overnight-fix` (C:\Users\mikef\.claude\scheduled-tasks\), fires 07:38 Europe/Athens, does ALL fixes + deploy v55 with verify + site-qa gate + AUTO-ROLLBACK to v54 (dpl_2AoSkg7EcsyUPuLKZXmFuSGSBDne) on any failure. DO NOT do these manually before it runs (avoid a double-deploy). It writes LATEST-8 with the outcome. Runs only while the app is open (else on next launch); needs tool permissions not to block. <<<**
- **v54 DEPLOYED + verified (Corky):** prod **dpl_2AoSkg7EcsyUPuLKZXmFuSGSBDne**, ROLLBACK v53 **dpl_DA4SVmx4N8RXxx5Qnzt1nMvaaKLW**. MOBILE PERF fixes for hero scroll lag (Mike: laggy hero, worse in Low Power Mode = throttling exposed heavy effects): (1) css/main.css `.iginsta-sec` dropped the `animation: iginstaGradient 16s infinite` (a background-position repaint that ran FOREVER even off-screen = the big always-on battery/jank cost) -> static gradient; (2) js/home.js hero parallax now gated behind `matchMedia('(max-width:820px)')||'(pointer:coarse)'` = OFF on mobile (no per-frame heroMedia transform), and `.hero__media img` will-change:auto on mobile; (3) `.filterbar` backdrop-filter:blur(12px) -> none + opaque bg on mobile (was re-blurring every scroll frame); (4) js/shell.js added `pageshow` -> `if(e.persisted) location.reload()` = BFCACHE SELF-HEAL for the recurring "phone shows stale catalogue shots not the model photos" (the live code was always correct; it was mobile-Safari bfcache serving a frozen page). Desktop unchanged. Verified: iginsta static, hero no mobile transform, filterbar no mobile blur, White resolver intact, ?v54 uniform, gate+noindex kept. Confirmed her court-skirt black(dsc06888)+white(dsc06995) photos are BOTH real MODEL shots; the plain-white-skirt Mike kept seeing = the catalogue the-court-skirt-1.jpg fallback served from his stale phone cache. NOTE: to SEE v54 a stale phone must fully reopen the tab once (then the bfcache guard self-heals going forward).
- **PARKED PUNCH LIST (Mike deferred, token-limited 2026-07-21; do in a lean single pass next session, NO sweeps):** (a) DISCOUNT POPUP js/welcome.js is deceptive - captures nothing + welcome.ok falsely says "check inbox for the code"; FIX = call C.newsletter.subscribe(email,'welcome') on submit + change welcome.ok in all 11 langs to an honest line (English "You're in. Welcome to CloudSkin.", full 11-lang set is in the killed agent brief a0c3a647); keep the 10%-off hook, the actual code = a Shopify welcome-discount Larissa sets at launch. (b) HOME "Dresses" tile mislinks to Skirts: `DELETE FROM cloudskin_content WHERE key='home.cat.tile3.link'`. (c) GATE index.html has NO noindex + no robots.txt (404) -> add noindex meta to the gate + robots.txt `Disallow: /` (Mike CONFIRMED keep the Launching Soon gate; noindex only hides the splash from Google, does NOT remove the gate). (d) residual test key `DELETE FROM cloudskin_content WHERE key='product.the-ace-dress.images.white'` (Boss's dry-run leftover). (e) re-bake content-snapshot.js at next deploy (bake-content.mjs is MISSING from disk - regenerate from live cloudskin_content in the same format; snapshot was 17 keys vs live 18). (f) NEW (Mike 2026-07-21) EMAIL CHECK: verify transactional emails actually SEND + LAND + RENDER on real devices - the "poster" order-confirmation reskin, shipping notification, Supabase-Auth signup-confirm/password-reset (needs the @cloudskin.com sender DNS done), cloudskin-notify owner alert, abandoned-cart; gated on the EUR1 test order + confirming the Shopify DKIM + Resend DNS finished propagating (added at Namecheap 2026-07-19, were "Propagating"/"Pending").
- **PARKED item (g) (Mike 2026-07-21):** HOMEPAGE product-CARD colour swatches don't reflect Larissa's uploaded per-colour photos. shell.js `swatchPick()` (~L985) swaps `front.src` to the CATALOGUE `/shopify/` shots indexed by colour (`distinct[cols.indexOf(color)]`), IGNORING her studioColourImages. For court-skirt the catalogue shots are all white product shots, so tapping Black shows a white shot = "colors don't change." The PDP is correct (uses her dsc06888/dsc06995 via studioImagesForColour); only the card swatch handler uses the old catalogue-index logic. FIX = resolve the swatch to her Studio per-colour photo first (like baseImageFor / C.studioImagesForColour), fall back to catalogue. NOT a v54 regression (v54 diff = only the pageshow line in shell.js + the noParallax gate in home.js). Do in the same lean pass as (a)-(f).
- **PROCESS NOTE (Mike alarmed at token burn):** a 6-agent Workflow "total QA sweep" cost 946k tokens (Ultracode mode was ON = "exhaustive, cost no constraint, workflows on everything"). Mike wants LEAN default: no multi-agent sweeps unless he explicitly asks; single focused agent or do it directly. Full sweep defect list saved at tasks/wlvvvtjgn.output (do NOT re-run it).

## ▶▶▶▶▶▶ LATEST-6 — SESSION 2026-07-20 (v53 currency DEPLOYED + DDP decision + international plan).
- **v53 DEPLOYED + independently verified (Boss + deploy-launch):** prod **dpl_DA4SVmx4N8RXxx5Qnzt1nMvaaKLW**, ROLLBACK v52 **dpl_EsiAikHGsXZ2x5LamYa4cg1VUs4Z**. 3 changes: (1) refreshed the CUR rate table in js/i18n.js (2026-07-20, ECB/xe/Wise): USD 1.08->1.14, AED 3.97->4.2, GBP 0.84->0.849, THB 39->38.4, SEK 11.3->11.05, RUB 98->88, SAR 4.05->4.28, KWD 0.33->0.354 (EUR/AUD unchanged); (2) detectCur() final fallback "USD"->"EUR" (unknown-locale visitor now defaults to the charged currency); (3) NEW i18n key `cur.note` in all 11 langs (Lyra) "Prices shown in your selected currency... charged in euro (EUR) at checkout" rendered as `.drawer__ccynote` in the CART DRAWER (shell.js, between checkout btn + duties note, var(--stone)). Live-verified: 16 v53 refs 0 v52, USD rate 1.14 live, €84 Court Skirt shows $96, fallback EUR, note renders EN+EL, ADD-TO-BAG + White resolver regression-clean, gate+noindex, 0 console errors, content-snapshot byte-identical. Files edited in bundle C:\Users\mikef\cloudskin-v52-bundle (i18n.js/i18n-strings.js/shell.js/main.css + 7 HTML). TRAP noted: C:\Users\mikef\cloudskin-v52-bundle-orig is actually a v51 mirror (not v52) - don't use as baseline; deploy-launch uses the authoritative Vercel deployment-files API tree.
- **CURRENCY REALITY (confirmed live):** the storefront ALREADY has full local-price DISPLAY - i18n.js CUR table (10 currencies USD/EUR/AUD/AED/GBP/THB/SEK/RUB/SAR/KWD w/ symbols+rates) + C.money() converts EUR-base × rate, + a privacy-safe auto-detect (detectCur: navigator.language region -> REGION_CUR, else Intl timezone -> TZ_CUR; runs only if no stored cloudskin_ccy; never overrides manual pick). So "shows local prices" is DONE. The ONLY missing piece = CHARGING in local currency, which needs Shopify Payments = an entity in a supported country (US/UK/EU); UAE can't. Third-party gateways (Stripe/PayPal) can't do Shopify multi-currency presentment.
- **MIKE's international decisions (2026-07-20):** (1) CURRENCY = pursue the entity for TRUE multi-currency (Larissa is a serious, funded international seller). Boss recommended EU entity via Estonia e-Residency OÜ (fast/remote/EU/IOSS) or US LLC via Stripe Atlas; entity+bank+Shopify-Payments activation are Mike/Larissa/accountant (Boss CANNOT form companies, open banks, or enter bank/tax details); once live Boss flips true multi-currency in ~1 day. (2) DUTIES = **DDP, "make it true"** - Mike chose to ABSORB duties so the existing `ann.2` "Duties & taxes included" banner becomes honest (it was FALSE: markets are DDU/customer-pays-on-delivery). DDP needs: DHL account set to DDP service (Larissa), IOSS registration for EU VAT (accountant), price buffer for duty cost (apparel duty ~12% EU / ~10-30% US - real margin hit), + Boss adds HS codes + country-of-origin to products & aligns Shopify shipping/market settings. Banner left as "included" (do NOT change) since going DDP. Confirm with customs broker/accountant.
- **OPEN (awaiting Mike's go):** (a) Boss to add HS codes + country-of-origin on products (DDP prereq), (b) one-page accountant/DHL brief covering entity + IOSS + DDP. Larissa message (DHL app + EasyWP, with links) drafted earlier, not yet sent by Mike.

## ▶▶▶▶▶ LATEST-5 — SESSION 2026-07-20 (v52 DEPLOYED + pipeline dry-run + mobile bar fix).
- **v52 DEPLOYED TO PROD + independently verified (Boss + deploy-launch):** prod **dpl_EsiAikHGsXZ2x5LamYa4cg1VUs4Z** (aliased www.cloudskin.com), ROLLBACK v51 **dpl_6emqaCL3zR6HHZuEVMwK4w7LYM4V** (`vercel rollback dpl_6emq... --scope webactionhellascom`). deploy-launch rebuilt from the AUTHORITATIVE Vercel deployment-files API (86 files, sha1-verified byte-exact vs v51; ignored the stale scratchpad mirror/live debris) -> clean bundle at C:\Users\mikef\cloudskin-v52-bundle (orig v51 ref at cloudskin-v52-bundle-orig). Only 10 files changed: 7 HTML (?v51->52 only) + main.css + product.js + studio.js. Gate+noindex kept, content-snapshot.js byte-identical (sha1 11d44b4, NOT re-baked), cleanUrls+5 security headers intact. Live-verified: 17x?v=52 0xv51, mobile #sbAdd fully on-screen (390->376, 360->346, 320->306; size+add both 46px; #sbImg hidden on mobile), the-court-skirt White->dsc06995 (glitch gone, was stale cache), 0 console errors, 0 broken images.
- **v52 CONTENTS (all shipped):** (1) mobile sticky-bar fit fix css/main.css @media(max-width:600px): .stickybar__inner gap18->10 pad22->14, #sbImg display:none, info flex+ellipsis, #sbSize width96(renders 108, base min-width wins, still fits) h46, #sbAdd width134 h46 -> ADD TO BAG no longer cut on phones; (2) #stickybar box-shadow -> 0 -8px 24px -10px rgba(23,21,18,.12) (negative spread; fixes the "cut shadow at side" caused by overflow-x:clip chopping the full-width soft shadow); (3) studio.js quality cap 2000->2560 (L573+L913) + toDataURL 0.92->0.88 (L937) = sharper desktop, same file size/speed, NOT WebP; (4) product.js idle-preload each colour's photo#1 + decoding=async (instant colour switch); (5) studio.js beforeunload guard when `pending` non-empty (verified inert on clean load); (6) ?v51->52 cache flush = THE White-glitch fix (resolver code untouched, already correct).
- **FULL PHOTO-PIPELINE DRY-RUN done LIVE by Boss (Ace Dress, then cleaned up):** logged into /studio (CloudskinStudio1 works), injected a portrait test JPEG via the app's own file input (override HTMLInputElement.prototype.click + DataTransfer, since the Studio builds the <input type=file> on demand and it's not in the a11y tree), Save -> key product.the-ace-dress.images.white written, image resized 1600x2400 -> 1333x2000 (2000 cap on v51), rendered LIVE as a sub-picture thumbnail (#2; seed keeps catalogue shot as #1). PROVES the whole pipeline (login->file->resize->Storage->Save->live render incl sub-pictures, no redeploy). Cleaned up via the Studio "Remove" button + Save (key reverted to just the catalogue shot; NOTE: Supabase BLOCKS direct SQL delete on storage.objects -> the test JPEG the-ace-dress/1784509497036-pipeline-test-delete.jpg is an orphan in product-media, harmless, purge from Storage dashboard; a harmless residual key product.the-ace-dress.images.white=catalogue shot remains, functionally identical to original).
- **NEW BACKEND NIT (flag to Echo, not urgent):** a real NEW upload came back `Cache-Control: no-cache` (not the immutable the edge fn v2 claims to set) -> repeat/cross-page image loads revalidate. In-page switching still instant (memory cache + v52 preload). Fix = studio-upload edge fn / Storage cache-control.
- **PRE-EXISTING em-dashes (NOT touched in v52, left surgical):** login.html L18+L333 (2, in comments), product.js 24 en-dashes in template copy. Non-customer-facing. Optional tiny cleanup.
- **MIKE's routing this session:** DHL app -> LARISSA (Mike tells her); EasyWP purchase -> LARISSA; Shopify-Payments entity -> Mike will register (wants per-country currency); reviews still NONE. Larissa message drafted (DHL + WordPress only, with links: apps.shopify.com/search?q=DHL Express + namecheap.com/wordpress). Cookie banner is CORRECT (hidden because consent recorded; recommend adding ~12-month expiry). Studio LOGIN verified working in both browsers.

## ▶▶▶▶ LATEST-4 — SESSION 2026-07-20 (photo-pipeline assurance + White "bug" + Mike decisions).
- **v51 CONFIRMED LIVE** (the "V51 DEPLOYING" note below DID land): prod `dpl_6emqaCL3zR6HHZuEVMwK4w7LYM4V`, rollback v50 `dpl_HBM5FtD4cVG5S6Xy4fzo7xf9r97v`. All assets ?v=51, gate+noindex ON. (An early curl showed v50 = stale edge cache; deploy-launch verified live bytes = v51.)
- **Court Skirt WHITE "glitch" = NOT a code bug. It was a STALE BROWSER CACHE on Mike's view.** Verified twice: Corky (every path) + Boss (in-app clean browser on live product.js?v=51): White swatch -> her dsc06995 photo, Black -> dsc06888, initial=her white, NEVER the catalogue `the-court-skirt-1.jpg`. Data is correct in DB + baked snapshot; both photo files 200. Handle is `the-court-skirt`; colours in products.js are exactly "White"/"Black" (slugs match keys, no White-Mist drift for this product). Resolver `studioImagesForColour` already correct. FIX = global ?v cache-flush (v51->v52), NO code change. LESSON (propose to house traps): a live bug you cannot reproduce on current deploy is usually a stale cached asset - check Last-Modified/ETag + bump ?v before writing a code fix. **Studio login re-verified working: `CloudskinStudio1` unlocks the 5-group editor (no error).**
- **MIKE DECISIONS 2026-07-20:** (1) MULTI-CURRENCY = PURSUE per-country currency (his legal step: register a Shopify-Payments entity in US/UK/EU; a UAE entity can't get Shopify Payments; verified live every country still checks out EUR). (2) DHL = INTEGRATE live carrier rates; CONFIRMED via Shopify admin (Larissa Admin, Browser 1) "Carrier accounts: None" + "Shipping labels: None" + Storefront probe shows only manual flat "International" €17 -> so Larissa set up NOTHING for DHL; needs his DHL account + carrier-calculated shipping (Advanced plan / annual add-on / official DHL app), then Echo wires it. (3) REVIEWS = NONE (no reviews, no fakes). (4) WORDPRESS = Larissa buys Namecheap EasyWP -> then Boss wires blog.cloudskin.com DNS + store SEO.
- **PHOTO PIPELINE AUDIT (5-agent workflow) findings:** Studio downscales client-side to 2000px long edge / JPEG q0.92 (studio.js resizeImage L929-943, called L573+L913; toDataURL L937); originals NOT preserved. Her 4160x6240 -> 1333x2000, ~500KB. Tomorrow's uploads APPEAR WITHOUT REDEPLOY via live Supabase fetch (public SELECT RLS), but cold-load shows a brief default-then-swap flash until snapshot re-baked. Subs (thumbnails #2+) render + switch per colour correctly (PDP caps 8/colour vs Studio MAX_PHOTOS=12). Switching: sub-thumbs instant (shared full-res URL), colour-switch has NO preload (~0.5s 4G pause). Legacy 5 stored images = 1500px + Cache-Control: no-cache; edge fn v2 sets immutable so NEW uploads cache 1yr. HIGH risks flagged: (a) SAVE TRAP - uploads live only in in-memory `pending`, persist ONLY on Save (studio_save), NO beforeunload guard -> closing tab loses them; (b) verify her login (DONE, works). Also: iPhone EXIF orientation untested; object-fit:contain over #fff -> white pillar-bars if photos NOT shot on white; P3->sRGB dulling. TOP ACTION: 15-min supervised dry-run with her REAL iPhone photo (login+add+Save+refresh+upright) + brief her "shoot on white, hold upright, click Save".
- **v52 BUNDLE (Corky building, STAGED, NOT deployed, gated on Mike's go):** (1) ?v51->52 cache flush = the White stale-cache fix, global; (2) QUALITY cap 2000->2560 + q0.92->0.88 (Mike confirmed 2560; 0.88 holds file size ~500KB so NO speed regression; NOT WebP - canvas.toDataURL('image/webp') silently returns PNG on iPhone Safari); (3) colour-switch idle-preload (instant switching); (4) Studio beforeunload unsaved-guard. deploy-launch method: mirror live, overlay, bump ?v, deploy --prod, keep gate+noindex, re-bake snapshot AFTER Larissa's ~Jul21 upload for zero-flash. RECOMMEND deploying v52 BEFORE her upload so her photos get 2560px + all caches flush.
- **Status artifact for Mike:** https://claude.ai/code/artifact/fdf9a096-87a2-460f-868c-abdda775a511 (current state + tomorrow plan + complete our-side list).

## ▶▶▶ LATEST-3 — SESSION 2026-07-20 (mobile cookie).
- Mike (phone): "the cookie didn't open". Corky verdict: the BANNER not reappearing is EXPECTED/GDPR-correct (only shows when no recorded `localStorage cloudskin_consent` AND not seen this session `sessionStorage cs_consent_seen`; footer "Cookie Preferences" link reopens it, works on mobile — delegated click handler shell.js ~L791). BUT Corky found a REAL mobile bug + fixed: the Cookie Preferences modal's action buttons ("Reject non-essential"+"Save preferences") are `flex:1`+`white-space:nowrap`, so "Save preferences" was CLIPPED off-screen at 390/375/360/320 (off-screen at ≤330). Fix (CSS-only, main.css `@media(max-width:620px)`): `.cprefs__actions{flex-direction:column}` (stacks full-width). Boss also added iOS safe-area: `.cconsent{padding-bottom:env(safe-area-inset-bottom)}` (banner action row was likely tucked under Mike's iPhone home-indicator = probably what he actually saw) + cleaned 1 em-dash in home.html L136 comment. **V51 DEPLOYED + VERIFIED LIVE 2026-07-20 (deploy-launch agent was killed mid-verify but deploy READY+aliased; Boss curl-confirmed): served css/main.css has BOTH `env(safe-area-inset-bottom)` + `.cprefs__actions{flex-direction:column}`, /home 0 em-dashes, ?v=51 uniform 16/16, gate ON, 200s. All 5 security headers + cleanUrls intact.** Rollback = v50 dpl_HBM5FtD4cVG5S6Xy4fzo7xf9r97v. LEARNING (proposed): a hidden/background tab (or throttled preview pane) PAUSES CSS transition timelines, so a slide-in `transform:translateY()→none` looks stuck off-screen; verify the RESOLVED target with `transition:none` or force a real render before calling it a bug (it self-heals when visible). OPEN/FLAGGED (not fixed): footer "Cookie Preferences" tap target only 18px (<44px) — mis-tap could feel like "nothing opened", optional enlarge; confirm on real iPhone whether banner sat under the home bar (safe-area fix should resolve). Full v50 known-good rollback if v51 bad = dpl_HBM5FtD4cVG5S6Xy4fzo7xf9r97v.
- BIG PICTURE at this point: ALL our-side code DONE (site + i18n + €200 banner/shipping + intl markets US/UK/CA/AU all-EUR + email DNS propagated + colour-photo hardening + Track My Order + mobile cookie). Larissa message SENT (photos/model-specs/size-chart/pairs-well-with). Larissa's tasks + Mike's 2 business steps (Shopify-Payments entity for per-country currency; buy Namecheap EasyWP for blog.cloudskin.com then wire DNS + store SEO) are all that remain. Site stays gated+noindex until Mike's public-launch go.

## ▶▶ LATEST-2 — SESSION 2026-07-19 (continued, "fix everything + close chapter"). Key updates on top of LATEST below.
- **SHIPPING RE-RETUNED to EUR €200 (Mike overrode 850 AED → clean €200).** Echo PROVED the store is **EUR-ONLY at checkout**: no Shopify Payments (UAE business can't get it), `enabledPresentmentCurrencies=["EUR"]`, identical EUR price in every country. So per-currency ($/AED) is impossible; the €200/$230/850 banner was misleading. FIX: in the shipping rate modal use "Change currency"→Store currency (EUR) [it RELABELS, does NOT convert — 25→€25, 850→€850, so re-enter], then set **Domestic flat €6 / International flat €17, both FREE over €200**. Store base currency = **EUR** (Settings→General "Currency display: Euro"; business entity UAE; shipping rates were just entered in AED). Saved+verified both zones read "Free for orders €200.00 and up". NOTE these are **manual flat rates, NOT DHL/UPS** — no carrier integrated; Larissa fulfills+pays the courier herself; €17 likely subsidizes real intl DHL (Mike's open decision: keep flat / raise / integrate live DHL — "Integration for Shipping DHL" is on Larissa's list, NOT done).
- **INTERNATIONAL MARKETS BUILT (Mike: EU+US+UK+CA+AU).** Created one **"International" market** (Active, Currency EUR, Taxes/duties "Not collecting" = fee-free) covering US+UK+CA+AU via the URL trick `markets/new?regions=US,GB,CA,AU&name=International` (the region-picker is freeze-prone; the URL param pre-fills reliably — REUSABLE). Then fixed the EU market's shipping gap: Shipping profile 133566562603 → International zone → "..."→Edit zone → checked the **Europe continent checkbox (278/278 states)** to add all 27 EU incl the missing 13. VERIFIED via Storefront API: **32 countries now available** (AE+27 EU+US+GB+CA+AU), all EUR. Fee-free (no Managed Markets, no paid duties). Verify cmd: `curl -s -X POST https://rta3sf-47.myshopify.com/api/2025-01/graphql.json -H "X-Shopify-Storefront-Access-Token: 54674b6ccdc6cdd3817ee38afd8db4aa" -H "Content-Type: application/json" -d '{"query":"{ localization { availableCountries { isoCode currency{isoCode} } } }"}'`. Markets now: UAE(domestic) / European Union(27) / International(US+UK+CA+AU), all Active. Zone-warning "N countries not in a market" is the harmless inverse (Asia/non-market European countries in the zone can't sell — cosmetic).
- **CODE BATCH DEPLOYED LIVE v49 (2026-07-19, verified):** prod `dpl_53H8vKaipr1h4zg2yqjep5w4Khri`, ROLLBACK `dpl_Ejj4JV5MmTaSj6PJWbV6MkQDAtLc`. Translations live (Arabic nav.categories="الفئات" etc), banner now "COMPLIMENTARY SHIPPING OVER €200" (single), em-dashes cleaned, gate+noindex kept, ?v=49 uniform 86/86, 0 console errors. deploy-launch CAUGHT the 75ea6c2d bundle was STALE vs live v48 and rebuilt from a FRESH live mirror (else it'd have reverted welcome.js/config.js/studio.js/HTML) + fixed the bake-content.mjs template em-dashes then re-baked (17 keys, 0 data-URI, About intact). RESIDUAL for v50: 3 em-dashes still in COMMENTS in `product-content.js` (L2,L8) + `studio.js` (L899) — not in Corky's batch, pre-existing, non-customer-facing (chip queued). Bundle the em-dash residual + the "Track My Order" footer repoint into one small v50 deploy. --- (was: CODE BATCH DEPLOYING deploy-launch v48→v49:) LYRA filled the 14 missing i18n keys in 9 langs + 4 Greek (fixed English leaking into Arabic/es/it/fr/de/nl/pt/sv/ru nav + all-lang sold-out/checkout strings; file `scratchpad/lyra-i18n-strings.js`). CORKY collapsed the banner to a SINGLE €200 (config.js: removed FREESHIP_BY_CUR, freeShipDisplay→"€200", cart qualifies €200; `scratchpad/corky-edits/`) + stripped 46 em-dashes from source comments across 13 files. CORKY FLAG: `bake-content.mjs` header template has em-dashes that reintroduce 2 into content-snapshot.js on re-bake — deploy-launch instructed to strip them post-bake. Confirm deploy landed + verify live.
- **DECISIONS I gave Mike (his awareness):** (1) FAKE REVIEWS = **I refused to create them** (illegal FTC/EU, house Gate J); offered real system (Judge.me free + genuine buyer reviews). PARKED "discuss where later" but answer stays: real only. (2) WORDPRESS-for-SEO = do NOT replace the store; put a WordPress BLOG on a **subdomain (blog.cloudskin.com)** via **Namecheap EasyWP** (Mike buys the plan — I can't purchase/create accounts), then I point the subdomain DNS (add-only) + do technical SEO on the store directly. (3) 2% Shopify third-party txn fee is pre-existing (no Shopify Payments).
- **V50 DEPLOYED LIVE + VERIFIED (2026-07-19):** prod `dpl_HBM5FtD4cVG5S6Xy4fzo7xf9r97v`, ROLLBACK `dpl_53H8vKaipr1h4zg2yqjep5w4Khri` (v49). Sanity-diff 0 unexpected (only 5 Corky files + ?v49→50). deploy-launch CAUGHT that every on-disk bundle's `vercel.json` was wrong/incomplete and recovered the REAL deployed one (cleanUrls + 5 security headers) from the deployment API — reusing a stale copy would have regressed routing + dropped security headers (REUSABLE TRAP: never trust an on-disk vercel.json; pull from the live deployment). Live-verified: colour resolver works on the real PDP (White→white, Black→black exact, "White Mist"→white rename family-match, "  BLACK "→black, never cross-assigns), Track My Order = `href="account.html"`, 0 em-dashes in the 5 files, gate+noindex ON, /home+/product 0 console errors 0 broken images Larissa photos intact, ?v=50 uniform 86/86, content-snapshot byte-identical (not re-baked). ALL our-side code work now DONE. --- (was DEPLOYING:) 3 fixes. (1) PER-COLOUR PHOTO HARDENING — Mike reported PDP colour-switch showing the catalogue default not Larissa's photo. Corky proved via live Supabase + running the real code that CURRENT data resolves correctly (no reproducible bug; the "White vs White Mist" mismatch does NOT exist now) — what Mike saw is most likely a colour Larissa hasn't uploaded yet (data gap → catalogue fallback, expected, resolves on her upload). BUT fixed a real LATENT risk: colour-name DRIFT (e.g. Shopify re-sync renaming White Mist→White, or case/spacing) would orphan her uploaded photo → catalogue. Added `C.studioImagesForColour(bySlug,colour)` in shell.js (exact slug match first, then unambiguous colour-family/token match white↔white-mist, guard: 0 or ≥2 candidates → no match so never cross-assigns); routed product.js galleryFor + shell.js baseImageFor through it. 13/13 harness pass, regression-safe. So per-colour system is bulletproof for Larissa's ~Jul21 upload. OPEN: if Mike names a SPECIFIC product/colour where he KNOWS she uploaded that colour but it showed default, pin the exact trigger. (2) "Track My Order" footer: config.js `foot.track` target "legal:contact"→**"account.html"** (real orders+tracking page; signed-out→login→back). (3) the 3 residual comment em-dashes cleared. Files in `scratchpad/corky-v50/js/`. deploy-launch: mirror live v49, overlay 5 files, ?v49→50, KEEP snapshot byte-identical (no re-bake — avoids re-introducing bake-template em-dashes), verify live. CONFIRM v50 deploy id + rollback + live checks.
- **CURRENCY DECISION (Mike 2026-07-19):** Mike WANTS per-country currency (US→USD, UAE→AED) "because we sell internationally". REALITY (proven live: every country → €84 EUR; `enabledPresentmentCurrencies=["EUR"]`): impossible without **Shopify Payments**, which a UAE business entity can't have (why they run Stripe+PayPal). Per-country currency REQUIRES registering the business entity in a Shopify-Payments country (US/UK/EU) — a legal/business step on Mike's side; once done, flip multi-currency in ~a day. Until then EUR-for-all is correct + live. WORDPRESS: Mike will have Larissa buy **Namecheap EasyWP** (Mohammed needs WordPress) → then wire blog.cloudskin.com DNS (add-only) + do store technical SEO. --- (older LEFT note:) footer "Track My Order" repoint to account/orders page (currently opens Contact modal) — NOT yet done, needs a code edit + deploy. **Waiting on Larissa:** photos(~Jul21), model specs, size-chart image, "pairs well with" picks, the €1 test order (then remove test product).

## ▶ LATEST — SESSION 2026-07-19 (Boss ran the 2026-07-18 NEW-SESSION ACTION LIST). Nothing deployed to prod by this session except the popup (deploy-launch, see below).
**Verified live against the site + admin at session start:** the 2026-07-18 follow-up batch was ALREADY LIVE (v47): men's `nav.shirtsTanks` corrected in all 10 non-EN langs; GDPR cookie banner (shell.js `cs_consent_seen`/`cloudskin_consent`/`consent.allows`) live; the whole Larissa catalog batch live. The ONLY unshipped follow-up piece was the fresh-start popup.
1. **SHIPPING — DONE + verified (live Shopify change).** Mike: "exactly what Larissa said = 200 EUR / 230 USD / 850 AED." Since Shopify shipping rates are denominated in AED, set BOTH zones' free-ship threshold to **850 AED** via the rate modal's "Offer free shipping → Minimum order amount". Domestic (UAE "قياسي") 200→**850 AED** (flat AED 25 below); International zone: enabled "Offer free shipping" + **850 AED** (flat AED 70 below). Both saved ("Profile updated"), verified reading "Free for orders 850.00 د.إ and up". Shipping profile id 133566562603, fulfillment Dubai Warehouse. HOW-TO (the modal freezes CDP screenshots intermittently but IS in the a11y tree): open rate "..."→"Edit shipping option", the Name/Rate/Price/free-ship fields render in the accessibility tree — but the MIN-AMOUNT input is edited by triple-click+type on a screenshot coordinate (no ref); "Done" then the page-top "Save".
2. **DNS branded email — DONE (add-only, site unharmed).** Mike chose sender **info@cloudskin.com** (replies land in Larissa's EXISTING Google Workspace mailbox — the domain already has MX `@`→SMTP.GOOGLE.COM pri 1 at Namecheap). Set it at Shopify Settings→Notifications→Sender email→Save→"Email domain authentication" generated **6 Shopify DKIM CNAMEs** (wxg._domainkey→dkim1.5563702dec1d.p386.email.myshopify.com ; wxg2._domainkey→dkim2… ; pdk1._domainkey.mailerie8→dkim3.db623b0a87a5.p445… ; pdk2._domainkey.mailerie8→dkim4… ; mailerwxg→5563702dec1d.p386.email.myshopify.com ; mailerie8→db623b0a87a5.p445.email.myshopify.com). Re-verified the **Resend** records live (acct webactionhellas, domain still "Not Started"): TXT `resend._domainkey`=p=MIGf…1QIDAQAB ; TXT `send`=v=spf1 include:amazonses.com ~all ; MX `send`=feedback-smtp.eu-west-1.amazonses.com pri 10 ; shared TXT `_dmarc`=v=DMARC1; p=none;. **Added all 10 at Namecheap** (acct Cloudenterprise26, cloudskin.com Advanced DNS) as ADD-ONLY new rows; **A `@`→216.198.79.1 (Vercel), CNAME `www`→cname.vercel-dns.com, TXT `@` google-verify, 2× TXT `_vercel`, MX `@`→SMTP.GOOGLE.COM all left INTACT** (site not repointed). Triggered verification: **Shopify = "Propagating"** (auto→Authenticated, up to 48h), **Resend = "Pending"** (auto-polls). NAMECHEAP HOW-TO (works reliably): each new row via "ADD NEW RECORD" renders in the a11y tree → set Type combobox + Host/Value textboxes via `form_input` by ref (re-read after Type change since the value field ref changes: A→"IP Address", CNAME→"Target", TXT→"Value"), then the green per-row "Save Changes" checkmark saves to DNS immediately (AJAX spinner). The checkmark is a coordinate-click (a link, no ref) and the page auto-scrolls between screenshot and click so the FIRST click usually misses — click again on the settled position (edit row settles ~y=363-387). MX goes in the separate MAIL SETTINGS section (mail setting already "Custom MX"; added host `send`, don't touch the Google `@` MX). Record table saved at scratchpad dns-records.md. Still TODO after propagation: confirm Shopify shows "Authenticated" + Resend "Verified"; optional real @cloudskin.com mailbox/forwarding for info@ (Google Workspace already receives it).
3. **POPUP — deploy-launch running (background).** New `js/welcome.js` written (once-per-SESSION via sessionStorage `cloudskin_welcome_session` + PERMANENT stop via localStorage `cloudskin_welcome_subscribed` set ONLY on successful subscribe; was once-per-visitor-forever). node --check ok, 0 em-dashes (also cleaned 2 pre-existing em-dashes in its comments). deploy-launch tasked to re-mirror LIVE v47, overlay welcome.js only, re-bake content-snapshot.js (bake script at 75ea6c2d bundle/scripts/bake-content.mjs), bump ?v47→48, deploy prod `cloudskin`, keep gate+noindex ON, verify live. CONFIRM it finished + record new/rollback deploy ids.
4. **INTERNATIONAL MARKETS — Echo tasked (background, verified pass).** Mike: full coverage + auto currency + "everything included but NO Shopify fees." Created **EU market** (Active, Currency EUR auto, Taxes/duties "Not collecting" = fee-free DDU, 27 EU countries). DISCOVERED the real blocker: the International shipping zone only covers ~14 of 27 EU countries ("customers in 13 regions can't check out"), AND a critical unknown — multi-currency €/$ checkout normally needs **Shopify Payments** but this store uses **PayPal + Stripe** (may not actually show €/$; customers might be charged AED). Handed to **Echo (backend-integrator)** to VERIFY the currency/payment reality first, then build US + Rest-of-World markets + expand shipping to all sold countries, fee-free (no Managed Markets, no paid duties), keep the EU market, test via Storefront API. AWAIT Echo's report — the currency/payment finding may be a decision for Mike.
KEY FLEET LESSONS this session (propose to Mike before writing): Namecheap Advanced DNS add-record flow is a11y-tree-driven (form_input by ref) — reliable house method for DNS writes; the per-row save + Shopify shipping/market modals auto-scroll and freeze CDP screenshots (2-click settle pattern). Shopify multi-currency vs non-Shopify-Payments is a real gotcha to check on every headless store before promising €/$.

## ▶ RESUME HERE — NEW SESSION AGENDA (Mike, 2026-07-18: "context is getting full, run the rest + GDPR in a new session")
Bundle = `C:\Users\mikef\AppData\Local\Temp\claude\C--Users-mikef\75ea6c2d-9aa2-4169-ad82-275f54c1b095\scratchpad\bundle` (gate `Cloudskin2026!`, studio pw `CloudskinStudio1`).

**✅ DEPLOYED TO PROD 2026-07-18 (session 951ae556) — GDPR + final gate + 2 badge removals + RTL fix + security headers all shipped and live-verified on www.cloudskin.com (gate KEPT ON, noindex KEPT).** What shipped: (a) GDPR cookie-consent banner shown ONCE PER SESSION (sessionStorage `cs_consent_seen`) + granular preferences modal (Essential always-on, Preferences+Analytics default-OFF, `C.consent.allows()` gates any future non-essential cookie; localStorage `cloudskin_consent` v1) in shell.js + main.css `.cconsent`/`.cprefs`; footer "Cookie Preferences" reopens it (withdraw path). (b) login.html signup consent line + self-contained Terms/Privacy modal (loads legal.js). (c) legal.js privacy: new "Who We Are" **data-controller identity** (CloudSkin, based in Dubai UAE, contact support@cloudskin.com) + GDPR rights (supervisory authority/Hellenic DPA, data portability, objection) + consent-withdrawal line; date bumped to July 2026. (d) cookie UI localized all 11 locales (Lyra, 15 `cookie.*` keys × 11 = 165, 0 em-dashes). (e) removed the "New"/"Best Seller" product-card badges (shell.js cardHTML — only Sale/compareAt badge kept; collections still work via isNew/bestSeller flags) AND the editorial "New Color" `.tape` span (home.html); Mike dislikes promo tags on photos. (f) fixed the RTL (Arabic) product-rail carousel: direction + disabled logic (negative-scroll model) + arrow side-swap + chevron mirror (was: prev always disabled/dead). (g) vercel.json security headers (nosniff, X-Frame-Options SAMEORIGIN, Referrer-Policy, HSTS, Permissions-Policy; strict CSP deferred — inline scripts/Shopify/fonts). (h) all `?v` cache tags reconciled to a single **v46** (killed the config v35/v37 + products v35/v38 splits). Adversarial 4-lens review (Workflow) found 4 minor/nit issues (scroll-lock leak when the cookies policy opens over the prefs modal; banner-reject scroll-lock; RTL toggle knob off-edge; OFF-toggle contrast) — all FIXED + verified. security_audit "service-role JWT" FAIL is a FALSE POSITIVE (token decodes to `role:anon`, public by design). NOTE: Shopify Settings→General "business entity" is still the placeholder **"My Store - entity" (UAE)** — Mike may want to set the real registered company name there, and as a UAE controller serving the EU may later want an Art. 27 EU representative. STILL PENDING (unchanged): Mike's launch-day manual €1 test order → confirm My-Orders+tracking, then remove `zz-test-payment-check-delete-after`; after-launch branded @cloudskin.com email DNS (Namecheap, Larissa's `Cloudenterprise26`+2FA).

--- ORIGINAL AGENDA (now done) — kept for reference. Do in order:
1. **Echo image-to-Storage fix — DONE 2026-07-18 (verified):** studio.js `pickImage` now uploads FIELDS images to Storage (dest:'site') via `studio-upload` (redeployed v2, accepts `dest`) → URL not base64; product-photo path unchanged (`uploadImage(...,dest)` generalization). Migrated the base64 banner → Storage URL. Re-baked `js/content-snapshot.js`: 597KB → **1,746 bytes**; DB = 9 rows, 0 data-URIs, 6 Storage URLs. Verified: banner first-paints from the snapshot URL (single render, no flicker), cold load shows photos+banner from snapshot, a new Studio upload stored a Storage URL (test key deleted). Cache bumps `content-snapshot.js?v=2`, `studio.js?v=43`. TWO FLAGS: (i) `studio-upload` `verify_jwt` flipped false→true as an MCP-redeploy side effect — Echo verified SAFE (all real callers send the anon JWT; uploads 200, wrong pw 401); set back to false only if you want to match the earlier cert (needs Supabase dashboard toggle). (ii) one harmless ORPHANED storage object `product-media/site/home-hero-image-1784329204883-echo-upload-test.jpg` (referenced by nothing) — purge from the Storage dashboard whenever.
2. **FINAL site-qa gate** on the finished bundle — the earlier site-qa run happened while files were still being edited (stale); RE-RUN clean. Confirm: single-render/no flicker, photos land (card=PDP first image), gallery premium (single-photo no rail; mobile peek), Studio 5-group works + saves reflect, mobile 390/360/320 no overflow, console clean, cache-version (?v) hygiene, perf.
3. **GDPR (Mike's request):** site HAS privacy/terms/cookies policy modals (legal.js + footer links; privacy already lists GDPR rights) but is MISSING: (a) a first-visit **COOKIE CONSENT BANNER** (accept / reject / preferences) — the headless custom storefront needs its OWN (Shopify's built-in banner doesn't cover it); non-essential/analytics cookies must be gated behind consent, wired to the existing "Cookie preferences" (data-cookie-prefs) + `foot.cookiePolicy`. (b) login/signup page (`login.html`) has NO terms/privacy consent text+links — add "by creating an account you agree to Terms & Privacy" near the signup. Review policy copy for EU/Greek completeness (Lyra for Greek). 
4. **DEPLOY on Mike's explicit go** (deploy-launch): FIRST re-run `node scripts/bake-content.mjs` (regenerate snapshot from live cloudskin_content), deploy WITH the "Launching Soon" gate kept ON (Mike wants it to stay), noindex stays until a true public launch. Verify all changed files' ?v tags are bumped/consistent so no stale cache.
5. **Launch-day (Mike's manual actions):** place the €1 test order (confirms live order→webhook→tracking — the only unexercised link, Echo certified the rest), then remove the €1 test product `zz-test-payment-check-delete-after` from Shopify.
6. **After-launch:** branded @cloudskin.com email (Namecheap DNS SPF/DKIM, needs Larissa's `Cloudenterprise26` + 2FA), optional Greek product DESCRIPTIONS (product-content.js renders English on the EL site — Lyra), minor: account.html wishlist previews use `C.baseImageFor(p)` (snapshot loaded there but inert).
STATUS AT HANDOFF: photo pipeline + open-flash + collection flicker all FIXED+verified; gallery premium (Nami); 5-group editable Studio + merchandising blocks + Complete-the-Look + unified product editor BUILT+verified (Corky); copy em-dash-free + men's labels fixed (Lyra); commerce/backend CERTIFIED ready-to-sell (Echo, order chain proven live); bulletproof photo-snapshot BUILT+verified (cold load shows her photos); banner-to-Storage perf fix DONE+verified (Echo — snapshot 597KB→1.7KB, all owner images now Storage URLs). ALL BUILD WORK COMPLETE. New session = final site-qa gate + GDPR (cookie banner + signup consent) + deploy on Mike's go. FINAL PRE-LAUNCH GATE Mike wants (2026-07-17): a COMPLETE commerce+backend+payments inspection (ECHO-led) — full Shopify money-path (products/prices/inventory/variants/checkout/payments/shipping/tax/notifications), Supabase backend (auth, cloudskin-order-webhook create+fulfilled+updated, customer_orders + tracking, RLS), and the whole order chain cart→checkout→pay→order→webhook→account/My-Orders+tracking — "can the business start the eshop, is anything missing." KNOWN likely gaps to flag (from [[cloudskin-site]]): **Stripe/card IS now set up (Mike confirmed 2026-07-17)** — so both PayPal + card are live pay methods; Echo just VERIFIES the card money-path works end-to-end (card checkout → order → webhook → tracking). Store is behind the "Launching Soon" password gate (must remove to sell); the €1 test order still needs placing to prove order→tracking end-to-end; remove the €1 test product; branded @cloudskin.com email sender still needs Namecheap DNS (orders currently send via shopifyemail.com) — **Mike NOW wants this FIXED (as an "after" follow-up, 2026-07-17), reversing his earlier "too complex, skip it" deferral.** It's a DNS job: add Shopify's (and Resend's, for Supabase auth emails) SPF/DKIM/verification records on the cloudskin.com domain. DEPENDENCY/blocker: DNS lives at Namecheap under Larissa's account `Cloudenterprise26` with HER 2FA (prior save attempts failed on a stale session) — so it needs Larissa's Namecheap access or her adding the exact records we specify. A dormant Resend domain cloudskin.com was already added (does nothing without the DNS). noindex/no robots-sitemap until launch. **ECHO CERTIFICATION DONE 2026-07-17: VERDICT = READY TO SELL.** Order chain PROVEN live end-to-end: fired a real HMAC-signed test webhook (orders/create + orders/fulfilled) to the live cloudskin-order-webhook → 200 + a customer_orders row appeared with line_items + tracking; tampered signature → 401 (HMAC gate works); RLS proven (owner sees own row, other emails 0, anon denied, case-insensitive lower(email)); test row deleted (table back to 0). Edge function production-grade (constant-time HMAC, idempotent upsert, correct 200/401/500 codes). Advisors: nothing blocks launch (deny-all app_secrets/studio_secret by design; definer auth RPCs studio_auth/save/is_admin intended; known Pro-gated leaked-password toggle; 5 negligible RLS-initplan perf WARNs). Sale does NOT depend on Supabase (Shopify owns cart/checkout/pay/order/email); Supabase = on-site My-Orders+tracking, now certified. THE ONE BLOCKER for a first sale = remove the "Launching Soon" storefront gate (deploy-gated). **UPDATE 2026-07-18: Mike wants the "Launching Soon" gate to STAY for now — do NOT remove it; deploy WITH the gate in place (controlled/not-fully-public launch).** Then: Mike places €1 test order (confirms Shopify's live network delivery — the only unexercised link), remove €1 test product, noindex→robots/sitemap. Minor: on-site My-Orders matches by email so a checkout under a different email won't show there (expected); optional grant-revoke + RLS (select auth.fn()) polish.

## LIVE on cloudskin.com now (deployed + verified, do NOT redo)
- Mobile nav fix, Facebook removed everywhere, studio photo per-photo position dropdowns + multi-upload, account-page redesign, corrected-cloud favicon (real `/favicon.ico` at root; corrected cloud = gate hero `M48.493`, not the old `M47.051`).
- **Studio "edit everything"** (Larissa: cloudskin.com/studio, pw `CloudskinStudio1`): per-product controls Best Seller / New-Trending / gender (Women/Men/Both) / reorder / **category (Tops/Bottoms/Dresses)**; full **About page** editing (text + images + focal dot); **footer + homepage headings** editable. Saves apply live: content.js `mergeProducts` overrides window.CLOUDSKIN_PRODUCTS from `cloudskin_content` then fires `cloudskin:products-updated`; home.js/collection.js re-render. Keys: `product.<handle>.{bestSeller,isNew,gender,order,category}`, `about.*`, `footer.*`, `home.*headings`. No Supabase migration needed (studio_save upserts arbitrary key/value).
- Larissa guide PDF: `C:\Users\mikef\Downloads\Larissa-Studio-Guide.pdf` (7 pages).
- Orders backend already built + live: `customer_orders` table + RLS, `cloudskin-order-webhook` edge function (HMAC), account page renders orders + "Track shipment".

## FINAL POLISH FIX LIST (Mike, 2026-07-17/18 — run as ONE Corky+Lyra pass AFTER site-qa returns, then re-verify + go/no-go)
- CORKY DONE 2026-07-18: flicker FIXED + test product purged from all shopper surfaces. Root cause was a DOUBLE render (static parse-time paint from curate defaults, then re-render after content.js applied Supabase overrides → image swap/reshuffle). Fix: collection.js/home.js HOLD the grid until content.js fires a single `cloudskin:content-ready` (on every settle path incl zero-overrides/errors), then render ONCE + fade; 1200ms fail-open timeout paints defaults if Supabase slow/down, and that fallback paint is TERMINAL (guard) so late data can't reshuffle; added Supabase `preconnect` on the 3 shop pages. Test product `zz-test-payment-check-delete-after` (category:"Test") excluded from grids/rails/CTL/search/pairs via `config.js shopProducts()`+home.js+product.js+shell.js, but its own PDP still works (checkout test). Files: config.js/content.js/collection.js/home.js/product.js/shell.js + HTML preconnect+?v bumps. NO CSS touched. Verified BEFORE 2 renders → AFTER 1 render on Women/Men/Trending/Bestsellers/Collection/home via MutationObserver; Boss re-verified Women = stable single render showing her studio photo dsc06995. **BULLETPROOF PHOTOS (Mike chose 2026-07-18): ECHO building a deploy-time content SNAPSHOT** — bake cloudskin_content into a static js/content-snapshot.js loaded synchronously so first paint is instantly correct (her photos show even on a COLD load, no wait/fallback); live fetch still applies post-deploy edits. Must re-bake at each deploy (deploy-launch step). --- ORIGINAL: (1) exclude the €1 test product `zz-test-payment-check-delete-after` from "Complete the Look" + all customer-facing rails (it was showing as a suggested match). (2) Fix the collection-grid FLICKER: opening any shop/collection page (e.g. via a "see all" link) paints the default product set, then content.js loads owner gender/category/order overrides → collection.js build() re-renders → products visibly appear/disappear/reshuffle. Make the grid settle with NO visible reshuffle (same async-override class as the PDP flash Corky already fixed; e.g. hold/fade the grid until overrides applied). Mike: "I don't want it anywhere." This is ALSO why the Men page can look like it's "missing a product" (a card blinks out mid-render) — men's catalog really has only 3 (Performance Tank/Tee/Shorts, all render on the fixed build). Men's menu: KEEP all 6 sub-links (Mike's choice).
- LYRA DONE 2026-07-18: (3)+(4) fixed. `nav.shirtsTanks` → EN "Tees & Tanks" / EL "Μπλούζες & Αμάνικα" (all 11 langs; several others literally said "shirts" too, corrected). `nav.bottoms` EL "Κάτω Μέρη"→"Κάτω Μέρος" (idiomatic, works women+men; EN kept "Bottoms", other langs already fine). node --check ok, 0 em-dashes, browser-verified. NOTE: i18n-strings.js edited again at same ?v=36 (not re-bumped) — final deploy must bump/verify all script ?v tags for fresh cache. --- ORIGINAL (3): men's nav label `nav.shirtsTanks` "Πουκάμισα & Αμάνικα / Shirts & Tanks" is WRONG — they DON'T sell shirts (πουκάμισα); change to accurate tees+tanks wording (e.g. "Μπλούζες & Αμάνικα / Tees & Tanks"). men-only key, safe. (4) `nav.bottoms` Greek "Κάτω Μέρη" is clunky/literal — replace with an elegant Greek term (shared women+men, so must read well for both). (5) OPTIONAL/flagged: product DESCRIPTIONS render English on the Greek locale (product-content.js not translated) — Greek descriptions are a Lyra follow-up IF Mike wants (not yet confirmed).

## Deploy facts
- Deploys go through the **deploy-launch** agent. Live project `cloudskin` = prj_6d0gmXALxXTKZFj7AAVoBZfns12W, team webactionhellascom = team_fAnVpAdOPwPZAR4kW0o2BwID. **CORRECTION 2026-07-18: the plain `vercel` CLI in Bash IS authenticated** as `webactionhellas-dev` (member of team webactionhellascom, access to the `cloudskin` project) — supersedes the old "CLI not authenticated" note. deploy-launch used `vercel deploy --prod` for the upload (bundle ~10MB / ~9MB images, so the Vercel-MCP `deploy_to_vercel` path is NOT viable — it would inline ~13MB base64 in one call; for pure-MCP deploys, convert to a git-linked project or a `.vercel/output` prebuilt tree). Boss-launched deploy-launch was blocked once by the auto-mode permission classifier (production push) until Mike approved. **Current prod (2026-07-18) = `dpl_9jkYRByaEUgmXv6bDphTRM9Atj66`** (aliased www.cloudskin.com); rollback candidate = `dpl_xn5UpPi3WPG1eovH5qr1HiPY9Eud` (`vercel rollback dpl_xn5... --scope webactionhellascom`). Method: mirror + `vercel deploy --prod` (site is CLI-uploaded, not git). The full mirrored bundle (source of truth) is at `C:\Users\mikef\AppData\Local\Temp\claude\C--Users-mikef\75ea6c2d-9aa2-4169-ad82-275f54c1b095\scratchpad\bundle\` (prior session temp — may need re-mirror of live). Store stays behind the "Launching Soon" gate (pw `Cloudskin2026!`); noindex; no robots/sitemap until launch.

## PENDING — finish in the new session
1. **Shopify order webhook + tracking** (Shopify Admin is logged in on **Browser 2**, deviceId 6243a587-f457-4fc6-b407-fabd6fa92db7, as "Larissa Admin"/CLOUDSKIN, cloudcollective.enterprise@gmail.com; store `rta3sf-47`). At admin.shopify.com/store/rta3sf-47/settings/notifications/webhooks: **orders/create is ALREADY registered** → https://ocszztflphqsaoyhlerx.supabase.co/functions/v1/cloudskin-order-webhook (JSON). (b) SECRET — **DONE 2026-07-17**: read the signing secret on that page and wrote it to Supabase `public.app_secrets` key `SHOPIFY_WEBHOOK_SECRET` (edge fn `cloudskin-order-webhook` reads env `SHOPIFY_WEBHOOK_SECRET` first, else this app_secrets row; verify_jwt=false; handles create/updated/fulfilled generically → no code change needed). Now MATCHES live Shopify. (a) WEBHOOKS — **DONE 2026-07-17**: Mike added **Order fulfillment** + **Order update** by hand (agent gave a screenshot walkthrough; the Create-webhook modal's native <select> freezes CDP screenshots so it can't be automated safely). Verified live: all THREE (Order creation + Order fulfillment + Order update) registered → the SAME cloudskin-order-webhook URL, JSON. Pipeline fully wired. STILL TO DO: (c) Mike places the €1 test order (`zz-test-payment-check-delete-after`) to confirm My Orders + tracking populate. NEVER re-login to Shopify (Larissa 2FA); only the already-authed browser.
2. **Collection banners — REMOVED 2026-07-17 (Mike: "I never asked for banners").** Echo had built per-collection hero banners (image+title atop each collection.html page) + 17 "Collection Banner — X" Studio panels; Mike disowned them. Stripped: collection.html banner `<section>`, collection.js banner block + `bannerImg` consts, `.collbanner--page` CSS in main.css, and studio.js `COLLBANNERS=[]` (COLLECTION_SLUGS left unused/harmless). KEPT the ORIGINAL homepage "The CloudSkin Collection" band (.collbanner, home.html) + its 1 "Homepage — Collection Banner" Studio panel (that's original design, not this session). DON'T re-add collection-page banners. Also this round: card object-fit reverted cover→**contain** (Mike saw the Performance Tank "zoomed"; catalogue shots are 2048² squares so contain shows full garment, no zoom); Studio upload quality raised 1500px/0.82 → **2000px/0.92** + imageSmoothingQuality high (studio.js resizeImage).
3. **Photos + home-images + category-labels build — DONE + verified locally 2026-07-17 (session ed713ddb), NOT deployed (gated).** Echo (banner agent) was STOPPED by Mike but had already written its banner code cleanly (all bundle JS parses); I built the photo work MYSELF inline (told not to spawn agents). Changes in the `75ea6c2d` bundle: (a) **studio.js** `currentImages()` now SEEDS each colour with the product's current bundled `/shopify/` shot(s) via new `bundledSeed()` when never edited → photo #1 = existing main, she ADDS after it (never replaces); relabeled control checkboxes "Best Seller"→"Best Sellers", "New / Trending"→"New Arrivals"; section heading "Product controls"→"Product categories & order" with a note mapping to the Women/Men menus; photo notes say photo#1=home image. (b) **FINAL photo model (2026-07-17, after 2 wrong turns — this is the settled truth):** Larissa CHOOSES the base. **Photo #1 (per colour) = the base = the home/shop CARD preview AND the big first image on the product page (they MATCH — click a card, it opens on that same photo → continuity).** Photos #2,#3,#4 = extra views (PDP thumbnails, left on desktop / under on mobile). Implementation: `shell.js` cardHTML mainImg = `p._studioImgs[slug][0] || catalogue shot`, hoverImg = `studioList[1] || alt-colour shot`; `content.js` `mergeStudioImages` attaches `p._studioImgs`; `product.js` galleryFor = studio photos IN HER ORDER (first=#1) else catalogue; studio.js SEEDING kept (un-edited product's manager starts with the catalogue shot as #1, she can reorder/replace). **The "photo bleeds behind on hover" bug** was NOT an image-choice problem — it was `object-fit:contain` letterboxing two stacked card imgs so the one behind peeked through; FIX = `.pcard__media img{object-fit:cover}` in main.css (fills the 3:4 frame, hover fully covers). DON'T revert cards to the catalogue shot again — Mike explicitly wants the card = her chosen photo #1, matching the PDP. **Hover image-swap REMOVED (Mike's call 2026-07-17):** the card now renders ONLY photo #1 (one `<img>`, no `.alt` hover image) — he saw a mini-flash of the 2nd image on hover and wanted zero risk, so there's no second card image at all now (the `.pcard__media img.alt` CSS rules are dead no-ops, harmless). Keep the subtle hover zoom + quick-add sizes. DON'T re-add a hover/alt card image. **Quick-add panel glitch fixed:** `.pcard__quick` was `rgba(247,244,239,.97)` + `backdrop-filter:blur(4px)` → the photo showed through it + flickered on slide-up; changed to solid `background:var(--paper)` and removed backdrop-filter (fully opaque now). Card `object-fit` kept as `cover` (fills the 3:4 card; comment updated). NOTE: previews now show whatever Larissa set as photo #1 — currently model photos for court-skirt + flow-dress (her uploads), catalogue product shots for the rest; that's per Mike's "Larissa chooses the base" rule, she can swap #1 anytime. Verified: flow-dress card + PDP both = her img-5288.jpg; court-skirt forced-hover black shot covers clean (no white bleed); object-fit=cover; 0 console errors. Verified in-browser (port 5252, live Supabase): seeding shows on un-edited products (signature-bra → img/shopify seeds), Flow Dress card = her Supabase photo on home+collection, desktop gallery `72px 1fr` left column fills, mobile 360/320 single-col + horizontal thumb row, 0 overflow, 0 console errors, Echo banners render (Women banner OK). CAVEAT: the 4 products Larissa already reduced to 1 photo (flow-dress, court-skirt, form-bra, club-quarter-zip) keep her saved photo — seeding only pre-fills UN-edited products; she just adds more after her photo. "Collections" nav (c=collection) = ALL products of that gender by design (not a per-product toggle); Larissa curates New Arrivals (isNew) / Best Sellers (bestSeller) / gender only.
4. **Remove the €1 "Test Checkout" product** (`zz-test-payment-check-delete-after`) so it stops showing in the Trending rail (optional; after the webhook test).

## Larissa feedback (July 17 2026, delivered 2026-07-18 via WhatsApp zip; extracted to session c1d4da25 scratchpad\larissa-feedback) - LIVE-SITE FIX LIST
Sources: a numbered TO-DO with screenshots + a short "WEBSITE FEEDBACK" list + the big "WEBSITE" project checklist + a screen video.
QUICK LIVE FIXES (mirror + deploy, deploy-gated): (1) free-shipping threshold "COMPLIMENTARY SHIPPING OVER $270" -> **200 EUR / 230 USD / 850 AED** (per currency); (2) remove the SKU line "STYLE FE-xx" under the PDP price; (3) homepage: move the "Follow on Instagram" band to sit UNDER the "Join the CloudSkin World" band (currently above it); (4) men's "Shop by Category" label "Shirts & Tank Tops" -> **"Shirts & Tanks"** (Larissa's exact wording; note earlier Lyra note said they sell tees not shirts - she still wants "Shirts"); (5) footer links point to wrong pages - audit + fix each; (6) the 10%-off discount popup is broken/awkward on MOBILE; (7) surface/verify a currency switcher + the language option; (8) size-guide link on PDP - verify it opens the uploaded chart.
P0 BUG - STUDIO IMAGE REPLACE: **FIXED by Corky (mirror js/studio.js, DEPLOY-GATED, not shipped).** ROOT CAUSE (proven): the global `imgPickBusy` single-upload lock in pickImage() silently swallowed any "Replace image" click made WHILE another site-image upload was in flight; her 4160x6240 photos take 1-4s each, so rapid multi-image replaces (tiles 2/3, editorial) were dropped with no picker + no error. Synchronous edits (focal-drag, "opens" dropdown) saved fine -> matches the live DB state (tile1.image + all .pos saved; tile2/3.image + editorial.image absent; Storage had only tile1+collbanner objects; edge-fn logs only 200s/401s, no 4xx/5xx = failure was client-side before any fetch). FIX (minimal, frontend only): (1) removed the global lock (the per-field btn.disabled still blocks same-image double-click; other fields stay live; concurrent uploads safe - independent keys + timestamped paths); (2) replaced the blanket `pending={}` on save with a per-key clear (fixes a SECONDARY save-wipe race that discarded an image finishing during a save round-trip while showing a false "Saved"). Verified against real studio.js (before/after harness), node --check OK, 0 em-dashes added, throwaway key round-trip done+deleted, Larissa's real keys untouched. FLAG (possible 2nd blocker, unconfirmed): HEIC - pickImage rejects empty-MIME files, so .HEIC iPhone photos on Chrome/Firefox would be blocked (couldn't confirm her format); cheap mitigation = export as JPEG or use Safari; a real in-studio HEIC decoder is a larger change (NOT built - flag). Pre-existing em-dash in studio.js L899 comment (not Corky's) to clean on next edit. STILL BLOCKS her ~Jul 21 upload UNTIL DEPLOYED (Mike's go) + HEIC confirmed.
DECISION NEEDED - FACEBOOK: live source currently has FB REMOVED (verified in mirror: no FB href in footer/gate; only a dormant unused icon glyph in shell.js L90). But Larissa's feedback asked to FIX the FB link (login + footer) and gave the correct URL **facebook.com/people/Cloudskin/61590555028212** and marked "Facebook done". So confirm with Mike: keep FB removed (then her FB items are already done), or RESTORE it with that correct link.
**MIKE DECISION 2026-07-18 (relayed via Boss): FACEBOOK = add it ONLY IF Larissa has sent a Facebook link/handle. She HAS (facebook.com/people/Cloudskin/61590555028212, marked "Facebook done") → so RESTORE FB with that exact link (footer + login, wherever the other socials live). Wire it into the FB glyph already dormant in shell.js L90. Also: polish EVERYTHING else to perfection, but DO NOT DEPLOY — Mike reviews the whole site himself before anything goes live. Deploy stays hard-gated on his explicit go.**
FLAG - REVIEWS: checklist says "Add reviews + rating (we can make fake ones)". House rule = NO fabricated reviews (Gate J + legal risk). Use a real review system, not fake ones.
Much of this overlaps the in-flight Vellum editor build (currency/language, image replace, complete-the-look, model specs).
STATUS 2026-07-18 (batch APPLIED in mirror, DEPLOY-GATED, verified locally by website-builder; independent site-qa gate running): 7 files changed (config.js, i18n.js, shell.js, product.js, i18n-strings.js, main.css, home.html) - bump all `?v=46`->47 at deploy. DONE: (1) free-ship threshold now per-currency EUR200/USD230/AED850 via new FREESHIP_BY_CUR + freeShipDisplay()/freeShipEur() + moneyExact(); cart qualification agrees with banner (the other 7 currencies use EUR-200 converted approximations - confirm/adjust); (2) SKU "Style FE-xx" line removed from PDP (product.js; .buybox__style CSS now unused/harmless); (3) home Instagram band moved BELOW the "Join the CloudSkin World" newsletter band; (4) men's nav.shirtsTanks EN "Tees & Tanks" -> "Shirts & Tanks" (LIVE EN was "Tees & Tanks", NOT "Shirts & Tank Tops"); (6) welcome/discount popup mobile hardened (max-height 90dvh, 40x40 close); (9) PDP description line-height 1.9->1.8 + max-width ch measure. NO CHANGE NEEDED (already work): (5) footer links all functional, (7) currency+language switcher present/working (3 access points), (8) size-guide link opens the modal tables. FLAGS needing Mike/Larissa: footer "Track My Order" points to the Contact modal (no tracking page exists) + policy links open MODALS not routed pages (could be her "not linked to right page" complaint - confirm); discount-popup exact mobile issue NOT reproduced headlessly (need her device/screenshot); size-guide shows built-in tables, so if she uploaded a specific chart IMAGE it is not wired (need her file); men's nav.shirtsTanks in the OTHER 10 languages still say Tees/T-shirts concept -> LYRA to align natively to "Shirts". No em-dashes introduced; no regressions (0 overflow/0 console errors across all pages incl Arabic RTL); studio.js untouched by this batch. INDEPENDENT site-qa gate = PASS (all 6 applied changes correct, all 3 "already work" claims true, 0 regressions, clean in all 11 langs). Review board for Mike published: https://claude.ai/code/artifact/099a76c7-34d9-4547-b88b-b8f2dfe288f2 . NEXT: deploy is gated on Mike's go (apply to live source, bump ?v46->47, deploy studio.js + the 7 batch files); decisions pending = Facebook keep/restore + reviews approach; clarify w/ Larissa = which footer link, popup device/screenshot, size-chart image, model specs data + HEIC photo format.
DEPLOYED TO PROD 2026-07-18 (session c1d4da25, via deploy-launch): all the above catalog fixes + the Studio image-replace fix (studio.js) + Facebook RESTORED (footer via config.js socials + the Launching-Soon gate index.html; URL https://www.facebook.com/people/Cloudskin/61590555028212/) are now LIVE on www.cloudskin.com. New prod deploy **dpl_4kiXqpcPu2KFGpYHFQzUveko7M2R** (project `cloudskin` prj_6d0gmXALxXTKZFj7AAVoBZfns12W, team webactionhellascom); ROLLBACK candidate **dpl_9jkYRByaEUgmXv6bDphTRM9Atj66**. deploy-launch re-baked content-snapshot.js from live cloudskin_content (9->17 keys incl her tile photos + about.story.p1, no first-paint flash), bumped ?v46->47 on the 7 storefront HTMLs, RE-LINKED .vercel/project.json off the wrong "cloudskin-mirror" onto the real `cloudskin` project. Gate KEPT ON (Cloudskin2026!) + noindex preserved (controlled launch). Verified live: gate + IG/TikTok/FB working links, unlock->storefront, Larissa's tile photos + About text survived, free-ship EUR200/USD230/AED850, no SKU line, IG band below newsletter, "Shirts & Tanks", FB in footer, 0 broken images, 0 site console errors. STUDIO LOGIN CONFIRMED WORKING: `CloudskinStudio1` at cloudskin.com/studio (studio_auth returns true; Mike's earlier "can't login" was input/wrong-page, likely the gate pw Cloudskin2026! vs studio pw confusion). STILL OPEN (not in this deploy): reviews (real system, Mike's call - no fakes), model specs (her data), size-chart image (her file), footer "Track My Order" target, DHL/SEO/test-orders/social-manager, email @cloudskin.com DNS, remove the EUR1 test product.

## ▶ HANDOFF / CONTINUE-HERE STATE (session c1d4da25, 2026-07-18; context filling, preserve for a fresh session)
LIVE (DEPLOYED + verified): the big batch is on www.cloudskin.com - deploy **dpl_4kiXqpcPu2KFGpYHFQzUveko7M2R** (rollback **dpl_9jkYRByaEUgmXv6bDphTRM9Atj66**): all Larissa catalog fixes + Studio image-replace fix + Facebook restore (footer + gate). Snapshot re-baked, gate KEPT ON, noindex kept.
FOLLOW-UP BATCH - STAGED in the mirror (`...\c1d4da25...\scratchpad\cloudskin-mirror`), NOT deployed; must pass the double-check gate + Mike's explicit deploy go:
  1) `js/i18n-strings.js` men's `nav.shirtsTanks`: 10 non-EN locales corrected to the accurate casual-TEE word (ar تيشيرتات / el Μπλούζες / es Camisetas / it Magliette / fr T-shirts / de T-Shirts / nl T-shirts / pt Camisetas / sv T-shirts / ru Футболки); EN stays "Shirts & Tanks". Mike chose the accurate tee word (men's category = performance tees+tanks, NOT woven shirts). node --check ok, 0 em-dashes, Boss-verified. Minor open: IT Magliette vs loanword; PT Brazilian vs EU register.
  2) `js/welcome.js` discount popup: Mike wants it on a FRESH START -> Corky changing to once-per-SESSION (sessionStorage gate) + PERMANENT stop on email-subscribe (localStorage). Superseded the earlier 14-day-cooldown version. 
  BATCH NEXT STEPS: independent site-qa gate (all pages / 11 langs / no regressions / node --check / 0 em-dash) -> deploy-launch (record rollback, RE-BAKE snapshot from live content, bump ?v, deploy, verify live) ON MIKE'S GO. Cookie banner left as-is (once-per-session, correct GDPR).
IN FLIGHT: Echo (READ-ONLY) = (a) verify the Shopify free-shipping rule matches the banner (200 EUR/230 USD/850 AED) and report if checkout honors it; (b) gather the EXACT @cloudskin.com email DNS records (Shopify Settings->Notifications->Sender email authentication + Resend if used). Corky finalizing the fresh-start popup.
NAMECHEAP DNS (branded @cloudskin.com email): the automatic write was BLOCKED by the auto-mode safety classifier (gates live-DNS). PLAN: Echo gathers exact records -> SHOW MIKE the host/type/value list -> write to Namecheap on his approval via the Chrome he connected (Mike confirmed Namecheap is logged in on Chrome; 3 browsers connected, confirm which; Shopify admin = deviceId 6243a587 "Browser 1"). GUARDRAIL: only ADD email-auth records, NEVER modify existing DNS, never re-login (2FA).
STILL OPEN (decisions/data): reviews = Mike's call, REAL system only NO fakes (not built); model specifications = need Larissa's numbers; specific size-chart image = her file if any; footer "Track My Order" -> currently the Contact modal (no tracking page) - Mike to decide target; 2 vague checklist lines ("integration with new front end", "newsletter/network integration") = need Larissa to clarify.
DECIDED: Complete-the-Look = HANDPICK (Larissa picks per product in Studio; auto-fallback if empty) - ready, no code change. Facebook = RESTORED. Studio login = `CloudskinStudio1` at cloudskin.com/studio WORKS (earlier "can't login" was input / wrong-page vs the gate pw `Cloudskin2026!`).
MIKE'S STANDARD (reinforced hard): NO mistakes, double-check EVERYTHING - every change independently verified + an independent site-qa gate before any deploy; DNS records shown before any write; deploys always gated + verified live after.
VELLUM on-canvas editor = PARKED for next week (Nami design + Echo backend were interrupted; resumable; see [[live-site-editor-product]]). Mike review board artifact: https://claude.ai/code/artifact/099a76c7-34d9-4547-b88b-b8f2dfe288f2

### ECHO read-only findings (2026-07-18)
SHIPPING vs banner = **MISMATCH (material)**: 1 shipping profile, fulfilled from Dubai (UAE); Athens listed but not fulfilling. Domestic UAE: flat AED 25, free-ship ENABLED at min **AED 200** (banner says 850 -> mismatch, store more generous). International (27 countries): flat AED 70, **NO free-ship threshold at all** -> "free over 200 EUR / 230 USD" is FALSE for international customers (they always pay ~AED 70). Also the 27 intl countries are not yet in a Market (intl selling may not be live). FIX = MIKE'S DECISION: (a) add a Shopify international free-ship rule (min ~850 AED equiv) + align domestic, to honour the banner; or (b) change the banner to match reality. Shopify config change (was classifier-gated).
DNS - **RESEND records CAPTURED exact + complete** (Supabase/app auth emails; Resend acct webactionhellas, cloudskin.com added, status Not Started). ADD at Namecheap (host = subdomain only): TXT `resend._domainkey` = `p=MIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQDCbIBaMQIoObXO7fIP+0wfFAk71r48aMvTGI8uLaTkSgMz5MeR2rJE993ROb6MFXy/DMW/61W19UUAujB5LgkpG550yq204h5COzIejme7SgJGptZ7xC/FhcgZOjfKXPpu0nV4LtQrFHs98qcBkzPtzMOJkbov9uMor+UP1Fh/1QIDAQAB` ; MX `send` = `feedback-smtp.eu-west-1.amazonses.com` (pri 10); TXT `send` = `v=spf1 include:amazonses.com ~all` ; TXT `_dmarc` = `v=DMARC1; p=none;` (the _dmarc covers BOTH Resend + Shopify).
DNS - **SHOPIFY sender-auth records NOT captured** (blocked): Shopify only generates the per-domain CNAMEs after an @cloudskin.com sender is entered in Settings->Notifications->Sender email, and that field entry was classifier-blocked. Shopify manual auth = multiple CNAMEs (DKIM+SPF; no separate SPF TXT; no IPs) + the shared _dmarc TXT. To get them: Mike enters an @cloudskin.com sender + screenshots the "Email domain authentication" modal, OR grant the typing permission. Current Shopify sender field = cloudcollective.enterprise@gmail.com; customers see store+100215259435@shopifyemail.com.
DNS - **CRITICAL, DO NOT repoint cloudskin.com to Shopify**: Shopify Domains shows cloudskin.com "Invalid DNS" and prompts changing A `@` 216.198.79.1 (Vercel) -> 23.227.38.65 and CNAME `www` cname.vercel-dns.com -> shops.myshopify.com. DO NOT apply these - the site is headless on Vercel; repointing takes it DOWN. Only ADD the email records (separate hosts). "Invalid DNS" is expected/harmless for headless.
Namecheap WRITE = gated on Mike's approval of the record list (auto-write was classifier-blocked).

### ▶ NEW-SESSION ACTION LIST (Mike 2026-07-18: "yes do everything, no mistakes, DON'T harm the site with the DNS, run all the stuff in a new session")
Mike wants ALL remaining work run in a FRESH session (this one's context is full). He confirmed Larissa gave the complimentary-shipping policy and approved the DNS. Execute in order, double-checking each (hard standard = no mistakes):
1. **DEPLOY the follow-up batch** (staged + verified in the mirror): the 10-language men's tee labels + the fresh-start popup. First run the independent site-qa gate (all pages, 11 langs incl Arabic RTL, no regressions, node --check, 0 em-dash), THEN deploy-launch (record rollback, RE-BAKE snapshot from live content, bump ?v46->47, deploy to project `cloudskin`, verify live). Gated on Mike's explicit go.
2. **COMPLIMENTARY SHIPPING** - Larissa confirmed the policy; make Shopify checkout AND the banner AGREE (target = the live banner: free over 200 EUR / 230 USD / 850 AED). Current: domestic UAE free at AED 200 (mismatch), international NO free-ship (flat AED 70). CONFIRM the exact thresholds/zones with Mike/Larissa (esp. domestic 200->850 and whether international free-ship is truly wanted - a margin call), then set Shopify to match + keep the banner matching. Shopify settings change (was classifier-gated).
3. **DNS branded @cloudskin.com email** (Mike APPROVED, carefully). GUARDRAIL Mike stressed ("don't harm the site"): NEVER repoint cloudskin.com A/www to Shopify (takes the Vercel site DOWN); ONLY ADD email records; never modify/delete existing; never re-login (2FA). (a) ADD the 4 RESEND records (captured above) to Namecheap. (b) Get the SHOPIFY sender-auth CNAMEs: Mike enters an @cloudskin.com sender in Shopify Settings->Notifications->Sender email (or grants the typing permission) -> capture the "Email domain authentication" records -> ADD to Namecheap. Shared _dmarc TXT covers both. Verify in Shopify/Resend after.
NEEDS FROM MIKE in the new session: (i) explicit deploy go for the batch; (ii) the exact complimentary-shipping thresholds/zones (or "match the banner"); (iii) enter the @cloudskin.com Shopify sender (or grant permission) for the Shopify DNS records; (iv) approve the DNS write (classifier may re-gate).
STILL OPEN (unchanged): reviews (real system, NO fakes - Mike's call); model specs (her data); size-chart image (her file); footer "Track My Order" target; 2 vague checklist lines (need Larissa). CTL = handpick (ready). Vellum on-canvas editor parked for next week.
