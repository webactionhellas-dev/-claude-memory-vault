---
name: cloudskin-vercel-deploy
description: cloudskin-preview.vercel.app is the live CloudSkin site the user iterates on; its REAL SOURCE lives on another desktop, so fix via a local mirror in cloudskin-live-fix
metadata: 
  node_type: memory
  type: project
  originSessionId: ef77e4c4-73b8-41d1-882f-1dc6c3d1fd02
---

**★ 2026-07-11 — LIVE SOURCE IS ON ANOTHER DESKTOP (user confirmed).** The user iterates on **https://cloudskin-preview.vercel.app** and now builds it on a DIFFERENT machine, so NO local folder matches production. Live was at `main.css?v=14`, ahead of every local folder (`cloudskin-current` was only v=10), with content not present locally (an extra "A Way of Living" statement section) and a DIFFERENT language set: **en, ar (RTL), el, es, it, fr, de, nl, pt, sv, ru** (European + Arabic — NOT the older ur/fa/hi/zh/tr set). The `[aria-label="Account"]` mobile-hide selector broke i18n. **Workflow to fix the live site when the source isn't local:** curl-mirror every live text file (5 HTML + css/main.css + 12 js + robots/sitemap) into a folder that already has the images (copied `cloudskin-current` → **`cloudskin-live-fix`**, only 2 imgs missing: `img/brand/sg-figure-{men,women}.png`), re-add `vercel.json` (X-Robots-Tag noindex + Cache-Control no-cache — confirmed via live response headers), bump `?v=`, apply fixes, verify with launch config **cloudskin-live-fix** (port 5216), then `vercel deploy --prod` (blocked by auto-mode classifier → needs user OK). `cloudskin-live-fix/.vercel` links to the cloudskin-preview project (prj_uzDzqcjDXqctUDSUwsi7LIoYoNwc, team webactionhellascom, CLI authed webactionhellas-dev). **Fixes staged 2026-07-11 in cloudskin-live-fix, NOT deployed (user said "we have more changes so don't deploy" — batching):** (1) carousel `.railnav:disabled` opacity 0→.34 so BOTH prev+next arrows always show (was right-arrow-only); (2) header search-icon-over-logo "in some languages": changed `.nav__icons a[aria-label="Account"]`→`a[href="login.html"]` (language-independent hide), tightened mobile `.nav__left/.nav__right` gap to 12px, and added ≤400px `.header .logo` letter-spacing reduction. Verified no overlap en/ar/de at 320/375/430 + desktop unchanged.

**★ 2026-07-11 (batch 2, Panos WhatsApp brief) — staged in cloudskin-live-fix, NOT deployed (still batching, user said don't deploy).** Client Panos sent (WhatsApp today) alo-style flow refs. Built in cloudskin-live-fix, bumped to **?v=16**, all verified in preview (port 5216), 11-lang i18n done, no console errors: (1) **Announcement bar** now the 3 texts he asked for — `ann.1/2/3` → "Complimentary shipping over {amount}" (amount = `C.money(CLOUDSKIN.FREESHIP=250)`, shown in active currency), "Duties & taxes included", "Secure worldwide delivery" (translated all 11 langs in js/i18n-strings.js). (2) **"Added to Bag" popup** (mini-cart, replaces the toast) — `showAdded()` in shell.js: product img/name/color/size + "Order Subtotal (N)" + View Bag/Checkout + **"Pairs Great With"** row via new `C.pairsWith(handle)` (ported Complete-the-Look logic). Top-right dropdown (`#addBag`), top set in JS from header bottom; full-width top sheet ≤560px; RTL flips to left. (3) **Shipping logic** in cart drawer — under €250 = "Calculated at checkout" + "Add {amount} more…" hint; ≥€250 = "Complimentary" + unlocked msg; + duties note "The total you pay includes all applicable customs duties & taxes. We guarantee no additional charges on delivery." (`updateShipping()`, `C.shipInfo()`). DHL will calc real shipping later. (4) **Wishlist** — PDP "Add to Wishlist" btn under Add to Bag (`#wishBtn`, `data-wishbtn`), header heart `#wishBtnHeader` (hidden ≤560 via `.nav__icons .wishbtn`—needs the `.nav__icons` prefix to beat `display:grid`), mobile-menu "Wishlist" entry (`data-open-wish`), and a wishlist drawer `#wishDrawer` (renderWish/openWish). `C.wishlist` extended (list/count/remove/open). (5) **Search** now works — was a dead button; built `#searchOverlay` top-sheet + `openSearch/runSearch` (live filter over title/color/category/gender/fabric/blurb/activities, links to product.html). (6) **Search-icon collision** — the earlier `[href="login.html"]` account-hide + 12px gap + ≤400 logo already handle it; heart is desktop-only so mobile stays 2 icons (search+bag), verified no overlap en/de/ar(RTL) at 320/375. Order-confirmation modal (checkout) already existed and matches his "we received your order" ref. NOTE: `toast.added`/`cart.ship` strings now unused (harmless). (7) **Added RUB currency** to js/i18n.js `CUR` (₽, rate 98 EUR-base) + `REGION_CUR` RU→RUB + Moscow/RU timezones→RUB, so it shows in the currency switcher and Russian visitors default to it (live currency list is now USD/EUR/AUD/AED/GBP/THB/SEK/RUB/SAR/KWD — NOT the old AED/SAR/INR/CNY/TRY/RUB set).

**★★ 2026-07-11 DEPLOYED: the whole 2026-07-11 batch (announcement 3-texts, added-to-bag popup + pairs, shipping/duties, wishlist + heart icon, search, RUB currency, header/arrow fixes, Supabase backend scaffold) is now LIVE on cloudskin-preview.vercel.app at `?v=18` (vercel deploy --prod from cloudskin-live-fix). The live site is now NEWER than the home-machine source — anyone editing the real source must re-mirror the live site first or they'll revert this. DECISION: going **headless Shopify** (keep the custom design, Shopify powers products/cart/checkout/accounts/orders/analytics → the Supabase backend will be REMOVED and replaced by Shopify). Owner does the Shopify step on their home machine; I gave them a copy-paste prompt for it. Fixed the wishlist heart: replaced a lopsided hand-drawn path with the Feather heart `M20.84 4.61a5.5 5.5 0 0 0-7.78 0...` in js/shell.js (header) + js/product.js (PDP).**

**★ 2026-07-11 (batch 3, BACKEND) — Supabase auth + members + owner dashboard, staged in cloudskin-live-fix, NOT deployed, NEEDS the owner's Supabase keys to activate.** User wants real sign up/sign in (email + Google; **Apple skipped** per user — needs paid Apple Dev acct) so members register for email marketing, plus an owner-only analytics dashboard at **/admin** (login hello@cloudskin.com / **CLOUDSKINBACKEND1** — the L was a typo in COUDSKINBACKEND1, user confirmed CLOUD). Built (all guarded so the site runs untouched until keys are pasted): `js/supabase-config.js` (PASTE url+anonKey here — currently empty), `js/auth.js` (Supabase client `CLOUDSKIN.sb` + `CLOUDSKIN.auth` API), reworked `login.html` (email/pass + Google + sign-in/sign-up toggle + account view; Apple removed), `admin.html`+`js/admin.js`+`css/admin.css` (gate = Supabase Auth admin-email only; KPIs, 30-day SVG revenue chart, most-bought bars, recent orders/members tables), `supabase/schema.sql` (members/orders/order_items + RLS + signup trigger + admin_dashboard() RPC + **place_order() RPC**), `SUPABASE_SETUP.md` (owner's 15-min steps), `/admin` rewrite in vercel.json (cleanUrls). supabase-js loaded via jsdelivr CDN on all pages. Admin password is a real Supabase user (hashed), NOT hardcoded in client. **Ran an adversarial audit via Workflow (ECHO+CORKY agentTypes, 20 agents, verify stage) — confirmed 4 real schema bugs, killed 13 false positives; ALL 4 FIXED:** (1) order writes were broken under RLS (client `.insert().select('id').single()` filtered by admin-only SELECT → line items never saved) + abusable → now go through `place_order(p_email,p_currency,p_items jsonb)` SECURITY DEFINER RPC (shell.js `recordOrder()` calls `sb.rpc('place_order',...)`, no direct table insert); (2) dropped orders_insert/order_items_insert policies; added orders_select_own/order_items_select_own for members; (3) `handle_new_user()` marketing_opt_in now parsed with a CASE (no ::boolean throw) + exception-safe so it never blocks signup. **Known residual (documented in SUPABASE_SETUP.md "hardening"):** checkout has no payment yet so prices come from the client — before real money, add a server-side products/price table + rate-limit place_order(). Assets bumped to **?v=17**. Preview: launch config cloudskin-live-fix port 5216.

**(historical) SOURCE-OF-TRUTH for cloudskin-preview.vercel.app was `claude projects/cloudskin-current` (2026-07-08), NOT the old `cloudskin-preview` folder.** The live cloudskin-preview.vercel.app had been pushed to `?v=9` by the user/another process — AHEAD of every local folder (which were v3–v4) — and it added an **i18n language+currency switcher** (`js/i18n.js` + `js/i18n-strings.js`, a globe "EN · USD" button in the header) plus reworked category tiles (Tops/Bottoms/Dresses). None of my local folders had this. So I **downloaded the live site** (code + the 53 images it actually uses) into `cloudskin-current`, fixed the mobile header (icons were sitting behind the centred logo — the ≤1080px grid used `minmax(0,1fr) auto minmax(0,1fr)` so side groups shrank below content; changed to `auto minmax(0,1fr) auto` + content-sized side gaps + logo `clamp(13.5px,3.6vw,16px)`), bumped to **?v=10**, linked the folder to the cloudskin-preview project (`vercel link --project cloudskin-preview --yes` → team webactionhellascom, prj_uzDzqcjDXqctUDSUwsi7LIoYoNwc) and deployed to production (`vercel deploy --prod --yes`). Live now = v10 with the header fix. Preview locally with launch config **cloudskin-current** (port 5215). To update again: edit `cloudskin-current`, bump ?v, `vercel deploy --prod --yes` from it. Ignore the stale `cloudskin-preview` (v4) / `cloudskin-epitelous` (v4) folders unless reconciling.

**cloudskin-epitelous.vercel.app (2026-07-08):** the user's own "cloudskin epitelous" build (uploaded via WeTransfer — Greek for "finally"; = a copy/derivative of the cloudskin-preview site with all the 2026-07 work) is LIVE at **https://cloudskin-epitelous.vercel.app** (deployment-specific: cloudskin-epitelous-6a2tftotg-webactionhellascom.vercel.app). New Vercel project `cloudskin-epitelous` under team `webactionhellascom` (CLI authed as webactionhellas-dev). Public (200, no auth wall), unlisted (noindex via vercel.json + robots.txt). First `vercel deploy --yes` on the fresh project auto-became production + got the clean alias. Local folder: `claude projects/cloudskin-epitelous` (flattened from the zip, old .vercel link to cloudskin-preview removed). To update: `vercel deploy` (preview) from that folder — `--prod` is blocked by the auto-mode classifier, so it needs manual approval.

**CURRENT LIVE URL (use this): https://cloudskin-store.vercel.app** — deployed 2026-07-07 from folder `claude projects/cloudskin-store` (Vercel-linked to a NEW project "cloudskin-store"). Created because the user's iPhone Safari kept serving a stale cached stylesheet from the old URL and they asked for a brand-new link instead of updating the existing one. GOING FORWARD: edit `claude projects/cloudskin-store` and `vercel deploy --prod --yes` from it; it's a full copy of cloudskin-preview + the size-guide modal. `cloudskin-preview` (folder + local preview port 5213 + old url below) is now the legacy mirror — if you edit it, sync to cloudskin-store before deploying, or just edit cloudskin-store directly (spin up a no-cache preview server on it if needed).

The older CloudSkin "preview" build is also still live at **https://cloudskin-preview.vercel.app** (works on any device's browser). Deployed 2026-07-07 under the Vercel account **webactionhellas-dev** (same account as [[webactionhellas-vercel-migration]]). Project name "cloudskin-preview".

Deployed folder: `claude projects/cloudskin-preview` (extracted from the user's `Downloads/cloudskin preview.zip` and served locally by preview server **port 5213**, launch config `cloudskin-preview`).

**Unlisted by request** (2026-07-07): user wants it viewable by link only, NOT a "real live"/indexed site. Enforced 3 ways — `noindex,nofollow` meta on all pages, `robots.txt` `Disallow: /`, and `vercel.json` sets `X-Robots-Tag: noindex, nofollow, noarchive` on all routes. Anyone with the link can still view it (free Hobby plan = no password gate; true password protection would need Vercel Pro).

**Cache gotcha (important):** the user's iPhone Safari aggressively cached the stylesheet, so redeploys looked like they "didn't work" (they kept seeing the original popup across 3 rounds of fixes). Fixed by (a) `vercel.json` header `Cache-Control: public, max-age=0, must-revalidate` on all routes, and (b) **versioned asset URLs** in every HTML file: `css/main.css?v=2` and `js/*.js?v=2`. When you ship a visual change the user says they "can't see", BUMP the `?v=` number (v3, v4…) across the HTML files and tell them to hard-refresh / open a fresh Private tab.

**To update the live link:** `vercel deploy --prod --yes` from `claude projects/cloudskin-preview` (Vercel CLI is authed; `.vercel/` project link now exists in that folder).

**Divergence note:** this deployed "preview" build is NOT identical to the [[cloudskin-site]] canonical (`claude projects/cloudskin`, preview port 5212). The deployed build has a "10% off" welcome popup on load, a light-blue Instagram-follow section, and the restored original login page with transparent buttons. The canonical has the dark Instagram section and (had) the editorial login redesign. User has been iterating on the 5213/preview build — treat it as current. Offer to reconcile the two into one before any custom-domain launch.

**Founders Edit adaptation (2026-07-07):** changes in `cloudskin-preview` only (5213) — NOT yet synced to `cloudskin-store`/`cloudskin` or deployed. Source: `Downloads/THE FOUNDERS EDIT WEBSITE PRODUCT CONTENT.pdf`. User FIRST asked for a homepage `.founders` section, then told me to REMOVE it — so the homepage is back to its original layout (hero → Trending → collbanner → statement → editorial); no `.founders` markup/CSS remains (unused `img/founders/look-*.jpg` + `campaign-duo.jpg` left on disk, harmless). What STAYS: (1) Three NEW products in js/products.js + js/curate.js: Form Bra (White), Ace Dress (White), Performance Shorts (Black, gender Men, category "Shorts") — images `img/shopify/the-{form-bra,ace-dress,performance-shorts}-1.jpg`. Catalog now 15 (was 12). Full PDF details (desc/features/fit/fabric) for all 15 live in `PRODUCT_CONTENT` in js/product.js. (2) config.js colorHex Black #1E1E1E / White #F7F7F7. (3) PDP: product description + FULL feature list now render in the buybox (under Add to Bag); the old "Product Details" accordion was removed (Fabric&Care / Fit / Shipping remain). (4) Trending/rail click fix in js/home.js — drag-to-scroll now only starts after 8px movement so plain clicks navigate (the old code captured the pointer on every pointerdown and cancelled clicks). (5) Perfect gender sorting: js/collection.js now passes a `gender` URL param into resolveCollection; every Women nav link carries `&gender=Women`, every Men link `&gender=Men`; men's nav = Tops+Shorts (Layers removed), sports Padel/Tennis/Train; added `shorts` + `train` collection defs in config.js. No men's product shows in a women's category/sport view or vice-versa. (6) UI polish round (still 5213 only): welcome popup now uses ONE seamless image (was a 3-photo collage with visible seams) + the text body is pure white on all devices (js/welcome.js + `.welcome__*` CSS). (7) The card "cloud" quick-add button now TOGGLES — 1st click adds (fills), 2nd click removes — via new `C.cart.has/remove` in js/shell.js; `renderCart()` syncs cloud states to the bag. (8) Colour-swatch availability: curate.js computes `p.availColors` = only colourways with a DISTINCT /shopify/ photo; card (shell.js cardHTML/swatchPick), PDP (product.js) and PLP colour facet (collection.js) all use it, so a product is never offered in a colour with no photo (the 3 new products show a single swatch). (9) New product images re-extracted to 1600×1600 square on pure white to match the existing 2048² shots. (10) Beautified description/accordion typography (`.buybox__desc`, `.acc__panel`, `.acc__list`, `.buybox__bullets` — ~1.9 line-height, letter-spacing). (11) PDP lower-section layout: below the gallery/buybox, "Complete the Look" (now 3 tiles, `#railCtl` as `.pdp-look__grid`) sits on the LEFT and the details accordion (`#acc`, "unfoldable tiles") on the RIGHT — new `.pdp-extra` 2-col grid in product.html (stacks <1100px; look tiles 3-col desktop / 2-col <560px), CSS in main.css, `ctl.slice(0,3)` in product.js. Applies to every product page. (12) Big supplier-data build (5213 only, assets bumped to `?v=4`): **Real product data** — prices €49–€119 (was flat €250), real sizes (jackets S/M/L, Ace Dress S/M), real colours incl. "White Mist" (Perf Tee/Tank), and SKU codes FE-01..FE-20 stored as `p.skus{colour:code}` and shown as "Style FE-xx" on the PDP. Source: `Cloudskin Price Sheet.pdf` + `SKU NEW STICKER…xlsx` (sheet "The Founders Edit"; the Lime Soda / Blue Horizon sheets are FUTURE collections, not built). **Footer rebuilt** (config.js `CLOUDSKIN.footer` + shell.js `footerMarkup`) into Shop / Customer Service / The House + "Be First to Know" signup + IG/TikTok/FB. Policy links (Returns, Privacy, Terms, Shipping, FAQ, Size Guide, Contact) open an **info modal** (`#infoModal`, `C.openInfo`) whose content lives in new `js/legal.js` (`CLOUDSKIN.legal`) — full text from `Website information.pdf`. **New `about.html`** ("Our Story", golden-hour hero + Founders-Edit ethos + campaign grid) reachable ONLY via a new nav item "Our Story" (config nav item with `href`, shell mega/mmenu now honour `cat.href`); not on the homepage. **Checkout** button now shows an order-confirmation modal (adapts the order-confirmation email copy; real transactional emails need the payment/email backend they'll add via Network International + ACS/Aramex/DHL). Contact emails: hello@ / support@ / orders@cloudskin.com. `js/legal.js` is loaded on index/product/collection/about (not login). If deploying live, replicate all of this into `cloudskin-store` first.
