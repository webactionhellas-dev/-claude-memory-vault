---
name: cloudskin-shopify-theme
description: "Rebuilding cloudskin.com as a native Shopify OS 2.0 theme (fork of Dawn) so the owner edits design/content/prices in Shopify — plan, approach, design tokens, phases"
metadata: 
  node_type: memory
  type: project
  originSessionId: 07aa652a-3b99-4a00-8a98-54dc0efbb111
---

**⛔ SHELVED 2026-07-15 — do NOT resume this theme build.** After building header/footer/home (+ a product/collection workflow), the owner decided they do NOT want a rebuild at all: "i want it to BE cloudskin.com, not match it — don't change anything, just make it editable, owner only changes texts & images." A native Shopify theme can't be the literal exact custom site AND be visually editable (Shopify's editor only edits Shopify-format themes) — so we ABANDONED the theme rebuild and kept the EXACT existing site. **FINAL DIRECTION: keep cloudskin.com exactly as-is (custom Vercel site); owner edits text/images via the existing Content Studio (cloudskin.com/studio, pw CloudskinStudio1 — js/studio.js + js/content.js + Supabase `cloudskin_content` table; covers the home page's 11 fields: hero img+sub, collbanner img+title, editorial img+title+copy, statement title1/title2/copy, ig title — FUNCTIONAL & LIVE) and products/prices via Shopify.** Payments = PayPal active, NO card provider (owner must add one). Owner guide published as an Artifact (☁️ CloudSkin — Owner's Guide). The `cloudskin-theme` folder + `Downloads\cloudskin-theme.zip` + the uploaded "cloudskin-theme" Shopify draft are all VESTIGIAL now (owner can delete the draft). Everything below is kept only as a record of the abandoned attempt.

**[ABANDONED] Owner had (briefly) confirmed 2026-07-15: FULL REBUILD of cloudskin.com as a native Shopify theme** so the owner edits design + content + prices all inside Shopify (see the request in [[cloudskin-shopify-integration]]). The current site is a bespoke headless build in `claude projects/cloudskin-live-fix` (LIVE on cloudskin.com via Vercel at ?v=33) — it stays live until the theme is ready + approved.

**APPROACH (decided): fork Shopify's Dawn theme, rebuild its sections + styling to match cloudskin.com, keep Dawn's robust commerce plumbing** (cart, product form, variant selection, predictive search, customer accounts, checkout hand-off, localization, a11y) — this is how we hit the owner's "no errors / nothing missing" bar. Products/prices become native Shopify objects; every section gets theme-editor settings so the owner edits text/images/colors/layout in Shopify.

**FOLDER:** theme build lives in **`claude projects/cloudskin-theme`** (Dawn v15.5.0 cloned 2026-07-15, `theme_info.theme_name` renamed to "CloudSkin" v1.0.0). Design SOURCE to port = `claude projects/cloudskin-live-fix` (css/main.css, js/*, *.html).

**DESIGN TOKENS (from cloudskin-live-fix/css/main.css :root) to port into Dawn's colors/fonts:**
- Colors: `--paper #f7f4ef` (page bg), `--oat #efeae1` (panels), `--linen #ded4c5`, `--ink #171512` (text/buttons/logo), `--slate #55504a` (2ndary text), `--stone #857d72` (meta), `--mist #b4ab9f` (faint), `--line #e6e0d6` / `--line-2 #d8d0c3` (hairlines). Accent: a soft sky blue (announcement bar).
- Fonts: `--sans "Inter"` (UI/nav/body/buttons/logo/wordmark) + `--serif "Fraunces"` used ITALIC ONLY for feature headlines. Logo wordmark = Inter, wide letter-spacing, uppercase "CLOUDSKIN".

**PHASES (multi-session):** (1) Foundation — fork Dawn ✅, port design system (fonts/colors/spacing) into theme settings + logo. (2) Header+footer — announcement bar, mega-menu nav, globe/currency, search, wishlist, account (→ the CUSTOM Supabase login form login.html, NOT Shopify accounts — owner reversed this 2026-07-15; sign-up creates a public.profiles row via trigger), bag; footer w/ all links. (3) Home sections — hero, trending rails, collection banner, editorial, statement ("A Way of Living / Quiet Luxury. Active Living."), Instagram masonry, newsletter "Join the CloudSkin World" (all editable sections). (4) Product page — gallery, colour/size, add-to-bag, size guide modal, accordion (start CLOSED), sticky bar, "Complete the Look" + quick-add-from-card ("ADDED TO BAG" modal w/ PAIRS GREAT WITH). (5) Collection+search+cart+account wiring; i18n (Shopify Translate & Adapt / locales) + Markets currency (owner keeps multi-currency). (6) QA pass (no errors/missing) → draft preview link for owner → go-live.

**UPLOAD / PREVIEW:** Shopify CLI not on PATH — use `npx @shopify/cli theme push --store rta3sf-47 --unpublished` (auth opens browser; owner is logged in via claude-in-chrome Browser 2, deviceId 6243a587) OR zip the folder + upload via Online Store → Themes → Add theme → Upload zip. Preview the DRAFT theme (don't publish). Node v24 + npm 11 available.

**GO-LIVE (final step, owner-approved):** publish the CloudSkin theme in Shopify, then point cloudskin.com's domain at the Shopify online store (currently → Vercel). **MUST THEN REMOVE the redirect** we added to the old Prestige theme (theme id 188108276011, the `<script>...replace("https://cloudskin.com")...` in layout/theme.liquid) or it'll loop — actually the new theme becomes live so the Prestige theme is replaced anyway; just don't leave that redirect in the published theme. The Vercel site (cloudskin-live-fix) becomes the fallback/retired. Coming-soon gate handling TBD at launch.

**STATUS 2026-07-15: Phase 1 done + Phase 2 BUILT & WIRED & VALIDATED (not yet previewed).** Files created (via a 3-agent parallel Workflow, then I integrated): `assets/cloudskin-base.css` (tokens + reset + buttons + section helpers + reveal; Google-fonts @import Inter+Fraunces), `sections/cloudskin-header.liquid` + `assets/cloudskin-header.css` (announcement blocks, mega-menu from link_list 'main-menu', EN·USD localization globe, search=Dawn predictive, wishlist stub, account→settings.custom_account_url|routes, cart→Dawn cart-drawer via edited `sections/cart-icon-bubble.liquid`), `sections/cloudskin-footer.liquid` + `assets/cloudskin-footer.css` (column blocks, "Join the CloudSkin World" newsletter form, socials, legal, payment icons). WIRED: theme.liquid loads cloudskin-base.css after base.css (line ~281); `sections/header-group.json` → cloudskin-header (3 announcement blocks); `sections/footer-group.json` → cloudskin-footer (3 column blocks). All JSON + section schemas validated (node JSON.parse, 0 errors). Namespaced `cs-` classes (no Dawn collisions). **A ready zip is at `claude projects\cloudskin-theme.zip`** (assets/config/layout/locales/sections/snippets/templates at root, ~1MB).

**⚠ UPLOAD/PREVIEW BLOCKED by tooling:** admin.shopify.com BLOCKS JS injection (like checkout) AND the claude-in-chrome pane can't reliably click Shopify Polaris dropdowns (Import / theme "..." menus) AND clicking "Upload zip file" opens a native OS file dialog that FREEZES the tab (CDP screenshot timeouts). Shopify CLI is not on PATH + needs auth. So to preview: **owner uploads `cloudskin-theme.zip` via Online Store→Themes→Import→Upload zip file** (30s, becomes draft "CloudSkin" → Preview), OR generate a **Theme Access token** (install the free "Theme Access" app) so the assistant can `SHOPIFY_CLI_THEME_TOKEN=xxx npx @shopify/cli theme push --store rta3sf-47 --unpublished --path <folder>` headlessly.

**⁉ SURPRISE: a draft theme "cloudskin-shopify" (CloudSkin thumbnail) was added to the store at ~6:57am 2026-07-15** — origin unknown; owner said "don't know it, use mine" so we proceed with the Dawn fork, but keep an eye on it (someone may be building a competing CloudSkin theme).

**★ PHASE 3 DONE (home page) 2026-07-15 — built via 4-agent Workflow, all editable Shopify sections:** `sections/cs-hero.liquid` (slide blocks: image/mobile-image/heading/subheading/2 CTAs; wordmark; fallback image = bundled `assets/cs-hero-default.jpg`), `cs-product-rail.liquid` + `snippets/cs-card-product.liquid` (Trending rail from a `collection` setting → real Shopify products; AJAX quick-add via Dawn cart for 1-variant & size-only products, multi-option cards link to PDP), `cs-collection-banner.liquid` (fallback = bundled `assets/cs-banner-default.jpg`), `cs-editorial.liquid`, `cs-statement.liquid` (defaults "A Way of Living"/"Quiet Luxury. Active Living."), `cs-instagram.liquid` (image blocks), `cs-newsletter.liquid` (Shopify customer form, "Join the CloudSkin World"). Each has settings/blocks + presets. Wrote **`templates/index.json`** = hero → rail(collection "all") → collbanner → editorial → statement → instagram(6 img blocks) → newsletter. Bundled 2 real CloudSkin photos into assets (hero-founders-duo.jpg→cs-hero-default.jpg, padel-trio.jpg→cs-banner-default.jpg) + wired the hero & banner fallbacks to them. ALL JSON + 7 schemas validated (0 errors). Repackaged zip → `Downloads\cloudskin-theme.zip` (1.53MB, forward-slash tar). **KNOWN: editorial + instagram still show placeholder images (owner uploads in editor, or bundle CloudSkin photos next); rail shows real products from Shopify.**

**⚠ USER FRUSTRATION NOTE:** after the Phase-2 (header/footer only) upload, owner said it "looks the same/generic" — because only header+footer were built, the body was still Dawn. Reset expectation: it looks like CloudSkin incrementally as each page is built. Owner confirmed they want the **fully-editable-in-Shopify** path (not a quick static port). REASSURED owner: the finished theme IS edited normally in Shopify's theme editor — the "Theme Access password" was only for the assistant to push during the build, NOT for the owner's editing. Owner couldn't find/create the Theme Access token, so the loop is: assistant builds a phase → hands `Downloads\cloudskin-theme.zip` → owner uploads (Themes→Import→Upload zip, creates a fresh "CloudSkin" draft; delete old drafts) → owner screenshots → assistant fixes. Browser-upload BY ASSISTANT does not work (admin.shopify.com blocks JS + Polaris dropdowns unresponsive in the pane + native file dialog freezes the tab).

**NEXT:** get owner's screenshot of the home preview → fix render issues → Phase 4 (product page) → Phase 5 (collection/search/cart/account) → images + i18n + polish. Continue phase by phase.
