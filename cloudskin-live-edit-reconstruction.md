---
name: cloudskin-live-edit-reconstruction
description: "How to edit the LIVE cloudskin.com from THIS machine (v67 source is on another computer) — reconstruct-bundle-from-live + vercel CLI deploy, and the Supabase-persists / code-reverts rule"
metadata: 
  node_type: memory
  type: project
  originSessionId: d932d7ff-6a7c-4978-81d6-5bbf4fcbc0b9
  modified: 2026-07-27T13:36:37.962Z
---

**UPDATE 2026-07-27: THE v67 SOURCE IS NOW ON THIS MACHINE.** Mike provided it at `C:\Users\nospa\Downloads\cloudskin-v67.zip`; extracted + verified (120 `?v=67` markers, full deploy pipeline `scripts/deploy.mjs` + `bake-content.mjs`, `supabase/functions/*` incl. create-checkout-session + stripe-webhook + _shared/shopify.ts, all 26 js modules incl. creator/studio/edit-mode, money-path `tests/`) to **`C:\Users\nospa\claude projects\cloudskin-v67`**. For durable work, EDIT THE REAL SOURCE THERE and deploy with `node scripts/deploy.mjs` (re-bakes js/content-snapshot.js from live cloudskin_content, then `vercel deploy --prod`, then verifies the served snapshot deep-equals live). deploy.mjs's bake IS the zero-swap first-paint mechanism (snapshot == live => content.js finds no delta => no re-render/flash). The mirror-reconstruction workaround below is now SUPERSEDED for durable changes; the live-mirror at claude projects/cloudskin-live-mirror stays useful only as a scratch for pure live-content stopgaps. Everything the durable half was blocked on (System A/B Studio focal control, blog, canonical send_receipt, 404 success_url default) can now be built in source. Deploy stays gated on Mike.

Historical (pre-2026-07-27, now moot): The current cloudskin.com (v67) SOURCE WAS NOT ON THIS (nospa/office) MACHINE — it was built/deployed from another computer (highest local copy was v34 in claude projects/cloudskin-live-fix, do NOT deploy it, it would roll the site back). To change the live site from here (the old workaround):

**Reconstruct-from-live + deploy** (works, done 2026-07-27):
1. Crawl every served file from the live site: fetch all HTML pages (cleanUrls: /home,/about,/collection,/product,/login,/studio,/creator — NOT /admin which 404s), parse them + all JS for asset refs, `curl` each (text + binary). ~91 served files. Product images referenced in the JS catalog are needed too; `media/*` refs 404 (those load from Supabase, skip).
2. Rebuild `vercel.json` from the live response HEADERS (exact CSP + `cleanUrls:true` + `X-Robots-Tag: noindex`; no /admin rewrite, no /coming-soon redirect in v67).
3. Edit only the target file(s), add `.vercel/project.json` (projectId prj_6d0gmXALxXTKZFj7AAVoBZfns12W, orgId team_fAnVpAdOPwPZAR4kW0o2BwID), then `vercel --cwd <dir> deploy [--prod] --yes`. **vercel CLI is authed here as webactionhellas-dev.** Deploy preview first, verify via `get_access_to_vercel_url` (previews are SSO-gated), then promote.

**PERSISTENCE RULE (critical):** Supabase content (`cloudskin_content` table) PERSISTS across the owner's own source deploys. Any code/image/CSS I deploy from here REVERTS on their next source deploy (their `scripts/deploy.mjs` re-bakes `js/content-snapshot.js` from Supabase, then uploads THEIR home.html/main.css/img/). So durable code fixes must go in their source — I stage them + hand over instructions (see Downloads/CLOUDSKIN-hero-durable-changes.md pattern).

**Hero break root cause:** /studio & /creator (edit-mode.js / Vellum) let the owner set `home.hero.image` + focal (`home.hero.image.pos`) → baked into content-snapshot.js by deploy.mjs. `home.html` hard-codes the built-in `<img>`+preload, then `content.js` (deferred) swaps to the Studio image after first paint = the flash/double-render; a bad focal (52% 42%) crops heads on desktop. Permanent fix for a bad hero = delete `home.hero.image` from `cloudskin_content` (Supabase, persists). Editable-but-bugfree = bake the resolved hero into home.html first paint in deploy.mjs. See [[cloudskin-site]], [[live-site-editor-product]], [[cloudskin-deploy-setup]].

**HANDOFF (2026-07-27, session end — continue in new session):** LIVE on cloudskin.com now, all STOPGAP (revert on the owner's next source deploy) EXCEPT the Supabase hero-removal which is permanent: (1) `home.hero.image` deleted from `cloudskin_content` → built-in founders photo, `home.hero.image.pos`=`50% 18%`; (2) sharper wider desktop hero `img/instagram/hero-founders-duo.webp` (now 2502×1407) + desktop de-zoom `@media(min-width:721px){.hero__media img{object-position:50% 40% !important}}`; (3) cookie banner + welcome poster forced on every `/home` load via an early inline script in home.html clearing `cs_consent_seen`/`cloudskin_consent`/`cloudskin_welcome_session`/`cloudskin_welcome_subscribed`; (4) `/creator` hero smooth-swap in `js/content.js` (hero-key-scoped: `new Image()`+`decode()` then apply src+object-position atomically) so Larissa's hero photo change no longer double-renders. Durable source patches staged at `C:\Users\nospa\Downloads\CLOUDSKIN-hero-durable-changes.md`; new hero image at `Downloads\cloudskin-hero-founders-duo-desktop.webp`. **PENDING — all need the v67 SOURCE which is NOT on this nospa PC (it's on the home/mikef machine; `C:\Users\mikef` doesn't exist here):** (a) per-viewport focal so Larissa can reposition DESKTOP (one Studio focal can't be responsive — needs a separate desktop-focal key); (b) zero-swap first-paint bake in `scripts/deploy.mjs`; (c) Panos's Studio-editable footer BLOG (footer link + index + post pages, SEO, their dedicated writer adds posts). NEXT SESSION FIRST STEP: get the `cloudskin-v56`/v67 source onto a machine Claude is on (copy into claude projects, or run on the home PC), then apply the doc + build the blog. **Owner has NO source file to hand over**, so meanwhile continue via the live-mirror: the FULL reconstructed bundle (all 91 served files + today's 4 edits + `.vercel/project.json` linking the cloudskin project) is PERSISTED at `C:\Users\nospa\claude projects\cloudskin-live-mirror` (also holds `_HANDOFF-durable-source-changes.md`). Deploy further live stopgaps straight from it — NO re-crawl needed: `vercel --cwd "C:\Users\nospa\claude projects\cloudskin-live-mirror" deploy --prod --yes` (preview first = drop `--prod`, then `get_access_to_vercel_url` to view the SSO-gated preview). Note: a fresh crawl of cloudskin.com would MISS the 4 edits (they exist only in this mirror + on the live deploy), so always start from the mirror.
