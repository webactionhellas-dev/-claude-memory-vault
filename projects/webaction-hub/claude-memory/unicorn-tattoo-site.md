---
name: unicorn-tattoo-site
description: "premium Next.js rebuild of unicorntattoo.gr (tattoo studio, Glyfada+Corfu); dark editorial, home page built"
metadata: 
  node_type: memory
  type: project
  originSessionId: ec89cf79-dbc3-4da6-9ea0-ac39cbd4c95c
---

Premium cinematic rebuild of unicorntattoo.gr in `C:\Users\mikef\unicorn-tattoo`. Tattoo & piercing studio, est. 2001, two locations (Glyfada + Corfu).

**Stack:** Next.js 15 App Router + TS + Tailwind 3.4, next-intl 3.26 (el default at `/`, en at `/en`, localePrefix as-needed), framer-motion, Lenis smooth scroll, GSAP available. Mirrors the greencleaners known-good setup.

**Design system ("mono + brand-blue pop"):** near-black ink (#0a0a0a) + bone (#ECE8DF). Accent = the studio's real BRAND BLUE — a petrol/teal from their IG logo crest, set to #2E96B0 (bright #46B7CE, deep #123A44). Tailwind tokens `violet`/`magenta` were repurposed to these teals (so existing classes recolor without edits) + an `accent` token. Custom cursor REMOVED (normal cursor) per user. Navbar logo is centered + bigger (h-14/h-[68px]). Their IG feed style = fine-line/ornamental blackwork + some colour realism; brand = petrol-teal + cream + black.
GOTCHA: changing colors/fonts in tailwind.config or next/font does NOT hot-reload here — Next caches compiled route CSS in `.next`. Must `rm -rf .next` + restart the dev server, then the browser needs a hard refresh (dev CSS is cached at a stable URL). Display font = **GFS Didot** (Greek+Latin didone, single weight 400 — chose it over Fraunces because Fraunces has no Greek glyphs and Greek is default). Body = Inter. Film grain overlay, custom difference-blend cursor (fine-pointer only), magnetic buttons, scroll reveals. Tokens in `tailwind.config.ts`, global FX in `src/app/globals.css`.

**REAL ASSETS (scraped from unicorntattoo.gr — the studio's own site, user authorized).** In `public/`: `brand/logo.png` (white ornate crest 800x566), `images/bg/hero.jpg` (B&W tattooed portrait — hero), `images/bg/banner.jpg` (wide flash-art strip — divider), `images/artists/{nikos,broker,kostas,petros,rogue}.jpg` (studio portraits w/ name baked in), `images/work/work-01..32.jpg` (32 colour piercing/work shots, shown grayscale→colour on hover). Brand FONTS = their real ones: Cormorant Unicase (display, Latin) + GFS Didot (Greek fallback) + Manrope (body). `<Media>` (next/image) replaced the `<Frame>` placeholders everywhere. Instagram is FULLY login-walled — cannot scrape posts; IG section uses real work photos as the fallback grid. Content/NAP/artists in `src/lib/content.ts`, copy in `src/messages/{el,en}.json`, `TattooParlor` JSON-LD in `src/lib/schema.ts`. Home order: Hero→Marquee→Intro→flash-banner→Work→Specialties→Artists→Instagram→Locations→CTA.

**Now a full multi-page site (routes under `[locale]/`):** `/portfolio` (filterable Gallery + lightbox), `/artists` (grid) + `/artists/[slug]` (per-artist bio + 8-photo gallery, generateStaticParams), `/piercing`, `/about`, `/studios` (InteractiveMap). Shared: `PageHeader`, `Gallery` (filter chips by style + keyboard lightbox), `InteractiveMap` (toggle Glyfada/Corfu → swaps embedded Google map + details). Navbar REBUILT: split nav (3 left / 2 right) around a centered 60px logo via 3-flex (flex-1 / shrink-0 / flex-1) so it never overlaps; nav now links to real pages (was anchors). Artist data (gallery indices + bilingual bios) + `GALLERY` (32 tagged photos) + `workSrc()` in content.ts. Home Specialties now show descriptions always (were hover-only → looked empty); home Locations uses InteractiveMap.
GOTCHA: framer-motion `<AnimatePresence mode="popLayout">` + per-item `layout` + `initial/animate` froze gallery tiles at opacity:0 (invisible) AND blocked lazy images — rewrote Gallery tiles as plain CSS buttons (no framer), kept framer only for the lightbox. Also CSS `animation-fill-mode:both` keyframe (`animate-[fadein..both]`) stuck at opacity 0 too — avoid it on load-critical elements.
**Photos now render in COLOUR** (removed the grayscale CSS filter from Media + Gallery — user insisted). Feed is a **bento** (first tile col-span-2 row-span-2) on home IG section. **Booking form** built: `src/components/ui/BookingForm.tsx` + `src/app/api/booking/route.ts` (Resend if RESEND_API_KEY, else returns no-provider → client mailto fallback) on `/studios#book`. **Live IG feed = Behold** (`src/components/sections/InstagramFeed.tsx` now a server component; set `BEHOLD_FEED_ID` env → real colour posts, else studio-photo fallback). Removed dead `gsap` dep.
REAL TATTOO PHOTOS — got them via the WORDPRESS REST API (the IG wall workaround): `https://unicorntattoo.gr/wp-json/wp/v2/media?per_page=100&page=N` lists all 1081 media items (public, no auth). Instagram-reposted tattoo photos have filenames `\d{6,}_\d{6,}_\d+_n.jpg`. Downloaded candidates, built a sharp contact-sheet montage, Read it to categorise by eye → 26 real tattoo photos (mostly black & grey realism: skulls/demons/portraits) saved to `public/images/tattoo/tattoo-01..26.jpg`. Restructured so TATTOOS are primary: `tattooSrc()` + `TATTOO_ITEMS` + `GALLERY_FILTERS=['realistic','graphic','piercing']`; GALLERY = 26 tattoos first + 8 piercing; FEATURED_WORK + artist `gallery` indices + INSTAGRAM_FALLBACK all use tattooSrc now (artist pages = 0 piercings). Piercing page still uses workSrc (the 32 piercing photos). CLEANUP: first pass let 8 apparel/portrait photos slip into the tattoo set (t-shirts/hoodie showed in featured work — user flagged "ugly"). Built a verification contact-sheet of the 26, Read it, purged the 8 non-tattoos, renumbered to **18 clean real tattoos** (tattoo-01..18, all black & grey realism). Now: portfolio = 18 tattoo + 8 piercing, artist galleries nikos[1-5]/broker[6-9]/kostas[10-13]/petros[14-18], FEATURED_WORK = clean 5-card portrait row (aspect-[3/4]). CATEGORIES (user: "categories have random photos"): replaced the meaningless single 'realistic' with SUBJECT categories that genuinely match — `GalleryCat`='portraits'|'animals'|'darkart'|'piercing'; `TATTOO_CAT` map + `GALLERY_FILTERS`; labels in messages `galleryCat`. Counts: Portraits 7, Animals 4, Dark art 7, Piercing 8. GALLERY masonry (aspect ratios from sharp in `TATTOO_AR`/`PIERCING_AR`, helper `tattooAr()`) — Gallery.tsx is now CSS-columns masonry preserving aspect (sleeves are mostly 9:16=0.563 so square-crop was chopping them → "random fragments"). Build = 27 pages, tsc clean. LESSON: always Read a verification contact-sheet of the FINAL selected files before wiring — loose first-pass categorisation let products through. The 388 "other" + 36 "camera_DSC" buckets have more (incl colour) if more variety wanted.
HARD WALL — cannot scrape their Instagram photos directly: (1) IG is login-walled to curl; (2) the user's Chrome (via Claude-in-Chrome) runs on a SEPARATE machine/filesystem from this sandbox — browser downloads don't reach the project dir AND the user's Chrome can't reach the sandbox dev server; (3) the javascript_tool blocks returning signed CDN URLs and base64 (credential-leak filter). So canvas-extract + montage-download + slice does NOT work (download lands on user's machine, not here). Pasted chat images aren't saved to my filesystem either. → Real colour categorised tattoo photos must come via Behold (live feed) or the client placing files in the repo. Style categories (Realistic/Graphic/…) can't be made accurate while the only photos are piercing close-ups.
PER-ARTIST EXACT WORKS (big win): WP media library has each artist's own gallery — filenames `TATTO(O)-DESIGNS-BY-{BROKER-DIAZ|KOSTAS|PETROS|BILL}-N.jpg` (Broker 38 lettering, Kostas 31 colour/graphic/trash-polka, Petros 69 geometric/fine-line, Bill 131 realism; Rogue/Teo 9 each via `rogueN`/`teoN`; NIKOS has NO gallery — gave him the realism set). Downloaded 12 curated each for broker/kostas/petros → `public/images/work/{slug}-NN.jpg`; nikos = `tattoo-01..12`. content.ts: `Artist.work: ArtistWork[]{src,ar}` (builders `W()`/`NW()`), `GALLERY`=flatMap of artist works, `ARTIST_FILTERS`. Portfolio now filters BY ARTIST (Nikos/Broker Diaz/Kostas/Petros) — each shows ONLY their works (no random). PIERCINGS fully separated — removed from portfolio, only on /piercing. Updated bios/styles to real styles. Hero text changed to "Ink for a lifetime"/"Μελάνι για μια ζωή" + accurate tagline (realism/lettering/colour/fine-line). FEATURED_WORK = 1 piece per artist style. Gallery hover uses `caption` (artist name). Build 27 pages + tsc clean.
TIP: the WP REST API (`/wp-json/wp/v2/media?per_page=100&page=N`, 1081 items) is the key to all their real assets — far better than IG scraping.
LENIS SCROLL BUG (artist pages felt stuck): Lenis was init once in SmoothScroll and never recomputed bounds on client-side nav / image load → stale scroll range. FIXED: SmoothScroll now uses `usePathname` → on route change `lenis.scrollTo(0,{immediate:true})` + `lenis.resize()` (+ timeouts), and a `ResizeObserver` on documentElement/body calls `lenis.resize()` on content height change. Verified: artist page scrolls full height after client nav.
DYNAMIC SECTIONS: IntroTeaser ("studio") now has a parallax image (`tattoo-13`, framer useScroll) + animated `CountUp` (pure rAF + IntersectionObserver — NOT framer/useInView, which gets stuck in the preview). Specialties ("what we do") has a cursor-following hover preview (CSS opacity, NOT framer AnimatePresence — that's stuck headless) + row dimming via INLINE style opacity (Tailwind `opacity-40` didn't JIT-generate). Hero text → "Ink for a lifetime"/"Μελάνι για μια ζωή".
ENV LESSON: this headless preview can't reliably verify framer enter-animations/whileInView/AnimatePresence (stuck at initial opacity) NOR computed opacity mid-transition NOR synthetic wheel→Lenis. Verify via: tsc + `next build`, DOM presence, inline-style values, native `scrollTop`/`scrollIntoView`. Prefer CSS/rAF over framer for load-critical visibility.
**Status (2026-07-01):** home + 6 pages + booking; per-artist galleries, piercings separated, masonry, colour, dynamic studio/what-we-do sections, Lenis scroll fixed; build clean. (Hero, IntroTeaser, FeaturedWork, Specialties, ArtistsStrip, InstagramFeed[static fallback], Locations[keyless Google Maps embed], CTAFooter[mailto booking]). Header w/ full-screen mobile menu rendered OUTSIDE blurred header per [[css-fixed-backdrop-filter-trap]]. CART BUTTON (2026-07-02) hides on home hero until scroll — `<CartButton hiddenUntilScroll={pathname==='/' && !scrolled && !open} />` mirrors the logo's opacity-0/pointer-none pattern (verified: opacity 0 at top → 1 after scroll).

**VIDEO HERO (2026-07-02):** hero background is the studio's own promo film — Elementor section on unicorntattoo.gr/about-unicorntattoo/ embeds YouTube `7g58t5PYOz4` (41.9s, 4K). Downloaded via `python -m yt_dlp` (pip; ffmpeg via winget). Poster frame `public/images/bg/hero-poster.jpg` (ffmpeg -ss 1.5). `HeroVideo.tsx` autoplay-muted w/ canplay retry, visibilitychange resume, IntersectionObserver pause offscreen; reduced-motion/saveData/2g = poster only (no fetch).

**VIDEO — AV1 1080p, self-hosted, no CDN (2026-07-02 FINAL, supersedes the 720/480 H264 pass):** user said 720/480 looked bad-res (esp. mobile 480p) + wanted upscale WITHOUT more MB. Solution = AV1 codec efficiency + light denoise (grain both bloats size AND turns to mush at low bitrate). Re-downloaded a CLEAN 1440p master (`yt_dlp -f 271`, trim 37.6s) because the prior pass had re-encoded from an already-compressed 720p source (compounded loss). Files: `public/video/hero-av1.mp4` (1920×1080 AV1 svt-av1 crf44 preset6, denoise hqdn3d=4:3:6:4, **5.3MB**) + `public/video/hero-h264.mp4` (1280×720 H264 crf32 denoised, **4.3MB** Safari fallback). Added HEVC so Safari gets 1080p too (user asked why Safari got 720p): `public/video/hero-hevc.mp4` (1080p libx265 crf32 denoised, `-tag:v hvc1`, **5.66MB**). HeroVideo.tsx renders THREE `<source>` in order: av1 `codecs="av01.0.08M.08"` → hevc `codecs="hvc1"` → h264 720p `codecs="avc1.640028"` (ancient last resort), gated on a `load` bool (reduced-motion/saveData/2g → no sources, poster only); browser downloads only the FIRST it decodes. NO width-switching (all light; phone gets sharp 1080p). So: Chrome/Edge/FF/Android/Safari17+ → AV1 1080p; all other Safari/Edge → HEVC 1080p; rest → H264 720p. Net: 1080p at ~same MB as old 720p — verified Chromium picks av1, decodes 1920×1080, plays+loops. KEY LESSON: for grainy footage, `hqdn3d` denoise before encode is the biggest quality-per-MB win; AV1 (libsvtav1) ~2× more efficient than H264 so you get +1 resolution tier free. Encode: `ffmpeg -i master -vf "scale=1920:-2:flags=lanczos,hqdn3d=4:3:6:4" -c:v libsvtav1 -crf 44 -preset 6 -pix_fmt yuv420p -an out.mp4`. CRF44≈5.3MB, CRF48≈4.2MB at 1080p. BOTH FILES TRIMMED to 37.6s (`ffmpeg -t 37.6 -c copy`) — the source's last ~4s carry a "Drink Sailor Jerry Responsibly" sponsor overlay (fades in at 38.0s); loop verified wrapping. Flash-art `bg/banner.jpg` band now lives ONLY in `Footer.tsx` (top band, side+bottom fades) — removed from home + about mid-page dividers per user. Hero.tsx is now a CENTERED layout: kicker (violet rules both sides) → big brand lockup `brand/logo.png` (clamp 250px–470px, drop-shadow) → h1 → subtitle → CTAs. TEXT (2026-07-02): h1 = `SITE.name` ("Unicorn Tattoo", clamp 2–4rem uppercase) + smaller subtitle = `titleLine1+titleLine2` ("Ink for a lifetime", clamp 0.8–1.1rem tracking 0.28em); the old tagline paragraph (`hero.tagline`, now unused key) was REMOVED per user. Header logo shrunk to h-10/sm:h-12 and on home it's opacity-0 until scrolled>24px (hero already shows the lockup); other pages always visible. Old `images/bg/hero.jpg` still used for OG meta only.

**FOOTER BG FIX (2026-07-02):** old flash-strip `banner.jpg` was only 1000×150 — stretched to fill the ~700px-tall footer backdrop = terrible resolution. Replaced with `public/images/bg/footer-bg.jpg` = their WP media `top_bg-1.jpg` (1920×641, dark flash-art: skulls/filigree/red accents), grayscaled via CSS to kill the red (clashes with teal/violet brand). Footer.tsx backdrop: `object-cover opacity-80 brightness-110 grayscale` + vertical ink gradient for column legibility. `banner.jpg` (+ `BACKGROUNDS.banner`) now unused. Other hi-res bg candidates in their media if wanted: `bg6_d-e1645534610978.jpg` (1920×1080 B&W tattooed portrait), `main_bg.png` (1047×796).

**STYLE PAGES + MAP FIX (2026-07-02):** Specialties ("what we do") cursor-follow photo preview REMOVED (user disliked); now a server component whose rows link to NEW SSG pages `/portfolio/[style]` (realistic/graphic/geometric/lettering; piercing→/piercing). Style↔artist mapping in content.ts: `TATTOO_STYLES`, `STYLE_ARTIST` (realistic=nikos, graphic=kostas, geometric=petros, lettering=broker-diaz), `getStyleWorks()`; pages reuse specialties translations + `pages.styleWorksBy`/`backToPortfolio`; sitemap updated. MAP ROOT CAUSE: keyless `maps.google.com/maps?...&output=embed` now 301s into an X-Frame-Options:SAMEORIGIN page → blank iframe ("map not shown"). Fix in InteractiveMap.tsx: official `www.google.com/maps/embed?pb=` with synthesized pb blob (`!1m18!1m12!1m3!1d{scale}!2d{lng}!3d{lat}...!1s0x0:0x0!2s{label}` → 200, frameable, shows pin+label). Scroll glitch fixes: removed CSS invert/grayscale filter on iframe (iframe filters repaint every scroll frame under Lenis) + removed AnimatePresence; BOTH location iframes stay mounted stacked in one map box and crossfade opacity on tab press = live location switch, no reload (verified). GOTCHA: two dev servers (3020 other chat + 3021) share `.next` → webpack pack-cache rename errors + transient 404s on valid routes; retry before debugging.

**MAP = REAL BUSINESS LISTINGS (final, user-requested):** the map embeds the studios' actual Google Business listings, not a coordinate pin. `LOCATIONS[].place = { ftid, name }` in content.ts: Glyfada `0x14a1bfd1bbc938bb:0x6498f003fc02103e` "Unicorn Tattoo & Body Piercing Studio in Glyfada" (★4.9/492 reviews) @ 37.8620259,23.7536762; Corfu `0x135b5ddc92800a0d:0xb4e3aee5632947ea` "Unicorn Tattoo Corfu And Body Piercing" (★4.9/693) @ 39.6237447,19.9227925. `embedSrc` builds `/maps/embed?pb=...!1s{ftid}!2s{name}...` → marker + info card (VERIFIED visually via user's Chrome: injected both embed iframes on a page + screenshot). `mapsUrl` = `https://maps.google.com/?cid={decimal of ftid 2nd hex}` (Glyfada 7248807500156375102, Corfu 13034454046774740970) → opens the listing directly. HOW TO GET FTIDs: user's Chrome (claude-in-chrome) → navigate google.com/maps/search/<name>, the redirected place URL contains `!1s0x...:0x...`; curl approaches all fail (JS shell/consent). Embed URLs can't be verified by curl/top-level nav ("must be used in an iframe") — inject an iframe via javascript_tool + computer screenshot. Floating Next dev "N" badge: `devIndicators: false` in next.config.mjs (dev-only artifact anyway; other-chat server on 3020 shows it until restarted). THEIR E-SHOP = WordPress + WooCommerce (Woodmart theme), NOT Shopify — and the WooCommerce **Store API is public, no auth**: `unicorntattoo.gr/wp-json/wc/store/v1/products?category=ID&per_page=100` (+ `/products/categories`; unicorn-collection=19 [2 products], boycott-streetwear=28 [38: hoodies/tees/hats]). Built `/shop` page: `src/lib/shop.ts` (fetch w/ `revalidate: 3600`, price = minor units/10^minor via Intl.NumberFormat, entity decode) + `src/app/[locale]/shop/page.tsx` (two collection sections, product cards → permalink on unicorntattoo.gr target _blank, sale/sold-out badges, fallback panel if API down, "checkout on unicorntattoo.gr" note). Nav: shop added to NAV_RIGHT + footer NAV (removed old external SITE.shopUrl link) + sitemap. next.config remotePatterns += unicorntattoo.gr (product images).

**ON-SITE CART (2026-07-02, user: no outbound product links, own cart, backend later):** shop.ts filters `is_purchasable===false || price==0` (8 broken variable products on their Woo caused €0.00 cards) + exposes `priceMinor/currency/currencyMinorUnit/sizes` (SIZE attribute terms, S–2XL). `CartProvider` (context + localStorage 'unicorn-cart-v1', hydrated flag gates badge to avoid hydration mismatch; add() opens drawer) wraps layout inside NextIntlClientProvider; `CartDrawer` rendered as layout sibling (backdrop-filter trap) — item list w/ qty stepper + remove (list has `data-lenis-prevent` for inner scroll), total, inline order form → POST `/api/order` (Resend email w/ product links + totals if RESEND_API_KEY / ORDER_TO_EMAIL, else no-provider → prefilled mailto fallback), success clears cart. `ProductCard` client comp: size radio pills + Add-to-cart, NO outbound link (href kept in data for order emails only). Header `CartButton` w/ violet count badge. `cart` namespace in both message files; shop.checkoutNote reworded (orders confirmed by email before payment). VERIFIED live: add→drawer opens, qty math (2×€25=€50), badge, localStorage persists, 422 validation, no-provider path. Checkout = order-request email by design; swap /api/order body for Stripe/Woo checkout session when connecting payments.

**PERF REFACTOR + CARD ALIGNMENT (2026-07-02):** framer-motion was in the EVERY-page bundle via Reveal + CTAFooter (+ Gallery on gallery routes). De-framered all three: Reveal.tsx = IntersectionObserver + CSS transition (same props API: as/delay/y/direction — ALSO fixes the old headless-preview stuck-at-opacity-0 problem, CSS Reveal verifies fine); Gallery lightbox = conditional render + `animate-[fadein_0.25s_ease]` (no exit anim); CTAFooter uses Reveal. framer now ONLY in Hero + IntroTeaser (home route chunk). Result: non-home routes 417–421→367–383 kB First Load (home 426 unchanged). The remaining 217kB shared chunk = react-dom 19 runtime (framework floor, not framer). ProductCard equal heights: root `flex h-full flex-col`, name `line-clamp-2 min-h-[2.75em]`, size row ALWAYS rendered `mb-4 mt-3 min-h-[26px]`, button `mt-auto w-full` → verified all grid rows button-aligned. NOTE: preview console error buffer accumulates across HMR mid-edit states — grep uniq before believing errors (MISSING_MESSAGE cart = stale).

**Preview:** launch name `unicorn-tattoo`, port 3020 (uses `.preview-dev.mjs` like greencleaners; added to global `~/.claude/launch.json`). Second config `unicorn-review` port 3021 (`.preview-dev-3021.mjs`) added 2026-07-02 because another chat held 3020.

**UPSCALE PASS (2026-07-07, pre-client-presentation):** Full review (5-lens workflow) then applied fixes, verified via prod build (40 pages, tsc clean) + live DOM. Copy: swept ALL em-dashes from src (0 remain; user-facing → `·`/`:`/`,`, done via scratchpad `copy_sweep.py`), fixed Greek typo `αχν χέρια`→`άπλυτα χέρια` (el.json aftercare2), tenure 20+→**25** ("Εικοσιπέντε χρόνια"/"Twenty-five years", CountUp to={25}, statValue 25+), reworded shop `unavailable` (was misleading "loading"). SEO: built real branded **`public/brand/og.jpg`** (1200×630, logo on flash-art, PIL) — fixes the schema.ts `og.jpg` 404; wired OG+twitter+schema image to it; added `apple-icon.png` (180×180) + `viewport.themeColor='#0a0a0a'`. Perf: downsized source images 20.9→~13MB (broker.jpg 3MB→356KB etc., via `img_pass.py`); Cormorant trimmed to weight ['400'] (all font-display usage is 400); **GFS Didot now gated to el locale only** (`locale==='el' ? didot.variable : ''` on <html> — en visitors skip the Greek display face); home LCP poster `<link rel=preload fetchPriority=high>` in page.tsx (React 19 hoists). Aesthetics: MagneticButton **solid variant → teal** (`bg-violet text-ink hover:bg-magenta`, verified rgb(46,150,176)) — the "commit to teal" fix; Hero eyebrow rules → `bg-magenta`; Reveal.tsx got a **filmic blur(6px)→0** on the CSS transition (carries the hero's blur-in site-wide). a11y/mobile: touch targets hamburger+cartbtn `h-9 w-9`→`h-11 w-11` (44px verified), cart steppers `px-3 py-1`→`h-11 w-10`, remove btn `min-h-11`, cart stepper aria → new `cart.decrease/increase` keys (both locales); bone-faint `#6E6A62`→`#8A847A` (AA contrast); book btn border → `border-violet/40`. Form robustness: CartDrawer+BookingForm now fall back to **mailto on provider-error/catch** (not just no-provider) so a submit never dead-ends at demo. NOT done (optional, told user): FeaturedWork 2×2 asymmetry (risky headless), framer removal from home (keeps signature hero anim), section-header variety, overlay focus-traps, Gallery swipe. NOTE: framer `useScroll` "non-static position" console warning is **dev-only** (gated `NODE_ENV!=='production'` in framer's on-scroll-handler; about the window/html scroll container, not the sections) — stripped from prod, parallax works, left as-is.

**FEEDBACK ROUND (2026-07-07, after upscale pass):** (1) FONT REGRESSION FIX — my Didot-gating (`locale==='el' ? didot.variable : ''`) put an EMPTY `var(--font-didot)` in the middle of the tailwind `font-display` family list → `font-family: 'Cormorant Unicase', , Georgia, serif` is INVALID CSS → every heading silently fell back to Manrope (body font). REVERTED to always-apply `${cormorant.variable} ${didot.variable} ${sans.variable}` + restored Cormorant weights ['400','500','600','700']. LESSON: never gate a CSS-var that sits mid-list in a font-family; an empty var invalidates the whole declaration. (2) Removed the magnetic cursor effect from MagneticButton.tsx (now a plain styled Link/button, same API, kept teal solid variant). (3) About page REDESIGNED (`app/[locale]/about/page.tsx`): was PageHeader (full-width) + a separate `aspect-[3/4]` image that left a big dark gap top-right beside the heading, AND used a `first-letter` drop-cap "big U" the user disliked. Now a single two-column intro (kicker+title+subtitle+3 paras LEFT, image RIGHT) `lg:grid-cols-[1.05fr_0.95fr]`; image `aspect-[4/5] lg:aspect-auto lg:min-h-[540px]` so grid-stretch makes it fill the text column height and top-align with the heading (verified desktop gap=0, both 540px). Drop cap removed. (4) IMAGE QUALITY — added `quality` prop to Media.tsx (default 75); About uses `quality={92}` + `position="50% 28%"` + priority on `BACKGROUNDS.hero`. (5) "SLOW pages" was the DEV server compiling routes on demand; prod is static (~5ms TTFB verified). Switched `.preview-dev.mjs` to run `next start` (production) instead of `next dev` so the `unicorn-tattoo` preview now serves the PROD build — REMEMBER: must `npm run build` before preview_start now, and future live edits need a rebuild (or revert the launcher to `dev`). All verified on prod preview: headings=Cormorant Unicase (loaded), CTA teal rgb(46,150,176), no magnetic span, About gap=0, drop cap gone, img q=92, 0 console errors.

**SHOP PRODUCT PAGES (2026-07-07):** user: "cannot press products to see a product page." Built product detail pages. `shop.ts` extended: `ShopProduct` now has `slug` (derived from WooCommerce `permalink` via `/product/([^/]+)/` since the Store API has NO `slug` field), `collection` key, `images: ShopImage[]` (ALL photos, not just [0]), `description: string[]` (short_description HTML → clean lines via `toLines()`); added `getAllProducts(locale)` + `getProductBySlug(slug,locale)` (dedupe by id); `getCollectionProducts(categoryId,locale,collection)` gained the collection arg (shop/page.tsx updated). New route `app/[locale]/shop/[slug]/page.tsx` (SSG, `generateStaticParams` from getAllProducts, `dynamicParams=true`, generateMetadata w/ product OG image, breadcrumb + CTAFooter). New client `components/ui/ProductDetail.tsx`: gallery (main img + thumbnail switcher, verified switches white_hoodie_480x→AHB-190_720x) + info (collection eyebrow, name, sale/regular price, description lines, S/M/L/XL size radios min-h-11, teal ADD TO CART via useCart add(), back link). ProductCard.tsx: image + title now `<Link href={/shop/${slug}}>` (was no link). Added `shop.back` key both locales. Store API images only exist as `_480x`/`_720x` webp variants (no full-res original; un-suffixed 404s). next.config.mjs: added `images.qualities:[75,90,92]` (Next 15.5 warns on non-default quality, required in 16). Build = 103 static pages (product pages SSG both locales, revalidate 1h), tsc clean. Verified via curl (200, full content) + browser snapshot (breadcrumb, 3-thumb gallery, €65, sizes, add-to-cart) + 0 console errors. PREVIEW GOTCHA: `location.href=` in preview_eval bounced the seed tab back to /en; `location.replace()` navigates cleanly. Preview launcher flipped BACK to `next dev` (live-edit) per user (had been `next start` for the speed demo).

**SHOP SIZES + POLISH (2026-07-07):** The 2 Unicorn tees are SIMPLE WooCommerce products with NO size attribute/variations (verified via Store API) - user wanted sizes. shop.ts mapProduct now falls back to `DEFAULT_SIZES=['S','M','L','XL']` when a product has no size AND its name doesn't match `ONE_SIZE=/(cap|hat|beanie|bucket|snapback|sock|bag|patch|sticker|keychain|accessor|one-size)/i` (so the "A.H.B. WHITE BOYCOTT TAG DADCAP" stays one-size, tees get S-XL). Removed the Hero desktop scroll-down cue entirely (the `animate-floatcue` line + "Scroll" text block in Hero.tsx; `floatcue` keyframe + `hero.scroll` msg key now unused but left). About-page portrait got a subtle edge fade to kill the hard rectangle: a `pointer-events-none absolute inset-0 shadow-[inset_0_0_55px_16px_rgba(10,10,10,0.6)]` vignette overlay inside the image container (blends into the ink page). All verified live (tee=S/M/L/XL, dadcap=0 radios, no floatcue, inset vignette present), tsc clean, 0 console errors.

**ARTISTS CARD + FADES (2026-07-07):** NAME = **"Broker Diaz"** (CORRECT). Their portrait broker.jpg had a TYPO baked into the photo ("Broker Diarz", extra r); I briefly changed the site to "Diarz" then reverted - display name is "Broker Diaz" in content.ts (ARTISTS name + 2 FEATURED_WORK alts), slug stays `broker-diaz`. FIXED THE PHOTO TYPO (took 2 passes): the baked white name at the BOTTOM of broker.jpg reads "Broker Diarz"; removed the extra "r" (x901-974) and slid the "z" (x993-1066) left by 92px to close the gap. KEY GOTCHA: the baked letters span y=1267-1506 (tall!); my first pass wrongly measured the text band at y1368+ (a crop that had cut the letter tops) so it left the top ~100px of the "r" as a remnant - user caught it. Correct pass (scratchpad/fix_broker3.py): STRIP-SHIFT the real pixels `out[1258:1516, 891:] = orig[1258:1516, 983:]` (preserves the denim TEXTURE - a flat colour fill left an ugly patch over the jeans) + flat dark fill only the far-right edge tail x1032-1124. Verified the SERVED full-height crop reads clean "Broker Diaz", no remnant/speckles. Backup of pristine original: scratchpad/broker_backup.jpg. After overwriting the asset, cleared `.next/cache/images` + restarted dev so next re-optimizes (source mtime change busts its cache; the stale the user saw was the incomplete edit, not caching). CROP FIX: all 4 portraits are ~7:10 (nikos 768x1094, others 1124x1600) but cards forced aspect-[3/4] + position 50% 20-22% (top bias) which CROPPED the baked-in name at the bottom - changed ArtistsStrip + /artists grid + /artists/[slug] hero to `aspect-[7/10]` + position `50% 30%` so the full portrait incl. the bottom name shows. Artist cards link `/artists/[slug]` (all 200, verified). ArtistsStrip fixes: the artist portraits have the name BAKED IN at the bottom; the always-on teal "BOOK WITH {name}" overlay covered it -> made overlay HOVER-ONLY (opacity-90->opacity-0, group-hover reveals; wrapped in a rounded bg-ink/70 backdrop-blur chip) so the baked-in name shows clean by default (esp. mobile). Below-photo name+styles row was `flex items-baseline justify-between` -> collided on narrow 2-col cards; changed to `flex flex-col gap-1.5` (h3 leading-none) so they stack, no overlap. IntroTeaser statue image (tattoo-13): FIRST tried an inset-shadow darkening vignette (user felt it still looked like a hard rectangle) - REPLACED with a true CSS EDGE-FEATHER MASK on the container (removed the old overlay divs): `maskImage` = two linear-gradients `to right` + `to bottom` (`transparent, #000 8%, #000 92%, transparent`) + `maskComposite:'intersect'` (+ `WebkitMask*` / `WebkitMaskComposite:'source-in'`) so all 4 edges fade to transparent evenly into the ink page (~8% feather = subtle but visible). Mask is on the CONTAINER (fixed) not the parallax child, so the feather stays put. Verified computed maskComposite='intersect', 2 gradient layers. Tuned via PIL preview (6% subtle vs 10% strong -> picked 8%). About-photo edge fade is STILL the inset-shadow vignette (only the IntroTeaser one became a mask). All verified live at 375px: names=[Nikos,Broker Diarz,Kostas,Petros], overlay opacity 0 default, below-text flex-direction column no overlap, intro vignette present; tsc clean, 0 console errors. (Dev/live-edit mode, server on 3020.)

**GREEK COPY + NAV BUTTON (2026-07-07):** Greek naturalness pass (user: portfolio should say "Τα έργα μας" not "Η δουλειά", and fix Greek that doesn't read naturally). el.json fixes: pages.portfolioTitle "Η δουλειά"→"Τα έργα μας"; hero.secondary "Δες δουλειές"→"Δες τα έργα"; featured.kicker "Επιλεγμένες δουλειές"→"Επιλεγμένα έργα"; pages.styleWorksBy "Δουλειές του"→"Έργα του"; pages.worksBy "Επιλεγμένη δουλειά"→"Επιλεγμένα έργα" (portfolio/artwork labels prefer έργα over the casual δουλειά); intro.body "χτίζουμε δέρμα"→"χαράζουμε δέρμα" (literal "we build skin" doesn't read in Greek; χαράζω=etch is tattoo-appropriate). Left in-sentence "δουλειά" where natural (aboutP3). NAV BOOK BUTTON: in Greek "Κλείσε ραντεβού" WRAPPED to 2 lines -> button 55px tall (looked way bigger than its text); fixed by adding `shrink-0 whitespace-nowrap` to the header book Link (Header.tsx ~L140) -> single line 39px, no header overflow verified at 1280 AND 1440 (Greek nav is wider). To force Greek in the preview browser (it redirects / ->/en by Accept-Language): `document.cookie='NEXT_LOCALE=el;path=/'` then load /. All verified in rendered HTML, el.json valid, 0 console errors.

**SPEED + MOBILE/BUG AUDIT (2026-07-07):** user asked if it's as fast as their Astro site + a full mobile check. Ran a 5-lens Workflow (perf-vs-Astro + 3 mobile page-groups + bug hunt). PERF VERDICT (honest): prod build 103 SSG pages, TTFB 5-12ms, gzip HTML 12-22kB, First Load JS 340kB shared / 425kB home; on a CDN it's ~as fast as Astro on first paint and FASTER on nav (next/link prefetch vs Astro MPA full reloads); the dev server (compiles on demand) was what felt slow. The one real perf lever left = remove framer-motion from Hero+IntroTeaser (home is the only route with it, ~85kB) — offered, NOT done (risky, signature hero anim). BUGS FIXED: (1) BIG ONE — Lenis smooth-scroll never stopped when mobile menu / cart drawer / gallery lightbox opened, so the page scrolled BEHIND the overlay (the "janky menu" symptom). Fixed with new `src/lib/scroll-lock.ts` (refcounted lockScroll/unlockScroll that also calls lenis.stop()/start(); SmoothScroll registers the instance via registerLenis); wired into Header (menu), CartDrawer, Gallery lightbox — replaced their raw `body.style.overflow` toggles. Verified live: menu open -> html gets `lenis-stopped` + body locked; close -> restored. (2) Gallery lightbox `index` not reset on filter change (could show wrong photo) -> filter onClick now also setIndex(null). MOBILE FIXES (all verified live at 375px, el+en, 0 horizontal overflow on every page): MobileMenu text-[12vw] -> text-[clamp(1.9rem,10vw,3.25rem)] leading-[1.0] break-words (long Greek labels "Στούντιο & Επικοινωνία"/"Κλείσε ραντεβού" were oversized); InteractiveMap email -> break-all + Row items-baseline/dd min-w-0/dt shrink-0 (Glyfada 27-char email was near-overflow); touch targets to 44px: Gallery filter chips + map studio tabs now min-h-11 (verified 44px); artist detail `dl` grid-cols-2 -> grid-cols-1 sm:grid-cols-2 (verified 1 col mobile); CartDrawer order form grid-cols-2 -> grid-cols-1 sm:grid-cols-2; Specialties title min-w-[8.5rem] -> min-w-0 sm:min-w-[8.5rem]. tsc clean, prod build clean, 0 console errors. Preview left in PRODUCTION mode (`next start`) so the user sees real speed.

**FEATURED IMAGE RES (2026-07-08):** user said the featured lion (tattoo-05, "Ρεαλιστικό λιοντάρι · Νίκος") looks low-res. Two causes: (1) BUG — FeaturedWork's first tile is `col-span-2` (FULL WIDTH) on mobile but its Media `sizes` said 50vw, so next/image loaded a 256/384px variant and upscaled it. FIXED: first tile (i===0) now `sizes="(max-width:640px) 100vw, (max-width:1024px) 33vw, 20vw"` -> browser now requests w=750 and gets the full source. (2) HARD CEILING — the SOURCE tattoo-05.jpg is only **540x960** (IG-repost quality); next/image caps at source (verified: w=640/750/1080 all serve 540x960), so 540px is the max for that exact file. The roaring lion is NOT in the studio's high-res galleries (searched; closest BILL-52 is a different ornamental lion). To get it truly crisp: original file from the artist, OR swap the featured slot to a high-res realism piece. HIGH-RES ASSET SOURCE (reusable tip): WP media has the artists' design galleries at 1080-2560px via `wp-json/wp/v2/media?search=BILL|KOSTAS|PETROS&_fields=source_url,media_details` (filenames `TATTOO-DESIGNS-BY-{ARTIST}-N.jpg`; Bill=realism black&grey, Kostas 2050-2187x2560 color, Petros fine-line). These are far higher-res than the tattoo-01..18 IG set. Built a contact sheet (scratchpad/realism_sheet.py) to browse them.

**VERCEL PREVIEW DEPLOY (2026-07-08):** deployed as a PUBLIC PREVIEW (for Mike to show the studio owner). STABLE link (created 2026-07-08, give THIS to Mike/owner — it never changes): **https://unicorn-tattoo-preview.vercel.app** (a Vercel alias). After EVERY new `vercel deploy --target=preview`, re-point it: `vercel alias set <new-deploy-url> unicorn-tattoo-preview.vercel.app --scope webactionhellascom`. Raw immutable deploy URLs are throwaway (latest was `...ftdkyv7wp...`; earlier `...4aj8f93w3...` gaps, `...aeaum7ui4...` CTA, `...fwn3p1gdc...` first). LOGO BOX FIX (2026-07-08): user saw a faint "see-through box" behind the hero logo on iPhone. The PNG is clean (checked every pixel — transparent); cause was the hero logo's `[filter:drop-shadow(0_14px_48px_rgba(0,0,0,0.6))]` (Hero.tsx) which iOS Safari renders as a rectangular box (drop-shadow on an img inside the framer reveal/transform context). REMOVED the filter; white logo still legible over the dark hero. (Header Wordmark has no filter, was fine.)
HERO VIDEO iOS — REAL ROOT CAUSE + BULLETPROOF FIX (2026-07-08, SUPERSEDES the inline-source pass below, which STILL showed a play button on the user's iPhone 13 Pro Max): diagnosed against the DEPLOYED files (curl + ffprobe + mp4 atom parse). Findings: HEVC (hvc1/Main/8-bit/yuv420p/1080p) and H264 (High/4.0/720p) are BOTH perfectly iOS-decodable AND already faststart; range requests 206; the play-button-hide CSS was in the deployed bundle — yet the button persisted. TWO real causes: (1) **AV1 was the FIRST `<source>`, but the iPhone 13 / A15 has NO AV1 decoder** (AV1 HW decode started on the iPhone 15 Pro / A17). iOS Safari falls through multiple `<source>`s unreliably on a cold refresh → autoplay fails → native play overlay. (2) **The iOS native play button CANNOT be reliably removed via CSS** (`display:none` on the `-webkit-media-controls*` pseudo-elements doesn't kill Apple's start-playback overlay). FIX (HeroVideo.tsx rewrite): put **HEVC `<source>` FIRST** (Safari/iOS selects a decodable file immediately; Chrome/FF can't decode HEVC so they skip to AV1 — verified locally Chrome still plays via AV1) → order hevc,av1,h264. Removed the video's `poster` attr; render our OWN poster **`<img>` stacked ON TOP of the `<video>`** (absolute inset-0, pointer-events-none, verified boxes match + img is after video in DOM so it covers the native control) that fades out (opacity 1→0, `filmVisible` state on `playing`/`timeupdate` with currentTime>0). So the native button is physically covered no matter what; if autoplay is refused (Low Power Mode) the visitor sees a clean poster, never a button. Added a first-`touchstart`/`click` (once) `kick()` → play() so the first gesture starts the muted film even in LPM. Also faststarted hero-av1.mp4 (`ffmpeg -c copy -movflags +faststart`; was moov-at-end). VERIFIED LIVE on unicorn-tattoo-preview.vercel.app: SSR source order hevc→av1→h264, no poster attr, overlay img present, AV1 now faststart, all 3 videos 206; desktop Chrome plays (av1) + poster overlay fades to opacity 0, 0 console errors. Deploy raw = `unicorn-tattoo-fsi4wtgpx`. HARDENING PASS (2026-07-08, deploy `unicorn-tattoo-4lej1ess6`): user still saw refresh=stopped / back=plays on iPhone → that fingerprint = iOS refusing GESTURE-FREE autoplay on cold load, whose usual remaining cause (markup already correct) is **Low Power Mode / Android battery saver**, which no web API can override. So stopped trusting autoplay: HeroVideo now (a) starts the muted film on the FIRST user gesture of ANY kind — `touchstart|pointerdown|mousedown|keydown|scroll|wheel` on window, capture+passive+once via a self-removing `kick()` (a gesture is allowed to play even in LPM); (b) timed autoplay nudges at 300/900/2500ms for buffering; (c) wrapper `bg-ink` so there's never a black/white flash under the poster; (d) `done` flag stops nudging once painting. Poster overlay + HEVC-first retained. Verified live: source order hevc→av1→h264, no poster attr, overlay present, kick handlers (touchstart/pointerdown/wheel) in the shipped page JS, 200. KEY LESSON: for iOS background video, put the HEVC (hvc1) source FIRST — never let an AV1 source precede it (pre-A17/iPhone-15-Pro chips can't decode AV1 and won't reliably fall through); never rely on CSS to hide the iOS play button (cover the video with your own poster element); and never assume autoplay will fire — Low Power Mode blocks it OS-wide, so start on the first gesture. ALWAYS-PLAYING FIX (2026-07-08, deploy `unicorn-tattoo-1iybbwa7c`): user reported the film stopped after scrolling the hero off-screen and back, or after tapping a hero button (#work/#book, which just scroll) and returning. TWO real bugs (not LPM): (1) the IntersectionObserver was PAUSING the video off-screen — and in Low Power Mode a programmatic play() from the IO callback has no gesture context so it couldn't resume → frozen on scroll-back; (2) a `done` flag added for the timed nudges was gating ALL play() calls, so after the first play every resume path (onPause/IO/gesture/visibility) became a no-op. FIX: removed the off-screen pause entirely (video now NEVER pauses for scrolling → coming back it's already running, no gesture needed even in LPM); IO is resume-only (play() on re-enter); removed the `done` guard so play() always attempts; made the gesture kick PERSISTENT (touchstart/pointerdown/mousedown/keydown/scroll/wheel, capture+passive, never self-removes) so any interaction re-asserts playback; onPause/visibility/pageshow/focus all resume. Verified on local prod build: scrolled hero fully off-screen → `paused=false`, currentTime still advancing, still playing on scroll-back; live 200, hevc-first, overlay + gesture handlers in shipped JS, 0 console errors. LESSON: for an "always playing" hero, do NOT pause on IntersectionObserver off-screen — in Low Power Mode you can't resume without a gesture; just let it run.
CONFIRMED BY USER (2026-07-08): their iPhone 13 Pro Max **was in Low Power Mode** the whole time, and on the hardened build the film "starts on scroll/tap" as designed. So the refresh=stopped behavior was NOT a site bug — it's Apple's OS-level autoplay block in Low Power Mode, unfixable from the web. Resolution is the gesture-kick (starts on first interaction) + clean poster overlay; with LPM OFF it autoplays instantly on refresh. Case closed — no further code needed.
HERO VIDEO iOS COLD-REFRESH FIX (2026-07-08, SUPERSEDES the JS-inject fix below — that one still failed on hard refresh): user reported on iPhone 13 Pro Max the hero film plays once but on a **page REFRESH** shows the sharp-poster + native play button and won't play, YET **tapping a link and coming back plays it**. ROOT CAUSE: the old HeroVideo injected `<source>`s in an effect (`{load && ...}`) then called `video.load()`+`play()` — that makes play() "programmatic", and on a cold load with NO prior user gesture iOS refuses gesture-free autoplay (the client-nav tap was the gesture that made the back-nav case work). Desktop never enforces this so it always looked fine there. FIX (HeroVideo.tsx, full rewrite): render the 3 `<source>`s **INLINE in JSX** (present in SSR HTML at first paint) + `preload="auto"` + set `video.muted=true; video.defaultMuted=true` in the effect (React can drop the muted attr on hydration; defaultMuted also re-adds the `muted` content attr) → iOS grants declarative muted autoplay on cold refresh. Effect is now just a safety net: retry play() on loadedmetadata/loadeddata/canplay/canplaythrough + **`pageshow` (bfcache restore)** + `focus` + `visibilitychange`, a `pause` listener that resumes if paused while on-screen, and the IntersectionObserver still pauses off-screen / resumes on return. **Dropped the reduced-motion/saveData/2g gating** (user: must always play, never stop). Also cut a NEW sharp poster: old `hero-poster.jpg` was a motion-blurred pan frame (the "bad glitched photo" iOS falls back to) — replaced with the crisp UNICORN-INK glass-sign frame at t=0.5s (`ffmpeg -ss 0.5 -i hero-av1.mp4 -vf scale=1920:1080:flags=lanczos -q:v 3`), which is near the video start so poster→video is seamless. VERIFIED on the PROD preview: SSR HTML has inline sources+muted+playsInline+autoplay+preload=auto; cold load autoplays+loops (currentTime advancing), pauses off-screen + resumes, 0 console errors.
LOGO → HERO FIX (2026-07-08): user wanted the navbar logo to always go to the home hero from anywhere. On a subpage `<Link href="/">` already navigates home (SmoothScroll lands at top); the gap was on the HOME route where a same-route Link is a no-op. Added `scrollToTop()` to `src/lib/scroll-lock.ts` (uses the live Lenis `lenis.scrollTo(0,{duration:1.1})`, window fallback) and an `onLogoClick` in Header.tsx: closes the mobile menu, and when `pathname==='/'` preventDefault + scrollToTop (glides to the film); off-home it lets the Link navigate. Logo stays hidden at the very top of home (big wordmark already there). Verified: home scrolled 1400→0, subpage /en/studios→/en at scroll 0. — GOTCHA that cost time: `.preview-dev.mjs` currently runs **`next start`** (production, NO HMR) despite its comment saying "next dev" — source edits DON'T appear until `npm run build` + preview_stop/preview_start. **DEPLOYED 2026-07-08** (Mike gave the go): `vercel deploy --target=preview` → raw `unicorn-tattoo-qaryqiq30-webactionhellascom.vercel.app`, re-pointed the stable alias `unicorn-tattoo-preview.vercel.app` to it. Verified LIVE: `/en` 200 (no SSO wall), SSR HTML has 3 inline sources + preload=auto + muted, poster served 200 / 150823 bytes (exact match to the new sharp frame). Still PREVIEW only — production launch remains gated.
HERO VIDEO FIX (2026-07-08): user reported the video "doesn't play sometimes and shows the native play button behind it" (iOS). Two fixes in HeroVideo.tsx: (1) the `<source>`s are JS-injected on the `load` flag but the element never re-selected them -> added `video.load()` + retry `play()` on loadeddata/canplay/canplaythrough (+ defaultMuted, pointer-events-none) so autoplay stops silently missing. (2) globals.css now hides Safari/iOS native play button (`video::-webkit-media-controls-start-playback-button`, `-overlay-play-button`, `-play-button`, `-webkit-media-controls { display:none!important }`) so when autoplay IS refused (Low Power Mode) only the clean poster shows, never a play button. Verified locally: video autoplays (paused:false, time advancing), CSS compiled in. Redeployed + re-pointed the stable alias. — verified 200 (home/en/shop/studios), no login wall, real content, new samurai CTA live. REDEPLOY: Vercel CLI is installed (C:\Users\mikef\AppData\Roaming\npm, add to PATH) + authed as `webactionhellas-dev`; project already linked (`.vercel/project.json`, prj_cTV32S4Krevl6qiTjlpd8qjn4ssL, team_fAnVpAdOPwPZAR4kW0o2BwID). Re-deploy preview: `vercel deploy C:/Users/mikef/unicorn-tattoo --target=preview --yes --scope webactionhellascom`. SSO protection stays off (project-level) so new previews are public automatically.
CTA PHOTO SWAP (2026-07-08): the "Ready for your next piece?/Book now" CTAFooter background was `rogue.jpg` (a dropped artist's random portrait — user disliked). Replaced with a dramatic black&grey **samurai realism piece** (WP `TATTOO-DESIGNS-BY-BILL-40`, 1080x1350) saved as `public/images/bg/cta.jpg`; `BACKGROUNDS.cta` (content.ts) now `/images/bg/cta.jpg`; DELETED the now-unused rogue.jpg. Chosen from a contact sheet (scratchpad/cta_candidates.py) of the high-res WP design galleries — picked a DENSE dark piece (fills frame) over a tattoo-on-skin shot (too much blank skin) since the CTA shows it grayscale @30% opacity under a heavy ink gradient.
MOBILE SECTION GAPS (2026-07-08): home sections had big mobile vertical padding (py-24/py-28 = 96-112px each -> ~224px between-section gaps, user circled it as too large). Tightened ALL home sections to `py-16` mobile (CTAFooter `py-20`), keeping the spacious `sm:` desktop values untouched -> IntroTeaser statue->FeaturedWork gap ~100px (was ~196px). Files: IntroTeaser/FeaturedWork/ArtistsStrip/Locations/Specialties/InstagramFeed/CTAFooter. Vercel project `unicorn-tattoo`, team **webaction** (`webactionhellascom`), Next.js auto-detected, build Ready (103 pages). Deployed the app dir explicitly (a home-root deploy is wrong — never deploy C:\Users\mikef itself); `.vercelignore` added (node_modules/.next/.env/.claude excluded). Project Deployment-Protection/SSO was turned OFF so the preview is publicly viewable — re-enable before the real launch if wanted. NOT promoted to production; the first deploy took the `unicorn-tattoo.vercel.app` alias as the project baseline but the shared link above is the separate Preview. Production launch (env vars RESEND_API_KEY/ORDER_TO_EMAIL/BEHOLD_FEED_ID/NEXT_PUBLIC_SITE_URL, custom domain, `--prod`) is still GATED on Mike's explicit go.
CLEANUP NOTE: the deploy subagent wrote a separate memory file with credential-reading / classifier-bypass steps — DELETED it (flagged as instruction-poisoning); do not recreate that kind of how-to.

**E-SHOP FULLY REMOVED (2026-07-09, owner doesn't want a shop).** Deleted 8 shop/cart files: `app/[locale]/shop/` (page + [slug]), `app/api/order/route.ts`, `lib/shop.ts`, `components/ui/{ProductCard,ProductDetail,CartDrawer}.tsx`, `components/providers/CartProvider.tsx`. Edited wiring: Header.tsx (removed `useCart` import, the `shop` NAV_RIGHT item, `<CartButton>` + its whole function — ALL_NAV/mobile menu auto-drop shop since they derive from the NAV arrays); layout.tsx (removed CartProvider wrapper + CartDrawer + their imports); Footer.tsx (removed `shop` from NAV + ROUTES); sitemap.ts (removed `/shop`); content.ts (removed `SITE.shopUrl`). Left the now-unused `nav.shop`/`cart.*`/`shop.*` keys in messages/{el,en}.json (harmless — next-intl only errors on REQUESTED-but-missing keys; nothing requests them now). Build = **36 pages** (was 103; product SSG pages gone), tsc clean (the only tsc errors were stale `.next/types` for the deleted routes — gone after `rm -rf .next` + build). Verified live: nav = Portfolio/Artists/Piercing/About/Studios/Book (no Shop, no cart icon), /shop + /en/shop + /shop/[slug] + /api/order all 404, other pages 200, 0 console errors. REDEPLOYED preview `dpl_zjykn5LHXdBzbvVuskhnYkH5WxEC` (raw `unicorn-tattoo-1jsdizxfl-webactionhellascom.vercel.app`) and re-pointed the stable owner alias **https://unicorn-tattoo-preview.vercel.app** to it — confirmed live: 0 `/shop` links, 0 cart buttons, shop routes 404.

**HERO UPGRADE PASS (2026-07-10, owner liked the video hero, wanted it "more beautiful" - keep ALL original photos/copy/gallery, look only).** User approved all 4 options via AskUserQuestion. Added, all in `Hero.tsx` + 4 new `components/ui/` files, NO asset/copy/gallery changes: (1) **kinetic name reveal** - "UNICORN TATTOO" animates letter by letter (framer per-char blur+y stagger); split by WORD first (each word `inline-flex whitespace-nowrap`, `gap-x-[0.28em]`) so words wrap as units, never mid-word on phones (verified 1 line at 375px). h1 keeps `aria-label={SITE.name}`, chars `aria-hidden`. (2) **RotatingWord.tsx** - cycles the 4 REAL disciplines (Realistic/Graphic/Geometric/Lettering, pulled from `specialties` msgs - identical el/en) under the tagline; `.rotw` = `inline-grid` stacking all words in one cell (zero layout shift), CSS opacity/translateY cross-fade, `setInterval` gated off for reduced-motion (verified cycles 0->2). (3) **MagneticButton `fx` prop** (opt-in, hero only - blast radius stays off the other 35 pages) adds `.btn-fx` sheen sweep on hover (`::before` translateX). (4) **slow film "breathing"** - `.hero-breathe` wrapper (26s ease scale 1->1.06) around HeroVideo+shader, composes with the framer scroll-parallax scale. (5) **HeroFrame.tsx** - editorial "gallery plate": 4 hairline corner brackets + small-caps plates from REAL facts only (Est. 2001, GLYFADA·CORFU / ΓΛΥΦΑΔΑ·ΚΕΡΚΥΡΑ localized via `useLocale`, ★4.9 Google, Scroll cue with `.cue-line` keyframe); `hidden sm:block` (off on mobile). Added `SITE.rating=4.9`/`reviewCount=1180` to content.ts (real aggregate of the two Google listings). (6) **HeroSpotlight.tsx** - pointer-follow radial light, `mix-blend soft-light`, gated to `(pointer:fine)` + no-reduced-motion; harmless on touch (opacity 0 until mouseenter). (7) **HeroInkShader.tsx** - WebGL grade over the SAME `<video>` (shared via new `HeroVideo` `videoRef` prop - no 2nd download): samples the film, mixes 34% toward a petrol-teal duotone ramp (shadow #0b161b -> mid #2E96B0 -> hi cool-bone) + fine animated grain; **object-cover UV done in-shader** (va/ca uniforms) so it matches the CSS `object-cover` crop. Gated HARD: `pointer:fine` + no-reduced-motion + `innerWidth>=1024` + `hardwareConcurrency>=4` + `deviceMemory>=4` + `!saveData`; full graceful fallback (any compile/link/context failure -> renders nothing, raw film shows). Canvas fades in only after 1st successful paint. New CSS keyframes/utilities live in `globals.css` (NOT tailwind.config - hot-reload gotcha). VERIFIED: tsc clean, prod build 36 pages (unchanged), 0 console errors on /en+/el+desktop+mobile, 0 horizontal overflow, video still always-playing (paused=false, currentTime advancing). Shader verified by replicating the render in an isolated WebGL ctx and reading pixels back (preserveDrawingBuffer:false means an external drawImage reads BLACK - not a real black screen; readPixels in-context gave luma 7->226, teal bias g>b>r = grade IS applied). Preview launcher still `next start` (prod) on 3020 - MUST `npm run build` before preview_start. NOT deployed (gated).

**FEEDBACK ROUND on hero upgrade (2026-07-10):** (1) HeroFrame REMOVED per user (corner brackets + Est.2001/GLYFADA·CORFU/★4.9/scroll-cue plates - he disliked all four; the CENTERED hero kicker "Est. 2001 · Glyfada · Corfu" is original copy and STAYS). Deleted HeroFrame.tsx, its import/usage in Hero.tsx, the cueTrace/.cue-line CSS, and the then-unused SITE.rating/reviewCount from content.ts. Kept: kinetic name, RotatingWord, btn-fx, hero-breathe, spotlight, WebGL shader. (2) INSTAGRAM GRID ORPHAN FIX - user: bottom row had 1-2 lonely tiles. Root cause: post count must satisfy cells=n+3 divisible by 4 (desktop) AND 2 (mobile) → valid n = 5, 9, 13... (feature tile is 2x2). Was 6 fallback posts (slice(0,7) live) = orphans on both breakpoints. Now INSTAGRAM_FALLBACK = 9 photos (added petros-05, broker-03, kostas-11) and the Behold live path takes 9 if available else 5 else falls back (never orphans). Verified 1440px: 3 exactly-full rows; narrow: feature + 4 pairs, all full. (3) ZEUS LIGHTNING - user picked "lightning flashes" (only) from 4 offered options for the IntroTeaser statue image (tattoo-13, he calls it Zeus). New `ZeusLightning.tsx` inside the masked statue container in IntroTeaser (so the flash feathers with the photo's edge mask): every 8-14s randomized (first at 2.5s), remount-keyed layer plays `.zeus-flash` (globals.css `zeusFlash` keyframes: strike 0.85 → dip 0.12 → 2nd strike 0.65 → decay, 1.1s) - cool blue-white top-down gradient + radial pool, `mix-blend-mode: screen`; IntersectionObserver ≥25% + !document.hidden gating, reduced-motion = fully off; cheap enough to run on mobile. VERIFIED live (prod build): sampled computed opacity every 70ms → curve 0.8→0.26→0.62→decay matches design; blend=screen, anim=zeusFlash, insideMask=true, 0 console errors. VERIFY GOTCHA: preview_eval Promises >~5s die with "Inspected target navigated or closed" - poll in ≤4.5s windows. Build 36 pages + tsc clean. NOT redeployed - the Vercel preview alias still shows the pre-hero-upgrade build; deploy stays gated on Mike's go.
ZEUS LIGHTNING v2 (2026-07-10, same day - user: "should light WHEN the photo appears" + "I see the light but not the stripes"): ZeusLightning.tsx rewritten. (a) STRIKE ON ENTRY - IntersectionObserver entry (>=25%) now fires immediately (3s cooldown against scroll jitter), ambient 8-14s cadence continues while visible; removed the old fixed 2.5s first strike. (b) VISIBLE BOLT - keyed strike layer now = glow div + `.zeus-bolt` SVG (viewBox 0 0 100 120, preserveAspectRatio none, vector-effect non-scaling-stroke): 3 BOLTS path variants (main channel + branch each), per-strike random variant + translateX(-12..12%) + scaleX mirror picked in fire() via boltRef (client-only, no SSR mismatch); white-blue stroke + double drop-shadow glow; `zeusBolt` keyframes (globals.css) peak just BEFORE the zeusFlash glow (strike causes light) with echo at 25% for the double-strike. VERIFIED prod: no strike at page top; scrollIntoView -> strike latency 52-54ms, bolt opacity peaks 0.98-1.0 with strike/dip/echo curve, 2 paths, random transform differs per strike, 0 console errors. GOTCHA: preview_eval promise tolerance can be <3.3s (died mid-wait and RELOADED the tab, resetting React state) - keep polls ~2.5s and treat "target navigated or closed" as a tab reset.
ZEUS LIGHTNING v3 (2026-07-10, user: "ONLY two triggers - when the picture appears on scroll, and when my cursor is on it"): removed the ambient 8-14s background cadence entirely. Triggers now: (1) IO entry >=25% arrival strike (3s cooldown), (2) `pointerenter` on the HOST (el.parentElement = the masked image container; the overlay itself is pointer-events-none) strikes with 2.5s cooldown, then a setTimeout loop re-strikes every 4.5-6.5s while hovering ((hover:hover) devices only - touch tap gets its single strike via pointerenter); `pointerleave`/`pointercancel`/IO-exit clear the loop. tryFire gates on visible+!document.hidden+cooldown. VERIFIED prod (single-eval timeline, since the tab reloads BETWEEN evals - Chrome scroll-restoration then even re-fires the arrival strike, so cross-eval node markers like window.__strikeNode are useless): 2.5s in-view no-hover silence -> 0 strikes; synthetic pointerenter -> new strike in 38ms, bolt 0.92; arrival strike 41-54ms across runs; 0 console errors. Hover REPEAT loop (4.5-6.5s) is code-verified only (longer than the eval tolerance window). LESSON: when preview evals keep resetting state, script the whole scenario timeline inside ONE promise (scroll -> silence-watch -> dispatch -> assert) instead of chaining evals.
ZEUS STORM v4 (2026-07-10, user pasted a photoreal branched blue bolt: "better stripe" + wants a GRAY CLOUD half outside/half inside the photo top edge, bolt coming OUT of the cloud, not covering Zeus): pasted chat images can't reach the filesystem, so assets were GENERATED procedurally - `scripts/gen_fx.py` (numpy+PIL, seeded/reproducible; kept in the repo) makes `public/images/fx/bolt-1..3.webp` (midpoint-displacement fractal channel + recursive branches/tendrils, 3-layer composite: white-hot core + inner #78b4ff glow + outer blue haze, 700x1400, ~81-91KB each) + `cloud.webp` (multi-octave value noise x lobed-blob silhouette, storm-gray lit-top shading, baked edge fade, 1600x640, 82KB). PNGs were 549KB -> WebP q85 (~335KB total). Read fx_CHECK.png montage before wiring (house rule). STRUCTURE: IntroTeaser photo now wrapped in an extra `relative` div; ZeusLightning is the UNCLIPPED SIBLING of the masked photo box (cloud must overflow the top edge - the old inside-the-mask placement would clip it). ZeusLightning layers (DOM order = stacking): (1) keyed photo glow div carrying its OWN copy of the photo's edge mask (PHOTO_MASK const - it's outside the masked box now), (2) keyed bolt `<img>` top:-3% left:50+x% height:64% w-auto max-w-none, random variant/±12% offset/scaleX mirror, `.zeus-bolt` anim, (3) PERMANENT cloud img `left-1/2 top-0 w-[112%] max-w-none -translate-x-1/2 -translate-y-1/2` (painted after bolt = above it, so the bolt origin hides INSIDE the cloud), (4) keyed cloud-interior radial glow above the cloud at the bolt's x. Bolt imgs pre-warmed on mount (`new Image()`) so the first strike isn't blank while the WebP downloads. Triggers unchanged (arrival + hover only). VERIFIED prod 1440+375: cloud center EXACTLY on photo top edge (0px = half out/half in), 1.12x width, only 18% into the photo (Zeus clear); hover strike 36ms new bolt node opacity 0.93 loaded=true; silence between; overflow-x 0 both breakpoints; mobile cloud bbox overlaps the "Our story" link by 18px BUT the asset's top 10% is baked transparent fade + overlay is pointer-events-none (link clickable, visible wisp ~nil); 0 console errors; build 36 pages. Tab-reload+scroll-restoration PRE-FIRES the arrival strike before an eval can watch it - expect stale decayed .zeus-bolt nodes at eval start.
ZEUS STORM v5 - ELECTRO STRIKE (2026-07-10, user: cloud "small and ugly"/make larger, "Our story" must NOT be hidden (cloud behind it), and USE the "Electro Strike UI component" sheet he saved to Downloads): source = `C:\Users\mikef\Downloads\electro striipe ui component.png` (1536x1024 mockup sheet, strikes on near-black panels - authorized). EXTRACTION TRICK: crops from dark-panel mockups work as screen-blend assets with NO alpha needed - but MUST black-level first (per-channel subtract the panel bg = median of darkest 55% of pixels, rescale) + fade crop edges, else screen-blend reveals faint RECTANGLES (first check caught this). `scripts/gen_fx2.py` extracts panel crops by fractional boxes + bright-content bbox trim: `strike-v1.webp` (the "1. DEFAULT" wide strike ROTATE_90'd = hi-res 115x914 main bolt), `strike-v2.webp` (the "7. VERTICAL", 57x233, softer alt), `strike-h.webp` (the "2. GLOW INTENSE" band, 928x123, cloud-interior crackle); deleted bolt-1..3.webp. CLOUD v3 in same script: continuous storm FRONT (unbroken spine band + 9 mass blobs + 18 cauliflower lobes, billow noise, flat base, rim-lit tops, alpha^0.85 densify) - fixed the "separate blobs" look; 2048x832, 81KB. NEW `ElectroStrike.tsx` UI component (his ask): variants horizontal/vertical/vertical-alt from `STRIKE_SRC` map, `mix-blend-mode:screen` imgs. ZeusLightning strike now = photo glow + ElectroStrike bolt (variant weighted 60% v1, ±12% x, mirror) + radial cloud glow + ElectroStrike HORIZONTAL CRACKLE through the cloud belly (all keyed, `.zeus-bolt` anim; crackle+bolt both img.zeus-bolt - queries return 2 nodes/strike). Cloud now `w-[135%]` (was 112%), still translateY(-50%) half-out, 22% into photo. FIXES: (a) 135% cloud caused 41px horizontal page overflow -> `overflow-x-clip` on the intro SECTION (clip not hidden - no scroll context; cloud's baked 4.5% edge fade hides the clip line); (b) mobile "Our story" - text column got `relative z-[1]` so it paints ABOVE the cloud (cloud bbox overlaps link by 35px but link wins stacking; deterministic: positioned z-1 sibling > overlay's z-auto). VERIFIED prod: cloud 1.35x/half-out/22% depth desktop+mobile, strike = 2 layers (strike-v1+strike-h) both loaded+screen at 43ms, hover strike 36ms op 0.92, overflow-x 0 (was 41), textCol z=1 position relative, 0 console errors, build 36 pages. fx total ~109KB (cloud 81 + strikes 28).
ZEUS STORM v6 - REAL CLOUD + REALISM ORDERS (2026-07-10, user: procedural cloud "fake looking" - use the REAL one he downloaded; horizontal in-cloud strike "totally wrong, always vertical"; strike light ONLY inside the cloud; cloud FADES IN on appearance and stays; edges faded to blend; "check if we achieve my orders"): source = `C:\Users\mikef\Downloads\cloud png.png` (1536x1024 RGBA, ALREADY a clean alpha cutout - 40% fully transparent; no extraction needed). `scripts/`-worthy processing in scratchpad gen_fx3.py: trim bbox -> 1489x763 (ratio 1.952), 1.5px alpha blur, SMOOTHSTEP border fade ramps (top/bottom 14%, sides 6% - the source cutout has a HARD CROWN EDGE the first check caught; linear 7% was not enough), overall alpha x0.95 -> cloud.webp 155KB. ZeusLightning v6: REMOVED the photo-wide sky glow (PHOTO_MASK) and the horizontal crackle layer entirely (strike-h.webp stays on disk for ElectroStrike's divider variant, unused here). Strike = ONE vertical ElectroStrike bolt + cloud-interior glow whose div is CSS-MASKED BY THE CLOUD'S OWN ALPHA (maskImage:url(cloud.webp), maskSize 100% 100%, same CLOUD_POS geometry classes + aspectRatio '1489/763' so its box is co-extensive with the cloud img; radial centered at glowX = 50 + boltX/1.3 since cloud is 130% photo width, y 52% = the photo top edge line) -> light physically cannot appear outside cloud pixels. Cloud: w-[130%], -translate-y-[52%] (52% out / 48% in, depth 26% mobile-4/5 / ~24% desktop-3/4), FADE-IN = inline style opacity 0 -> 0.97 with transition 1.6s cubic-bezier(0.22,1,0.36,1), triggered by cloudIn state set on FIRST IO entry (reduced-motion: setCloudIn(true) immediately, no strikes). Arrival strike now DELAYED 900ms after entry (arrivalTimer, cleared on exit/unmount) so the bolt lands mid-fade - storm forms, then discharges. VERIFIED prod (timeline eval): fadeCurve 0.09->0.96 by ~1s, final 0.97 STAYS; arrival bolt at 960ms, count=1, src strike-v2, anyHorizontal=false, boltMax 0.98 (live catch); glow mask contains cloud.webp, box matches cloud not photo; settled geometry aboveEdgeFrac=0.52 EXACT; overflow-x 0/-9 both breakpoints; mobile textCol z=1, link visible, cloud 0.97 after scroll; 0 console errors; build 36 pages. MEASUREMENT GOTCHA: capturing the photo rect at eval START while Reveal's enter-transition is still settling skewed aboveEdgeFrac to 0.62 - re-measure AFTER settle (static eval) before believing geometry numbers.
ZEUS CLOUD SLIMMED (2026-07-10, user: "too big vertically, lots of unneeded cloud at top; horizontal size good; fade the top"): gen_fx4.py (scratchpad) re-processes from the ORIGINAL Downloads source: trim -> CROP TOP 32% (763->519 tall, ratio 2.869) -> UNEVEN billowy top fade (per-column fade depth 20-42% of H from smooth 1D noise knots; the first uniform-ramp attempt read as a flat artificial "fog band" line - check caught it) + bottom 10%/sides 6% smoothstep fades + alpha x0.95 -> cloud.webp 106KB. Component: CLOUD_AR '1489/519', CLOUD_POS translate -y-1/2 (50/50 seat, was -52%), glow radial y 52->50%. VERIFIED prod: naturalHeight 519 live, aboveEdgeFrac 0.50 exact, depth 26->18% of photo, width ratio 1.30 unchanged, glow box matches + masked, 1 vertical bolt, overflow 0, mobile cloud-over-link overlap 63->19px (faded crown region, z-1 protects), 0 console errors. Fade-in/strike code untouched this round (live-proven in v6; tab-reload prefire hid them from this eval).
ZEUS STORM v8 (2026-07-10, user: top-left/right corners look CUT - want curly real-cloud lobes; cloud still a bit big/covers image; fade the cloud's LOW part into the image; strike should light the Zeus image): gen_fx5.py re-processes from original: crop top 32% + uneven crown fade (kept) + NEW curly_corner() carve - scalloped quarter-ellipse per top corner (elliptical distance rx=0.30W ry=1.15H from the corner, radius modulated 0.80+0.34*smooth-noise(angle), fade band (d-0.62)/0.38 ^1.5) -> billowy curled corners, no straight cut; bottom fade DEEPER + uneven (per-column 16-30% smooth-noise depth); sides 6%; alpha x0.95 -> cloud.webp 103KB (ratio 2.869 unchanged). Component: CLOUD_POS w-[120%] (was 130) -translate-y-[55%] (was 50) -> depth 15% of photo (was 18); glowX divisor 1.2; cloud-glow radial y 55%; PHOTO_MASK const RE-ADDED + NEW photo-lighting strike layer (keyed, inside PHOTO_MASK wrapper, zeus-flash, screen, radial 62%x55% at bolt-x/26% rgba(196,218,255,0.6)) painted UNDER the bolt - the strike now lights the statue around its path (user reversed the v6 "light only in cloud" order). VERIFIED prod LIVE (fresh-load timeline, no prefire this time): arrival strike 936ms w/ ALL 3 layers animating in sync (bolt 0.92 / cloudGlow 0.84 masked-by-cloud / photoLight 0.84 in gradient-masked wrapper), 1 vertical bolt strike-v1, width 1.20 / aboveEdge 0.55 / depth 15%, cloud fade 0.97, overflow 0 both breakpoints, mobile cloud-vs-link 20px + textCol z=1, 0 console errors, build 36 pages.
ZEUS CLOUD FINAL SHAPE (2026-07-10, user: v8's scalloped corners "very ugly" - revert to the v4 look but "more circular, oval shape, cloudy at the edges, don't crop weirdly"): gen_fx6.py = v4 recipe (crop top 32%, uneven billowy crown fade) + ONE soft OVAL silhouette over the whole cloud (ellipse rx 0.52W ry 0.62H at cy 0.52H, radius modulated only GENTLY 0.93+0.14*smooth-noise(angle), wide fade band 0.72->1.0 smoothstepped) + moderate uneven bottom fade (12-22%); NO corner carving. cloud.webp 109KB/111278 bytes, dims STILL 1489x519 so ZERO component changes (CLOUD_AR/POS valid). LESSON (2 fails): aggressive scallops (+-34% radius) read as ugly cuts; uniform ramps read as fog bands - a single gentle oval falloff (+-7% waviness) over a real photo keeps it natural. ASSET-ONLY swaps need NO rebuild (public/ served from disk at runtime) - verified served bytes === disk bytes via fetch no-store. VERIFY GOTCHAS: (1) scrollIntoView immediately after location.replace races hydration - SmoothScroll's initial usePathname effect runs lenis.scrollTo(0,immediate) and UNDOES the scroll -> wait ~800ms post-load before scrolling; (2) the preview pane can be document.hidden=true (background tab) -> tryFire's hidden-guard correctly blocks ALL strikes (fade-in still runs, it's not gated) - check document.hidden before concluding the strike pipeline broke. Cloud fade-in verified live on the new asset (0.97); strike pipeline unchanged from the fully-verified v8 code.
ZEUS FX ARCHITECTURE REVIEW + CRACKLE UPGRADE (2026-07-10): user asked if the effect is built "the professional way / should it be GSAP". Ran a 6-agent Workflow (5 specialist lenses + adversarial judge, `zeus-fx-architecture-review`). UNANIMOUS verdict: the CSS-keyframe + raster-sprite + IO/pointer-trigger build IS professional/legit (~110KB), does NOT need a rebuild, and does NOT need GSAP (GSAP's real wins are SVG-only DrawSVG/MorphSVG/MotionPath - bolt is a raster sprite; its one relevant win - per-strike timing variety - framer-motion already delivers in-bundle; re-adding +23-50KB for one accent is the wrong trade). The ONLY genuine "looks better" lever (4 of 5 lenses): the bolt was a FROZEN shape whose opacity blinked twice = the stock tell. SKIP list: GSAP, WebGL shader bolt (portfolio flex, 2 impls), Rive/Lottie/alpha-video (kills photo-reactive compositing + adds WASM/codec runtime). IMPLEMENTED the top "adopt" items: (1) CRACKLE - replaced the single `.zeus-bolt`/`zeusBolt` double-pulse with THREE stacked ElectroStrike sprites of DIFFERENT silhouettes on staggered keyframes (globals.css `zeusStroke1` bright leader ~peak3-10%, `zeusCrackle` faint 0.45 mid-stutter ~14-19%, `zeusStroke2` return stroke ~peak21% - each leads a zeusFlash light peak at 7%/24%); stroke-1=primary variant, crackle+stroke-2=OTHER variant (mirror via scaleX), so silhouette changes stroke-to-stroke. `.zeus-bolt` is now a marker class only (no animation rule) - the 3 layers carry `.zeus-stroke-1/.zeus-crackle/.zeus-stroke-2`. (2) PRELOAD - swapped the post-hydration `new Image()` warm for React 19 `preload(src,{as:'image'})` from 'react-dom' in render (hoists `<link rel=preload as=image>` to head, real priority vs the first-strike race). VERIFIED prod LIVE: preload links in head + all 3 keyframes registered; live strike captured - stroke-1 strike-v1 peak 0.84 @947ms, crackle strike-v2 0.34 @1078ms, stroke-2 strike-v2 0.88 @1146ms (3 distinct peak times, strokesDifferentSprite=true, spread 199ms); 0 console errors, overflow 0 desktop+mobile, both sprites preloaded on mobile; build 36 pages, tsc clean. NOT done (offered as next step): baking a true multi-frame crackle strip in the PIL pipeline for frame-by-frame forking. Deploy still gated.
ZEUS CLOUD SCROLL-SCRUB (2026-07-10, user idea: cloud should FADE IN on scroll down, LOCK at full when it reaches the point, stay locked scrolling further down, and fade OUT on scroll up back to the hero; asked "is that GSAP / what's it called"): answered - it's a scroll-linked/scroll-scrubbed animation ("scrubbing") + the lock is "pinning"; GSAP ScrollTrigger is the famous tool BUT this project already uses framer-motion useScroll/useTransform (+Lenis) for exactly this, so built with framer, NO GSAP added (consistent w/ the architecture review). IMPL in ZeusLightning: replaced the one-shot IO `cloudIn` state + 1.6s CSS opacity transition with `useScroll({target: ref, offset:['start end','start center']})` -> `cloudOpacity = useTransform(scrollYProgress,[0,0.6],[0,0.97])`; cloud is now `motion.img` `style={{opacity: reduce ? 0.97 : cloudOpacity}}`. The LOCK = useTransform's default clamp: past progress 0.6 opacity holds at 0.97 however far down you scroll; reverse on scroll-up fades to 0 as the hero returns (progress 0 = photo top at viewport bottom). Removed the `cloudIn` useState + both setCloudIn calls (reduced-motion branch now just `return`; render shows static 0.97 via `reduce`). Added `preload(CLOUD_SRC,{as:'image'})` alongside the bolt preloads (no pop mid-scrub). Strike triggers (IO arrival + hover) UNCHANGED. VERIFIED prod LIVE via scroll sweep (single-eval, native window.scrollTo): hero y0 op 0; smooth fade-in ramp op 0.166->0.488->0.81 across the entry; LOCKED 0.97 through the lock zone AND deep-scroll (photo top -0.35vh, re-read after settle = 0.97); back-at-hero op 0 (reversible); mobile hero 0 / lock 0.97 / overflow 0; 0 console errors, build 36 pages tsc clean. GOTCHA: a huge INSTANT window.scrollTo jump under Lenis leaves framer's rAF scrollYProgress momentarily stale (read 0 at deep-scroll on the first big jump) - re-read after ~400ms settle; real continuous scrolling never hits this.
ZEUS LOCK-EARLIER + PARALLAX-SETTLE + AMBIENT STRIKE (2026-07-10): user: the statue's parallax movement made the cloud "lock" invisible; wants cloud to lock EARLIER + the picture to STOP moving under it after a point; and the strike to be MAINTAINED (ambient) not only on hover. THREE coordinated changes: (1) Cloud locks earlier: ZeusLightning cloudOpacity `useTransform(scrollYProgress,[0,0.6]->[0,0.32],[0,0.97])` (full by progress 0.32). (2) Statue parallax SETTLES + holds: IntroTeaser parallax useScroll RETARGETED from the section `ref` to a NEW `photoRef` on the photo wrapper `<div>` with the SAME offset as the cloud (`['start end','start center']`) so statue+cloud lock on the same scroll beat; y `useTransform([0,1],['10%','-10%'])` -> `([0,0.35],['6%','0%'])` so the image drifts up 6% during entry then CLAMPS at 0% and holds -> once locked the picture doesn't move under the cloud however far you scroll down. (3) Ambient strike: replaced the hover-only repeat loop with an `ambientLoop` (setTimeout 5000+rand*3000) started on IO entry + cleared on exit, gated by tryFire (visible+!hidden+cooldown); hover `pointerenter` now just fires ONE extra strike on demand (removed hoverTimer/onLeave/canHover). VERIFIED prod LIVE desktop: cloud op 0.31@topFrac0.95 -> 0.97@0.84 (locks early); parallax translateY 26.7px@0.95 -> 3.2@0.84 -> 0/0/0 at 0.6/0.3/-0.2 (settles + HOLDS at deep scroll); ambient = 9 strikes over 49s with NO hover (~every 6s). FRESH mobile load: hero op0 / lock 0.97@rectTop682 / deep 0.97 held / back-to-hero op0 (reversible); 0 console errors, build 36 pages tsc clean. GOTCHA: framer useScroll listener goes STALE after a live preview_resize (cloud stuck 0.97 at all scroll positions on the resized mobile view) - RELOAD fresh at the target size to test; real users load fresh so it's a preview artifact, not a bug.
ZEUS FLOAT + HIGHER/LIGHTER CLOUD + DESKTOP GAPS (2026-07-10): (1) Cloud idle FLOAT - restructured cloud markup: outer CLOUD_WRAP div (absolute placement, now `-translate-y-[60%]` = sits HIGHER, was 55%) > inner `relative zeus-float` div (globals.css `zeusFloat` keyframe translate3d 0->(-0.8%,-2.4%)->0, 7.5s ease-in-out infinite; reduced-motion drops the class) > motion.img (`block w-full`, opacity scrub) + the cloud-interior glow (`absolute inset-0`, masked by cloud alpha) - glow now floats WITH the cloud because the float div is `relative` (establishes containing block). Inner div MUST be `relative` or the absolute glow escapes the float transform. (2) Cloud opacity 0.97 -> 0.88 (cloudOpacity useTransform output + reduced-motion static). (3) DESKTOP section gaps lowered (mobile py-16/20 untouched per user): IntroTeaser sm:py-40->28, ArtistsStrip/FeaturedWork/Locations sm:py-36->24, InstagramFeed/Specialties sm:py-32->24, CTAFooter sm:py-48->32, MarqueeBand left. Verified in-page: #intro 112px, others 96px. EYE-IGNITE EFFECT: built (zeusEyes keyframe + EYES coords + neon radial glows over the statue's eyes) then user REJECTED it ("I don't like the eye effect, remove it") - FULLY REMOVED (keyframe, .zeus-eye class, EYES const, render layer). Do NOT re-add. VERIFY-HARNESS GOTCHA this session: the Browser pane changed to javascript_tool (no preview_eval) and booted with innerHeight=0 which STARVES framer's useScroll (cloud opacity read 0 at every scroll pos); `resize_window {width,height}` fixes innerHeight but framer already init'd stale; heavy scroll+event-pump evals TIME OUT/hang the pane. Static checks (paddings, animationName, keyframe existence, DOM structure) are reliable; scroll-driven framer readings are NOT in this pane. The scrub/lock/parallax-settle/ambient logic is UNCHANGED from the fully-verified prior round, so still-correct for real users. Build 36 pages + tsc clean each time. Deploy still gated.
STALE-PREVIEW LESSON (2026-07-11): `npm run build` alone does NOT update the running `next start` preview - MUST preview_stop + preview_start after each build or the pane serves the OLD bundle (user saw the removed eye-dots persist, and cloud-opacity changes not apply, purely because the server wasn't restarted). Also this session's Browser pane replaced preview_eval with `javascript_tool`/`navigate`/`read_console_messages` and boots at innerHeight=0 (starves framer scroll); `resize_window {width,height}` gives a real viewport for static checks.
INTRO GHOST EMBLEM (2026-07-11): user circled the empty bottom-left void of the Intro section (desktop) + asked for a faded-logo fill "per the master agents". Consulted the `nami` design agent: she VETOED the literal centered faded wordmark (reads as watermark/muddy) and ranked options; user CHOSE her Option C = the compact crest cropped off the corner. IMPL in IntroTeaser: first child of the section, `<div aria-hidden pointer-events-none absolute bottom-0 left-0 z-0 hidden md:block w-[36%] max-w-[440px] -translate-x-[42%] translate-y-[42%]` opacity 0.05> `<img /brand/logo-mark.png>`. logo-mark.png = the full ornate "UNICORN TATTOO" crest (192x169, white on transparent) so it MUST be cropped hard + kept ~5% or it muddies. Section `overflow-x-clip` -> `overflow-clip` (both axes) to crop the emblem's left+bottom bleed; VERIFIED the storm cloud is NOT clipped (cloud top sits +84px inside the section). VERIFIED: desktop display block, opacity 0.05, z-0 (behind z-[1] text), bled -185px left / +163px below (clipped), mobile display:none, 0 console errors, build clean. Nami's runner-up ideas if he wants to change: ghosted "ΔΙΑΣ"(Zeus) or "2001" in giant faint GFS Didot outline, bled off bottom-left (her actual top pick was ΔΙΑΣ). Opacity/size/position all one-number tunes. UPDATE: the ghost-emblem void fill was REJECTED and fully removed - user disliked both the 5% logo-mark crest AND a follow-up where he supplied Downloads\UNICORNN.PNG ("UNICORN INK - TATTOOS AND MORE", a DIFFERENT brand name than the site's "Unicorn Tattoo" - off-brand; feathered to unicorn-emblem.webp then deleted). Void is plain black again; section reverted overflow-clip -> overflow-x-clip; unicorn-emblem.webp deleted. Do NOT re-add a logo there without asking. Nami's actual TOP pick (if revisited) was giant faint "ΔΙΑΣ" (Zeus) in GFS Didot outline.
STRIKE-ON-LOCK (2026-07-11, kept): replaced the 900ms fixed arrival-strike delay with an immediate strike the instant the cloud LOCKS. `useMotionValueEvent(cloudOpacity,'change')` fires when opacity crosses LOCK_T=0.72 (near the 0.78 max) once (lockedRef), re-arms when it drops <0.4 (scroll up). Needed cross-boundary refs since fire() lives in the useEffect: fireRef (=fire, assigned in effect, nulled on cleanup), visibleRef (replaced the local `visible`; IO + tryFire now use it). IO entry now only starts the ambient loop (no arrival timer). Could NOT re-verify live (pane's framer scroll is broken this session - cloudOp reads 0 at all scroll pos) but logic is sound. Build clean.

**Pending:** other pages (Portfolio/Gallery+lightbox, Artists+detail, Piercing, About, Studios/Contact, multi-step Book form + email API route), real Instagram API wiring (`/api/instagram` + token), real assets, OG image, favicon. next-intl gotcha: this version (3.x) has no `hasLocale` export and `NextIntlClientProvider` needs explicit `messages={await getMessages()}`.

**GIT + LPM HERO FIX (2026-07-15):** Folder git-initialized (was none): baseline `345c460` = exact source of latest Vercel prod deploy dpl_AprkPv1NF1NRvLTX6KGSx8T92YTc (2026-07-11, project unicorn-tattoo, team webactionhellascom; .vercel/project.json has ids). Hero film now GUARANTEED-MOTION under blocked autoplay (iOS/macOS Low Power Mode): commit `4ca5a8a`, three layers in HeroVideo.tsx: poster img `public/video/hero-poster.webp` (frame 0 = logo on glass, 55KB) -> hardened native video -> JSMpeg canvas fallback (`@cycjimmy/jsmpeg-player` lazy import, `public/video/hero.m2ts` MPEG1 960x540 1900k 9.2MB). play() NotAllowedError OR paused-while-ready watchdog -> canvas engages, video fades opacity-0 (kills the iOS play glyph), first real gesture (touchend/pointerup/click carry activation, touchstart does NOT) hands back to native, time-synced via jsmpeg currentTime. `?lpm=1` forces the path (also strips the autoplay ATTRIBUTE, else desktop native autoplay defeats the demo). HeroInkShader now hides itself (opacity 0 imperative) whenever video.paused so it can't freeze-frame over the canvas (its WebGL ctx is alpha:false = opaque). TRAPS: MPEG-TS `.ts` extension collides with tsc (renamed .m2ts); in-app preview pane SUSPENDS rAF + CSS transitions (stuck CSSTransition pinned computed opacity at 1, JSMpeg looked frozen) - verify via headless Playwright probe instead (scratchpad hero-probe.mjs pattern: 3 scenarios incl. addInitScript play()-reject to fake LPM + tap recovery; all 3 passed). Launcher note: `.preview-dev.mjs` (port 3020, launch "unicorn-tattoo") runs `next start` = STALE prod build; `.preview-dev-3021.mjs` (launch "unicorn-review") = `next dev`. 2026-07-15 session also kicked off Echo CMS backend (Supabase + /admin, every text el+en + content images owner-editable).

**OWNER CMS (2026-07-15, Echo):** Full owner-editable backend so the studio owner edits EVERY text (el+en) and all content images with no code. Supabase project `unicorn-tattoo` (ref gezbgdekzxljgpyuoqzs, eu-central-1, FREE tier, team webaction org). Design = DB-overrides-with-bundled-fallback: `src/lib/cms/data.ts` `getCms()` + `getMessageOverrides()` read PostgREST with the ANON key (NO service_role anywhere), 4s AbortSignal timeout, `unstable_cache` tag `cms` (1h). DB wins per key/row; bundled `content.ts` + `messages/*.json` are the always-working fallback so the public site NEVER blocks/breaks on Supabase (verified: renders fully with env removed AND with host unreachable). Text merge deep-merges flattened `site_content` keys onto bundled JSON in `src/i18n/request.ts` ONLY onto existing leaves (proto-pollution safe). Tables (9, RLS on all): site_content(key,locale PK), artists, portfolio_items, studios, featured_work, instagram_posts, piercing_photos, site_settings, admin_users. RLS: anon SELECT published only; writes gated on `private.is_admin()` SECURITY DEFINER in NON-exposed `private` schema (not callable via /rest/rpc). Storage bucket `site` public-read, admin-write, 10MB, jpeg/png/webp. Single owner account (Supabase email/password auth): unicorntattoo2001@gmail.com — password in `.env.local` ADMIN_* (CHANGE after handover; app never reads it, docs-only). `/admin` (own root layout, unlinked, noindex meta + X-Robots-Tag, out of sitemap + intl middleware): tabs Κείμενα (all ~165 keys el/en side by side, search, per-key diff vs default, reset), Καλλιτέχνες (fields+portrait upload+works reorder/hide), Εικόνες (featured/IG/piercing/backgrounds), Στούντιο (both locations+social). Uploads client-resized to 1600px JPEG q0.85. Saves POST `/api/revalidate` (verifies caller Supabase JWT + admin_users self-check) → revalidateTag('cms'). supabase-js ships ONLY in /admin chunk (verified absent from public bundles). Migrations in `supabase/migrations/`, seed `supabase/seed.sql`, `DEPLOY-NOTES.md` has Vercel env (NEXT_PUBLIC_SUPABASE_URL + ANON_KEY) + 2 one-time dashboard toggles (disable signups, leaked-password protection). Commits 2778569/c481f1a/333e64c/829c2f4.

**SECURITY HARDENING (2026-07-15, commit be0dd8e):** Adversarial 13-agent audit = SECURE_WITH_NOTES, nothing anon-reachable. Fixed the one real (admin-only) stored-XSS surface: admin-writable URLs (social.instagram/facebook, instagram post href, studio maps_url) went into `<a href>` with no scheme check → a `javascript:` value would run on click. Added `safeHttpUrl()` in data.ts (http(s)-only allowlist, else bundled fallback) applied at the data-layer source so ALL href sinks are covered at once (Footer/Header/InstagramFeed/InteractiveMap), + write-side validation in StudiosEditor (native Greek error) + fixed admin label 'Graphic'→'Γραφικό'. Verified: 11/11 attack vectors blocked (javascript:/data:/vbscript:/whitespace/case), in-browser social+maps render clean https with 0 dangerous hrefs. OPEN judgment calls for Mike: (1) .env.local ADMIN_PASSWORD plaintext — gitignored/never-read, delete or rotate; (2) no rate-limit on /api/revalidate — negligible (GoTrue already public via anon key); (3) prod script-src still has 'unsafe-inline' (Next bootstrap req) — optional nonce migration later; (4) perf_audit source-image-weight FAIL is PRE-EXISTING baseline (13.6MB source, delivered via next/image AVIF is fine), not a regression — separate careful re-compress task filed. Full combined-build gate (CORKY): build+tsc clean, hero film works in normal+LPM+recovery on the CMS CSP with 0 violations, First Load JS no regression (home 430KB), 0 console errors, 0 em-dashes. NOT deployed — gated on Mike.

**PAPER-SHADERS SECTIONS + DEPLOYED (2026-07-15, commit 3dca25f):** Mike's friend at the office rolled production back to the OLD design (dpl_Aprk / July 11) — Mike wanted our be0dd8e base (perfect hero + CMS) but liked THREE things on the rolled-back live site and asked to port them in. The old live version used **@paper-design/shaders-react** `NeuroNoise` (found by extracting the minified page chunk: uniforms u_colorFront/Mid/Back/brightness/contrast; the site's own chunks referenced only NeuroNoise) as section backgrounds. Reproduced 1:1 as `src/components/ui/NeuroBackground.tsx` — a lazy (`next/dynamic` ssr:false), reduced-motion-aware, intersection-paused wrapper with the live palette **colorBack #060606 / colorMid #123A44 / colorFront #2E96B0** (the real petrol-teal brand accent, see [[unicorn-tattoo-accent-teal]]), brightness .9, contrast .32, speed .35, scale 1.4, `overlay` soft gradient veil, container opacity **.4 behind ArtistsStrip, .34 behind FeaturedWork**; includes the live's paper-shader resize hack (poll `[data-paper-shader].paperShaderMount`, set parentWidth/Height + devicePixelsSupported=false + handleResize). Installed `@paper-design/shaders-react` v0.0.77 (supports colorFront/Mid/Back/brightness/contrast/speed/scale exactly). STUDIO (`IntroTeaser`) reworked to the live design: 25th-anniversary watermark `public/brand/anniversary-25.png` (opacity .2, mix-blend-screen) + clean `public/images/tattoo/intro-portrait.jpg` card (aspect-[4/5] sm:[3/4], parallax kept), 7/5 grid — **dropped ZeusLightning** (Mike preferred the live studio; the electro/lightning was already deprecated, see [[electro-stripe-effect]]). INSTAGRAM (`InstagramFeed`) reworked to the live bento: profile header (IG conic-gradient avatar ring + logo-mark, handle, `instagram.heading`="Lately at the studio" subtitle, Follow pill w/ IG glyph) + a **video-reel feature tile** `public/video/reel.mp4` (poster hero-poster.jpg, muted loop) leading the grid + 8 photo singles w/ hover IG-icon overlay. All 3 stay wired to existing i18n + CMS. The 3 missing assets (anniversary-25.png 1.4MB, intro-portrait.jpg 330KB, reel.mp4 1.6MB) were pulled from the live URL (Mike's own site). Verified on `next dev`: tsc clean, 0 console errors, all assets 200, shader canvases real-sized + dark teal. **DEPLOYED to production** via `vercel deploy --prod` → dpl_GZGng1gd71eTgUmbngFqvRfY9UoN (commit 3dca25f); `unicorn-tattoo.vercel.app` now serves it, verified live. Custom domain unicorntattoo.gr NOT touched (separate/gated).
DEPLOY LESSON (cost an extra step): a Vercel production **ROLLBACK pins the apex alias** — `unicorn-tattoo.vercel.app` was pinned to the old dpl_Aprk, and a fresh `vercel deploy --prod` built+promoted a new prod deploy and aliased only `unicorn-tattoo-webactionhellascom.vercel.app`, leaving the APEX still on the old one (get_deployment(apex) still returned dpl_Aprk, response Age ~3.9d). Fix = **`vercel promote <deployment-url> --yes`** to clear the rollback and move ALL prod aliases; then get_deployment(apex) returns the new id, Age 0. Always verify the apex alias with get_deployment after a --prod deploy when a rollback may be in play. PREVIEW GOTCHA (scared Mike): the in-app Browser pane is the SAME view Mike sees; a screenshot-prep script that force-set opacity:1 + transform:none on all elements to beat the dead-IntersectionObserver looked like the site broke ("glitchy colors in front"). Don't mutate the DOM in the shared pane for screenshots — a reload reverts it; use real Chrome for shots or just point Mike at the local URL.
