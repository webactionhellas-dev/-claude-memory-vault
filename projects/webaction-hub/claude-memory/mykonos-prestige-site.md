---
name: mykonos-prestige-site
description: ultra-premium cinematic rebuild of mykonosprestigevillas.gr in C:\Users\mikef\mykonos-prestige
metadata: 
  node_type: memory
  type: project
  originSessionId: 3d47f71a-3e5f-4779-bcfe-d2e7273177c9
---

Ultra-luxury (Aman-level) rebuild of **mykonosprestigevillas.gr** in `C:\Users\mikef\mykonos-prestige`. Same stack as [[greencleaners-site]] / [[unicorn-tattoo-site]]: Next.js 15.1.7 + React 19 + Tailwind 3.4 + Framer Motion 11 + lucide-react, TypeScript.

- **Design system "Aegean Whitewash"** (rebranded from dark "Cycladic Nocturne" per user, 2026-07-04): LIGHT professional Greek Cycladic — whitewash white (#F6F9FB), soft sky blue, azure accent #2478B5, deep-sea aegean #123E5E footer, ink text. **Brand fonts self-hosted from client site**: PP Hatton (display) + Averta (body) via next/font/local in `public/fonts/`. Tokens in `tailwind.config.ts` + `app/globals.css`. User dislikes em-dashes (keep copy em-dash-free) and gold; wanted their exact logo + font sitewide.
- **Content lives in `lib/data.ts`** (single source): contact (reservation@mykonosprestigevillas.gr, WhatsApp +30 6957 772858), villas — Villa Thalassa 500m²/8BR/16 guests, Villa Anemos 260m²/6BR/12 guests (Kastro), experiences, testimonials. `bookingUrl` defaults to WhatsApp — TODO swap for real reservation engine.
- **Pro villa photos + videos (2026-07-04, BEST source)**: their own villa pages expose full galleries by villa — `https://mykonosprestigevillas.gr/files/villas/2_N.webp` = **Thalassa** (32 photos, 1920px), `1_N.webp` = **Anemos** (17 photos, 1500px); SPA returns 200+empty for missing indices so verify with `file`. Downloaded to `public/media/villas/{thalassa,anemos}/{th,an}_NN.webp` (Thalassa ~100MB). This SOLVED villa categorization (definitive, not IG-guesswork). Villa hero videos `/files/videos/{thalassa,anemos}.webm` → `public/media/real/`. 17 service icons `/assets/service-icons/*.svg` → `public/media/services/`. Villa pages now use video heroes + real photos; IG photos kept ONLY in the Moments feed. Real specs: Thalassa 16g/8bd/**7ba** AMA 00003642850; Anemos 12g/6bd/6ba AMA 00003642871. **Real amenities/services (2026-07-04)** scraped from villa pages via browser innerText (SPA): each villa's exact amenities (13: infinity pool, firepit[Thal]/indoor fireplace[Anem], AC, Starlink, JBL, smart TVs, yoga mats, PS5, BBQ, baby cot, high chair, parking), `includedServices` (Daily housekeeping, Private parking) shown in Amenities section, and 14 on-request `services` (Pack-Unpack, Security, Babysitting, Catering, Events, Airport Transfers, Helipad, Chef, Concierge, Car Rentals, Yacht, Massage, Yoga/Pilates, Laundry) with icons. Removed my earlier invented specs (rooftop cinema, sauna, 18m/12m pool) for accuracy. Real detailed descriptions captured too (3 levels, bedroom breakdown).
- **ImageReveal blank-image bug (2026-07-04)**: clip-path `whileInView` reveal left random tiles (first in each grid row) permanently stuck at `inset(0 0 100%)` = blank. Fixed by switching `ui/ImageReveal` to opacity+scale (like the home Gallery, which never blanked). Rule: avoid clip-path reveals on grids. Villa videos have "VILLA THALASSA/ANEMOS · MYKONOS" baked in ~1s–6s of each loop (verified via ffmpeg frames). Final: VillaHero shows ONLY the villa name, anchored bottom-left, revealed AFTER the video's baked title (gate on `video.currentTime >= 6` via a requestAnimationFrame loop — timeupdate is unreliable at the loop boundary; re-hides each loop; 7.5s forced-reveal fallback if autoplay blocked). Slow 1.7s blur+rise fade; TITLE_END=5 (title vanishes ~5s; was 6 → 1s gap). Photo crispness: next/image default q75 double-compresses webp + small `sizes` served tiny (256px). Fixed: `images.qualities:[75,90,95]` in next.config + `quality={90}` on all photo Images + grid `sizes` bumped to `92vw/46vw` → tiles now serve 640px q90 (2.18× oversample, crisp). **GOTCHA (bit us 2026-07-05):** a `quality={N}` NOT in that allowlist makes the Next image optimizer return **400** and the image silently won't load — the Lightbox photo viewer had a stray `quality={92}` so clicking any photo showed nothing; fixed to `quality={95}`. Any new `next/image` must use 75/90/95 only. NOTE: preview runs DPR 1 so looks softer than real retina devices (which auto-serve 2×). NEVER run `npm run build` while `next dev` preview is running — corrupts shared `.next` → 500s (rm -rf .next + restart to fix). Perf: pages static/SSG, JS ~400KB, images optimized+lazy, mobile 0 overflow. DEPLOY-PREP TODO (not done): compress source photos (Thalassa 100MB→~30MB) + shrink 1080p videos (intro/anemos ~9MB). Videos upscaled 720p→**1080p** (ffmpeg lanczos VP9, `_1080` then mv over originals; source has no true 1080p so it's interpolated, mobile encodes stay 360p). AboutHero now h-[100svh] (full page). Villa home-carousels use PORTRAIT photos (landscape ones like th_04 looked soft cropped into the 4:5 tile — user flagged th_04; swapped everywhere on home). Real location coords **37.48358, 25.32849** (user-provided). About/villa heroes: no white-fade veil (bg-ink/40 dark scrim) so the photo fills the whole hero. `MagneticButton` cursor-follow removed per user (now a static wrapper). Buttons: hero + nav Book Now made transparent outline (white over video / azure when scrolled). Home hero headline restyled UPPERCASE HattonLight (weight 300) to match their site. Maps use exact coords `37.4989,25.3545`.
- **All dark sections → light (2026-07-04)**: user wanted "very light blue with white", so the Footer + all CTA bands (villa/about `bg-aegean-900`) became `bg-gradient from-mist/white to-sky-soft/mist` with ink text, logo-ink, btn-outline (not btn-ghost-light). Only remaining dark is the Lightbox photo-viewer backdrop (intentional). Home Thalassa carousel leads with th_01 (golden-hour exterior) per user.
- **Palette lightened again + premium button (2026-07-04)**: azure #5289BE→**#6699C7** (deep #3A6C9E for text), buttons = lit-from-top gradient w/ inset highlight + sheen. Home hero: removed subhead paragraph + divider line per user; added `.hero-scrim` + `.text-shadow-hero` for white-text legibility over bright imagery. Low-opacity photo backdrops on Experiences/Testimonials. Live Google Maps embeds on home + villa pages. NOTE on "4k": pro photos are 1500–1920px (highest the source provides); true 4K needs AI upscaling (offered, not done).
- **Instagram photos scraped (2026-07-04)**: 20 posts / 33 images at full res (up to 3072×4096) in `public/media/instagram/` (named `pNN_<code>_<k>.jpg` + `manifest.json`). Method that worked: user logged into IG on their Chrome → claude-in-chrome MCP `javascript_tool` in-page `fetch('/api/v1/feed/user/<uid>/', headers x-ig-app-id:936619743392459)` (paginate via next_max_id), fetch each image blob (CORS OK), build a store-only ZIP in pure JS (IG CSP blocks JSZip; extension blocks URL/base64 export), download to Downloads, unzip server-side. gallery-dl/instaloader FAILED (Chrome cookie DB locked/permission-denied while running). **Posts NOT labeled by villa** — only p18 "eight bedrooms"→Thalassa, p12 "six master bedrooms"→Anemos are certain; rest are best-effort in villa carousels (flagged in data.ts, need user verification). Wired into gallery+villa carousels+feed.json. Cursor hover-tilt (TiltCard) removed from image cards per user; scroll parallax kept.
- **Real assets** downloaded from the live site into `public/media/real/`: hero video `intro.webm` (+`intro_mobile.webm`, auto-served ≤768px), `thalassa_exp.webp`, `anemos.webp`, thumbs, logos, og. Instagram **cannot be scraped** (login wall) — feed uses Graph API (`INSTAGRAM_ACCESS_TOKEN` env) with static fallback `public/instagram/feed.json`; placeholders labeled "Replace with @mykonosprestigevillas". Full guide in `INSTAGRAM.md`.
- **Video title removed + more pages (2026-07-04)**: baked "VILLA…" title crops on mobile portrait (landscape video) + mobile encode was low-q. SOLUTION: trimmed the title intro out (ffmpeg `-ss 6.5`, re-encode 1080p VP9 → title-free `thalassa.webm` 11.8s / `anemos.webm` 14.8s / `intro.webm`), dropped the mobile-encode swap entirely (one 1080p source everywhere = consistent quality on narrow windows), VillaHero shows name always (no time-gate). Reload flash fixed: posters are now video frames (`{id}_poster.webp`, `intro_poster.webp`) so poster matches video. Buttons now rectangular (`rounded-sm`, was `rounded-full`). Gallery+Experiences moved to dedicated `/gallery` (FullGallery, all 49 photos) + `/experiences` (Experiences+Services) pages via `ui/PageHero`; removed from home; home ends with `ClosingCTA` ("sunset is waiting"). Footer CTA band removed (was duplicating villa/about page CTAs). Gallery tags made generic/safe (were mislabeled e.g. "Infinity pool" on interior); masonry `grid-flow-dense` fixes the blank cell.
- **Multi-page (2026-07-04)**: shared chrome (Navbar/Footer/FloatingCTA) moved to `app/layout.tsx`; routes `/` (home), `/villas/[slug]` (SSG thalassa+anemos, async params), `/about`. Villa/about content + per-villa `heroImage`/`tagline`/`longStory`/`photos`/`spaces` and `about` object all in `lib/data.ts`. Cinematic page heroes: `components/villa/VillaHero`, `components/about/AboutHero`, `components/villa/VillaGallery`.
- **Palette softened (2026-07-04)** per user "soft blue mix of white+blue": azure #2478B5→**#5289BE** (deep #38699C for legible small text/eyebrows), sky lightened, buttons now `bg-gradient from-azure to-azure-deep`. Nav made smooth: constant h-20 (no resize), no border line, crossfaded white↔ink logos, soft shadow only. Reveal animations: `ui/Reveal` (blur-in), `ui/ImageReveal` (clip-wipe+zoom), `ui/RevealText` (word stagger).
- **Sections**: Hero (video + golden particles + "Your Private Sunset") → WelcomeStory → Villas (carousels/tilt) → Experiences → Gallery (masonry+lightbox) → InstagramFeed (Moments/Journal) → Location (parallax+map) → Testimonials → Footer. UI primitives in `components/ui/` (Reveal, MagneticButton, TiltCard, Lightbox).
- **Hero white-fade is a BAKED DITHERED PNG, not CSS (2026-07-05)**: user kept seeing "layer/separation lines" in the home hero's bottom whitewash — that's gradient banding (piecewise-linear CSS stops = Mach-band lines + 8-bit quantization). Fix: the whole home-hero veil (navy-top darkening + white-bottom whitewash) is now a single pre-rendered RGBA image `public/media/hero-veil.png` with a **C2 smootherstep** alpha curve + **TPDF dither** baked in, generated by `gen-hero-veil.mjs` (uses `sharp`; 256×1400, tiled `background-size:256px 100%` repeat-x via `.hero-veil-img` so dither stays crisp). Whitewash tuned 2026-07-05: knobs are `WHITE_START` (where white begins, 0.66 — raise it to push the fade lower), `WHITE_EASE` (**convex ease-in** `u^EASE`, 2.8 — raise it for a dimmer body + seals full-white only at the very bottom seam). IMPORTANT shape lesson: an S-curve (smootherstep or smootherstep^gamma) keeps an inflection/**shoulder** near the top of the fade that reads as a faint bright "line" (Mach band); a pure ease-in has no interior shoulder (steepest point sits at the hidden white-on-white seam) so no line. `DITHER`=3.0 LSB. Current cache-bust `?v=5`.
- **Custom Cycladic-blue cursor (2026-07-05)** in globals.css: colored **arrow** (default) + colored **hand** (interactive selectors: a/button/summary/[role=button]/.cursor-pointer/submit-inputs) as inline-SVG data-URI cursors so the OS still swaps them contextually (stays reactive), only recoloured; fill `#A2C4D4` + `#33566B` outline for visibility on both light sections and dark hero; text caret preserved on real text inputs. User can request an exact hex. **To change the fade, edit gen-hero-veil.mjs, re-run `node gen-hero-veil.mjs`, AND bump the `?v=N` query on the `.hero-veil-img` url in globals.css** (else the browser serves the stale cached PNG). There is no CSS gradient to edit anymore (removed `bg-hero-veil` from tailwind.config + `.hero-grain`; also removed `vignette` from the home Hero — its edge-darkening fought the whitewash). Verified band-free objectively (canvas: max 1 LSB row-step, 100% rows dithered, 0 hard-band runs) since screenshots time out per [[preview-screenshot-timeout]]. Also `ui/PageHero` (gallery/experiences) bumped 72vh→`h-[100svh]` full-screen to match other heroes.
- **Bilingual EN/EL i18n (2026-07-05, COMPLETE)**: language toggle, English default. `lib/i18n.ts` holds `dict{en,el}` + `navFor(locale)`; `components/LanguageProvider.tsx` (context `useLang()` → `{locale,setLocale,t}`); `components/LanguageToggle.tsx` (EN/ΕΛ pill, `scrolled` prop). **COOKIE-based SSR locale (fixes "page shows English" flash):** `setLocale` writes BOTH localStorage AND a `mpv-locale` cookie; `app/layout.tsx` is `async`, reads the cookie via `next/headers` `cookies()`, sets `<html lang>` + passes `initialLocale` to the provider — so the SERVER renders the chosen language from first paint (no English flash; also correct SSR lang for fonts/SEO). Provider still sets `document.documentElement.lang` client-side + has a localStorage→cookie migration effect. NOTE: reading cookies() makes the app dynamic (no static gen) — fine here. Greek copy is native, iterated with the user (e.g. "χτισμένα/built γύρω από τον ορίζοντα" not "γραμμένα/written"; "απόλυτα ιδιωτική" not "αβίαστα"). Experiences hero: no subtitle, image = Anemos pool `an_02`. WelcomeStory 180° card now shows on mobile (was `hidden md:block`). **Greek is ORIGINAL professional copy, NOT a translation; no em dashes anywhere (user rule).** The `dict{en,el}` now holds ALL content (villas, experiences, services, testimonials, location, gallery tags, closing, about pending). **WHOLE HOMEPAGE WIRED + verified Greek (2026-07-05):** Navbar(+toggle), Hero, WelcomeStory, Villas showcase (villa content via `t.villas[id]`), InstagramFeed, Location, Testimonials, ClosingCTA, Footer. Experiences component also wired. **GREEK FONTS** (`app/layout.tsx`): `GFS_Didot` (--font-greek-display, Greek didone matching Hatton; ONLY greek-capable didone in next/font — Cormorant/Playfair have NO greek subset) + `Inter` (--font-greek-body). **GOTCHA (fixed 2026-07-05):** appending them after Hatton/Averta in the font stack did NOT work — next/font's auto metric-fallback for Hatton/Averta ALSO covers Greek, so Greek glyphs got intercepted by a default system serif ("greek title looks default"). FIX = unlayered CSS in globals.css: `html[lang="el"] .font-display,h1..h4 { font-family: var(--font-greek-display),Georgia,serif }` and `html[lang="el"] body,.font-sans { var(--font-greek-body)... }` — routes Greek pages straight to the Greek fonts (no Hatton in the stack → no interception). Verified via canvas measureText that Greek h1 renders GFS Didot, English still Hatton. **MOBILE FIT:** Greek words run long, so Hero headline uses locale-aware size steps (smaller in `el`) — verified 0 overflow on mobile+desktop both langs. **WHOLE SITE now Greek (2026-07-05):** inner pages done — `PageHero` takes a `pageKey` ("experiences"/"gallery") → `t.pages`; `Services`/`FullGallery`/`VillaHero` wired; the SERVER pages `/villas/[slug]` and `/about` were split into thin server wrappers (keep generateStaticParams/metadata) + client bodies `components/villa/VillaPageBody.tsx` and `components/about/AboutBody.tsx` that use `useLang`. Dict gained `pages`, `villaPage` (+ `includedServices`), `aboutPage` (full about content), and per-villa `tagline`/`longStory`/`spaces`. Verified every page renders Greek + GFS Didot titles + English/Hatton on toggle, mobile 0-overflow, no errors. Minor left-as-is: `site.address` + "260 m²" stay Latin (physical address/measurement). Villa Anemos gallery reverted to natural order so POOL shots (an_01-03) lead (user changed mind from an_09-first). Default EN.
- **Villa Anemos gallery now leads with an_09** (suite-with-a-view) via a reordered `photos` array in data.ts (was an_01); represents the villa better.
- **Preview**: launch "mykonos-prestige" port **3030** (added to central `C:\Users\mikef\.claude\launch.json`). Verify per [[preview-screenshot-timeout]]; mobile menu rendered outside blurred header per [[css-fixed-backdrop-filter-trap]]. `npm run build` passes clean (types valid).
