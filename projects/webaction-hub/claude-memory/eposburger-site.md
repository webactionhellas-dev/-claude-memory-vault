---
name: eposburger-site
description: "Epos Burger smash-burger site build in C:\\Users\\mikef\\eposburger — pink brand theme, flying burger cutouts"
metadata: 
  node_type: memory
  type: project
  originSessionId: 10ef7e46-bf58-49d3-8ee7-35c6429d0589
---

Professional site for **Epos Burger** (eposburger.com, a Greek smash-burger brand) in `C:\Users\mikef\eposburger\index.html` — single self-contained HTML (vanilla JS, **Fredoka** display + Hanken Grotesk body; Fredoka chosen to echo the rounded/blobby logo).

**Brand (authoritative, from the real logo):** the official logo is "est. epos burgers 2024", a soft organic blob wordmark in **slate-gray `#595961` on pink `#f5c1cd`** — pulled from the Wolt asset `imageproxy.wolt.com/assets/680b88c5e107bc344ee0650c` (the 586×586 square). Extracted to transparent `assets/logo.png|webp`; used in nav (replaces text wordmark) + footer (white via `brightness(0) invert(1)`). Site palette: page `--pink #f3ccd6`, band `#efbecb`, cards `#f9dee4`, **slate text** `--ink #4e4e57` / body `#3e3e46` (NOT black — user said black text "doesn't match"; brand uses slate-gray). No gold accent. Minimal text, no em/en dashes (user dislikes dashes).

**Burgers:** two menu items = House Cheeseburger (spicy mayo + pickle relish) and Classic Cheeseburger (ketchup/mustard/pickles/onions), both €9.50. **Best source = the user's own clean transparent cutouts** `Downloads\49693db5-2f67-4130-98bf-d603278572fe.png` (house) and `Downloads\Ανώνυμο σχέδιο (2).png` (classic) — no pink. `prep2.py` enhances (sat/contrast/UnsharpMask on RGB, preserving alpha) → `assets/burger-*.webp`. (Earlier `prep.py` cut the pink-bg photos `house_special-gy7jtS_e.jpg`/`cheeseburger-a2RMZFhl.jpg` but left a pink shadow the user rejected — prefer the clean cutouts.) Burgers "fly" via rAF float + scroll parallax; menu cards keep the burger in a fixed `.pic` zone with text in `.meta` strictly below (no overlap); hero shadow is soft neutral slate, not maroon. The **Story section uses a deconstructed/exploded burger** (`Downloads\83432850-4fd3-4d9b-b835-0d62dcdd41ac.png`, already transparent → `assets/burger-exploded.webp`); give floating `.fly` imgs a DEFINITE width (not `width:auto`) or native lazy-load deadlocks at 0×0 — and this story img is eager (lazy never fired here). Hero has no eyebrow/tagline (user removed "Smashed to order" + "Glyfada · Athens Riviera"). **Logo recolored to dark slate `#2e2e35`** for contrast (the soft `#595961` was too faint on pink).

**Locations — THREE shops** (Visit section, 3-col grid): (1) Glyfada flagship = Giannitsopoulou 3, Glyfada 166 74, +30 210 968 1181, Mon-Thu 17:30-00:00 / Fri-Sat 12:30-01:30 / Sun 12:30-00:00; (2) **Avana** (user-confirmed, Mon-Thu 12:00-00:30 / Fri-Sat 12:00-01:30 — address still unknown, ask); (3) Antíparos = Antiparos Square (Chóra), seasonal, where it began. Instagram likely @eposburger / @eposburgers; Bolt Food link still placeholder.

**Melting-sauce dividers:** at each section colour-change boundary the upper section's colour "drips" into the next. One reusable SVG path `#dripEdge` (generated organic drip silhouette, viewBox 0 0 1200 50, `preserveAspectRatio=none`) defined once in a hidden `<defs>`; each `.drip` is the FIRST child of the LOWER section (absolute top:0, h40, fill=currentColor) coloured with the UPPER section's colour via inline `style="color:var(--pink|--pink-2)"`. Placed at menu→story (--pink), story→extras (--pink-2), cta→footer (--pink, pink-into-dark-slate = the dramatic one). Putting the drip in the lower section avoids sibling stacking issues. Inspired by user's glossy pink-drip reference (kept flat-fill to match brand).

**Facts:** Glyfada flagship, Giannitsopoulou 3, +30 210 968 1181; origin Antíparos (summer). Order on Wolt (real link), Foody, Bolt. Preview server name `epos` (http-server on :5577) in [[web-action-site-build]]-style launch.json.

**Active iteration file = `C:\Users\mikef\Downloads\Epos-Burger.html`** (a self-contained copy the user keeps sending; edit THIS one, base64-embedded assets). Patch it with Python scripts (large base64 lines break the Edit tool). Recent work on this copy:
- **Hero burger** = clean transparent PNG (`Downloads\Untitled design (1).png` trimmed) — do NOT use lossy WebP (halo/box artifact the user rejected) and no CSS mask (also made a box). Simple `heroFade` opacity-in + existing rAF float ("move together").
- **Dividers**: menu→story keeps the glossy photo-drip (`Downloads\1828329d-...png`, recolored to upper section's pink). story→extras is a **scroll-melt SVG** (`.slime-melt` solid `<path>`, flat at rest, drips elongate via scroll progress with rAF lerp + IntersectionObserver). Footer = straight `.line-sep`. **Seam-fade fix:** each lower section's bg is a `linear-gradient` that STARTS at the upper section's exact colour then fades to its own (kills the visible colour line behind the drips). Solid SVG fill (not transparent tile) = no background bleed.
- **Story decomposed burger**: the user reworked it into a 6-layer `.blayer2` / `.bstage` stack (`data-i` 0=top bun,1=onions+ketchup+mustard,2=pickles,3=upper cheese+patty,4=lower cheese+patty,5=bottom bun) on a common 700×1358 canvas with its own scroll JS. To tweak one ingredient, edit just that layer's image content inside the SAME canvas (keeps alignment) — e.g. pickles narrowed by scaling layer-2 content around its centre. Hero burger brightened by baking ImageEnhance Color/Contrast/Brightness+UnsharpMask into the PNG to match the menu `.bimg` vividness.
- **Hero centre-top logo**: `.hero-logo` (absolute, `top:clamp(82px,9vh,112px)`, centred) using the dark-slate brand wordmark, with a white radial `::before` glow + drop-shadow so it doesn't blend into the pink.
- **⚠️ PATCHING PITFALL (caused a full-site outage):** removing CSS rules by regex like `\.slime-gloss[^{]*\{[^}]*\}` ALSO matched inside the shared `<script>` (`host.querySelector(".slime-gloss")`), eating JS up to the next `}` → unterminated-string SYNTAX ERROR → the whole shared IIFE dies → nav + ALL 21 `.reveal` elements stay `opacity:0` → site looks "everything removed/blank" (the preview console capture shows NOTHING, so a blank site = suspect a JS syntax error, check `.reveal` `.in` count via preview_eval). When deleting a CSS rule whose class name also appears in JS, anchor the regex to the stylesheet only or match the full literal rule — never a loose `[^{]*\{`.

**⚠️ 2026-07-21 INCIDENT + RESTORE:** a collaborator (`webactionhellas-dev`) broke the LIVE site with a burst of bad deploys — the site went 404. Diagnosis via `vercel ls epos-burger --scope webactionhellascom` + `vercel inspect`: today's deploys were all broken (most recent = `api/index` 9.77MB lambda instead of static; next = index.html replaced with the literal string `PLACEHOLDER_FOR_LARGE_HTML_FILE`; next = HTML present but ALL `assets/` 404). The last fully-good deploy was ~19h earlier (`n6qz7cqin`, index 103660b + all 29 assets load). Restored production with `vercel promote https://epos-burger-n6qz7cqin-webactionhellascom.vercel.app --scope webactionhellascom --yes`. Deployment URLs are SSO-protected (curl gets 302→sso), but the production alias epos-burger.vercel.app is public — so verify/pick versions by promoting a candidate then curling the alias.

**CANONICAL FOLDER IS NOW `C:\Users\mikef\eposburger-restored`** (pulled from the restored live deploy: index.html 103KB + 29 assets incl. `layer-0..5.webp` exploded-burger stack, all fonts). It is `.vercel`-linked to project **epos-burger** (team webactionhellascom, prj_h3TeTsT6U1d9zQR10yi8OlDkYpI7). After fixes, redeploy with `vercel --prod --yes --cwd C:\Users\mikef\eposburger-restored`. The old `C:\Users\mikef\eposburger` (Jun 17) is stale for content but still holds the Python build scripts (prep.py/prep2.py/cutout/slice_layers/patch_*).

Verify via preview_eval/inspect geometry — [[preview-screenshot-timeout]] holds (screenshots worked briefly then reverted to timing out, so don't rely on them). Preview scroll position can't be held in the harness tab; validate scroll-driven anims by setting `tl.progress()` / reading params directly, not by scrolling.
