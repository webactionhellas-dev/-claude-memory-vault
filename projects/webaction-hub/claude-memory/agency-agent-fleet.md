---
name: agency-agent-fleet
description: "Mike's web-agency agent fleet: the 4 existing agents plus new Corky fixer, security gate, house-playbook shared brain, and the gated after-build learning loop"
metadata: 
  node_type: memory
  type: project
  originSessionId: fa8ab9a1-e1a8-4894-b1bc-0bef3636e1a8
---

Mike's agency runs a lean fleet of focused Claude Code subagents (narrow scope keeps the main loop fast). Existing and kept as-is: **website-builder** (design + build, checkpointed), **backend-integrator** (Supabase/Stripe/Shopify/Woo/Resend), **deploy-launch** (Vercel, gated), **site-qa** (independent read-only PASS/FAIL gate).

Added 2026-07-09 under the decision "add the gaps, keep the rest":
- **Corky** = new debug-and-fix agent at `C:\Users\mikef\.claude\agents\corky.md`, the write-access counterpart to read-only site-qa: runs the quality + security gates, fixes failures in place, loops to green, flags judgment-call items. Honest scope: passes all automated checks, never promises "zero bugs".
- **Security gate** = `C:\Users\mikef\.claude\skills\website-builder\references\security-gate.md` (D1 secrets, D2 Supabase RLS, D3 payments, D4 inputs, D5 headers, D6 deps). Mike's top priority (no site hackable, no data leaked). backend-integrator + Corky run it; deploy-launch confirms headers live.
- **house-playbook skill** = `C:\Users\mikef\.claude\skills\house-playbook\SKILL.md`, the shared brain all agents read first (invariants, fleet map, hard-traps index, gate pointers, learning loop). Indexes the deep website-builder references, does not duplicate them.
- **Learning loop** (chosen: auto-harvest after each build, gated on approval): at build end the pipeline proposes genuinely new durable lessons for Mike to approve before they are written into a reference or memory. Wired into website-builder's "Finish clean" step. Runs inside Claude Code.

Added 2026-07-10 (Mike's persona specs, integrated permanently):
- **Twitch** = new master scrape agent at `C:\Users\mikef\.claude\agents\twitch.md`: the data-gathering phase of every build. Static-first then Playwright (reuses the install at `website-builder\scripts\visual\node_modules`), full-DOM + media harvest with provenance, structured data (JSON-LD, reviews, contact, products/prices), design forensics (real palette/fonts/layout), incremental+resumable via `data/manifest.json`, screenshots via `visual_check.mjs`. Output package `/raw /assets /code /data /screenshots` + `data/nami.json` that maps 1:1 to Nami's brief section 0 (REAL-DATA MANIFEST); products feed Echo. Trigger: "Twitch, scrape [URL]". Respect robots/rate limits; blocks are reported, override only with Mike's authorization.
- **Echo** = the persona now formally attached to **backend-integrator** (`C:\Users\mikef\.claude\agents\backend-integrator.md`), upgraded to a staff/principal-engineer spec: architecture-first workflow (understand, propose schema/auth/payment/security model + failure modes, then complete production TS: migrations, RLS, edge functions, idempotent webhook handlers, logging, tests on money paths, then audit via get_advisors + security_audit.py + security gate). Expanded mastery: Stripe Billing/Customer Portal/Connect/Tax/Radar, realtime, clean/event-driven architecture, GDPR/CCPA basics, indexing/pooling/caching/cost. Fleet integration section: Twitch seeds data, Nami's brief defines flows, deploy-launch gets env vars + webhook endpoints.

Intake: the `brief` skill (C:\Users\mikef\.claude\skills\brief\SKILL.md, run as /brief) is the prompt translator: it resolves loose or voice-garbled requests (e.g. "mikon's persist villas" to Mykonos Prestige) against the memory index + folders, classifies the task, drafts a short structured brief, confirms, then routes to the right agent (now including a Twitch route for "scrape X" / data-not-yet-gathered builds). Added because voice-to-text mangling target names was the main prompting friction.

Corky upgraded 2026-07-10 to the full CORKY persona (Mike's spec): full-stack debug + proactive performance-audit agent. Added Core Web Vitals audit (LCP/INP/CLS/TTFB, bundle+code-splitting, animation 60fps, DB/query/realtime, mobile vs desktop), the reproduce-gather-rootcause-fix-prevent process, severity-tagged output with before/after perf, and a team-integration section. Kept the honest-scope discipline (guarantees the automated tier, never claims zero bugs, never invents). The spec's backend agent name "UserMaster" was reconciled to Echo/backend-integrator (flagged in the file).

**Fleet tooling shipped 2026-07-10 (the 10/10 push):** three real, verified tools now back the agents instead of prose:
- `design_audit.mjs` (in website-builder/scripts/visual/, reuses the Playwright+axe install): mechanizes NAMI's checkable design-gate slice — WCAG AA contrast, brand-font-actually-rendered incl. the Greek H1 (measureText/GFS-Didot trap), zero em-dashes in rendered text, tap-target size, reduced-motion-not-hiding-content, mobile overflow. PASS/WARN/FAIL + exit code. Taste gates stay human. Verified against live drip staging (surfaced 8 real footer contrast fails). Used by Nami, Corky, site-qa.
- `scrape.mjs` (same folder): Twitch's house scrape engine — robots recon, same-origin crawl to depth, static+rendered extraction (JSON-LD/OG/meta, copy, images w/ natural dims), media harvest with provenance + heuristic bucket sorting, business facts, design forensics, screenshots, resumable manifest, and the `data/nami.json` handoff. Conservative logo picker: matches the business name from the FILENAME not the host (fixed a bug where the domain containing the brand made every asset a false match) and flags a gap rather than handing over a competitor's logo. Self-tested against drip staging.
- Commerce money-path tests + `TESTING.md` (see [[house-starters]]).

All three tools were HARDENED against an adversarial code review (4 opus verifiers) before sign-off: scrape.mjs fixed real blockers (robots.txt per-group parsing not sticky; same-SITE compares host with www stripped so a www redirect no longer drops every link; entry-disallowed now fails loud not silent-empty; image download rejects non-image content-types so hotlink HTML pages are not written as photos; favicon read from <link rel=icon>; single-nav-logo fallback for Greek brands whose logo filename is Latin). design_audit.mjs fixed its flagship reduced-motion check (now computes EFFECTIVE opacity up the ancestor chain so wrapper opacity:0 and GSAP autoAlpha visibility:hidden are caught; excludes intentional overlays by position:fixed + pointer-events:none, not just aria/role), made the font check reliable (document.fonts.check for load + monospace probe for Latin fallback; Greek coverage is a WARN not a false FAIL), surfaced axe incomplete (text over images) as WARN, and excluded scroll-container children from the overflow check. Corky spec + tool wiring passed clean (only cosmetic nitpicks).

Added 2026-07-10 (second wave, Mike's LYRA + BOSS MASTER specs):
- **Lyra** = content writer agent at `C:\Users\mikef\.claude\agents\lyra.md`: native-level EL+EN copywriter (website copy, product descriptions, about, blog, emails, social, SEO text), human voice with the AI tells banned, zero em-dashes, zero invented facts (Gate J), bilingual side by side (Greek written natively, never translationese), 2-3 variations on high-stakes lines. Sources facts from Twitch's nami.json, voice from Nami's approved tone coordinates, delivers keyed to the builder's lib/data.ts shape. Wired into house-playbook, brief routing, and the nami/twitch/corky/website-builder handoffs (builder now requests copy from Lyra instead of writing long-form itself).
- **Boss Master** = orchestrator implemented as the `boss` SKILL (`C:\Users\mikef\.claude\skills\boss\SKILL.md`, run /boss), deliberately NOT a subagent: it runs in the main session because it owns the checkpoints with Mike (a subagent cannot pause for approval). Activates one real agent at a time with MODE lines ("TWITCH MODE: ..."), default full-build sequence Twitch -> Nami (Checkpoint 1) -> Lyra -> Echo (if backend) -> build -> Corky -> site-qa (Checkpoint 2); Mike's spec order had Lyra before Nami, reconciled because Lyra writes to the approved tone coordinates. 2-4 sentence progress summaries, "USERMASTER MODE" maps to ECHO MODE, ends with a Team Evolution Note feeding the gated learning loop.

Added 2026-07-11 (Nami continuous-improvement, Mike's "develop her skills, better and better by time"): Nami's craft now compounds two ways. (1) Build-driven, CAPTURE-ONLY so it never slows a command — during a build she jots techniques into `design-standards\practice-queue.md`, no studying mid-command. (2) A weekly OFF-CLOCK scheduled routine `nami-design-study` (scheduled task, Mon 06:06, `C:\Users\mikef\.claude\scheduled-tasks\nami-design-study\SKILL.md`) drains the queue, studies award sites + new GSAP/shader/type techniques, and drafts GATED proposals to `design-standards\proposed-updates.md` for Mike's approval before anything enters the references. Also this session: the electro-stripe effect was REJECTED (baked black bg = fake transparency + low-res, exposed once Mike's gamma lift let him see blacks) and a house asset standard set — genuinely transparent (real alpha, no blend-mode fakery) + high-resolution/crisp. See [[crisp-transparent-assets]], [[electro-stripe-effect]], [[nami-design-standards]].

Naming context: Mike proposed Nami (designer), Echo (backend), Corky (debug), Twitch (scraper), Lyra (copy), Boss Master (coordinator). All are now real: Nami with the `design-standards` skill + NAMI constitution (see [[nami-design-standards]]); Echo + Twitch landed 2026-07-10; Corky upgraded 2026-07-10; Lyra + boss skill 2026-07-10. **Hermes** (separate Gemini agent, see [[hermes-agent-setup]]) cannot reach or "train" this fleet; the smartening loop is the native memory + skills, human-gated.
