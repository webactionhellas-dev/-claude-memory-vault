---
name: cloudskin-site
description: "CloudSkin activewear is a LIVE headless-Shopify + Supabase storefront on Vercel (cloudskin.com); architecture reference — for the CURRENT canonical source folder and deploy command see [[cloudskin-canonical-folder-check]] and [[cloudskin-deploy-setup]]"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7f2ab0f1-ba07-4d47-b681-3ddf35cce1d8
---

CloudSkin (cloudskin.com) is a **live, headless storefront on Vercel**. The old "Downloads\cloudskin-site\cloudskin Alo-clone" (detailed 2026-07-05/06 rebuild log, now removed) is a DEPRECATED ANCESTOR, do not edit it or treat it as the live site.

**Architecture (verified 2026-07-15):**
- Static HTML/JS (no framework, descended from the "Alo-light" design: pcard/prail/shell.js engine). Deployed on Vercel project **`cloudskin`** (team **`webactionhellascom`**, owns cloudskin.com + www). Vercel CLI is authed here as `webactionhellas-dev`.
- **STALE-FACT CORRECTION (2026-08-02): the source IS on this machine now** — it lives in a rotating `cloudskin-v*` folder (v52 -> v54 -> v55 -> v56 -> v67 -> ...), canonical as of 2026-08-01 = `C:\Users\mikef\cloudskin-v67`. The old "source is NOT on this machine, mirror the live site to edit it" instruction below is **DEPRECATED and dangerous** — mirroring live over the canonical folder is exactly what wiped hours of work on 2026-07-31. Always resolve the canonical folder dynamically first: [[cloudskin-canonical-folder-check]].
- Commerce = **headless Shopify** `rta3sf-47.myshopify.com` via `js/shopify.js` (public Storefront token `54674b6c...`, resolves variants by product handle, `cartCreate` -> hosted checkout; fulfillment Dubai/UAE). **SUPERSEDED since 2026-07-26:** checkout is now CloudSkin's own custom Stripe checkout (customers pay in their own currency), store base currency is **AED not EUR**, and a real **DHL** app is wired for shipping. See [[cloudskin-stripe-golive]] for the money path and [[cloudskin-dhl-noitems-fix]] for the open DHL line-item issue.
- Accounts/CMS = **Supabase** project `ocszztflphqsaoyhlerx` (`js/auth.js`+`supabase-config.js`, anon key public, confirm-email ON). Owner-notify on signup via Resend edge fn `cloudskin-notify` -> info@cloudskin.com. RLS is solid (profiles/orders locked to owner or is_admin, newsletter insert-only/unreadable, studio_secret bcrypt deny-all). No secrets exposed in frontend. Content injected via `textContent` (no XSS).
- **9 pages**: index.html (="Launching Soon" splash + client-side "Store access" password gate that sets `sessionStorage.cs_gate_ok=1` then redirects to `/home`; identical copy also served at /coming-soon), home, collection, product, login, about, **/admin** (Supabase owner dashboard, gated to hello@cloudskin.com which does NOT exist yet so no admin can log in), **/studio** (Content Studio, password via `studio_auth`/`studio_save` RPCs -> writes cloudskin_content). `vercel.json`=`cleanUrls:true`; assets cache-busted `?v=NN`.

**HOW TO EDIT/DEPLOY (current, 2026-08-02):** work in the canonical `cloudskin-v*` folder resolved per [[cloudskin-canonical-folder-check]], then `node <canonical-folder>/scripts/deploy.mjs` ([[cloudskin-deploy-setup]] — it bakes the content snapshot first, then deploys). Deploy is GATED: Mike must explicitly name the production deploy. Clean up _dbg/junk before deploy.
**(HISTORICAL, do not follow)** The original method was to mirror the live public files from cloudskin.com (grabbing every `<script>`/`<link>`/img ref, plus the orphan pages /admin /studio /coming-soon that are linked from nowhere) and deploy that mirror. Kept only because the orphan-page list is still useful; treating live as the source of truth is now an explicit anti-pattern.

**Shipped 2026-07-15:** Quick-Add label on product cards (i18n key `card.quickAdd`, 11 locales), removed the man/woman/"Men (Coming Soon)" strip on login.html, added €1 test product `zz-test-payment-check-delete-after` (for Larissa's PayPal test, gated so public can't see; REMOVE after test), replaced the coming-soon center + top-left clouds AND the favicon with the correct brand cloud (traced from img/brand/cloudskin-logo-white.png via skimage contour -> smooth Catmull-Rom bezier outline, fitted to each old cloud's exact bbox so same size; white stroke, not filled).

**EMAIL WORK - DONE 2026-07-16 (in the Shopify admin, his logged-in Chrome):**
- **Order confirmation** = editable Liquid at Settings->Notifications->Customer notifications->Order confirmation. RE-SKINNED to the approved CloudSkin poster by injecting a `<style id="cloudskin-brand">` block (+ a `<link>` to Fraunces) right after the template's existing `</style>`, PLUS one markup edit adding `<sup>&reg;</sup>` to the header wordmark. This is a RE-SKIN (kept all of Shopify's order-summary Liquid intact - bundles/discounts/split-fulfillment all still work), NOT a rewrite. Result: cream `#f7f4ef` bg, `CLOUDSKIN&reg;` letterspaced Inter wordmark, Fraunces serif headline, single ink `#171512` button (white label; hid the purple Shop-app button), muted line-item variants, hairline-bordered thumbnails. SAVED + LIVE (0 real Shopify orders currently, so low risk). **Backup of the original 160KB template: `Downloads\cloudskin-order-confirmation-BACKUP-2026-07-16.txt`** (also in this session's scratchpad); Shopify's "revert to default" is the other undo. To reproduce elsewhere, re-inject the same style block.
- **Global email branding**: Settings->Notifications->Customize email templates -> Accent color set to ink (`#17130E`; site ink is really `#171512`) -> turns the default blue buttons/links ink across ALL notifications. Logo left as the text wordmark (site logo IS a text wordmark, see [[unicorn-tattoo-site]]-style). Store name kept "CLOUDSKIN" (that field strips the `&reg;`).
- **Abandoned cart** is NOT a Liquid notification on this store - modern Shopify moved it to **Marketing->Automations->"Recover abandoned checkout"** (a Shopify Email drag-drop automation, app `shopify-messaging`). Its editor only allows web-safe fonts + block layout + colors (Background/Text/Button hex, logo, one font) - it STRUCTURALLY CANNOT be the exact poster (no Fraunces, no custom HTML). Editing it reverts the email to DRAFT (pauses sending until re-activated). I branded it (cream bg `#f7f4ef`, ink button `#171512`, font Georgia) but Mike saw it and wanted the exact poster -> **decided: do the abandoned-cart via Klaviyo** (Shopify app: pulls cart data + full custom HTML; needs Mike to create the free account - I cannot create accounts / grant OAuth). The native "Complete your order" email is currently LEFT IN DRAFT (paused) pending the Klaviyo build. Traffic ~0.
- **Email map** (tell Mike, he keeps forgetting): orders + abandoned-cart = SHOPIFY. **Sign-up confirm + password reset = SUPABASE Auth** (the branded `confirm.html`/`recovery.html` from last session still need the Resend + Namecheap-DNS step to send from cloudskin.com - Mike REJECTED that as too complex, so they are DEFERRED/plain). `cloudskin-notify` = owner alerts only.
NO em-dashes in any email copy (house rule).

**Also 2026-07-16:**
- **Checkout is Shopify-HOSTED** (`js/shopify.js`: "Shopify owns cart/checkout/payments/orders"), so real customer orders live in SHOPIFY. The Supabase `orders`/`order_items` rows are STALE Stripe TEST data (`cs_test_...`) from an ABANDONED headless-Stripe experiment - NOT the production path. Do NOT re-alarm that "checkout uses Stripe"; Mike confirmed no real Stripe is linked (PayPal on Shopify; Stripe to be added as a Shopify pay provider). Shopify Orders list was empty (0 orders) - store still password-gated.
- **cloudskin.com IS the Vercel site** (not Shopify): I fetched the Vercel deployment directly and it serves the SAME "Launching Soon / Enter Password" gate = the site's own `index.html` gate. Site is NOT publicly launched. Latest prod deploy last session: `cloudskin-4r4fo2nju-webactionhellascom.vercel.app` (Vercel project prj_6d0gmXALxXTKZFj7AAVoBZfns12W, team_fAnVpAdOPwPZAR4kW0o2BwID). The working mirror is `scratchpad/cloudskin-live` in session `7f2ab0f1` - confirm it matches the latest deploy before re-deploying.
- **login.html**: removed the dead "United Arab Emirates (AED)" country button (replaced with an empty `.lg-country` spacer to keep the logo centered) - edited in the mirror, NOT yet deployed (gated).
- **Profile / order-history page = being BUILT this session by Echo (backend-integrator):** the signed-in account view (`#lgAccount` in login.html) only showed Welcome/email/Sign out - Larissa can't see orders/personal details because it was never built. Approach: Shopify order webhook -> Supabase (NEW table, since the old `orders` is Stripe-shaped junk) -> account page reads own orders via RLS. Needs a Shopify custom app (read_orders + webhook HMAC secret) - flagged as the dependency. NOT deployed (gated).

**2026-07-16 (session 2 — accounts + login + photos + orders):**
- Fresh COMPLETE mirror of the LIVE site at `scratchpad/cs-live` (session 0fd3206c), 86 files, verified 0 missing/broken assets across all pages. Canonical host = **www.cloudskin.com** (apex 308-redirects to www); assets `?v=35`; gate = `sessionStorage.cs_gate_ok='1'` (every page redirects to `/` if unset). Preview = launch config **"cloudskin-live" port 5241**. Downloads\cloudskin-site is STILL the deprecated ancestor.
- **Login fix (DONE + verified):** root cause of "password doesn't work" + unwanted email = Supabase **"Confirm email" was ON**. Turned OFF (Auth → Sign In/Providers → User Signups → Confirm email). Now signup auto-logs-in, no email, password works; verified full signup/signout/signin cycle.
- **Store uses Shopify NEW customer accounts** (passwordless; Shop/Google/Facebook only, NO email/password; account URL shopify.com/100215259435/account). So Shopify CANNOT power a custom email/password login → **customer auth stays on Supabase** (js/auth.js). Shopify = orders data only.
- **NEW `/account.html`** — separate professional account page on the site design system (shell.js header/footer, Archivo/Inter). login.html now redirects to it on sign-in (showAccount→location.replace); auth.js syncHeader points the account icon to account.html when signed in; Orders/Details/Wishlist as real tabs; persistent session; REAL-DATA-ONLY (no fake orders). Verified.
- **Product photos DECISION (Mike, FINAL): managed in /studio (Supabase Storage), NOT Shopify.** [In flight: Echo building bucket `product-media` + `studio-upload` edge fn + studio.js "Product photos" panel; content key `product.<handle>.images` = newline-sep public URLs.] I wire js/product.js to read that key, fallback to local bundled images. IMPORTANT: earlier I made product.js read LIVE SHOPIFY images — that must be REVERTED to /studio-images→local (Mike does not want Shopify for photos). Storefront product images are otherwise hardcoded-from-sync in js/products.js (img/shopify/, img/studio/); js/shopify.js supplies only price/variants/availability.
- **Orders pipeline (in flight):** DON'T use an Admin-API custom app (its install triggers 2FA Larissa can't approve). Use **Shopify admin Notifications → Webhooks** (no app, no 2FA; store webhook signing secret is shown on that page). [Echo building: `customer_orders` table + RLS (customer reads own rows where lower(email)=auth.jwt email) + `cloudskin-order-webhook` edge fn, HMAC-verified, service-role upsert.] Then add the webhook URL in admin + wire account.html Orders tab to read `customer_orders` + place a €1 test order to prove it.
- **Abandoned-cart email (DONE):** was stuck (message "Complete your order" in DRAFT under the active "Recover abandoned checkout" automation → didn't send). Rebranded headline "Still thinking it over?" in **Baskerville** (editor's most Fraunces-like serif; block editor can't do real Fraunces), then **Set to active** → now sends. Order confirmation already re-skinned + live. Sender display name = CLOUDSKIN; address still "via shopifyemail.com" (Gmail sender; @cloudskin.com needs the deferred DNS).
- **DEPLOYED to production 2026-07-16** via `vercel --prod` (project `cloudskin`, team `webactionhellascom`, alias www.cloudskin.com; deploy id dpl_DfgBz6YRxxEKtHS8JrVT2pZhbpSR). Live + verified: Coming Soon gate intact (`Cloudskin2026!`), login+`/account`, per-colour studio photos + homepage focal-point (studio.js v38), product gallery, order-creation webhook, emails. `/admin` page REMOVED. `vercel.json`=`{cleanUrls:true}` added to the mirror. The deploy folder is the session-0fd3206c `scratchpad/cs-live` mirror (temp).
- **Studio password = `CloudskinStudio1`** (studio_auth). `/studio` has NO site-gate, so it needs ONLY that password. Content Studio now manages: homepage sections + focal-point, product text, and PER-COLOUR product photos (key `product.<handle>.images.<colourSlug>`; uploads via `studio-upload` edge fn to `product-media` bucket).
- **STILL MANUAL (Mike's, not automatable):** (1) Order-FULFILMENT webhook — Shopify Settings->Notifications->Webhooks->Create->"Order fulfilment"->JSON->the cloudskin-order-webhook URL->Save (the admin popup is a sealed cross-origin iframe, so Claude CANNOT add it; only Order-CREATION is done). (2) place a €1 test order to prove the chain. (3) Larissa's visual PDF /studio guide + WhatsApp-to-Panos send — handed to a fresh session (browser-automation agents kept stalling on this machine).

**2026-07-20 — v51 (mobile cookie fix) is LIVE; verify-before-deploy lesson:**
- Current prod = **`dpl_6emqaCL3zR6HHZuEVMwK4w7LYM4V`** (alias www.cloudskin.com, `cloudskin-1w3qfegos`, created 2026-07-20 ~02:52 EEST). Immediately-prior / rollback candidate = **`dpl_HBM5FtD4cVG5S6Xy4fzo7xf9r97v`** (v50, `cloudskin-opnf32adi`, ~6h earlier). Rollback: `vercel rollback <id> --scope webactionhellascom`.
- **v51 = CSS + one comment only, NO JS touched:** `css/main.css` `@media(max-width:620px)` → `.cprefs__actions{flex-direction:column}` (stacks the two Cookie-Preferences buttons full-width so "Save preferences" stops clipping at 390/375/360/320), plus `.cconsent{padding-bottom:env(safe-area-inset-bottom)}` merged into the single `.cconsent{` rule (banner clears the iOS home indicator). Removed 1 em-dash from a `home.html` comment. `js/content-snapshot.js` deliberately NOT re-baked. All assets bumped uniformly to `?v=51` (81 refs across the 7 pages, 0 left at v50).
- **LESSON: before deploying CloudSkin, verify the LIVE prod deploy id + served `?v=` first — a task premise like "live is still v50" can be STALE.** When I picked this up, a correct, complete v51 had ALREADY landed on prod (an earlier/parallel session succeeded); every acceptance check passed against the live bytes (fixes present in served main.css, uniform v51, 0 em-dashes incl. all JS, gate ON + noindex, cleanUrls + 5 security headers, apex 308→www, 0/13 broken images). Correct call was to VERIFY and NOT push a redundant deploy onto gated prod, not to blindly re-ship. Confirm current prod via `vercel inspect <prod-url> --scope webactionhellascom` before ever redeploying.

**2026-07-20 — v53 (FX rates + EUR-charge note) is LIVE:**
- Current prod = **`dpl_DA4SVmx4N8RXxx5Qnzt1nMvaaKLW`** (alias www.cloudskin.com, `cloudskin-3di7nn4ei`, created 2026-07-20 ~14:56). Rollback candidate = v52 **`dpl_EsiAikHGsXZ2x5LamYa4cg1VUs4Z`** (`cloudskin-asodaw3a2`). Rollback: `vercel rollback <id> --scope webactionhellascom`.
- **v53 = 4 code files + 7 HTML version bumps only.** `js/i18n.js`: refreshed the `CUR` rate table (USD 1.08→1.14, AED 3.97→4.2, GBP 0.84→0.849, THB 39→38.4, SEK 11.3→11.05, RUB 98→88, SAR 4.05→4.28, KWD 0.33→0.354; EUR/AUD unchanged) + changed `detectCur()` final fallback `"USD"`→`"EUR"` (unknown-locale visitors now default to the currency actually charged). `js/i18n-strings.js`: new key `cur.note` added to all 11 language blocks (inserted right after `switch.currency`). `js/shell.js`: cart-drawer footer renders `<p class="drawer__ccynote">${C.t("cur.note")}</p>` between the checkout button and the existing `drawer__duties` note. `css/main.css`: added `.drawer__ccynote` (12px, `var(--stone)` #857d72, centered, `margin:8px 0 0`). `?v=52`→`?v=53` bumped uniformly (86 refs, 0 residual). `content-snapshot.js`/`vercel.json`/`index.html` gate BYTE-IDENTICAL. The `ann.2` "Duties & taxes included" line was deliberately left untouched (Mike moving to duty-inclusive DDP). Verified live: USD $96 on a €84 item, detectCur=EUR, cur.note renders EN+EL at desktop+390px, gate ON + noindex, 5 security headers, 0 console errors, White swatch=dsc06995, ADD TO BAG fits.
- **TRAP: the on-disk `C:\Users\mikef\cloudskin-v52-bundle-orig` is actually a STALE v51 mirror (refs `?v=51`), NOT v52** — do not use it as a diff baseline. Correct method (used for v53): pull the authoritative file tree from the Vercel deployment-files API `GET /v6/deployments/<dpl>/files?teamId=...` (the `uid` field = plain sha1 of the file bytes), sha1-verify the working bundle 86/86 against it, edit, then sha1-verify the NEW deploy's file tree the same way. Token at `AppData/Roaming/xdg.data/com.vercel.cli/auth.json`.

**Notes for handoff:** Namecheap DNS was NEVER changed (all save attempts failed on a stale session; account is Larissa's `Cloudenterprise26`, needs her 2FA - avoid it). A dormant Resend domain `cloudskin.com` was added (does nothing without DNS; can be deleted, harmless). Only backend change made: Supabase min password length 6->8 (harmless). Security audit was green (only Pro-gated leaked-password toggle outstanding, NOT critical). Everything else (Quick-Add label, login strip, €1 test product, brand clouds + favicon) is LIVE and verified. Relates to [[mike-operator-profile]], [[house-starters]].
