---
name: drip-astro-v2-site
description: DRIP sneaker storefront (Astro+Supabase+Stripe); canonical folder now C:\Users\mikef\drip-astro-test (the updated zip); owner product admin + analytics + auto new-arrivals built
metadata: 
  node_type: memory
  type: project
  originSessionId: 73f3e2d5-4889-4815-90fc-78bb9f30c346
---

DRIP — luxury sneakers/streetwear storefront (Greek, Athens), separate from [[drip-store-site]]. Astro 5 + Tailwind v4 + Supabase + Stripe, Node adapter (SSR-capable). Lives in `C:\Users\mikef\drip-astro-v2\drip-astro-v2`. Preview via launch.json server name **drip-astro** (port 5188). Tiffany/teal "DRIP" brand theme; 4K hero video at public/hero/hero-bg.mp4 (720p backup kept).

Supabase project `ocszztflphqsaoyhlerx`. As of 2026-06-23: keys live in .env (new `sb_publishable_`/`sb_secret_` format). Created a **products** table + `product-images` storage bucket and seeded all 311 products from src/data/products.json via `scripts/seed-products.mjs`. Owner admin account = **mikefalcos2004@gmail.com** (profiles.is_admin=true), created via `scripts/create-owner.mjs`. Customer accounts were **removed** — guest checkout only; `/login` is now owner-only → `/admin`.

Marquee brand logos are cleaned/sharpened/size-balanced by `scripts/clean-marquee-logos.mjs` (pristine originals in `_logo_backup/`; re-run is idempotent). Cache-busted with `?v=N` query in Hero.astro (currently v=5) — bump N when logos change. Marquee has an edge fade-mask. **Stone Island & Stüssy render as bold tiffany text wordmarks** (`wordmarks` map + `.brand-word` in Hero.astro), not images — their detailed compass/script don't survive downscaling; user fought these for several rounds. Brand cover `cover-yeezy.webp` was recolored to a glowing-tiffany "YEEZY" wordmark to match the other dark covers (old at `cover-yeezy-OLD.webp.bak`).

**CANONICAL FOLDER is now `C:\Users\mikef\drip-astro-test`** (the "updated" zip; user chose it as main on 2026-06-25). Old `C:\Users\mikef\drip-astro-v2\drip-astro-v2` is retired-but-kept. **The preview MCP reads the GLOBAL `C:\Users\mikef\.claude\launch.json`, not the project one.** Repointed launch config `drip-astro` → `--root drip-astro-test` on port **5188** (canonical preview); old project kept as `drip-v2-old` on port 5189. Both folders share the same Supabase. There's also a prod-build smoke server pattern: `HOST=127.0.0.1 PORT=4399 node --env-file=.env ./dist/server/entry.mjs` (rebuild + restart to pick up source changes — running node doesn't hot-reload).

**Features added (2026-06-25, in drip-astro-test, verified on :5191):** (1) **New products auto-join New Arrivals** — `POST /api/admin/products` now auto-adds the `new-arrivals` category + stamps `release_date = today` when blank, so a created product lands first in the homepage rail + `/shop?category=new-arrivals`. (2) **Admin analytics** — new `/admin/analytics` page (+ Analytics tab in `AdminNav`, union type widened): KPI cards (revenue/orders/AOV/units), a server-rendered **SVG** monthly-revenue bar chart (last 12 mo, zero JS), top-5 sellers, and low-stock (≤2) list. Reads orders/order_items/inventory via service role; same auth gate as `/admin`.

**DEAL CLOSED — selling this site to the client (2026-07-03).** User sent `drip-astro-v2-updated-2.zip`; diffed vs the installed `drip-astro-v2\drip-astro-v2` = purely additive (adds `/admin/analytics` + AdminNav tab + new-arrivals auto-tag on create) — storefront/core backend byte-identical, so the installed folder is safe to run for checks. **Their existing live site = Shopify** (`drip.store`: `powered-by: Shopify`, Cloudflare, GR/EUR; public `products.json`; a product's `updated_at` was *today* → their catalog is still live-changing, ours is a June snapshot that has drifted). Our slugs match theirs → catalog was scraped from their Shopify. **Two go-live blockers: Stripe still unconfigured (checkout 503 "not configured") + not deployed (Node app, needs Node host — NOT static; README wrongly says "100% static").** **Open decision: backend ownership** — keep Supabase/Stripe/host under WebAction (managed/recurring, recommended) vs full handover to client. **Security (Supabase advisors):** `decrement_inventory` (+`handle_new_user`,`rls_auto_enable`) callable by `anon` via RPC → REVOKE EXECUTE; leaked-password protection off. Inventory 3866 rows are **synthetic/seeded**, not their real Shopify stock — re-pull real catalog+stock at cutover. Gaps vs Shopify to close: order-confirmation emails (currently NONE), shipping/VAT calc, coupon codes, GA4/Pixel, cookie consent, Supabase backups/pause-risk, domain/DNS + 301 redirect map.

**HEADLESS-SHOPIFY SYNC — BUILT & verified (2026-07-03, in `C:\Users\mikef\drip-astro-test`).** Decision: DRIP stays on Shopify (their team keeps working there); our Astro is just the storefront; **checkout hands off to Shopify's hosted cart** (no Stripe for DRIP — Stripe is reserved for Mike's non-Shopify clients). Built: (1) migration `add_shopify_sync_columns_to_products` → added `variants` jsonb `[{size,variant_id,price,compare_at_price,available}]`, `shopify_product_id`, `shopify_updated_at` to `products`. (2) `scripts/sync-shopify.mjs` — pulls `drip.store/products.json` (paginated, public, no token), maps → upserts Supabase; **dry-run by default, `--write` to commit**; VENDOR_MAP normalizes Shopify vendors→our brands (Air Jordan→Jordan, ADIDAS→Adidas, Stussy→Stüssy, Stone Island x Supreme→Stone Island × Supreme, etc.). **Rule: existing slugs update ONLY volatile fields (price/compareAt/sizes/variants/in_stock/shopify ids) + preserve curated name/brand/categories/images; new slugs full-insert with Shopify CDN image URLs** (ProductImage.astro renders remote http as `<img>`). Inventory = availability proxy (available→99, sold-out→0); dedupe per (slug,size) — inventory has a unique (product_slug,size) constraint. **Ran --write: all 311 slugs matched (0 new), real prices synced (fixed .99 rounding), 125 on sale, 110 sold-out sizes, real variant IDs stored.** (3) `src/pages/api/checkout-url.ts` (prerender=false) — takes cart `{lines:[{slug,size,qty}]}`, looks up variant IDs from Supabase, returns Shopify cart permalink `https://drip.store/cart/<vid>:<qty>,…`; `SHOPIFY_STORE_URL` env (default drip.store). Repointed both checkout buttons (`CartDrawer.astro`, `cart.astro`) `/api/checkout`→`/api/checkout-url`. **Verified end-to-end:** endpoint returns valid permalink → Shopify accepts it (HTTP 200, live cart) → PDPs show synced sale prices (€119.99 strike €350) + real availability. **Old Stripe `/api/checkout` kept for other clients.**

**AUTO-SYNC + ADMIN READ-ONLY — BUILT & verified (2026-07-03).** (A) Refactored the sync into a runtime-agnostic core `src/lib/shopify-sync.mjs` (`syncShopifyCatalog(sb,{storeUrl,write,log})`, bulk upsert, no import.meta/aliases) imported by BOTH `scripts/sync-shopify.mjs` (Node CLI) and the new route `src/pages/api/cron/sync-shopify.ts`. (B) **Vercel Cron** nightly 03:00 UTC via `vercel.json` (`crons:[{path:'/api/cron/sync-shopify',schedule:'0 3 * * *'}]`); route protected by `CRON_SECRET` (checks `Authorization: Bearer` — verified 401 without / ok:true with; full sync ran **server-side in 4.4s**, 311 upserts + 3861 inv rows, well under Vercel 10s limit). (C) New `src/lib/store-config.ts`: `SHOPIFY_STORE_URL` + `SHOPIFY_MANAGED` (true when the URL is set) + `shopifyStoreUrl()`. When managed: **admin product editing is READ-ONLY** — `/api/admin/products` (POST/PATCH/DELETE via `guard()`) + `/api/admin/upload` return 409; `/admin/products/new` + `/admin/products/[slug]` redirect to the list; `/admin/products` shows a "Synced from Shopify · read-only" banner and rows link to the storefront. `checkout-url.ts` now uses `shopifyStoreUrl()`. Added `SHOPIFY_STORE_URL=https://drip.store` + `CRON_SECRET` to `.env`/`.env.example`. **FULL EDIT-PARITY + SYNC-NOW — BUILT & verified (2026-07-03).** Upgraded `shopify-sync.mjs` so in managed mode **Shopify is the true master**: existing products now MIRROR name/description/images/badge/sizes/price/sale/variants/availability from Shopify (empty Shopify field won't clobber a good curated value: `p.title||prior.name`, `desc||prior.description`, `images.length?shopify:prior`); curated `categories` base + `featured` preserved, `new-arrivals`/`sale`/`trending` refreshed from tags/price. **Removal handling:** products with a `shopify_product_id` no longer in the feed get deleted (guarded: only when `shopify.length>=50`, so a partial fetch can't wipe the catalog). Ran --write: 311 mirrored, images now **Shopify CDN URLs** (all storefront paths render remote OK — ProductCard/QuickView/PDP use ProductImage/`isRemoteImage`; PDP gallery keeps remote, plate defaults white). **Manual trigger:** new admin-gated `src/pages/api/admin/sync-shopify.ts` (requireAdmin, calls same core + `invalidateCatalog()`); **"Sync now" button + "last synced" (=max(products.updated_at))** added to `/admin/products` managed banner. Verified: PDP shows mirrored Shopify image, sync endpoint 401 without auth, checkout still works. **So the client edits EVERYTHING in Shopify admin (products.json is the pipe); nightly cron or "Sync now" reflects it — incl. photo swaps, renames, deletes.**

**PLATE-COLOR PARITY for added photos — BUILT & verified (2026-07-03).** The "studio plate" look = each card tile painted the photo's own bg colour (`--plate-c`, radial soft fade) so a white-bg photo dissolves into it (soft edge, no rectangle); bundled photos used pre-sampled `plate-colors.json` (by filename), which stopped matching once images became Shopify URLs. Fix: migration `add_plate_color_to_products` (`plate_color`,`plate_src`); `shopify-sync.mjs` now **samples each primary photo's bg colour** (median of 8 edge blocks, via a cheap `?width=120` Shopify thumbnail + `await import('sharp')`), caches by `plate_src` so it only re-samples when the photo changes; opts `sampleImages`/`maxSamples` (CLI ∞, cron/admin cap 120). Wired `plateColor` through Product type + `catalog.ts` rowToProduct; `ProductCard` + PDP gallery + QuickView use `product.plateColor ?? …`. **DRIP's Shopify photos: ~305/311 pure white (default already matched), 6 non-white got real colours** (e.g. taupe hoodie `#c1b3af`, dark-bg Essentials hoodie `#161616` — was a black box on white, now blends). Also **converted `/api/qv/[slug].json` from static getStaticPaths → `prerender=false`+`loadCatalog`+remote-image-safe** (it used bundled-only `resolveImages`, which broke for Shopify URLs / new products). Ran CLI --write: sampled 311 in ~20s. Verified plate colours render on grid/PDP/QuickView; new photos auto-sample. **Same bundled-only bug bit the homepage category tiles** (`index.astro` used `resolveImage()`+`<Image>` → blank for Shopify-URL covers): fixed to `<ProductImage image={cover}>` (remote-safe) + tile `aspect-[4/5]`→`aspect-square` (landscape ~5:3 product shots were letterboxing to a thin strip in the tall frame → looked empty). Brands page unaffected (uses static `/brands/cover-*.webp`). **Lesson: any surface rendering a product image must go through `ProductImage`/`isRemoteImage`, never `resolveImage`/`<Image>` alone, now that images are Shopify URLs.**

**CINEMATIC ABOUT PAGE — BUILT & verified (2026-07-03).** Rebuilt `src/pages/about.astro` from their real content at drip.store/pages/about-us: DRIP = sneaker store + streetwear + **barbershop** ("Fashion, Kicks & Cuts"), est. **2023**, Kifisia. Pulled 8 editorial/store photos from their Shopify CDN (`drip.store/cdn/shop/files/*`), optimized via sharp → `public/about/*.webp` (hero=i8/G-wagon dusk, store=barbershop interior, kicks/fashion/bag/g1-3). Sections: full-bleed **cinematic hero** (Ken-Burns zoom, tiffany veil, huge clamp `Drip or Drown` title 146px desktop/67px mobile), CSS marquee band, story (bilingual EL/EN via `data-en`), **Fashion·Kicks·Cuts pillars** (barbershop photo = their store), stats band (2023/#1/311+/Κηφισιά), editorial gallery, DRIP-standard trust strip, **Visit/Contact** with embedded Google map (CSS-inverted to match dark theme) + real details: **Kassaveti 4, Kifisia 145 62 · +30 212 121 2147 · hello@drip.store · IG/TikTok @drip_athens, FB, YouTube**. Verified: all 8 imgs load, map embeds, no console errors, no mobile overflow. Design tokens: accent `#00c0b6`/soft `#57e1d6`, `--font-display: Archivo`, `.wrap/.eyebrow/.btn/.btn-fill/.btn-ghost/.reveal`, `--ease-lux`. **Revised per user 2026-07-03:** removed hero scroll-cue; **removed ALL em dashes** from visible copy (user dislikes them — use commas/periods, not `—`); **removed the sneaker-count** ("311+") everywhere as it changes over time — About stat replaced with stable "100% Αυθεντικά" (shop page already had no count); tightened Greek copy for EL/EN parity. **DRIP is opening a JEWELS shop (Est. 2026)** — replaced the Streetwear pillar with a pure-CSS "Drip Jewels · Σύντομα 2026" teaser card (glowing teal diamond SVG + wordmark, matches their logo; no photo needed → actually lighter). Pillars now Sneakers · Barbershop · Κοσμήματα(Jewels). Moved fashion.webp to the gallery. User is speed-sensitive: page stays fast (optimized webp, lazy map below fold, CSS-only jewels card, no added JS). **Visit-section map (2026-07-03):** Google's keyless `maps?output=embed` is DEPRECATED → renders blank/black (made zero network requests). Replaced with keyless **OpenStreetMap** `openstreetmap.org/export/embed.html?bbox=<minlon,minlat,maxlon,maxlat>&layer=mapnik&marker=<lat,lon>` (loads 200) styled dark/"drippy" via CSS `filter: invert(0.92) hue-rotate(185deg) brightness(0.92) contrast(0.88) saturate(0.7)` + tiffany tint overlay + custom centred CSS pin (pulsing halo) + "Οδηγίες πρόσβασης"/Get-directions button (Google dir link). **GOTCHA: bbox commas must be LITERAL, not `%2C`** — URL-encoded commas break OSM's bbox parse → zooms out to whole continents. **EXACT store coords = 38.0721406, 23.8124438** (from the venue's Fresha listing fresha.com/lvp/drip-kassaveti-kifisia-P2KzZJ — geocoders/Overpass/OSM don't have Kassaveti 4). Frontmatter `STORE={lat,lng}` builds bbox `lng±0.004, lat±0.003` centred on it; OSM centres the bbox at the iframe pixel-centre so the CSS-centred pin lands on the shop (earlier 38.0722,23.8130 was ~48m east → user flagged the pin was off). **Map REWRITTEN to Leaflet (2026-07-03)** — the OSM-iframe + CSS-centre-pin approach failed the user's zoom test (a fixed pixel pin drifts off the store when you zoom/pan the interactive map). Now a **Leaflet map** (`#drip-map`, lazy-loaded from unpkg on IntersectionObserver) with **CARTO `dark_all` tiles** (keyless, on-theme, no CSS filter needed) and a **geo-anchored custom `divIcon` marker** (teal teardrop + pulsing `.halo`, `iconAnchor:[20,40]`) at STORE coords → **stays locked to the shop at every zoom**. Leaflet chrome/marker styled in a `<style is:global>` block (scoped styles don't hit runtime-created elements); `scrollWheelZoom:false` so page scroll isn't trapped; zoom control top-right. Verified: 12 tiles load, marker stays on Kassaveti after 2× zoom, no console errors. Store hours (ref, not yet on site): Mon–Sat 10:00–18:00, Sun closed. **Stats band trimmed to 2 (2023 + 100% Αυθεντικά) per user — removed #1 and Κηφισιά (2-col, max-w-2xl centred).** **Jewels card: user wants their OWN unique "Drip Jewels" logo (diamond tittle, JEWELS overlapping the p, Est.2026) — NOT the regular Drip logo I used. It's NOT on their CDN and I can't extract chat-attached images to files → must ask user to host it (Shopify Content→Files) and give a URL. Current card is a placeholder using the real `drip-logo.webp`.** → **RESOLVED 2026-07-03: the user had DOWNLOADED the logo — found it at `C:\Users\mikef\Downloads\JEWELS.PNG` (search Downloads/Desktop by recent mtime!) → `public/about/jewels.webp` (719×862, 38KB), used full-bleed as the whole Jewels card (object-cover + "Σύντομα·2026" badge; dropped my composed lockup). LESSON: I can't extract chat-attached images, BUT the user's referenced images are usually on their disk (Downloads) and I HAVE filesystem access — always search local folders before saying "can't get the image".** **Map made brighter per user (was CARTO `dark_all`, "too dark, areas not shown like Google Maps") → switched tiles to CARTO `rastertiles/voyager` (bright, Google-Maps-like street/area labels); lightened vignette; light `#e6e4de` container bg.** **z-index bug FIXED: Leaflet controls (`z-index:1000`) bled OVER the mobile menu (`z-50`) → added `isolate z-0` (isolation:isolate) to the map cell so Leaflet's stacking context is trapped and the whole map sits below the menu (verified: opened menu, map dims behind it).** **Re-verified 2026-07-03 (user re-flagged, likely stale screenshot): `header` is `sticky z-50` = stacking context; menu panel is `fixed z-50` INSIDE header (so capped at header's z-50 in root); map cell isolated at z-0 → header/menu win. Confirmed via `elementFromPoint` over the map with menu open → topmost = MENU at all points. Fix is solid; user just needed a hard-reload. Do NOT bump header z-index above cart/search overlays.** **Homepage band: replaced the `<MeshGradient>` tiffany/milky band ("Η ΥΠΟΣΧΕΣΗ DRIP") with the wide store-interior photo bg** (`public/store-hero.webp`, 1347×680, from `Downloads\Capture.PNG` — the hi-res wide store shot; CDN `1.jpg` is only 1340² square) + dark L→R gradient + tiffany glow + white text. **Added "Σχετικά/About" → `/about` to the Header `nav` array (index 4, before Προσφορές) → auto-appears in BOTH desktop navbar + mobile menu.** **Homepage band made FULL-BLEED** (was boxed in `.wrap`+all-around border → the right border was the "white line"): now `section.relative isolate overflow-hidden border-y border-line` (no side borders), photo `absolute inset-0 object-cover`, content in inner `.wrap`, `min-h-[50vh] md:[54vh]`. **User then flagged a white line = the bright glass-shelf edge on the RIGHT of the store photo → cropped it out** (`store-hero.webp` re-cut from itself: drop left 1.5% + right 11.5% → 1172×680; NOTE `Downloads\Capture.PNG` got OVERWRITTEN by a later website screenshot, so crop from the existing webp, not Capture). **User REJECTED a real Google map** (I'd only *tested* Google `?pb=` embeds via throwaway preview_eval iframes — those WORK keyless and render a real Google map with `!3m3!1m2!1s0x0%3A0x0!2z<b64 DMS>` marker — but made NO source changes; Leaflet-Voyager map stays). **About STORY section: removed the duplicated barbershop `store.webp` (also in the Cuts pillar) → replaced with the storefront "Drip"-sign photo `Downloads\DRIPP.PNG` → `public/about/storefront.webp` (757×961)** in the 2-col `cine-frame`. **Store band image sharpened (2026-07-03):** source maxes at ~1340px on CDN (`1.jpg` 1340²), so `store-hero.webp` re-derived = crop the square to the band aspect + drop right bright edge + upscale to 2400px (lanczos3) + light sharpen + q88 (~500KB) → renders crisp on wide screens (real detail is source-limited). **Packaging: to zip the project, STOP the dev server first** — `public/hero/hero-bg.mp4` (8.9MB) stays locked by Vite → Compress-Archive fails ("being used by another process"). Delivered `C:\Users\mikef\Downloads\drip-astro-latest.zip` (32MB, 1155 files) via PowerShell `Compress-Archive` of top-level items EXCLUDING node_modules/dist/.astro/.git/.vercel + `.env`(secret)/build-log.txt; includes `.env.example`. Recipient: unzip → `npm install` → `npm run dev`/`build`; set env vars separately (Supabase + SHOPIFY_STORE_URL + CRON_SECRET). Added `store.webp` (barbershop) as a **ghosted background** (`.visit-bg` opacity 0.16 + dark veil) behind the contact card. **Real store photo + real logo (2026-07-03):** their **flagship store-interior photo** (blue-lit sneaker wall + Drip sign + bench) is `drip.store/cdn/shop/files/1.jpg` (1340²) → `public/about/store-interior.webp`, showcased in a new full-width cinematic band after the pillars ("Μπείτε στον κόσμο του DRIP"). Their **real teal "Drip" logo** (drip-drop tittle + ©) is `.../files/Add_a_heading.png` → `public/about/drip-logo.webp`; **replaced the hand-drawn CSS diamond on the Jewels card with this real logo** composed into the lockup (logo + spaced "JEWELS" + "◆ Est. 2026" on grain+beam dark bg). The exact "Drip Jewels Est.2026" lockup is NOT on their CDN (no jewels page; all guesses 404) — reproduced with the authentic logo. Other drip.store banner files seen: 5.jpg/6.jpg (Nike/Jordan in bushes), 2.png/Untitled_design_11/15 (product banners). **REMAINING unchanged: deploy to Vercel (adapter swap @astrojs/node→@astrojs/vercel + env + domain).** REMAINING: deploy to Vercel — must switch adapter `@astrojs/node` → `@astrojs/vercel` (node adapter builds a standalone server, not Vercel functions; Vercel Cron needs the vercel adapter), set all env vars incl. a strong `CRON_SECRET`, point domain drip.store. Optional: Shopify webhooks for real-time (needs Admin API token). Stripe NOT needed for DRIP.

**Backend re-verified end-to-end (2026-06-25):** Ran the `drip-astro-v2-updated.zip` at `C:\Users\mikef\drip-astro-test` (prod build server :4399, dev :5188). Admin login → orders dashboard (revenue/pending/status change), product CRUD, and live per-size stock all confirmed working against Supabase. **Bug found & fixed:** `scripts/seed-inventory.mjs` had `const URL = ...` shadowing the global `URL` constructor (line ~10) → `new URL(...)` threw "URL is not a constructor", so inventory could never seed (that's why `inventory` was empty). Fixed (renamed to `SUPA_URL`) in the drip-astro-test copy only — **the zip and the original v2 still have the bug.** `inventory` is now seeded (3866 rows / 311 products) in the shared Supabase. **Perf:** shopper JS is tiny (~20KB; the 217KB supabase bundle loads only on owner `/login`). The `/shop` page ships ~1.5MB uncompressed HTML (all 311 cards for instant client filtering) and the Astro **Node adapter sends no gzip/brotli** — it gzips ~18.5× to ~80KB, so deploy behind a compressing host/CDN or add compression middleware.

**Owner product admin — BUILT & verified (2026-06-23):** Storefront (index/shop/product/brands/search-index) is now **SSR** (`prerender=false`) reading the live catalog via `lib/catalog.ts` `loadCatalog()` (3s cache, `invalidateCatalog()` after writes) which swaps data into `lib/products.ts` (now `export let` + `applyCatalog`); falls back to static products.json when DB not ready. Mixed images handled by `components/ProductImage.astro` (bundled filename → `<Image>`, uploaded http URL → `<img>`; helper `isRemoteImage`). Admin at `/admin/products` (list+search), `/admin/products/new`, `/admin/products/[slug]` (edit) using `components/admin/ProductForm.astro` + `AdminNav.astro`; gated by `lib/admin-guard.ts`. APIs: `/api/admin/products` (POST/PATCH/DELETE) and `/api/admin/upload` (multipart→sharp WebP→`product-images` bucket→public URL). Per-size stock writes to `inventory`. Full CRUD + upload + storefront reflection tested green. Customer accounts already removed; Stripe still placeholder (checkout returns "not configured"). **Do not deploy yet.**


**DEPLOYED TO VERCEL, staging link live (2026-07-08).** Latest folder C:\Users\mikef\drip-astro-test is now on Vercel: **https://drip-store-orpin.vercel.app** (project `drip-store` / `prj_wLSkmsfdIpNOnQTrY3Ev88o5LInD`, team **webaction** `team_fAnVpAdOPwPZAR4kW0o2BwID`, its first project). Adapter swapped `@astrojs/node` to `@astrojs/vercel@8` (v8 is the Astro-5-compatible line; v11 needs Astro 7) in astro.config.mjs; `site` now reads `PUBLIC_SITE_URL`. Env vars set on Prod+Preview (Supabase url/anon/service-role, `SHOPIFY_STORE_URL=https://drip.store`, `PUBLIC_SITE_URL=https://drip-store-orpin.vercel.app`, freshly generated strong `CRON_SECRET`; Stripe keys stay placeholders, checkout hands off to Shopify, `checkout.ts` guarded by `stripeReady()` so no crash). Deployed via Vercel CLI (already authed as webactionhellas-dev; the `deploy_to_vercel` MCP only returns instructions, does not deploy). Verified live: /, /shop, /about, a PDP all 200; sitemap/robots/canonical correct; `POST /api/checkout-url` 400-guards (not 500); zero runtime errors. Vercel project Node shows 24.x but the function pins nodejs22.x (optional dashboard fix). NOT a git repo (CLI made a harmless .env.local OIDC token). **HERO VIDEO hardened for iOS/Android never-pause** in src/components/Hero.astro: added `webkit-playsinline`/`x5-playsinline`/`disableremoteplayback` plus a JS keep-alive watchdog (resume on pause/stalled/suspend/ended, visibilitychange/focus/online, pageshow=bfcache-on-refresh, every touch/scroll/click gesture=Low-Power-Mode unlock, plus a 1s setInterval backstop; respects reduce-motion). Verified inlined and intact in prod HTML and resumes under 300ms after a forced pause. iOS Low Power Mode still needs the FIRST user gesture (OS limit, unbeatable) then never pauses again. **STILL PENDING (gated): custom-domain cutover**, pointing drip.store (client's LIVE Shopify) at this Vercel deploy is a separate explicit step; DNS untouched for now.

**FIX (2026-07-08, redeployed): broken bundled images on Vercel + trust-badge mobile text.** (1) Homepage category-cover images (Sneakers/Basketball tiles) rendered broken on the Vercel deploy because the storefront is SSR, so Astro's `<Image>` deferred to its runtime `/_image` optimization endpoint, which **404s on the @astrojs/vercel adapter** (raw `/_astro/*.webp` assets were 200, only `/_image?...` 404'd). Remote Shopify images were unaffected (they render as plain `<img>`). Root-cause fix: set **`adapter: vercel({ imageService: true })`** in astro.config.mjs so bundled `<Image>` routes through Vercel's optimizer `/_vercel/image?url=...` (verified 200 live). This fixes bundled `<Image>` site-wide (ProductCard, PDP, category covers), not just the homepage. GOTCHA for any future SSR-on-Vercel Astro build: `imageService: true` is required or every `<Image>` 404s. (2) `TrustBar.astro` 4 badges: removed the small subtitle line (`sub`/`sen`) per Mike, keep only the big titles (100% Πιστοποιημένα, Δωρεάν Παράδοση 24ω Αθήνα, Επιστροφές 14 Ημερών, Βαθμολογία 4.9/5); title `<p>` now `leading-snug`, wraps cleanly, verified fits 2-col at 375px with no overflow/h-scroll. Redeployed to prod (same alias https://drip-store-orpin.vercel.app); hero-video mobile hardening survived the rebuild (re-verified live).

**SALE-READINESS PASS (2026-07-08).** (a) NEW page `src/pages/authentication.astro` ("Πώς λειτουργεί η πιστοποίηση" / How authentication works): bilingual, 5-step verification process + guarantee band, matches design tokens (.wrap/.eyebrow/.btn/font-display/accent), static, reuses /editorial-drip.webp hero. Homepage CTA (index.astro authenticity band) repointed /about -> /authentication. Live 200. (b) OWNER LOGIN is at /login (Supabase signInWithPassword -> /admin, gated by src/lib/admin-guard.ts); it was just UNLINKED. Added a discreet "Διαχείριση" link in Footer.astro bottom bar -> /login (data-en "Owner login"). (c) TrustBar.astro: removed the 4 subtitle lines (kept big titles only) AND fixed Greek clipping on mobile by stacking icon-above-text (flex-direction:column, text col went 70px->140px) with row layout restored at >=640px + overflow-wrap:anywhere; verified el+en at 375px, no clip/no h-scroll. (d) Backend audit (read-only, via backend-integrator): owner mikefalcos2004@gmail.com exists, email-confirmed, is_admin=true, real last_sign_in 2026-07-04 (loginable). Sync healthy: 311 products all shopify-mapped, 3861 inventory, nightly Vercel cron 03:00 UTC WILL run (needs @astrojs/vercel adapter, now deployed). RLS correct (anon reads products/inventory via `true` SELECT policies; writes service-role only). (e) SECURITY hardening APPLIED (2 migrations, Mike-approved): `harden_function_execute_grants` + `revoke_public_execute_on_definer_functions`. GOTCHA: revoking EXECUTE from anon/authenticated is NOT enough, functions carry a default EXECUTE grant to PUBLIC, must `REVOKE ... FROM PUBLIC`. Now decrement_inventory/handle_new_user/rls_auto_enable/touch_products_updated_at = postgres+service_role only; is_admin = +authenticated (RLS needs it, kept). touch_products_updated_at search_path pinned. **REMAINING for handover:** S5 leaked-password protection = Dashboard toggle (Auth > Password); client-owned admin via scripts/create-owner.mjs then demote/remove builder account; rotate Supabase anon+service-role keys at ownership transfer; backend-ownership decision (WebAction-managed vs client); drip.store domain cutover (separate gated step). is_admin-executable-by-authenticated advisor is an ACCEPTED exception (must stay SECURITY DEFINER + authenticated to avoid RLS recursion on profiles).

**Rail hover-lift clip fix (2026-07-08).** ProductRail cards (.card-pop) lift `translateY(-9px)` + tiffany glow on hover, but the scroll track (`overflow-x-auto`) clipped their rounded TOP corners: setting overflow-x:auto forces overflow-y to `auto` too (CSS won't scroll one axis + leave the other visible), and the track had `pb-2` but no top room. Fix in ProductRail.astro track class: `px-5 pb-2` -> `px-5 py-5 -my-3` (20px vertical padding = clip room for the 9px lift + ring; `-my-3` negative margin pulls the padded box back so layout doesn't grow). Verified live: card top sits ~17px inside the clip edge after lift, rail still scrolls, no h-scroll. Applies to all 3 rails (shared component). Reusable gotcha for any hover-lift inside a horizontal scroller.

**Mobile flicker on Brands + Shop FIXED (2026-07-08).** Symptom (user's phone): brand cover images flickered (load/unload) and the footer glitched on /brands (and /shop). Cause: both pages wrap content in `<EtherealShadow>` (src/components/EtherealShadow.astro), a "drifting smoke" bg that applies an ANIMATED SVG `feTurbulence`+`feDisplacementMap` filter (SMIL `<animate>`, indefinite) + `blur(8px)` + `will-change:filter` to a big element. Animated SVG displacement filters recompute every frame and overwhelm phone GPUs, thrashing compositing layers -> unrelated elements (lazy imgs, footer) flicker. Fix: added `@media (max-width:768px)` in EtherealShadow.astro swapping `.es-shape` to a STATIC `filter:blur(30px)` (no url filter reference -> turbulence never computes) + `will-change:auto`. Desktop keeps the animated `url(#es-filter) blur(8px)` drift. Verified: mobile computedFilter=blur(30px) usesAnimatedFilter=false, desktop still url(#es-filter). Reusable gotcha: animated SVG turbulence/displacement filters are a mobile-perf trap, gate them to desktop/non-reduced-motion and use static blur on phones. Related to [[css-fixed-backdrop-filter-trap]].

**PRICING MADE SITE-OWNED + SALE SECTION REMOVED (2026-07-08).** Client is taking the whole backend OFF Shopify (WebAction runs it on Supabase); wants StockX prices as fixed prices, doesn't need daily tracking. (1) SALE SECTION removed per owner: dropped 'Προσφορές/Sale' from Header nav + Footer links, removed the homepage On-Sale ProductRail (index.astro) + its onSale import. Kept the mechanism: on-card strike-through + /shop?category=sale filter still work, so marking a product down in admin resurfaces it. (2) PRICE OWNERSHIP FLIPPED: `src/lib/shopify-sync.mjs` update path (~line 165) changed `price: vol.price, compare_at_price: vol.compare_at_price` -> `price: prior.price, compare_at_price: prior.compare_at_price ?? null` so the nightly cron NO LONGER overwrites price/compare_at (they're site-owned; sale category now follows site-owned compare_at, not Shopify). New products still take Shopify price on first INSERT. This freezes current (StockX-derived, from his Shopify) prices as his own. MUST stay deployed before 03:00 UTC cron or prices get reset. (3) StockX price data: I CANNOT scrape StockX for ~300 products (no API, anti-bot, ToS) -> built tooling instead: `scripts/export-price-template.mjs` (wrote `stockx-price-template.csv`, 311 products, 9 'Drip Exclusive' flagged skip; copy at Downloads\DRIP-stockx-price-template.csv) + `scripts/import-stockx-prices.mjs <csv> [--write] [--clear-sale]` (matches by slug, skips Drip Exclusive + blank, dry-run default). Owner fills stockx_price column -> I run import. Per-size pricing NOT done (owner chose one-price-per-product). REMAINING: get filled price CSV -> import; later full Shopify decoupling (kill the sync, make admin price-editable in managed mode).

**HOMEPAGE LAYOUT + SELF-SERVICE ADMIN UNLOCKED (2026-07-08).** (1) LAYOUT: moved the Best Sellers ProductRail in index.astro from ABOVE the authenticity poster to the gap BETWEEN the authenticity poster and the About poster (where the removed Sale rail was). New home order: Hero, store band, TrustBar, New Arrivals, Category grid, Authenticity poster, Best Sellers, About poster. (2) SELF-SERVICE ADMIN: client is selling the site + wants the buyer to edit prices/products/sales/text without help. The product CRUD was already built but locked read-only in Shopify-managed mode. Added `ADMIN_EDITABLE` to `src/lib/store-config.ts` (`import.meta.env.ADMIN_EDITABLE === 'true' || !SHOPIFY_MANAGED`) and swapped the read-only gates from `SHOPIFY_MANAGED` -> `!ADMIN_EDITABLE` in: `api/admin/products.ts` (POST/PATCH/DELETE 409), `api/admin/upload.ts` (409), `admin/products/new.astro` (redirect), `admin/products/[slug].astro` (redirect), `admin/products/index.astro` (rowHref->editor, '+ New product' button, 'Edit' link, hid the 'synced from Shopify read-only' banner). Set **ADMIN_EDITABLE=true** on Vercel Production+Preview, deployed. So the owner can now log in at /login -> /admin/products and create/edit products, prices, sales (compare_at), stock via ProductForm. Checkout (Shopify handoff) + the price-preserving sync are UNCHANGED (still keyed off SHOPIFY_MANAGED). Build passed. Mike must log in to E2E-test (agent can't auth headlessly). REMAINING for 'completely ready off Shopify': (a) TEXT editing of site copy = NOT built, needs a content/settings table + admin UI + refactor hardcoded .astro strings to DB (product names/descriptions ARE already editable via product admin); (b) checkout replacement (Stripe real keys, currently placeholders + hands off to Shopify) before fully dropping Shopify; (c) eventually re-host product images (currently Shopify CDN URLs).

**EDITABLE SITE TEXT (content system) BUILT (2026-07-08).** Owner can now edit key site copy without help. (1) Supabase migration `create_site_content`: table `site_content` (key PK, section, label, el, en, updated_at), RLS public SELECT / service-role writes only; seeded 15 keys with current defaults (announcement bar x3, homepage promise/newarrivals/bestsellers/authenticity/about eyebrows+titles+taglines). (2) `src/lib/content.ts`: `loadContent()` (cached 3s, same supabaseAdmin as catalog) + `c(key, defaultEl, defaultEn)` -> {el,en} with hardcoded fallback (site safe if DB down / key missing) + `invalidateContent()`. (3) `src/pages/api/admin/content.ts` (GET list + PATCH save, requireAdmin + ADMIN_EDITABLE gate, calls invalidateContent). (4) `src/pages/admin/content.astro` = 'Content' admin page: EL/EN textareas grouped by section + Save-all button (PATCH). Added 'content' tab to AdminNav (widened Props type). (5) WIRED: Header announcement bar (loadContent + c()) and index.astro (promise band, both ProductRail titles/taglines/eyebrows, authenticity + about poster eyebrows/titles) via `import { loadContent, c as txt }` (aliased to avoid clash with index's existing `(c)=>` loop var). Build passed, deployed, verified live (home 200, /admin/content 302 auth-gate, /api/admin/content 401, announcement + headings render). NOTE: static pages (about/authentication/404) bake announcement copy at build; SSR pages (home/shop) update live within ~3s. To add more editable strings later: insert a site_content row + wire the page with `c('key', elDefault, enDefault)`. Only a curated homepage+announcement set is wired so far (About-page body, footer contact, hero not yet). Couldn't self-test a live edit (classifier blocks prod DB writes) -> owner verifies by editing in /admin/content. Also answered Mike: order-confirmation emails = Resend + Stripe webhook (built WITH Stripe checkout, replaces Shopify's emails); gave full business-info checklist (Stripe keys+legal/ΑΦΜ/IBAN, email sending domain+DNS, VAT/shipping/returns/legal pages, filled StockX price sheet, client admin email, domain DNS, ownership decision).

**STRIPE TEST KEYS STORED (2026-07-08).** Walked non-technical Mike through Stripe via the claude-in-chrome browser (his browser named "gg"; the first auto-picked "Browser 1" wasn't the one he was viewing -> used switch_browser broadcast so he Connects the right one). He signed in; account = **"Web Action sandbox" acct_1Tk55N2ZztFyv5QV**, TEST mode. Read pk_test_/sk_test_ off the /test/apikeys page and set them in Vercel env (Production+Preview, encrypted): `STRIPE_SECRET_KEY` + `PUBLIC_STRIPE_PUBLISHABLE_KEY` (replaced the __SET_ME__ placeholders). Values NOT stored here (secrets). STILL PENDING to turn checkout on: (a) redeploy so build picks up the keys; (b) BUILD the real Stripe checkout (Checkout Session from cart -> Stripe-hosted -> success), currently checkout still hands off to Shopify; (c) `STRIPE_WEBHOOK_SECRET` (set up a webhook endpoint first); (d) order-confirmation emails = Stripe built-in receipts as interim (Dashboard > Customer emails) OR custom via Resend; (e) LIVE keys later (needs business verification: legal name, ΑΦΜ, IBAN) — for live keys be more careful, prefer owner adds them in Vercel dashboard vs echoing. These are TEST keys = no real money, safe to build/test with card 4242 4242 4242 4242.

**4 HIGH npm-audit VULNS PATCHED + SECURITY HEADERS ADDED, STAGED (2026-07-09, in C:\Users\mikef\drip-astro-test).** Fixed the 4 high-severity vulns (Astro define:vars XSS GHSA-j687-52p2-xcff + 3x path-to-regexp ReDoS GHSA-9wv6-86v2-598j pulled via @astrojs/vercel->@vercel/routing-utils). SAME fix validated earlier on [[house-starters]] web-action-commerce-starter. package.json: astro ^5.6.1->^7.0.7, @astrojs/vercel ^8.2.11->^11.0.2, @tailwindcss/vite + tailwindcss ^4.1.4->^4.3.2, added `"overrides": {"path-to-regexp":"6.3.0"}`. `npm install` (path-to-regexp@6.3.0 overridden confirmed) -> **`npm audit --omit=dev` AND full `npm audit` = 0 vulnerabilities**. `npm run build` = Complete, ZERO code changes (Astro 5->7 jump clean, matches starter). SECURITY HEADERS: store shipped NONE; copied the starter's vercel.json header block (X-Content-Type-Options, X-Frame-Options SAMEORIGIN, Referrer-Policy, Permissions-Policy, HSTS 2y preload, full CSP) and adjusted CSP for the store's real domains: **added `https://unpkg.com` to script-src AND style-src** (about.astro lazy-loads Leaflet CSS+JS from unpkg); Carto map tiles + Shopify-CDN/Supabase images covered by `img-src 'self' data: https:`; Google Fonts, Stripe, Supabase already in the starter CSP. Checkout is a `window.location.href` navigation (not form-action/connect restricted); all API calls same-origin. SMOKE-TESTED via `astro dev` (vercel adapter has no `astro preview`) on :4331, all routes green: public pages+PDP 200, unknown 404, JSON endpoints (search-index/stock/qv) 200 live data, POST-only API (checkout/checkout-url/webhook) 404 on GET (Astro no-handler-for-method, not 500), cron 401 (CRON_SECRET guard), all /admin/* 302->login (auth gate intact), ZERO runtime errors in dev log, home renders real title+239 product nodes. Backups of pre-change package.json/lock/vercel.json/astro.config in scratchpad dripstore-secfix-backup. **NOTE the cron was deliberately removed from vercel.json on the 2026-07-08 standalone move, so headers-only vercel.json (no crons) is correct; the api/cron route is dormant.** **DEPLOYED TO PRODUCTION + VERIFIED LIVE (2026-07-09, Mike gave the go, wanted zero visual change).** `vercel --prod --yes` (authed webactionhellas-dev), build Complete on Vercel, aliased to **https://drip-store-orpin.vercel.app** (dpl_2NxaxNk6cpnW9xkDjBa3BLNV3FVM). LIVE VERIFICATION: (1) all 6 security headers present on prod response (CSP/HSTS/X-Frame/X-Content-Type/Referrer/Permissions), CSP served exactly as written incl. unpkg in script-src+style-src. (2) Routes: /, /shop, /about, /authentication, /cart, PDP, search-index.json all 200; /admin 302. (3) NO visual regression, proven in real Chrome (claude-in-chrome): home + about consoles ZERO CSP refusals; About page directly load-tested every external host under the live CSP -> unpkg leaflet.js LOADED (window.L defined), unpkg leaflet.css LOADED, Carto voyager tile LOADED 256x256, Google Fonts (Archivo+Inter) loaded; home 0 broken images incl. all 37 Shopify-CDN product imgs, hero video hero-bg.mp4?v=2 loading same-origin (networkState 2, no media error -> CSP fine, just buffering the 8.9MB file; autoplay watchdog resumes). Security patch causes zero functional/visual change. **The 4 high vulns + missing headers are now FIXED in production.**

**PRE-LAUNCH BACKEND VERIFICATION + HARDENING (2026-07-15, Echo full inspection, Mike-approved fixes).** Context: waiting on client's live Stripe keys; goal = prove full Shopify parity first. (1) **Shopify-independence COMPLETE:** last live dependency was product images (992 cdn.shopify.com URLs in products.images) -> severed via `scripts/relink-local-images.mjs` (311 updated / 0 unmapped; script had the SAME `const URL` shadowing bug as seed-inventory.mjs, fixed as SUPA_URL). DB verified 0 shopify URLs; images now bare local filenames -> resolveImage/bundled WebP. **Live prod picked it up with NO redeploy** (SSR reads DB; /_vercel/image serves the bundled WebP 200). Client can close Shopify anytime. Remaining Shopify code (sync routes/lib, checkout-url, variants columns) = dormant/migration-only, safe. (2) **CRITICAL security fix applied to live DB:** authenticated had table-level UPDATE on public.profiles + unrestricted update-own RLS -> any signed-in user could set is_admin=true. Fixed: `revoke update on public.profiles from anon, authenticated;` verified 0 UPDATE grants remain. (3) **Money-path test suite added** (was zero tests): `npm test` -> 27/27 pass; pure seams extracted behavior-preserving: src/lib/pricing.ts (priceCart, price-tamper rejected), fulfillment.ts (webhook idempotency, retry never double-decrements), ready.ts (stripeReady/adminReady predicates), stock.ts; TESTING.md documents tiers incl. Stripe-CLI replay. (4) **Checkout pre-flight stock validation:** /api/checkout now checks live inventory BEFORE order insert + Stripe session; sold-out/partial lines -> 409 with friendly per-line message (cart UI alert()s it, verified). Residual last-pair race = floor-at-0 + manual refund (Mike's chosen policy). (5) Guest-only checkout CONFIRMED as final decision; BACKEND.md corrected (no /account, owner via create-owner.mjs). DEPLOYED same day (Mike's explicit go via AskUserQuestion; the auto-mode classifier BLOCKS `vercel --prod` without it): pre-flight 409 verified LIVE on drip-store-orpin.vercel.app (sold-out slug nike-ja-3-all-star size 48), no orphan orders, headers intact. **SAME DAY wave 2 (2026-07-15): EMAILS+COUPONS+CONSENT built (Echo), deployed, Resend LIVE.** (1) Order-confirmation emails: src/lib/order-email.ts (pure builders, branded dark/#00c0b6, EL-first bilingual, receipt-precise EUR, XSS-escaped) + src/lib/email.ts (lean fetch wrapper, NO SDK, emailReady() predicate, never throws/never fails the webhook, sends only on first fulfilment so retries never re-send) + owner new-order notification (OWNER_NOTIFY_EMAIL). (2) Coupons: allow_promotion_codes:true in checkout.ts; email shows discounted amount_total. (3) Cookie consent: CookieConsent.astro DORMANT (renders only when PUBLIC_GA4_ID set; consent gates gtag injection; PUBLIC_ vars inline at BUILD time so activation = set env + rebuild + CSP additions documented in BACKEND.md). Tests 27->42/42. **Resend account created by Mike: owner = webactionhellas@gmail.com (NOT mikefalcos2004 - test mode delivers ONLY to that address, 403 otherwise), key in .env + Vercel encrypted env (prod+preview), sending key re_Uuz...J25 was pasted in chat - rotate at handover.** Real branded email verified delivered via scripts/send-test-email.mjs (handover utility: `node --env-file=.env scripts/send-test-email.mjs webactionhellas@gmail.com`). GOTCHA: Echo appends env placeholders to .env; blind `>>` append created DUPLICATE keys (node --env-file: last wins) - grep before appending. Chrome walkthroughs: Mike's real browser = "gg" via switch_browser broadcast (Browser 1/2 are NOT his viewing window, same trap as Stripe day). REMAINING/GATED: disable public sign-ups (Auth dashboard, currently ENABLED, store is owner-login-only) + leaked-password protection (still off); client's live Stripe keys -> 10-min go-live checklist in TESTING.md (test keys, stripe listen whsec, 4242 buy, event resend = no double-decrement, then live keys + prod webhook endpoint in Vercel env); at handover verify client domain DNS in Resend so EMAIL_FROM leaves onboarding@resend.dev test mode; client info still needed: shipping rates table, VAT stance, filled StockX CSV, legal-pages facts, GA4/Pixel ids.

**TAKEOVER ROADMAP (2026-07-15, 49 items, 3-lens completeness workflow):** live artifact https://claude.ai/code/artifact/e5382644-8848-40b5-a867-021bc7155dc1 (Phase 0 this-week fixes / Phase 1 client info pack / Phase 2 final build / Phase 3 cutover runbook). CRITICAL DISCOVERIES (verified live by critics): (1) **drip.store registrar = NAMECHEAP** (NS dns1/dns2.registrar-servers.com, NOT Shopify-managed, no domain transfer needed) and **EXPIRES 2026-10-13** - renew first; (2) **email = Google Workspace** (aspmx.l.google.com MX) + SPF (_spf.cretaforce.gr + google) + **Klaviyo TXT + Zoho Bookings/CRM records** (Zoho likely = barbershop bookings) live in that DNS zone - cutover flips ONLY apex A (now 23.227.38.65 Shopify -> Vercel) + www CNAME -> cname.vercel-dns.com, NEVER migrate nameservers; (3) **IRIS acceptance is Greek law since Nov 2025 with fines and Stripe does NOT offer IRIS** - needs client's bank or Viva alongside; (4) **myDATA/AADE receipts** (ΑΛΠ per B2C sale) unhandled - accountant + provider (Elorus/Epsilon/SoftOne/timologio), auto-issue hook = the paid webhook; (5) CODE BLOCKERS found: webhook doesn't check payment_status==paid (async methods = unpaid fulfilment), /admin never shows shipping address (owner can't ship), zero shipping_options (every order ships free), no shipped/refund emails, no checkout.session.expired cleanup, no monitoring; (6) Omnibus 30-day-low rule makes StockX-derived strike-through anchors illegal; (7) Shopify gift cards + order/customer/payout exports MUST be archived before closure (tax retention), keep Shopify paid 30+ days post-cutover; (8) COD (αντικαταβολή) + installments (δόσεις) currently absent = silent conversion decision nobody made. Full critic output: scratchpad tasks/w3tu8tl4j.output of session ec95566c.

**PHASE 0 BATCH BUILT + DEPLOYED (2026-07-15, Echo, Mike-approved, tests 42->58/58).** Shipped to prod drip-store-orpin.vercel.app + verified live (pages 200, sold-out 409, headers intact, orders still 3, 0 negative stock). Items: (1) webhook fulfils ONLY on payment_status paid|no_payment_required (async/unpaid stays pending; async_payment_succeeded fulfils; async_payment_failed->cancelled since DB CHECK orders_status_check allows only pending/paid/fulfilled/cancelled/refunded, NO 'failed'); routing seam actionForEvent() in fulfillment.ts + shared fulfilCheckoutSession in new src/lib/fulfil-order.ts (webhook+cron single-source). (2) admin order detail expandable row (shipping jsonb, items, session id, tracking+refund controls). (3) shipped email buildShippedEmail (EL-first, courierTrackingUrl for ACS/ELTA/Speedex/Geniki/BoxNow/DHL/UPS) sent on first flip to 'fulfilled'. (4) refund api/admin/refund.ts (stripe.refunds.create idempotencyKey refund_<id>, optional restock via increment_inventory RPC, buildRefundEmail) + charge.refunded webhook reconciles dashboard refunds (NO auto-restock). (5) checkout.session.expired->cleanupExpiredCheckout (conditional DELETE pending only). (6) 3DS needsStrongAuth>=50000 cents. (7) submit_type:'pay' + custom_text.submit 'Παραγγελία με υποχρέωση πληρωμής'; emails carry seller identity (DRIP, Κασσαβέτη 4, Κηφισιά 145 62, +30 212 121 2147, hello@drip.store) + 14-day withdrawal link /returns; ΑΦΜ/ΓΕΜΗ OMITTED (never faked, awaiting client). (8) 301 redirect map in vercel.json (8 rules permanent:true: /products/:slug->/product/:slug etc, DORMANT until domain but 308 fires now) + scripts/data/shopify-url-inventory.json (337 URLs); SLUG DRIFT ZERO (our 311 == their live sitemap 311). (9) SSR cache headers set in code BUT Vercel edge overrides to max-age=0,must-revalidate in prod (dev showed s-maxage=60; perf-only, Corky to chase). (10) api/cron/reconcile.ts (CRON_SECRET, pending>2h, retrieve session, paid->fulfil+owner alert, expired->cleanup) - **cron schedule had to drop from */6h to DAILY 0 3 * * * because Vercel team webactionhellascom is HOBBY plan (daily cron cap + Hobby prohibits commercial use = must upgrade to Pro before real launch, roadmap item 14)**; README 'static site' fixed, .vercelignore added, env split documented in BACKEND.md. **MIGRATION tracking_and_restock APPLIED to live DB (Mike-approved via AskUserQuestion): orders.tracking_number + orders.courier (text) + increment_inventory(slug,size,qty) SECURITY DEFINER (greatest(0,qty), revoked from public/anon/authenticated). Verified.** STILL NEEDED interactive (this session, pending Mike): paste Stripe TEST webhook signing secret whsec_ into Vercel (endpoint we_1TtGSJ2ZztFyv5QVQWSmfvMO 'drip-store-staging' created on Web Action sandbox acct_1Tk55N2ZztFyv5QV, events checkout.session.completed/expired/async_payment_succeeded/async_payment_failed + charge.refunded, URL https://drip-store-orpin.vercel.app/api/stripe-webhook) -> then full test-mode money path live; + 2 Supabase Auth toggles (disable signups, leaked-password). PAYMENTS ARCHITECTURE DECIDED with Mike: Stripe(cards)+Viva/bank(IRIS, legal mandate Stripe can't meet) side by side (option A, keeps the built+tested Stripe path); client sending their LIVE Stripe creds (slot in at cutover, live webhook is domain-coupled); Mike handles Namecheap (renew + DNS). Client 14-question info-pack message (EL) delivered to Mike this session.

**WENT FULLY STANDALONE (off Shopify) + CONTENT EDITOR VERIFIED WORKING (2026-07-08).** ROOT CAUSE of "admin read-only / Content page bounced / ADMIN_EDITABLE flag never worked": the project's local **`.env` is UPLOADED at `vercel --prod`** (not a git repo, no `.vercelignore`; Vercel doesn't honor `.gitignore` here) and its `SHOPIFY_STORE_URL=https://drip.store` **shadowed the Vercel dashboard env** at build time (`import.meta.env.SHOPIFY_STORE_URL` inlines from .env). So dashboard changes (removing SHOPIFY_STORE_URL, setting ADMIN_EDITABLE=true) had NO effect. FIX: edited **`.env`** -> `SHOPIFY_STORE_URL=` (empty) + `ADMIN_EDITABLE=true`; also removed the cron from `vercel.json` (deleted the nightly sync). Now SHOPIFY_MANAGED=false -> admin fully EDITABLE (Edit links + '+ New product', no 'Sync now'), Content page opens, sync gone. LESSON: to change this project's build-time config, edit `.env` (the source of truth on deploy), not just Vercel dashboard. GOTCHA: Stripe key reads at RUNTIME (process.env=Vercel value) while store-config reads BUILD-TIME (.env) — inconsistent. CONTENT EDITOR BUG FIXED: admin/content.astro Save button used textarea attrs `data-el`/`data-en-field` — `data-el` COLLIDES with the BaseLayout i18n (swap() sets el.dataset.el on [data-en] elements; toggle reads dataset.el). Renamed to `data-cel`/`data-cen` + made the collect null-safe (filter). VERIFIED end-to-end via the live UI: set textarea -> click Save -> `/api/admin/content` PATCH 200 {ok,updated:1} -> DB updated -> live homepage showed the edit -> reverted. Content editing WORKS. (Note: computer-tool `left_click` on the Save button didn't register; `btn.click()` via javascript_tool did — real user clicks work.) STILL PENDING: Stripe checkout blocked on invalid `STRIPE_SECRET_KEY` in Vercel (agent verified sk_test_...y00F rejected) -> re-grab a valid test secret from Stripe + finish webhook (agent left a bootstrap route `api/admin/provision-webhook.ts` + `WEBHOOK_BOOTSTRAP_SECRET` to create it, then delete both).